# Input Reconciliation — Platform / API-Contract Adversarial Review

- **Input:** `_bmad-output/planning-artifacts/prds/prd-eventstore-2026-07-05/review-platform-contract.md`
- **Target:** `_bmad-output/planning-artifacts/prd.md`
- **Reconciled:** 2026-09-10
- **Verdict:** All four Critical and four High findings are addressed in the PRD as binding requirements, corrective ownership, and fail-closed exit gates. None is represented as implemented, evidenced, approved, or closed.

## Critical Findings

| Finding | PRD reconciliation | Delivery and gate state |
| --- | --- | --- |
| **C1 — generated public API violates the canonical tenant boundary** | FR12 requires every generated controller to enforce NFR2 before routing or state access; FR15 prohibits a synthetic `system` tenant for platform operations. NFR2 now defines one explicit authorized, lowercase canonical tenant, the 1-64-character grammar, every required negative case, and rejection before any existence disclosure. Stable clause FR12-C2 binds corrective work. See PRD §§6.2, 7, 7.1. | Explicitly not closed: §6.2 says existing Stories 2.2/2.4/2.5 completion labels do not satisfy the correction; G-TENANT is `FAIL/BLOCKED`; OR20 requires reopened or successor stories plus compiled/runtime evidence. Current contradiction remains visible in `src/Hexalith.EventStore.RestApi.Generators/RestApiControllerEmitter.cs` and its generator tests. |
| **C2 — generated `Location` can select `CorrelationId` as command identity** | FR12 and stable clauses FR12-C3/C4 make valid canonical `MessageId` the sole selector, make `CorrelationId` diagnostic only, and require safe omission/rejection for missing or invalid identity. See PRD §§6.2 and 7.1. | Explicitly not closed: G-STATUS-ID is `FAIL/BLOCKED`; OR21 requires Story 2.9 to reopen or gain a corrective successor and requires compiled/runtime negative evidence. The current fallback remains recorded at `src/Hexalith.EventStore.RestApi.Generators/RestApiControllerEmitter.cs:281` and is pinned by `tests/Hexalith.EventStore.RestApi.Generators.Tests/RestApiControllerGenerationTests.cs`. |
| **C3 — reproduced silent append loss has no delivered guard** | NFR7 distinguishes append-race loss from admission fencing and says observation, deferral, or risk prose cannot satisfy the requirement. G-APPEND permits only provider-portable fencing or a mechanically enforced no-second-writer envelope, with production-path prevention evidence and no risk-acceptance waiver. OR4 assigns the required persistence-owned planning slice. See PRD §§7, 11.4, and 12. | Deliberately unresolved: G-APPEND is `FAIL/BLOCKED`; the reproduced `same-key-overwrite-raw-durable-write-lost` result and unowned DW-326 remain evidence of the blocker, not evidence of closure. MVP completion, readiness, release, and deployment remain blocked. |
| **C4 — OQ8 normative bytes and validation are not reproducible** | §1.1 records the external repository, path, commit, and digest and expressly says unavailable bytes and incomplete identity propagation invalidate closure. G-OQ8 requires available immutable bytes or an approved content-addressed equivalent across PRD, architecture, epics, packet, validator, and CI; OR11 owns that correction. | Deliberately unresolved: G-OQ8 is `FAIL/BLOCKED`; the PRD records that `python3 tools/validate-oq8-platform-evidence.py --pre-review` exits 1 on `docs/guides/configuration-reference.md` drift. No Story 4.15, release, consumer-handoff, or readiness authority is claimed. |

## High Findings

| Finding | PRD reconciliation | Delivery and gate state |
| --- | --- | --- |
| **H1 — backward compatibility omits most public platform surfaces** | NFR12 now inventories .NET source/binary APIs, generator attributes/diagnostics/output, OpenAPI/REST/status/header/problem contracts, gateway/domain endpoints, envelopes/serialization, DAPR methods/topics/CloudEvents, and NuGet identities/assets/dependencies/TFMs. It defines additive, deprecation, removal, and SemVer-major rules. | Explicitly not closed: G-COMPAT is `FAIL/BLOCKED`; OR22 requires the manifest-backed inventory, API/wire baselines, representative package-only consumers, and an enforcing release lane. |
| **H2 — NFR3 excludes Tenants and future JWT-binding hosts** | NFR3 is capability-scoped to every externally reachable or JWT-binding host and expressly includes EventStore, Admin, Sample, Tenants, generated fixtures, and future hosts. It requires one shared versioned contract and fingerprint and rejects hand-written subsets. | Explicitly not closed: G-AUTH-HOSTS is `FAIL/BLOCKED`; OR23 requires all-host adoption and conformance evidence. Story 5.3 cannot close the wider contract, and the current Tenants host remains relevant at `references/Hexalith.Tenants/src/Hexalith.Tenants.Api/Program.cs`. |
| **H3 — consumer-removal authority is weaker than AD-22** | FR36 now separates platform capability, release/deployment authority, and per-consumer removal authorization. It requires the consumer repository/commit, mode matrix, removal digest, role-registry identity, authenticated consumer-owner receipt, outcome, issuance, validity/expiry, and invalidation. The glossary defines Consumer owner. | Explicitly not closed: §6.8 labels per-consumer authority `OPEN`; G-CONSUMER is `FAIL/BLOCKED`; OR24 leaves the authority slice unassigned until implemented and evidenced. No consumer removal is authorized. |
| **H4 — prior reject provenance did not describe one coherent current baseline** | Frontmatter binds the 2026-09-10 assessment to `293c69c42d35dee26682d42f05c943c0b65786f4` and labels it `examined-dirty-unapproved`. §0 separates document finality from readiness and denies authority to the dirty observation. G-BASELINE requires a clean, content-bound, approved manifest; G-READINESS requires all mandatory rows to pass before a new report can compute `READY`. | Corrected as truthful current status, not closed as a baseline: G-BASELINE and G-READINESS remain `FAIL/BLOCKED`; OR14/OR15 retain cross-artifact and lifecycle reconciliation, and OR1 permits a readiness rerun only after all mandatory gates pass. The old 2026-08-01 `READY` result is explicitly historical and non-authorizing. |

## Remaining Input Gaps

**None at the PRD requirement-and-gate level.** Every Critical and High finding in the input has a stable requirement or clause, an explicitly failed gate, corrective ownership, and an invalidation/closure condition. The unresolved implementation, evidence, lifecycle, and cross-artifact work above is intentionally preserved as blocked delivery work; this reconciliation does not convert any of it into closure.

## Fail-Closed Readiness Posture

The PRD frontmatter remains `implementation_readiness_status: blocked` and `implementation_readiness_result: reject` (`prd.md:5-6`). Section 0 states that document finality is independent of readiness and authorizes no `READY`, MVP-complete, release, deployment, consumer-migration, or dependent-handoff claim. Section 11.4 requires every mandatory gate to pass against one clean, content-bound, approved source set; G-READINESS currently computes **`FAIL/BLOCKED — aggregate Reject`**. Section 12 retains the corrective blockers and requires OR1 to be last. This report therefore confirms input coverage only; it supplies no implementation evidence and changes no readiness result.
