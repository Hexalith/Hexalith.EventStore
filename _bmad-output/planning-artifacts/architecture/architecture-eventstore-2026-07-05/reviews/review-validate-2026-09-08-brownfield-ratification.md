# Review — Brownfield Ratification + Input Reconciliation (VALIDATE gate, 2026-09-08)

Lens: ad-hoc brownfield-ratification / input-reconciliation. Subject: `_bmad-output/planning-artifacts/architecture.md` (610 lines, 25 ADs, `status: final`, `updated: 2026-08-29`). Read-only; no builds, tests, edits, or submodule operations were performed.

## Verdict

**PASS-WITH-FINDINGS** — the spine ratifies almost every named seam, but it names one AD-19 result shape that does not exist anywhere in the code, and it is silent on a new deployable host (`Hexalith.EventStore.Operations`) that no AD, story, AppHost, DAPR YAML, or brownfield doc governs.

## Method

1. Read the full spine (lines 1-610) and anchored every checked assertion.
2. JOB A: for each AD-named type/seam/value, `grep -rlw` across `src/`, `samples/`, and `tests/` (excluding `obj/`), then read the declaring file to compare names, enum values, defaults, and routes. Read the `.csproj` and `Program.cs` / top-level files of the four `src/` projects and three `samples/` entries the Structural Seed omits. Compared `tools/release-packages.json`, `.github/workflows/release.yml` (`container-projects`), `Directory.Build.props`, `ServiceDefaults/Extensions.cs`, `AppHost/Program.cs`, `Aspire/HexalithEventStoreExtensions.cs`, `deploy/dapr/*`, `samples/dapr-components/*`, `samples/deploy/*` against AD-9/AD-11/AD-16/AD-21/AD-24.
3. JOB B: read `sprint-change-proposal-2026-09-07.md` in full; skimmed `epic-3-retro-2026-09-07.md` (findings + action items + verdict) and `sprint-change-proposal-2026-08-20-commit-message-line-limits.md`; extracted the current status of every story key the spine names from `sprint-status.yaml`; `ls`-verified every local path in the spine's `sources:`; checked `git log -1` dates for `docs/brownfield/architecture.md` and `integration-architecture.md`; checked `spec-shared-payload-protection-engine.md` frontmatter for the AD-23 gate.
4. Used the parent-verified repository facts from the brief (SDK, catalog versions, DAPR versions) without re-deriving.

## Findings

### H1 — high — AD-19's normalized result (`ProjectionDispatchResult` / `ProjectionDispatchResultEntry` / `ProjectionCheckpointAdvanceState`) exists nowhere in the code

- **Evidence:** `architecture.md:290` ("The server then emits one exact normalized result: `ProjectionDispatchResult(Version, Entries)` with `Version = 1`, where every `ProjectionDispatchResultEntry` contains … `ProjectionCheckpointAdvanceState` (`NotAdvanced = 0`, `Advanced = 1`)"), `architecture.md:292` (`Entries` bounded by `MaxOutcomes`), `architecture.md:480` (Consistency Conventions: "the normalized `ProjectionDispatchResult` makes each route's `Advanced`/`NotAdvanced` checkpoint decision explicit"). `grep -rlw` over `src/`, `samples/`, `tests/` for all three identifiers and for `NotAdvanced` returns **zero** files. What exists: `src/Hexalith.EventStore.Contracts/Projections/ProjectionDispatchResponse.cs` (`Version`, `Outcomes`), `ProjectionDispatchOutcome.cs` (`ProjectionType`, `Status`, `State`, `ReasonCode`), `ProjectionDispatchStatus.cs` (`Completed=0 … Failed=4`, matches), `src/Hexalith.EventStore.Client/Projections/ProjectionDispatchOptions.cs:14,41` (`DefaultMaxOutcomes = 32`, matches), `/project/v2` mapped at `src/Hexalith.EventStore.DomainService/EventStoreDomainServiceExtensions.cs:242`. Checkpoint advancement is decided inside `src/Hexalith.EventStore.Server/Projections/NamedProjectionDispatchCoordinator.cs` against `IProjectionDeliveryCheckpointStore`, with no named result type. The memlog records the shape as a 2026-07-15 reviewer-gate decision; `epics.md` names no story that creates it (line 168 is narrative only) and Story 1.18 (delivery idempotency) is `done`.
- **Why it diverges:** a domain-service author or a Server contributor following AD-19 will look for, or invent, a `ProjectionDispatchResult`/`Advanced` type; the Server contributor already made the same decision under different names. Two readers of the spine cannot agree whether the normalized shape is a wire-visible contract that is missing, or an internal decision that is present under other names.
- **Disposition:** discuss. Either (a) mark the normalized shape PLANNED and name the owning story (none exists today — Stories 6.3/6.4 are the nearest, both `backlog`), or (b) re-ratify AD-19 around the code: keep the wire contract sentence and rewrite the normalization sentence as "the server's dispatch coordinator applies the status/checkpoint matrix below per admitted route and advances the route checkpoint only after …", dropping the phantom type names, and delete the `ProjectionDispatchResult` reference at line 480.

### H2 — high — `Hexalith.EventStore.Operations` is a new deployable host that no AD, seed row, story, AppHost resource, DAPR YAML, or brownfield doc governs

- **Evidence:** `src/Hexalith.EventStore.Operations/Hexalith.EventStore.Operations.csproj` — `Microsoft.NET.Sdk.Web`, `IsPublishable=true`, `EnableContainer=true`, `ContainerRepository=eventstore-operations`, references `Dapr.Actors.AspNetCore` + `Admin.Abstractions` + `ServiceDefaults`. `Program.cs` registers `DeadLetterDrainActor` (writes actor state directly through `StateManager`, `Actors/DeadLetterDrainActor.cs:64-97`), `MapSubscribeHandler`, `MapDeadLetterOperations`, and a `DaprAppChannelTokenMiddleware`. `Configuration/EventStoreOperationsOptions.cs:12-27` defaults: `PubSubName="pubsub"`, `TopicName="deadletter.work.events"`, `AdminCallerAppId="eventstore-admin"`, `ReplayAppId="works"`, `ReplayMethodName="work/events"`. Admin.Server already calls it (`src/Hexalith.EventStore.Admin.Server/Configuration/AdminServerOptions.cs:26` `OperationsAppId = "eventstore-operations"`; `Services/DaprDeadLetterQueryService.cs:91`). Landed at `cd52ef2c` 2026-08-28 (`feat(operations): add durable subscriber dead-letter workflow`), one day before the spine's `updated:` date. **Absent from:** `architecture.md:515-537` (Structural Seed), the AD-14/seed mermaid topologies (`architecture.md:539-582`), `src/Hexalith.EventStore.AppHost/Program.cs` (no `Operations` resource), `deploy/dapr/*` (no access-control appId, subscription, or scope for `eventstore-operations`), `docs/brownfield/architecture.md` (updated 2026-08-29, lists SignalR/Testing.Integration/Admin.Server.Host but not Operations), `epics.md`/`sprint-status.yaml` (no story key; the only dead-letter AC near `epics.md:3084` belongs to Story 4.4's AggregateActor publication drain, a different mechanism), and `.github/workflows/release.yml:135-136` `container-projects` (only `eventstore`). Story 4.7's spec deferred the gap as ECH-01/ECH-02/VG-01 (`spec-4-7-tenants-query-provenance-follow-up.md:118-124`: "Admin invokes `eventstore-operations`, but the standard AppHost contains no Operations project or sidecar … AppHost pub/sub and state-store component scopes omit `eventstore-operations`").
- **Why it diverges:** (i) AD-9 (`architecture.md:141-145`) says runtime topology is one unit owned by AppHost + DAPR YAML; a subscribing, actor-hosting sidecar with no AppHost resource, ACL entry, or component scope is the exact split AD-9 prevents. (ii) AD-19 (`architecture.md:309`) says "No consuming-domain-specific logic enters EventStore", yet the platform ships a `works` app-id and `work/events` route as defaults. (iii) AD-11 (`architecture.md:159`) names only `eventstore-admin-ui` as a possible future released image; two other projects (`Operations` → `eventstore-operations`, `Admin.Server.Host` → `eventstore-admin`) declare `EnableContainer` + `ContainerRepository` and are outside `container-projects`. (iv) AD-3/AD-10: Operations exposes admin-computation endpoints guarded only by a DAPR app-channel token, not the application-layer credential AD-10 requires for admin-computation endpoints — whether that is a conforming "internal" surface is undecided.
- **Disposition:** discuss. Suggested wording if adopted as a seed/AD amendment: add seed row `Hexalith.EventStore.Operations/   # DAPR-hosted subscriber dead-letter capture/replay workload (eventstore-operations); AppHost/ACL/scope wiring pending (Story 5.6/5.7)`, add an `Operations` node to the topology diagram with an AdminServer → Operations "service invocation" edge, extend AD-11 line 159 to name `eventstore-admin` and `eventstore-operations` as declared-but-unreleased container repositories, and add to AD-19 or AD-2 that dead-letter replay targets are configuration, never platform defaults naming a consumer domain.

### M1 — medium — Structural Seed omits four `src/` projects (three of them release-inventory packages) and three `samples/` entries

- **Evidence:** `architecture.md:515-537` lists 10 `src/` rows and 4 `samples/` rows. Real tree: 20 `src/` projects and 7 `samples/` entries. Omitted: `Hexalith.EventStore.SignalR` (`Microsoft.NET.Sdk` class library, `EventStoreSignalRClient` + `ProjectionChangedDetail`; package #4 of the 14-package inventory per `docs/brownfield/architecture.md:80`), `Hexalith.EventStore.Testing` (named in the capability map at `architecture.md:588` but not in the seed; inventory package), `Hexalith.EventStore.Testing.Integration` (inventory package; DAPR/Aspire fixture harness), `Hexalith.EventStore.Admin.Server.Host` (container `eventstore-admin`, the resource AppHost actually adds at `AppHost/Program.cs:93`), `Hexalith.EventStore.Operations` (H2), `samples/Hexalith.EventStore.Sample.Tests` (quickstart smoke), `samples/dapr-components/{redis,postgresql}` (statestore + pubsub YAML), `samples/deploy/{azure,kubernetes}` (bicep, k8s templates incl. `secrets-template.yaml`).
- **Classification:** `.SignalR` — harmless lag, fits AD-8 (client consumes `ProjectionChanged` signal and additive `ProjectionChangedDetail`, backward compatible with signal-only clients). `.Testing` / `.Testing.Integration` — harmless lag but the seed's `tests/` row ("per-project tests") hides that two packaged test harnesses ship to consumers. `.Admin.Server.Host` — harmless lag; fits AD-21/AD-10 (JWT bearer, `ContainerRepository=eventstore-admin`). `.Operations` — see H2. `samples/dapr-components` + `samples/deploy` — lag, but `samples/deploy/kubernetes/secrets-template.yaml:61` ships a `secretstores.kubernetes` component, which a reader may mistake for the AD-24 overlay (see M3).
- **Why it diverges:** the seed is what a new contributor uses to place code; three packaged libraries with no seed row invite a duplicate "where do integration fixtures live" decision (the Tenants consumer already depends on `Testing.Integration`).
- **Disposition:** autofix. Add rows: `Hexalith.EventStore.SignalR/            # SignalR client for projection-changed signals and additive detail (AD-8)`, `Hexalith.EventStore.Testing/            # unit test kit (packaged)`, `Hexalith.EventStore.Testing.Integration/ # DAPR/Aspire integration fixtures (packaged)`, `Hexalith.EventStore.Admin.Server.Host/  # eventstore-admin container host`, plus `samples/dapr-components/` and `samples/deploy/` rows labelled "sample overlays — not the AD-24 platform overlay".

### M2 — medium — AD-21 FrontComposer composition and the `event-store-admin` module identity are PLANNED but written as present state

- **Evidence:** `architecture.md:321` ("The UI composes `Hexalith.FrontComposer.Shell` and `Hexalith.FrontComposer.Contracts.UI` … `Admin.UI` owns … the stable FrontComposer module identity `event-store-admin`"), `architecture.md:214-218` (AD-14 diagram draws `AdminUI --> FrontComposerShell/ContractsUI` as existing dependencies), `architecture.md:482` (Consistency Conventions, present tense). Code: `src/Hexalith.EventStore.Admin.UI/Hexalith.EventStore.Admin.UI.csproj:18-28` references Admin.Abstractions, Client, ServiceDefaults, SignalR, FluentUI, FluentUI.Icons, SignalR.Client, JwtBearer — **no FrontComposer package or project**; the string `event-store-admin` appears in no `.cs`/`.razor`/`.json` under Admin.UI; the only `FrontComposer` mention in `src/` is an XML comment in `Aspire/HexalithEventStorePlatformExtensions.cs:27`. Owning Stories `7-14`, `7-19`, `7-20` are all `backlog` (`sprint-status.yaml:218,224,225`). Catalog: `references/Hexalith.Builds/Props/Directory.Packages.props:53-57` lists FrontComposer Contracts/Mcp/Shell/SourceTools/Testing — still no `Contracts.UI`, consistent with AD-21's "absent from the catalog today". Stack row `architecture.md:506` says `4.1.1`; catalog is `4.3.0` at the committed gitlink and `4.4.0` in the worktree (parent-verified), and AD-21 points readers at "current value in the Stack table".
- **Why it diverges:** the resource identity `eventstore-admin-ui` (`AppHost/Program.cs:94`, `Aspire/HexalithEventStoreExtensions.cs:294`) and Fluent UI V5 are RATIFIED; the shell composition and module identity are not. A UI contributor reading AD-21 as ratified will look for a shell registration that does not exist; a Builds consumer reading the Stack row will pin the wrong version.
- **Disposition:** autofix. Prefix the composition sentence with "Under Stories 7.14/7.19/7.20 (backlog as of 2026-09-08), the UI will compose …" and change the AD-14 diagram edges to dashed "planned" edges; replace `4.1.1` at line 506 with the catalog value at the committed gitlink (`4.3.0`) or, better, with "see catalog" since the table already says the catalog always wins.

### M3 — medium — AD-24 OpenBao is entirely PLANNED (Story 7.6 `backlog`) but its artifacts are asserted as existing

- **Evidence:** `architecture.md:386` ("the value-free contract at `deploy/dapr/openbao-secret-contract.yaml`"), `:394` ("The Aspire AppHost provisions a pinned official OpenBao container … waits for it … makes the canonical `openbao` DAPR component available"), `:478` (Secrets convention), `:549-553` (topology diagram nodes `DaprSecrets`/`OpenBao`). Checks: `deploy/dapr/openbao-secret-contract.yaml` — **missing**; `grep -rli openbao` over `deploy/`, `samples/dapr-components/`, `samples/deploy/`, `src/Hexalith.EventStore.AppHost/`, `src/Hexalith.EventStore.Aspire/` — **zero hits**; no `name: openbao` component anywhere; `deploy/dapr/` holds statestore-cosmosdb/postgresql, pubsub-kafka/rabbitmq/servicebus, resiliency, three accesscontrol files and two subscriptions only. `sprint-status.yaml:209` `7-6-openbao-backed-dapr-secret-store: backlog`. The only secret-store component in the tree is `samples/deploy/kubernetes/secrets-template.yaml:61` `type: secretstores.kubernetes`, which AD-24 explicitly demotes to bootstrap-only.
- **Why it diverges:** the spine reads as if the overlay exists; a deployment-hardening contributor cannot tell whether to create the contract file or find it. The kubernetes sample overlay, unlabelled, is the nearest thing a reader will find and it is the pattern AD-24 forbids for application secrets.
- **Disposition:** autofix. Add one sentence at the top of the AD-24 rule: "Status 2026-09-08: no `openbao` component, contract file, or AppHost provisioning exists yet; Story 7.6 (backlog) creates them, and `samples/deploy/kubernetes/secrets-template.yaml` is a sample using the Kubernetes store that does not satisfy this decision."

### M4 — medium — AD-23's "Story 8.2 implementation is blocked until … records named approvals" is stale; the gate has been satisfied

- **Evidence:** `architecture.md:378`. `_bmad-output/implementation-artifacts/spec-shared-payload-protection-engine.md:4-6,27-30`: `status: approved-authorized`, `story_8_2_authorized: true`, "ADR status ADOPTED AND APPROVED", "Story 8.2 initial authorization AUTHORIZED for exact-digest preflight and implementation". `sprint-status.yaml:230-232`: `8-1 … done`, `8-2 … backlog`.
- **Why it diverges:** a reader of the spine believes Story 8.2 is architecturally blocked; the spec and tracker say it is authorized and merely unscheduled. Not a code phantom, but a false gate.
- **Disposition:** autofix. Rewrite to: "Story 8.2 implementation required `spec-shared-payload-protection-engine.md` to record named approvals and explicit authorization for the content-bound digest; that authorization was recorded (`status: approved-authorized`, 2026-08-01). Later Stories 8.3-8.11 require their predecessor evidence."

### L1 — low — Story-status adjectives in the spine lag the tracker (AD-25 "active Stories 4.9-4.15", AD-22 Story 3.15)

- **Evidence:** `architecture.md:58` and `:402` bind "active Stories 4.9-4.15"; tracker: `4-9`…`4-14` `done`, `4-15` `review` (`sprint-status.yaml:169-175`). `architecture.md:69-70,335,353-365` treat Story 3.15 as the pending positive-closure story — `3-15` is `in-progress` at 0/3 receipts, `3-16` is `backlog`; that framing is correct and unchanged by the 2026-09-07 proposal. AD-25 named concepts with code (all in `src/Hexalith.EventStore.Server/`): admission actor (`Actors/IdempotencyAdmissionActor.cs`), directory (`Actors/IdempotencyAdmissionDirectory*.cs`, `IIdempotencyAdmissionDirectoryInspectionActor.cs`), HMAC digest/protection (`Commands/IdempotencyKeyProtector.cs`, `IdempotencyExecutionContextProtector.cs`), verification tag (`Actors/IdempotencyAdmissionRecord.cs`), tombstone/purge/compaction (`Actors/IdempotencyAdmissionPurgeRequest.cs`, `IdempotencyAdmissionRecord.cs`), fence (`Commands/IdempotencyExecutionContext.cs`, `Actors/IdempotencyAdmissionAuthorityRequest.cs`, `Pipeline/SubmitCommandHandler.cs`), canonical intent/trusted descriptor (`Commands/CanonicalIdempotencyIntentEncoder.cs`, `TrustedIdempotencyDescriptor.cs`, `IdempotencyIntentAdapterRegistry.cs`), promotion phases (`Actors/IdempotencyAdmissionPromotionPhase.cs`, `…PromotionImportRequest.cs`), legacy inventory/migration (`Actors/IdempotencyLegacyInventoryActor.cs`, `IdempotencyLegacyMigrationPhase.cs`, `IdempotencyLegacySourceRedirectRecord.cs`, `IdempotencyTenantLifecycleActor.cs`), `idempotency_key_expired` (`Actors/AggregateActor.cs:622`). Not located by name: a readiness check that fails mixed software versions lacking directory routing (only `Projections/ProjectionDeliveryWriterProtocolHealthCheck.cs` surfaced) — unverified, not asserted absent.
- **Disposition:** autofix. Replace "active Stories 4.9-4.15" with "Stories 4.9-4.15" in both places; the spine should not carry lifecycle adjectives the tracker owns.

### L2 — low — The 2026-09-07 sprint change proposal and Epic 3 retrospective are not in `sources:` although they are the newest approved inputs re-establishing FR36's open state

- **Evidence:** `architecture.md:11-45` (`sources:` ends at `sprint-change-proposal-2026-08-29.md`). `sprint-change-proposal-2026-09-07.md` §2 "Artifact conflicts": "**Architecture** (`architecture.md:354`, `:591`) — no change. Already states 3.15 gates positive FR36 parity." Verified: the proposal changes tracker rows only (3.15 `done`→`in-progress`, 3.16 added `backlog`, guard-bound comment blocks restored, spec 3.15 frontmatter), and every one of those is consistent with `architecture.md:69-70`, `:335`, `:365`, `:591`. The retro's only architecture references are the same two anchors (`epic-3-retro-2026-09-07.md:826-827`). Retro AGG-8 reports two parallel canonical-bytes codecs (`tools/deployed_runtime_parity_handlers/v1.py`, `tools/release_evidence_handlers/v3.py`) against the "single versioned `ReleaseEvidenceCodec`" sentence at `architecture.md:163`; the identifier `ReleaseEvidenceCodec` appears in no file under `tools/`, `src/`, `tests/` (the tools use `CODEC_VERSION` constants). That is an AD-11 "single authority" assertion the code does not ratify, already deferred by the retro to an authorized consolidation story after 3.15 closes.
- **Why it matters:** the spine's own frontmatter says it is `final` as of 2026-08-29; the newest approved inputs confirm rather than contradict it, but a reader has no pointer to the document that establishes FR36 is still open on 2026-09-08 and that the AD-11 codec sentence is aspirational.
- **Disposition:** autofix (citation only). Append `sprint-change-proposal-2026-09-07.md` and `implementation-artifacts/epic-3-retro-2026-09-07.md` to `sources:`; optionally qualify `architecture.md:163` with "(today two codec generations coexist, `v1.py`/`v3.py`; consolidation is deferred until Story 3.15 closes)". The 2026-08-20 commit-message proposal is governance only (commitlint limits; its own §2 says "PRD / Architecture / UX — No change") and correctly stays uncited.

### L3 — low — `sprint-status.yaml` guard-bound comment lines are evidence-packet contract, not an architecture invariant

- **Evidence:** proposal §7 explains the five-line block is the declared output of a frozen `awk` transform in the Story 1.20 proof packet and that `ProofPacketValidatorIntegrityTests.cs:308-316` binds test constants to those literals; the Story 3.15 digest block is a drift guard against `closure.json`. Neither concerns how two build units talk to each other; both are AD-12 evidence-integrity mechanics for one packet family.
- **Disposition:** defer/ignore for the spine. At most, one clause under AD-12 ("evidence packets may bind narrative surfaces — tracker comments, spec frontmatter — and those bindings are contract") would tell future maintainers why a comment edit can turn Contracts.Tests red; the project memory and the in-file `>>> GUARDED <<<` fences already carry this.

### L4 — low — `docs/brownfield/architecture.md` asserts "events are write-once per sequence key", which the spine's Deferred row explicitly disclaims

- **Evidence:** `docs/brownfield/architecture.md:22` (Architectural Patterns table: "Event Sourcing — … events are write-once per sequence key") vs `architecture.md:604` (Deferred: "AD-5 preserves the sole-owner append path but does not claim physical write-once enforcement" after Story 4.5 proved silent overwrite on `state.redis`). The brownfield doc was last touched 2026-08-29 (`9fc0edb8`), same day as the spine; `integration-architecture.md` last changed 2026-07-30 (`59301827`) and predates Operations, the OQ8 admission stories, and AD-24/AD-25 — harmless lag, nothing there contradicts the spine (its Admin "reads go direct to state store" row matches `architecture.md:571`).
- **Disposition:** discuss (belongs to the brownfield doc's owner, not the spine). The spine could add "docs/brownfield/architecture.md's write-once claim is superseded by the AD-5 deferred row" beside the citation.

## Passes

JOB A — RATIFIED (code matches the spine):

- AD-2: `AddEventStoreDomainService` (`src/Hexalith.EventStore.DomainService/EventStoreDomainServiceExtensions.cs:66,78`) and `UseEventStoreDomainService` (`:121`) exist with those names; used in 9/6 files across `src/` and `samples/`.
- AD-4/AD-17/AD-18: `IEventStoreGatewayClient` (`Client/Gateway/IEventStoreGatewayClient.cs:10`), `AddEventStoreGatewayClient` (`Client/Registration/EventStoreServiceCollectionExtensions.cs:29`, returns `IHttpClientBuilder`), `AddEventStoreDaprServiceInvocation` with an optional `apiToken` argument (`:63`), `ICommandStatusLocationBuilder` (`Client/Gateway/ICommandStatusLocationBuilder.cs:9`); `CommandStatusLocationBuilder.cs:28` builds exactly `{base}/api/v1/commands/status/{statusKey}`.
- AD-7: `IQueryCursorCodec` (`Client/Queries/IQueryCursorCodec.cs:21`), `QueryCursorScope` (`Client/Queries/QueryCursorScope.cs:23`), `IReadModelStore` (`Client/Projections/IReadModelStore.cs:23`), `ReadModelWritePolicy` (`Client/Projections/ReadModelWritePolicy.cs:28`).
- AD-14/AD-15: `QueryResponseMetadata` (`Contracts/Queries/QueryResponseMetadata.cs:18`), `QueryResponseProvenance { Unknown=0, ProjectionBacked=1, HandlerComputed=2 }` (`Contracts/Queries/QueryResponseProvenance.cs:17-29`), `SelfRoutingETag.GenerateNew(string projectionType)` (`Server/Queries/SelfRoutingETag.cs:29`), `HandlerAwareQueryRouter` leaves `ProjectionType: null` (`src/Hexalith.EventStore/Queries/HandlerAwareQueryRouter.cs:80`), `IReadModelFreshness` (`Client/Projections/IReadModelFreshness.cs:22`), `ReadModelFreshnessExtensions` (`Client/Projections/ReadModelFreshnessExtensions.cs:11`).
- AD-19 wire contract half: `ProjectionDispatchResponse(Version, Outcomes)`, `ProjectionDispatchStatus` values `Completed=0, AlreadyCompleted=1, Retryable=2, Indeterminate=3, Failed=4`, `ProjectionDispatchOptions.DefaultMaxOutcomes = 32` (validated positive, `:98-100`), route `POST /project/v2` (plus `/project/v2/reconcile`).
- AD-11: `tools/release-packages.json` → `packages` array of exactly **14**; `release.yml:128` `expected-package-count: 14`; `release.yml:135-136` `container-projects` = single `src/Hexalith.EventStore/Hexalith.EventStore.csproj|eventstore`; `Directory.Build.props:48-51` resolves unset `UseHexalithProjectReferences` to `false` in every configuration including Debug, exactly as `architecture.md:157` states; `require-publication-authority: false` matches the release-dispatch memory.
- AD-16: `ServiceDefaults/Extensions.cs:23-25,156,184-186` — `MapDefaultEndpoints` maps `/health`, `/alive`, `/ready`, each `.AllowAnonymous()`.
- AD-21 (identity half): AppHost resource names `eventstore`, `eventstore-admin`, `eventstore-admin-ui`, `sample`, `sample-api`, `sample-blazor-ui`, `security` (`AppHost/Program.cs:86-237`, `Aspire/HexalithEventStoreExtensions.cs:215-294`, `HexalithEventStoreSecurityOptions.cs:11`) match the topology diagram at `architecture.md:541-548`; Admin.UI uses Fluent UI (`AdminUIServiceExtensions.cs`, `_Imports.razor`).
- AD-25: substantially implemented (see L1 list); Stories 4.9-4.14 `done`, 4.15 `review`.
- Stack rows spot-checked against the catalog: `Hexalith.Commons.UniqueIds` 2.30.0 (`Directory.Packages.props:6,36`), `OpenTelemetry.Instrumentation.StackExchangeRedis` `1.18.0-beta.1` (`:275`), SignalR `10.0.11` (`:192`); parent-verified rows (SDK, Aspire, DAPR, MediatR, FluentValidation, Roslyn, FluentUI, coverage, NBomber, NSubstitute, Shouldly, xunit.v3) accepted as given.

JOB B — held:

- `sources:`: all 27 local paths exist (`ls` OK on every entry).
- The 2026-09-07 proposal's "no architecture content change" claim is true against every AD it touches; the spine's Story 3.15/3.16/FR36 framing (`architecture.md:69-70,335,365,591`) is exactly what the proposal restored the tracker to.
- No story the spine describes as future shows `done`: 6.x, 7.6, 7.14/7.19/7.20, 8.2-8.11 are `backlog`; 5.4 `review`; 4.7 `in-progress`; 3.13/3.14 `done` as AD-22 narrates. The only inverse case is AD-23's satisfied gate (M4).
- `spec-shared-payload-protection-engine.md` (named at `architecture.md:378`) exists and carries the authorization the AD demands.
- The 2026-08-20 commit-message proposal is correctly uncited (governance only).
- Deterministic lint reported 0 findings (per brief); nothing here contradicts it.
