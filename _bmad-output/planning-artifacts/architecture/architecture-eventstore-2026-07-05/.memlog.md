---
scope: Hexalith.EventStore Phase 4 implementation readiness recovery
purpose: build-substrate
altitude: feature
source: _bmad-output/planning-artifacts/implementation-readiness-report-2026-07-05.md
updated: 2026-09-09T11:31
---

- (event) Fast path selected by user; user said continue after deliverable question, so assume the architecture spine plus readiness-discoverable mirror is the only deliverable for this run.
- (constraint) PRD is authoritative for Phase 4 FR1-FR35 and NFR1-NFR18; epics.md owns story slicing and sequencing; architecture must own component, integration, topology, and decision-record gates.
- (constraint) Brownfield code and docs already adopt CQRS + DDD + event sourcing on DAPR actors/pubsub/state with Aspire AppHost orchestration; architecture spine ratifies and tightens this pattern rather than introducing a new starter.
- (constraint) Hexalith LLM rules bind this run: use Hexalith.EventStore.slnx only for restore/build, keep package versions centralized, do not recurse submodules, do not modify submodule files without approval, and use ULID-safe handling for EventStore identifiers.
- (constraint) Hexalith UX rules bind UI-facing architecture: module UI uses FrontComposer and Blazor Fluent UI V5, prefers existing components over raw controls/CSS/JS, does not redefine theme primitives, and groups multi-section page surfaces with FluentAccordion.
- (version) Former repository seed (pre AD-11 baseline move): global.json pinned .NET SDK 10.0.299 with rollForward latestPatch; Directory.Build.props targets net10.0, nullable and implicit usings enabled, TreatWarningsAsErrors true, package version 3.31.0 when unset.
- (version) Repository reality: imported Hexalith.Builds package props pin Aspire.Hosting 13.4.6, Aspire Keycloak/Kubernetes 13.4.6-preview.1.26319.6, CommunityToolkit.Aspire.Hosting.Dapr 13.4.0-preview.1.260602-0230, Dapr Client/AspNetCore/Actors 1.18.4, MediatR 14.2.0, FluentValidation 12.1.1, Microsoft.AspNetCore SignalR 10.0.9, OpenTelemetry 1.16.0 core packages, FluentUI Blazor 5.0.0-rc.4-26180.1, xUnit v3 3.2.2, Shouldly 4.3.0.
- (version) Official docs reality-check: Aspire AppHost owns service discovery, dependency ordering, config injection, and health monitoring; Aspire Dapr integration configures sidecars; Dapr official docs support actors, state store, and pub/sub as intended building blocks; SignalR official docs support hub groups; Roslyn docs direct generators to incremental source generators.
- (decision) Adopt DAPR-backed hexagonal event-sourcing platform as the spine paradigm; binds all Phase 4 epics and prevents one team treating EventStore as a CRUD/web API while another builds actor-owned event sourcing.
- (decision) EventStore platform libraries own hosting, DAPR endpoint routing, discovery, telemetry, health, read-model store, cursor codec, projection dispatch, domain-event consumer plumbing, and Aspire wiring for domain modules; binds FR1-FR10 and prevents per-domain boilerplate forks in Sample, Tenants, or custom domains.
- (decision) Domain modules own only domain behavior and contracts: aggregates, commands, events, projections, query handlers, validators, and domain-specific options; a conforming domain-service host calls AddEventStoreDomainService then UseEventStoreDomainService; prevents domain modules from shipping reusable AppHost/Aspire/ServiceDefaults/projection/query actor/state-store/cursor/telemetry infrastructure.
- (decision) The EventStore gateway remains the policy boundary for command/query submission, tenant/RBAC validation, idempotency, status/archive, ETag/query behavior, problem details, and observability; generated APIs, UI hosts, admin writes, and domain services must not bypass it; binds FR11-FR16, FR23-FR32, NFR1-NFR4, NFR14.
- (decision) Generated REST controllers are emitted only into dedicated external API hosts and must delegate to IEventStoreGatewayClient; interactive UI hosts consume EventStore Client libraries directly and host no generated or hand-written per-message MVC command/query controllers; binds FR13-FR15, NFR14 and prevents UI-host/BFF controller drift.
- (decision) AggregateActor owns durable event mutation: it invokes pure domain processors/domain services, persists write-once events and metadata, assigns aggregate sequence/global position through server services, publishes CloudEvents, and records recovery state; domain code never writes events/state stores directly; binds FR23, FR27, FR29-FR31 and prevents split-brain persistence.
- (decision) Persisted event identity is stable: aggregate sequence is gapless per aggregate, GlobalPosition is non-zero and currently allocated by the DAPR-backed global allocator, CloudEvent id uses persisted event MessageId, and duplicate command replies preserve original result fields; binds FR23 and inherited global-ordering spec while preventing subscriber dedupe and replay divergence.
- (decision) Global-position scaling is spec-gated: the approved single global allocator remains binding until a renegotiated frozen spec chooses tenant/domain sharding and updates spec-dapr-global-event-ordering before code changes; binds FR24 and prevents incompatible position semantics between producers and consumers.
- (decision) Read models and query cursors use platform seams: IReadModelStore plus ReadModelWritePolicy for ETag-aware optimistic merge writes, IQueryCursorCodec plus QueryCursorScope for DataProtection-backed opaque scoped cursors; binds FR5-FR6, NFR8 and prevents hand-rolled state-store/cursor formats.
- (decision) Projection and domain-event consumers are at-least-once/ unordered and idempotent: handlers deduplicate by EventStore MessageId, sequence only where domain semantics make it meaningful, and projection notifications are metadata-only freshness nudges, never proof of command success; binds FR7, FR16, NFR5-NFR6, NFR12.
- (decision) Runtime topology is owned by AppHost plus DAPR component/configuration YAML: app IDs, sidecar component access, pub/sub scopes, ACL files, keyPrefix posture, placement/scheduler endpoints, and publish targets are changed and tested together; binds FR8, FR19-FR20, FR32, NFR2, NFR17 and prevents local/test/prod topology mismatch.
- (decision) Security fails closed above infrastructure scoping: public, internal, domain-service, projection-notification, and admin-computation endpoints require application-layer credentials and tenant authorization before data disclosure; never trust caller-supplied administrator flags or network location alone; binds FR26, FR28, NFR1-NFR4.
- (decision) Admin surfaces are operational facades, not privileged shortcuts: state-mutating admin actions delegate to gateway-owned write paths where applicable, audit authenticated user/tenant/action/outcome/correlation, keep support-safe output, and hide/disable or return 501 for deferred operations; binds FR34, NFR15.
- (decision) Release and dependency mode are manifest-governed: tools/release-packages.json is the package inventory, Release uses package references by default, source project references require explicit UseHexalithProjectReferences=true, and release validation must not publish submodule packages; binds FR10, FR21-FR22, FR25, NFR9-NFR11.
- (decision) High-risk verification must inspect persisted end-state evidence: Redis/state-store/read-model/CloudEvent bodies, topology YAML/sidecar arguments, package outputs, and security denials; API 202/200 or mock call counts are insufficient for Tier 2/3 and readiness-critical NFR coverage; binds NFR7, NFR10, NFR16 and prevents smoke tests being mistaken for integration proof.
- (decision) Cost and evolution changes are spec-first: folded snapshots, projection delivery cost, projection sequence guards, event schema versioning/upcasting, identity metadata validation, and cancellation-token public seam changes must have approved specs at named paths before implementation stories begin; binds FR33 and prevents irreversible persistence/API changes from story-local assumptions.
- (assumption) Because the user chose Fast path and said continue, unresolved purpose/audience question is treated as no extra audience artifact; if a deck/report is needed later, derive it from this spine and memlog without renumbering ADs.
- (question) UX artifact remains a separate readiness blocker per PRD; this architecture spine will bind UI-host boundaries and support-safe/projection-confirmed rules but will not replace ux.md.
- (event) Draft architecture spine written and mirrored to _bmad-output/planning-artifacts/architecture.md for readiness discovery.
- (decision) Read-model and query evidence metadata must cross the gateway through platform result/header contracts, not ad hoc payload fields; binds FR4-FR6, FR15, FR34 and prevents domain handlers, gateway, and UI hosts from disagreeing about freshness, projection version, paging, ETag, or projection-confirmed state.
- (event) Reviewer gate passed after applied fixes: dependency diagram direction clarified, OpenTelemetry stack row corrected, and AD-14 added for query/read-model evidence metadata across the gateway.
- (event) spine finalized
- (constraint) The final PRD updated 2026-07-13 remains authoritative for FR1-FR36 and NFR1-NFR18. Its FR36 parity gate, route-bound lifecycle evidence, asynchronous named projections, and replay-equivalent paged rebuild obligations are already governed by AD-7, AD-8, AD-14, AD-15, AD-19, and AD-20; preserve those IDs and rules.
- (constraint) The approved sprint-change proposal dated 2026-07-15 changes no PRD or canonical UX scope and directs architecture changes only for deterministic projection-dispatch result shape and consolidated EventStore UI ownership with FrontComposer.
- (version) Reality/currentness check 2026-07-15: src/Hexalith.EventStore.Admin.UI is the existing net10.0 publishable UI; AppHost and container repository both use eventstore-admin-ui. The root-declared Hexalith.FrontComposer source contains net10.0 Shell and Contracts.UI projects that depend on Microsoft Fluent UI Blazor; the official Microsoft fluentui-blazor dev-v5 branch remains active while EventStore locally pins its approved v5 RC.
- (decision) Amend AD-19 to make ProjectionDispatchResponse the sole versioned bounded result: outcomes are ordered by admitted route order, contain exactly one entry per (Domain, ProjectionType), use the closed stable ProjectionDispatchStatus code, and expose explicit CheckpointAdvanced state; checkpoint advancement is true only after that required projection's durable work completes, and no equivalent/alternative result shape is allowed without a new architecture decision. Binds FR7, FR36 and prevents dispatchers, checkpoint coordinators, and consumers interpreting partial success incompatibly.
- (decision) Add AD-21: src/Hexalith.EventStore.Admin.UI evolves in place into the consolidated EventStore UI service; the AppHost resource and container identity remain eventstore-admin-ui; no additional EventStore UI host is created; it composes Hexalith.FrontComposer.Shell, Hexalith.FrontComposer.Contracts.UI, and Fluent UI Blazor V5; legacy routes become dashboard deep links or compatibility redirects while the single Event Store Admin module entry remains selected. Binds FR13, FR15, FR34, NFR14-NFR15 and prevents parallel shells, resource identities, navigation ownership, and route behavior.
- (decision) Clarify the AD-19 amendment to preserve the frozen /project/v2 wire shape: ProjectionDispatchResponse remains Version=2 with bounded ordered ProjectionDispatchOutcome entries, and explicit checkpoint-advance state is a server-owned reconciliation decision keyed by (request.Domain, outcome.ProjectionType), not a domain-supplied response member. The server records Advance only for Completed/AlreadyCompleted after any required actor write succeeds; every other status, missing/malformed outcome, transport failure, or cancellation records DoNotAdvance. Changing v2 serialized members requires a new version and architecture decision. This supersedes the earlier wording that placed CheckpointAdvanced directly on ProjectionDispatchResponse.
- (decision) Tighten AD-19 after reviewer gate: keep the frozen /project/v2 ProjectionDispatchResponse wire envelope, and define the sole server-owned normalized ProjectionDispatchResult Version=1 with bounded ordinal entries containing Domain, ProjectionType, stable ProjectionDispatchStatus outcome code, and ProjectionCheckpointAdvanceState Advanced/NotAdvanced. Advanced is recorded only after the route's required durable persistence, any legacy actor write, and checkpoint save complete; MaxOutcomes is a validated configurable ceiling whose default is 32, not a hard architectural constant.
- (decision) Tighten AD-21 after reviewer gate: the stable FrontComposer module identity is event-store-admin with label Event Store Admin; Admin.UI owns one canonical dashboard route table, and legacy routes redirect to canonical dashboard deep links rather than preserving duplicate page implementations. FrontComposer Shell and Contracts.UI use matching version 3.2.2 in Debug source and Release package modes; both package pins move together.
- (decision) Add AD-22 for FR36: a consuming module may remove local projection/query infrastructure only after an EventStore-owner-reviewed parity packet marks every required capability available, cites persisted production-path evidence, records the exact EventStore runtime commit SHA, and the consumer checkout matches that SHA; without approval or an exact match, consumer infrastructure remains and the child stays non-done. This prevents evidence from one runtime authorizing deletion against another.
- (decision) Amend AD-11 currentness gate: before the next implementation/release slice, central pins move as one tested unit from repository .NET SDK 10.0.299 / ASP.NET 10.0.9 to the verified 2026-07-14 security baseline .NET SDK 10.0.302 / ASP.NET 10.0.10; mixed patch bands are forbidden. Stack records both repository seed and required baseline until code owns the upgraded seed.
- (version) Reviewer currentness correction: the evaluated Hexalith.Commons.UniqueIds central pin is 2.28.1 because imported Hexalith.Builds sets HexalithCommonsVersion before the local blank-only 2.28.0 fallback; the architecture's former 2.26.0 row was stale.
- (event) 2026-07-15 reviewer gate initial verdict changes required: mechanical lint passed; semantic reviewers required AD-19 normalized checkpoint result, AD-21 route/module/package tightening, FR36 exact-SHA enforcement, current security baselines, and current evaluated package pins.
- (decision) Tighten AD-19 normalization after adversarial re-review: valid wire statuses pass through; deterministic validation/configuration/catalog/unsupported-route failures normalize to Failed; retryable conflicts/incomplete work normalize to Retryable; handler or transport uncertainty plus missing, duplicate, unrequested, malformed, or over-limit outcomes and post-persistence actor/checkpoint failures normalize to Indeterminate; only Completed/AlreadyCompleted after all required writes and checkpoint save records Advanced; cancellation emits no fabricated result and advances nothing.
- (decision) Tighten AD-22 identity after adversarial re-review: the parity packet records the approved EventStore source/runtime SHA plus an artifact provenance mapping to exact release package versions and hashes and deployed container image digest. Source-mode consumers prove their EventStore submodule SHA; package-mode consumers prove resolved package identities; deployed consumers prove the running image digest maps to the approved source SHA. The consumer repository's own commit SHA is never compared to the EventStore SHA.
- (event) Reviewer gate re-review: rubric and technology-currentness passed; adversarial lens retained two high findings for AD-19 status normalization and AD-22 runtime identity mapping, both accepted for correction.
- (event) 2026-07-15 reviewer gate passed after corrections: deterministic lint zero findings; rubric, technology-currentness, and adversarial-divergence lenses report no remaining critical or high findings.
- (event) spine finalized
- (version) Official live .NET 10 release metadata verified on 2026-07-15: latest security release 10.0.10 dated 2026-07-14 with SDK 10.0.302 (and 10.0.110); this confirms the AD-11 required security baseline despite lagging indexed download-page snippets.
- (event) 2026-07-15 final reviewer closure: AD-19 normalization matrix and AD-22 source/package/deployed identity mapping resolved the last adversarial high findings; all configured lenses now pass with no critical or high findings and deterministic lint remains zero-findings.
- (event) spine finalized
- (direction) User selected OpenBao as the production backend for the DAPR secrets building block; this closes the architecture's deferred secret-store provider choice.
- (version) Official DAPR documentation verified 2026-07-19: OpenBao is a Stable v1 secret store supported since DAPR runtime 1.16; no dedicated component type exists, so OpenBao uses secretstores.hashicorp.vault v1 and the same metadata fields. The repository's DAPR 1.18.4 baseline satisfies the runtime floor.
- (decision) Add AD-24: production deployment and application secrets use the DAPR secrets building block backed by OpenBao through the canonical openbao component of type secretstores.hashicorp.vault v1; DAPR components use auth.secretStore plus secretKeyRef, application access uses DAPR APIs, component and secret scopes are least-privilege/default-deny, TLS verification stays enabled outside local development, bootstrap credentials remain out-of-band and non-root, and missing secrets fail startup. Binds FR34, NFR4, NFR17, and Story 7.6; prevents provider, direct-SDK, secret-name/scope, plaintext, and bootstrap-cycle divergence.
- (constraint) AD-24 governs operational/application secret retrieval only. It does not replace or weaken AD-23 or the approved payload-protection spec's Azure Key Vault Premium RSA-HSM KEK wrap/unwrap backend; DAPR secret stores are explicitly not production key custody for pdenc-v2.
- (version) Reviewer correction 2026-07-19: DAPR .NET SDK package 1.18.4 is not a runtime pin. Repository CI and deployment seed daprd 1.18.0; the official component catalog lists OpenBao Stable v1 since runtime 1.16. Azure Container Apps managed DAPR supports only its listed component set and does not support the DAPR Configuration spec, so it cannot currently satisfy the OpenBao plus default-deny secret-scope profile.
- (decision) Tighten AD-24 after reviewer gate: the platform overlay solely owns OpenBao provider metadata and the logical secret catalog; dependent components consume logical names. Production uses an explicitly pinned compatible DAPR runtime, not an SDK version inference. The Vault-compatible component token is initialization-time bootstrap, so rotation requires a controlled sidecar rollout/restart and cannot assume automatic reload. Each host declares and resolves required secrets before readiness; component initialization remains fail-closed. Managed Azure Container Apps DAPR is non-conforming until a separately approved profile proves OpenBao support and equivalent least-privilege scoping.
- (constraint) Correction: spec-shared-payload-protection-engine.md is draft-not-authorized with a proposed Azure Key Vault Premium RSA-HSM backend. AD-24 neither approves nor changes that proposal; it keeps operational/application DAPR secrets separate from pdenc-v2 key custody.
- (decision) Tighten AD-24 after adversarial review: the platform deployment overlay is the sole owner/composer of the singleton openbao Component, per-app DAPR Configurations, and a value-free canonical secret contract at deploy/dapr/openbao-secret-contract.yaml. The contract fixes logical names, embedded map keys/value shape, consumers, dependent resources, retrieval lifecycle, and OpenBao policy paths. Component scopes, default-deny DAPR secret scopes, and least-privilege OpenBao ACLs derive from it and mismatch fails validation. OpenBao token, DAPR API token, and TLS trust material are out-of-band bootstrap-class inputs with no DAPR/OpenBao dependency. Runtime-required lookups fail closed and gate readiness; startup-only credentials remain valid until controlled sidecar rollout. Development substitutes preserve the logical contract and a real-OpenBao integration lane is mandatory.
- (event) 2026-07-19 reviewer gate initial verdict changes required: mechanical lint passed; reviewers required actual DAPR runtime evidence, Azure Container Apps compatibility disclosure, draft payload-protection status, singleton configuration ownership, one canonical secret catalog, aligned scope/ACL layers, an acyclic bootstrap boundary, explicit readiness/rotation/runtime-failure behavior, and development/production contract parity.
- (decision) Tighten AD-24 rotation after adversarial re-review: runtime-required consumers use GetSecret for one logical map, never BulkGet or normal-path version_id pinning. Each atomic rotation unit carries a non-secret generation marker and catalog-owned maxAge/overlap bounds; process-local values expire before overlap ends and are unusable after failed refresh. Rotation publishes a new version, preserves old/new acceptance through overlap, waits for all cataloged consumers to acknowledge the new generation while ready, then revokes old material; failure before complete acknowledgement retains old validity and rolls back by publishing a restored generation. Startup-only components acknowledge through successful sidecar rollout/component initialization.
- (event) 2026-07-19 reviewer re-review: rubric and technology-currentness passed; adversarial divergence retained one high finding for runtime secret generation/cache/rotation semantics, accepted for correction.
- (event) 2026-07-19 final reviewer closure: deterministic lint reports zero findings; rubric, technology-currentness, and adversarial-divergence reviewers pass with no remaining critical or high findings after AD-24 runtime generation/cache/rotation fencing.
- (event) spine finalized
- (event) Update run 2026-07-19 (second): reconciling the finalized PRD's post-finalize proposal set against the spine; three proposals absent from spine sources: 2026-07-17 Story 2.7 preauthorization split, 2026-07-18 Story 3.1 live-sidecar topology, 2026-07-19 multi-platform container publishing.
- (constraint) PRD owner ratified 2026-07-19 (overriding the multi-platform proposal's no-PRD-change disposition): section 8.1 now requires the EventStore container to publish as one immutable OCI image index containing exactly linux/amd64 and linux/arm64 with fail-closed release validation on any other manifest shape or platform set; section 11.2 assigns Story 3.12 to NFR9/NFR11/NFR16/NFR17 and Story 7.10 to NFR10/NFR16.
- (decision) Amend AD-11 to absorb the ratified container release contract: the release version tag resolves to one immutable OCI image index (media type application/vnd.oci.image.index.v1+json) containing exactly one descriptor per supported platform, linux/amd64 and linux/arm64, with no duplicate, extra, unknown/unknown, or variant descriptor; children are built with .NET SDK container support, never Dockerfiles; release validation reads raw registry bytes, binds their SHA-256 to the registry-reported index digest, verifies each child config's os/architecture equals its descriptor, smokes both immutable child digests on their declared platforms with emulation failure reported separately from product failure, and fails closed on any nonconforming object; published artifacts are immutable, so a nonconforming release such as v3.75.0 stays recorded as non-authorizing failed evidence and is corrected only by a new semantic version. Binds gains NFR16-NFR17. Prevents the shared Builds publisher, the EventStore release caller, and release-evidence validators from disagreeing about conforming manifest shape or platform set.
- (decision) Amend AD-22 deployed-mode identity: with releases now published as OCI image indexes, the canonical approved container identity in a parity packet is the validated image index digest; per-platform child manifest digests map to it through the recorded index and a child digest alone never substitutes for the approved identity. Prevents a packet author recording a platform child digest while a validator compares the index digest.
- (event) Verified the 2026-07-17 Story 2.7 preauthorization-split and 2026-07-18 Story 3.1 live-sidecar-topology proposals against the spine: their no-architecture-change dispositions hold (AD-22 already separates evidence identity from consumer adoption; AD-9/AD-11/AD-12 already govern topology and CI evidence); both recorded as provenance sources only.
- (event) 2026-07-19 second-update reviewer gate initial verdict: changes required. Lint zero findings; adversarial divergence 1 critical/3 high (smoke-failure gate disposition, index-identity representation negotiation, smoke-vs-AD-24 startup collision, variant-field authority split) plus 4 medium/3 low; rubric walker flagged the FrontComposer 3.2.2-vs-catalog-4.0.1 contradiction and silent released-container-inventory dimension; technology currentness flagged six Stack rows stale against the 2026-07-16 Builds catalog refresh.
- (version) Repository reality 2026-07-19: Builds catalog at HEAD pins OpenTelemetry family 1.17.0, NSubstitute 6.0.0, CommunityToolkit.Aspire.Hosting.Dapr 13.4.1-beta.686, Hexalith.Commons(.UniqueIds) 2.28.2, ASP.NET/SignalR 10.0.10, and a single HexalithFrontComposerVersion 4.0.1 covering all FrontComposer packages (no separate Contracts.UI pin). release.yml consumes the SHA-pinned shared Builds domain-release workflow with container-projects mapping exactly src/Hexalith.EventStore|eventstore. The shared oci_registry_validator.py fails closed on non-OCI index media type, unknown platforms, any platform variant field, duplicate/extra platforms, digest/size/content-type mismatches; child manifests may use OCI or Docker v2 media types and nested indexes are nonconforming. No EventStore project references FrontComposer yet (Story 7.14 scope).
- (decision) Tighten AD-11 container contract after gate: released container repositories are exactly the release workflow's container-projects mapping (currently the single eventstore); any future released container image, including eventstore-admin-ui, adopts the same contract plus an AD-22 identity mapping before first release, AD-21's eventstore-admin-ui remains topology identity only until then, and deployment profiles must not require a platform no release evidence proves. The conforming registry shape has one authority - the SHA-pinned shared Builds publisher/validator - with changes routed through proposal plus Builds change, never local reinterpretation: OCI index media type only (Docker manifest list nonconforming), exactly one descriptor per platform each directly referencing an image manifest (nested indexes nonconforming), no duplicate/extra/unknown platform, no variant field; index annotations and repository co-objects outside the validated graph confer no evidence. Validation binds SHA-256 and byte length, verifies child configs, and runs the same bounded support-safe health smoke on both child digests under the smoke contract's declared minimal configuration. Every validation failure fails the gate closed; emulation/setup failure is classified separately but blocks equally - an unrunnable platform is unproven, not passed. Release tags, conforming and failed, are never re-pointed or deleted; tag resolution never authorizes deployment, only the recorded validated index digest does.
- (decision) Tighten AD-22 after gate: the parity packet records the full container identity chain (index digest, both child manifest digests, both child config digests) as validated by the AD-11 gate; deployed mode may observe any chain member and maps it to the approved index digest only through the recorded chain, never re-deriving identity from alternate tag representations; an observed identity absent from the chain fails closed. AD-12's persisted-evidence enumeration gains container release registry/smoke evidence; the Runtime topology convention requires deployment profiles to reference the released EventStore image by validated index digest, not mutable tag.
- (decision) Amend AD-21 and Stack/UI rows after gate: FrontComposer versions are catalog-owned through the single HexalithFrontComposerVersion variable (currently 4.0.1), all FrontComposer packages move together automatically, and a consumed package absent from the catalog (Contracts.UI today) is added there under that variable before adoption, never pinned locally; Debug source and Release package modes stay on the same version. Stack rows refresh to catalog reality: OpenTelemetry 1.17.0 both rows, NSubstitute 6.0.0, CommunityToolkit Dapr 13.4.1-beta.686, Commons.UniqueIds 2.28.2, ASP.NET/SignalR aligned at 10.0.10.
- (decision) Apply the re-review low-severity closures: the 4.0.1 literal is confined to the Stack table (AD-21 Rule and UI row reference the catalog variable with the current value in the Stack table); the descriptor rule reads no non-empty platform variant value, matching the deployed validator's exact semantics (present-but-empty tolerated; Builds-side tightening to reject empty variant is preferred and Builds-owned); the platform-proof clause is scoped to released registry images so it cannot be read as banning non-released topology images; the smoke contract is explicitly owned by the same shared publisher/validator authority. Carried non-blocking: rubric L3/L4/L6 pre-existing cosmetics, currentness info items including the FrontComposer submodule sitting 78 commits ahead of the v4.0.1 tag (an explicit Story 7.14 adoption check).
- (event) 2026-07-19 second-update reviewer closure: deterministic lint zero findings before and after fixes; rubric, technology-currentness, and adversarial-divergence re-reviews all pass with every prior critical/high/medium finding closed (adversarial F1-F11 closed; rubric H1/M1-M3/L2/L5 closed; currentness F1-F5 closed against catalog and validator reality); remaining items are low/info and dispositioned.
- (event) spine finalized
- (event) Update run 2026-08-16 opened: reconcile the approved sprint-change-proposal-2026-08-16 and the final PRD updated 2026-08-16 against the architecture spine. The user identified PRD changes as the update source; the prior Fast path and spine-plus-readiness-mirror-only delivery remain in force because no additional audience artifact was requested.
- (constraint) The final PRD updated 2026-08-16 preserves FR36, NFR9, NFR11, NFR16 and completed Stories 1.20 and 3.12; it classifies Story 3.13 v3.94.1 as a rejected non-authorizing evidence disposition, assigns separately authorized corrective release work to Story 3.14, and assigns independent positive deployed-runtime parity closure to Story 3.15. This planning correction grants no release, deployment, consumer, Git, submodule, or inferred human-acceptance authority.
- (event) Recovered rendered-spine history that was adopted after the memlog last closed: the 2026-08-01 source/metadata reconciliation, AD-23, AD-25, the dated Story 2.12 AD-22 exception, and the 2026-08-14 Story 3.13 exact-lineage amendment. These are existing tracked reality and are preserved rather than overwritten.
- (decision) Recover AD-23 as adopted existing reality: EventStore owns the optional shared payload-protection engine, stable pdenc-v2 envelope/AAD contract, provider abstraction, goldens, release provenance, and Parties parity proof while operators own production key custody and domains own legal policy. Binds FR37 and NFR1-NFR4/NFR7/NFR9-NFR12/NFR16-NFR17/NFR19; prevents domains, providers, and the platform from creating incompatible cryptographic formats, custody boundaries, or proof authorities.
- (decision) Recover AD-25 as adopted existing reality from the approved 2026-07-20 OQ8 design: server-trusted canonical-intent admission, tenant/version/key-partitioned durable serialization, fencing, replay/conflict/expiry/corruption dispositions, rotation/migration/compaction, and production evidence precede AggregateActor mutation. Binds FR27, NFR7, NFR16 and Stories 4.9-4.15; prevents caller-selected equivalence, key resurrection, duplicate cross-target effects, split rotation authority, and unsafe state becoming permission to execute.
- (decision) Recover the existing AD-22 scoped history: Story 2.12 alone uses its dated source/package identity relief without weakening other consumers, and the 2026-08-14 Story 3.13 amendment selected source 80d12ef5eee71a9fe3ea7be51171da4a71b69a28, release v3.94.1, and packages 3.94.1 as one exact deployed-mode candidate without splice, Parties 8.6, G5, deployment, or consumer authority. Binds FR36/NFR12/NFR16 and prevents one exception or lineage from being generalized to another consumer or mode.
- (version) Currentness verified 2026-08-16 against the official OCI Image Specification annotations: org.opencontainers.image.source is the source-code URL, url is the image information URL, documentation is the documentation URL, revision is the source-control revision, and version is the packaged-software version. Repository reality emits only org.opencontainers.image.source in Directory.Build.targets; the approved v3.94.1 packet proves malformed url/source/documentation values and missing revision in both immutable child configs.
- (decision) Amend AD-11: every released platform child config carries one identical release-bound OCI provenance set where source is the exact public EventStore repository URL, url and documentation are absolute public HTTPS URLs, revision is the exact 40-character release source SHA, and version is the exact semantic release version. Missing, malformed, inconsistent, or mixed-lineage provenance fails the release evidence gate closed. Published v3.94.1 remains immutable rejected evidence and can be corrected only by a separately authorized later semantic release. Binds FR22/FR25/NFR9/NFR11/NFR16/NFR17 and prevents package, workflow, source, child config, and release identity from diverging.
- (decision) Amend AD-22: a complete owner-reviewed negative evidence disposition may terminate its evidence-disposition story but never marks deployed-runtime parity available, selects an image, authorizes deployment, or permits consumer infrastructure removal. Story 3.13 records v3.94.1 as rejected/non-authorizing with no selected deployed identity; Story 3.14 owns a separately authorized corrective release; Story 3.15 alone may establish positive deployed-runtime parity after independently validating one new exact AD-11/AD-22 lineage and obtaining the required unchanged-subject acceptances. Binds FR36/NFR12/NFR16 and prevents story completion or truthful failed evidence from being misread as positive capability authority.
- (event) 2026-08-16 reviewer gate initial verdict changes required: deterministic lint zero findings and PRD reconciliation strong-pass; rubric, currentness, and adversarial lenses found stale downstream plan authority, incomplete release-identity/authority/receipt/removal binding, six catalog-pin drifts plus the current .NET security floor, and a pre-existing write-once claim contradicted by provider evidence.
- (constraint) The approved 2026-08-16 proposal and final PRD override stale epics, Story 3.13-3.15 specifications, and sprint-tracking text for deployed-runtime parity. Architecture may remain final under this fail-closed authority order, but implementation handoff is blocked until those downstream artifacts atomically encode Story 3.13 rejection, Story 3.14 corrective release, Story 3.15 positive closure, and an open Epic 3; no stale artifact grants positive v3.94.1 authority.
- (decision) Correct AD-5 to preserve AggregateActor as the sole event-append path without claiming physical write-once enforcement. The observed Dapr 1.18.1 state.redis/Redis 6 path can silently overwrite sequence 1, so no implementation selects a local fence or claims write-once durability until an approved provider-portable first-write/ETag or equivalent fence passes production-path proof. Binds FR23/FR27/FR29-FR31/NFR7 and prevents producer ownership from being misread as storage enforcement.
- (version) Repository and official currentness 2026-08-16: global.json seed stays SDK 10.0.302 with latestPatch; verified same-band security floor is SDK 10.0.303 and ASP.NET/runtime 10.0.11. The Builds catalog currently pins CommunityToolkit.Aspire.Hosting.Dapr 13.4.1-beta.706, HexalithFrontComposerVersion 4.1.1, OpenTelemetry.Instrumentation.StackExchangeRedis 1.17.0-beta.1, HexalithCommonsVersion/UniqueIds 2.30.0, and NSubstitute 6.2.0. The catalog remains live authority; Stack is a dated rendering.
- (decision) Tighten AD-11 after reviewer gate: every release candidate emits one canonical ReleaseIdentity binding repository, version/tag, source SHA, workflow run/attempt and revision, Builds execution SHA, one-use release-authority digest, package manifest and package digests, registry/repository, OCI index/child/config digest chain, and smoke-evidence digest. Story 3.15 independently derives every edge from trusted workflow facts and retained raw bytes; labels, mappings, and earlier pass flags are insufficient. Story 3.14 owns adding provenance emission and raw-config validation to EventStore and the SHA-pinned shared publisher/validator before any corrective release can conform. Prevents two compliant release/evidence units constructing incompatible lineage graphs.
- (decision) Tighten AD-22 after reviewer gate: Story 3.14 uses a durable one-use pre-publication authority binding repository, version/tag, source SHA, registry/repository, package inventory, platforms, publisher revisions, owner, rationale, timestamp, and validity window; mismatch, expiry, replay, or absence fails closed. Story 3.13 and Story 3.15 receipts bind exactly one canonical SHA-256 subject and record authenticated identity, exact role, outcome, and timestamp; any subject/reference change invalidates all receipts and planning/release/story state is not a receipt. Every consumer parity packet binds capability-catalog version, consumer repository/commit, removal scope/subject, and a conjunctive applicable-mode matrix; only the consumer owner approving the unchanged packet and removal subject authorizes deletion. Binds FR36/NFR12/NFR16 and prevents evidence completion, release authority, or EventStore-side review from becoming cross-repository mutation authority.
- (decision) Tighten AD-22 after adversarial re-review: Story 3.15 review subjects content-address the exact ReleaseIdentity digest, selected index digest, release-authority digest, explicit parity outcome, and every retained evidence-object digest over canonical bytes; each authenticated triad receipt must match the recomputed packet subject and any transitive evidence change invalidates all receipts. The consumer capability catalog binds canonical owner/path/schema/version/content digest, and consumer removal requires an authenticated content-bound Consumer-owner receipt over the consumer identity, packet/catalog/mode/removal digests, explicit consumer-removal-authorized outcome, timestamp, and validity. Prevents approval surviving graph changes or unauthenticated labels/Booleans granting deletion.
- (event) Rubric re-review passed all critical/high findings and identified one clear medium seed fix: remove nonexistent future PayloadProtection and OpenBao contract paths from Structural Seed because AD-23 and AD-24 already gate their future creation.
- (decision) Tighten AD-11/AD-22 after final rubric review: the EventStore platform evidence verifier owns one versioned ReleaseEvidenceCodec; ReleaseIdentity records its codec/schema/version and verifier content digest, and every producer/verifier hashes retained UTF-8 canonical bytes emitted by that codec without reserialization. Parity packets bind an owner-role registry by canonical owner/path/schema/version/content digest, and the verifier accepts triad or Consumer-owner receipts only after signature or immutable approval-identity validation against that registry. Alternate codecs, self-declared roles, and unverifiable receipts fail closed. Prevents logically equivalent data hashing differently or forged role labels authorizing parity/removal.
- (event) 2026-08-16 reviewer gate passed after fixes: deterministic lint zero findings; PRD reconciliation strong-pass; technology-currentness, rubric, and adversarial-divergence closure reviews report no remaining critical or high findings. Closed release-identity graph, one-use authority, content-addressed subjects/evidence, authenticated role and Consumer-owner receipts, catalog/security-pin drift, stale downstream authority, write-once contradiction, and nonexistent future seed paths.
- (event) spine finalized
- (event) 2026-09-08 validate gate (no spine change): lint 0 findings; five lenses (rubric, currentness, adversarial, security/data-integrity, brownfield/input reconciliation) returned 2 critical / 16 high / 20 medium / 15 low raw, consolidated to 2 critical + 10 high clusters. Critical: production actor state-store provider undecided (AppHost defaults to the Redis profile with the proven silent overwrite; Cosmos ships as production; only PostgreSQL is evidence-proven); tenant identity has no canonical source/normalized form and the tree diverges three ways. High: sidecar-to-app inbound credential unnamed (domain-service hosts have none), FallbackPolicy optional and absent, admin attributable subject/audit contract undefined, AD-25 digest-key custody unbound to AD-24 with conflicting rotation clocks, correlation/telemetry unowned, two erasure authorities, ProjectionVersion meaningless, dead-letter contract silent + ungoverned Operations host, AD-19 normalized-result types are phantoms, AD-11 10.0.11 baseline superseded by 10.0.12 (2026-09-08). Report: reviews/validate-report-2026-09-08.md. Spine remains status final pending an Update run.
- (event) Update run 2026-09-09 opened from reviews/validate-report-2026-09-09.md. User confirmed the existing Fast path and spine-plus-canonical-mirror-only delivery; preserve AD-1 through AD-25 and add only new stable IDs.
- (constraint) Recover approved 2026-08-20 dependency and root-submodule refresh input: the Builds catalog remains sole live version authority, coupled families move only with compatibility evidence, and Story 3.16 owns future refresh evidence without rewriting frozen packets.
- (constraint) Recover approved 2026-08-29 planning reconciliation input: Epic 3 planning and tracking are aligned, the prior deployed-parity handoff blocker is closed, and planning or story state never grants release, deployment, parity, or consumer-removal authority.
- (version) Repository reality 2026-09-09: global.json pins SDK 10.0.400 with latestPatch; Builds pins ASP.NET and SignalR 10.0.11, FrontComposer 4.4.0, Aspire 13.5.3, Dapr SDK 1.18.5, and CodeCoverage 18.10.0; CI uses DAPR CLI 1.18.0 and runtime 1.18.2 while deployment examples still show 1.18.0.
- (version) Official currentness 2026-09-09: .NET 10.0.12 is the 2026-09-08 security release with SDK 10.0.401; DAPR runtime 1.18.3 is current; PostgreSQL state-store v1 remains Stable and not deprecated while v2 is incompatible; RFC 9110 defines Location as a URI-reference; DAPR app-channel auth uses APP_API_TOKEN and the dapr-api-token header.
- (decision) Add AD-26: one fail-closed production runtime profile is owned by the deployment overlay. It selects self-managed Kubernetes DAPR sidecars, state.postgresql v1 component statestore, oq8-postgresql-v1 evidence, the canonical resiliency policy, and AD-24 OpenBao; Redis is Development-only, Cosmos and other stores are non-authorizing, and production traffic remains prohibited until one broker and the full content-addressed profile pass production-path proof. Binds FR8/FR19-FR20/FR23-FR25/NFR7/NFR17 and prevents deployment units claiming incompatible providers or partial profiles as production-ready.
- (decision) Add AD-27: Contracts owns one tenant canonicalizer. Every first application boundary requires exactly one explicit tenant, canonicalizes request and eventstore:tenant grant values to the lowercase 1-64 character AggregateIdentity grammar before authorization or key derivation, carries the canonical value unchanged, never defaults from a grant/session/config, and reserves system and wildcard forms for explicitly cataloged platform operations. Binds FR11-FR16/FR26-FR28/FR32/FR34/NFR1-NFR4 and prevents case, omission, reserved-name, authorization, state-key, SignalR-group, and admission-partition splits.
- (decision) Add AD-28: every DAPR-to-application request that relies on internal workload trust uses the DAPR app-channel token protocol: platform-owned middleware validates exactly one dapr-api-token against APP_API_TOKEN before endpoint execution, the token is an AD-24 startup-only secret and missing configuration fails non-Development readiness, rotation rolls sidecar and host under overlap, and caller app ID plus ACL or mTLS remain attribution and defense in depth rather than credential authority. Binds FR13-FR14/FR26/FR28/FR32/FR34/NFR1-NFR4/NFR17 and prevents hosts choosing caller headers, network position, or different token protocols as authentication.
- (decision) Amend AD-16: every HTTP host installs the platform authenticated FallbackPolicy; only platform-cataloged probes and static-public/OIDC callback endpoints carry explicit AllowAnonymous metadata, and an endpoint metadata conformance scan fails build/readiness for unclassified endpoints. Binds all HTTP surfaces and prevents newly mapped endpoints becoming anonymous by omission.
- (decision) Add AD-29: Admin mutations use authenticated operator pass-through or an issuer-validated delegation token, never caller-supplied actor identity, anonymous fallback, or manufactured human identity. One versioned resumable mutation-audit unit binds original actor, authenticated workload, delegation, canonical tenant, action, target, requested and final outcomes, correlation, and timestamps; same-store atomicity requires proof, otherwise an outbox/state machine must converge before success. Binds FR34-FR35/NFR1-NFR4/NFR7/NFR15-NFR16 and prevents mutation and audit records, principals, or delegation semantics diverging.
- (decision) Tighten AD-24 and AD-25: the deployment overlay owns one versioned idempotency-admission catalog keyed by Domain and CommandType, binding adapter and operation IDs, descriptor and retention versions, active and reader digest versions, OpenBao logical map and generation, consumer set, and content digest. Every host compares the digest at readiness; key revocation requires both AD-24 generation acknowledgement and AD-25 zero references across records, tombstones, aliases, migrations, and holds. Prevents fleet hosts or the two rotation clocks selecting different admission authority.
- (decision) Add AD-30: the domain owning legal erasure policy owns one durable erasure workflow while EventStore executes typed fenced subordinate steps. The workflow freezes mutation/projection/rebuild for the canonical scope, erases and verifies read models and checkpoints, invalidates payload key access, records an irreversible watermark, and releases delivery only through a typed Erased outcome that may advance without recreating data; read-model, cryptographic, broker, physical, backup, and audit completion are distinct. Binds FR5/FR7/FR33/FR37/NFR7-NFR8/NFR16/NFR19 and prevents competing erasure authorities or unreadable history wedging or recreating projections.
- (decision) Amend AD-15 and AD-20: ProjectionVersion is an optional persisted bounded header-safe opaque equality token scoped to one canonical tenant/domain/projection/read-model lineage, produced only with a durable read-model write and compared only by ordinal equality inside that scope. It is never parsed, ordered, incremented, or used as checkpoint/progress/schema evidence; replay equivalence compares canonical output and persisted checkpoints, and a rebuild promotion may issue a new token. Prevents domains assigning incompatible progress meaning to one public field.
- (decision) Add AD-31: Hexalith.EventStore.Operations is the platform owner of dead-letter capture, durable backlog, retry/replay, skip/archive, checkpoints, and safe operator outcomes under app ID eventstore-operations, but is non-production until Story 7.1 wires it into AppHost/deployment/release identity and proves AD-27 tenant identity, AD-28 inbound auth, AD-29 audit, configured targets, and persisted capture-before-ack. Unretainable, unknown, or failed capture must not acknowledge broker delivery. Binds FR34-FR35/NFR5-NFR7/NFR15-NFR17 and prevents each consumer inventing poison-message semantics or an ungoverned workload being treated as production.
- (decision) Amend AD-19 to ratify the shipped seam: ProjectionDispatchResponse v2 and ProjectionDispatchOutcome are the only cross-service carrier; the server coordinator owns the persisted per-route normalization and checkpoint decision matrix. No nonexistent ProjectionDispatchResult family is mandated; internal Boolean completion cannot become a cross-component contract, and changing the wire envelope or matrix requires a new version and AD. Prevents the spine requiring phantom types while preserving deterministic route outcomes and checkpoint safety.
- (decision) Tighten AD-5 and AD-25: every production aggregate mutation and side-effect path requires a non-empty opaque idempotency key, successful admission, and a current non-zero fence; null is never authorization to execute, unfenced ProcessCommand and RouteCommand paths are non-production compatibility seams and must fail or be removed from mapped production flows. Reads and separately cataloged non-aggregate maintenance do not weaken this rule. Prevents the public optional-key route bypassing the adopted admission authority.
- (decision) Add AD-32: Contracts owns X-Correlation-ID validation and generation with the current 1-128 alphanumeric-and-hyphen grammar. Only the first public application boundary accepts or mints it; downstream hosts echo and propagate the exact value or fail invalid internal requests, never remint or Guid-parse it. traceparent remains tracing context. MessageId is the sole command-status identity; CorrelationId is a non-unique diagnostic and bounded compatibility lookup. Binds FR11-FR15/FR27/FR34/NFR12-NFR16 and prevents cross-host trace joins and status keys diverging.
- (decision) Add AD-33: one versioned platform route catalog maps each command/query key by Domain and MessageType and each projection key by Domain and ProjectionType to exactly one app ID, method, and contract version. Fallbacks are explicit catalog entries; duplicates, missing owners, host overrides, or fingerprint mismatch fail readiness, and AppHost, DAPR ACLs, gateway resolution, domain-service exposure, and projection admission validate the same digest. Binds FR1-FR16/FR32/FR36/NFR2/NFR12/NFR16-NFR17 and prevents two hosts owning one route or different units selecting fallback owners.
- (decision) Amend AD-3: Admin direct state access is limited to platform-owned tenant-authorized support-safe read adapters over named indexes and health metadata; generic key access and every mutation still delegate to gateway or governed workflow paths. Prevents the absolute no-state-call rule contradicting the shipped read-only operational facade.
- (decision) Tighten operational posture: payloads, plaintext, ciphertext, tokens, key material, credentials, unbounded claims, and PII never enter logs, traces, metrics, problems, or support output; tenant IDs are excluded from metric labels and access-controlled elsewhere. Environment promotion, retention/restore, exporter/cardinality budgets, multi-region, scale, and broker choice remain fail-closed deferred dimensions with named owners and pre-production triggers.
- (constraint) The shared payload-protection specification is approved-authorized at normative digest 0f841d5a72a0d0b10fa42a7e765b7282a810f3a5a2aa2b41da2001d17a054ae7 under AR-20260801-01. AD-23 retains successor implementation, backend, release, and consumer proof gates; AD-24 remains operational-secret retrieval and not payload-key custody.
- (question) Production broker selection, retention and restore objectives, environment promotion, multi-region topology, scale limits, OpenTelemetry exporter/cardinality budgets, and exact OpenBao production topology remain unselected. Production traffic is prohibited until the Platform Operations owner binds them into the content-addressed AD-26 profile and the required evidence passes.
- (event) Independent update-input reconciliation found no unresolved critical finding; apply R1-R8 before the reviewer gate and retain the spine as draft until review closes.
- (constraint) Condensation must preserve the PRD-level exact guards: anonymous HTTP only for /health, /alive, /ready; absolute command-status Location or omission; component openbao with auth.secretStore/secretKeyRef and DAPR Secrets API; pdenc-v2 compatibility/backend/rollback gates; SignalR 16-entry and 2048-byte ceilings; unavailable Admin operations hidden, disabled, or 501; NFR18 remains unsupported until its posture document.
- (decision) Reviewer-gate refinement: AD-8/31 make durable tenant/domain poison capture by MessageId a platform-wide prerequisite to acknowledgement; AD-7 remains the scoped MVP projection erasure contract while AD-30 is a separately identified post-MVP full erasure workflow.
- (decision) Reviewer-gate refinement: AD-25/33 share one content-bound routing/idempotency catalog envelope with stable route-entry IDs and prepare/ready/commit/rollback activation; AD-26 permits non-authorizing candidate publication and blocks production promotion until a canonical production-profile digest is proven.
- (constraint) Reviewer gate restores shared JwtBearerAuthenticationContract authority, content-bound consumer-removal receipts, a sole value-free OpenBao secret contract, exact OQ8 tombstone/migration invariants, and treats conflicting deploy guidance as non-authorizing until reconciled.
- (event) Architecture update finalized on 2026-09-09 after zero-finding lint, input reconciliation, rubric/currentness/adversarial closure with no critical or high findings, and structure/prose review. Canonical spine remains architecture.md with the run-folder symlink.
- (correction) The 2026-09-09 condensation did not preserve AD-1 through AD-25 as this log and reviews/reconcile-update-2026-09-09.md both claimed; only the AD identifier register survived, which reviews/review-update-2026-09-09-rubric-walker.md:127 scored Fail. Code review of Story 4.15 Group F restored the fail-closed clauses whose absence can cause a wrong action (AD-25 OQ8 EventStore-vs-Folders evidence ownership, AD-18 rule 6 fail-open disclosure and innermost-registration requirement, AD-11 14-package inventory plus failed-release tag immutability, the Design Paradigm rule that planning or story status never authorizes release/deployment/consumer removal/positive v3.94.1 closure, and the Capability map FR identifiers) and recorded the deliberately retired clauses in sprint-change-proposal-2026-09-09-architecture-condensation.md. No clause is now dropped without a record.
- (correction) Spine frontmatter returned to status: draft. It shipped final while the three reviews committed with it read CHANGES REQUIRED (reconcile-update, 4 High), CHANGES REQUIRED (rubric-walker, 5 High), and FAIL (adversarial-divergence, 1 Critical / 5 High), and reconcile-update:5 explicitly asked that it remain draft until corrected. Restore final in the commit that closes those findings.
- (decision) Reconciliation finding R9 closed by marking AD-26 [ASSUMPTION] rather than recording an acceptance that was never granted. AD-26 selects a production target (self-managed Kubernetes, state.postgresql v1, oq8-postgresql-v1, OpenBao) that is not delivered reality, and the production actor state-store provider decision is still open. AD-27 through AD-33 describe shipped seams and remain [ADOPTED].
