---
title: 'Story 4.15: OQ8 Platform Closure And Handoff'
type: 'feature'
created: '2026-08-10'
status: 'done'
review_loop_iteration: 0
story_key: '4-15-oq8-platform-closure-and-handoff'
baseline_commit: '699ca71206cd280dc6b770d83c338495bfe70fab'
context:
  - '{project-root}/_bmad-output/project-context.md'
  - '{project-root}/_bmad-output/implementation-artifacts/epic-4-context.md'
---

<frozen-after-approval reason="human-owned intent — do not modify unless human renegotiates">

## Intent

**Problem:** Story 4.14 produced source-bound multi-host evidence, but its packet deliberately leaves architecture, security, and test reviews pending, binds a dirty candidate rather than the landed capability commit, and cannot validate after unrelated repository changes. Status contradictions and stale documentation prevent a truthful EventStore OQ8 platform handoff.

**Approach:** Add an immutable closure layer over the Story 4.14 capture that crosswalks Stories 4.9-4.14, independently binds the landed OQ8 source and captured artifacts, records content-bound named reviews and limitations, and distinguishes EventStore platform completion from release or Folders-owned final closure.

## Boundaries & Constraints

**Always:** Preserve the checksummed Story 4.14 evidence directory unchanged. Bind every approval to one frozen subject containing the design reference, invariant/evidence crosswalk, exact source/artifact identities, limitations, and reviewer scope. Keep the approved test seams and sanitized structural-state limitation explicit. Advance tracking only when the final fail-closed validator passes.

**Ask First:** Changing OQ8 design 1.0.0, durable-admission behavior, the production profile, public contracts, or any release, package, registry, deployment, consumer pin, external repository, or submodule state.

**Never:** Fabricate unavailable Folders design bytes or reviewer approval; treat historical Story 4.8 checkboxes as child completion; rewrite captured observations or pending-review history; commit protected values, raw PostgreSQL state, diagnostics, identifiers, keys, intent, payloads, secrets, or private paths; claim release approval, Folders OQ8 closure, or consumer migration authority.

## I/O & Edge-Case Matrix

| Scenario | Input / State | Expected Output / Behavior | Error Handling |
|----------|--------------|---------------------------|----------------|
| Approved handoff | Exact 4.9-4.14 evidence, landed source, artifacts, limitations, and three bound approvals | Record EventStore platform completion and a source-only Folders handoff | Keep release and Folders closure false |
| Evidence drift | Missing, changed, rejected, ambiguous, or unbound evidence/review | Reject closure and preserve non-done lifecycle state | Name the failed field or receipt without protected data |
| Later repository work | Unrelated commits after the landed OQ8 commit | Verify the pinned OQ8 path set and current byte equivalence | Reject any changed or incomplete bound path |
| Overstated authority | Packet claims publication, pinning, deployment, or Folders closure | Fail validation | Do not advance Story 4.15 |

</frozen-after-approval>

## Code Map

- `_bmad-output/implementation-artifacts/4-8-eventstore-oq8-platform-evidence.yaml:1-33` -- v1 handoff packet to evolve with explicit EventStore-platform, release, and Folders authority fields while retaining the immutable capture reference.
- `_bmad-output/implementation-artifacts/evidence/story-4-14/e60a3777c581d70b62f67173ccc2372b5b64a425/` -- immutable seven-file production evidence and manifest; `source-state.json` binds 14 candidate and 12 source-input paths, while `review-records.json` preserves pending history.
- `tools/validate-oq8-platform-evidence.py:16-73,526-717` -- hard-coded v1 packet, unsafe baseline-to-HEAD changed-file inference, leakage gates, identity crosswalk, and pending-review assertions.
- `tests/Hexalith.EventStore.Contracts.Tests/Packaging/DeployedRuntimeParityClosureTests.cs` -- golden content-bound subject/receipt, immutable-manifest, limitation, and negative-mutation test pattern for a new focused closure contract.
- `_bmad-output/implementation-artifacts/4-8-durable-tenant-scoped-idempotency-admission-and-expired-key-precedence.md:20-150,392-403` and `spec-4-{11,12,13,14}-*.md` -- historical ledger plus child implementation/test/review evidence; do not infer completion from unchecked parent tasks or malformed/status-divergent metadata.
- `docs/{concepts/command-lifecycle.md,concepts/architecture-overview.md,reference/command-api.md,guides/configuration-reference.md}` -- final behavior, limitation, evidence, and source-only consumption references.
- `_bmad-output/implementation-artifacts/sprint-status.yaml:109-125` -- authoritative child and Epic lifecycle tracking; unrelated Epic 4 work keeps the epic in progress.

## Tasks & Acceptance

**Execution:**
- [x] `_bmad-output/implementation-artifacts/evidence/story-4-15/**` and `4-8-eventstore-oq8-platform-evidence.yaml` -- create a checksummed closure subject/crosswalk, exact landed-source and captured-artifact identity record, limitation set, named content-bound review receipts, and source-only handoff with external authorities false.
- [x] `tools/validate-oq8-platform-evidence.py` -- validate the immutable v1 capture plus closure layer, replace baseline-to-current diff inference with a pinned commit/path identity proof, and fail closed on drift, missing approvals, unsafe content, or overstated authority.
- [x] `tests/Hexalith.EventStore.Contracts.Tests/Packaging/Oq8PlatformClosureTests.cs` -- prove manifest/subject/receipt/source/status/document contracts and mutate each critical field to demonstrate rejection.
- [x] `docs/**` OQ8 references and Story 4.11-4.14 metadata -- reconcile final behavior, source-only consumption limits, malformed frontmatter, and truthful child status without rewriting approved historical authority.
- [x] `_bmad-output/implementation-artifacts/sprint-status.yaml` -- advance only evidence-approved children and Story 4.15; keep Epic 4 `in-progress` and all release/Folders authority external.

**Acceptance Criteria:**
- Given Stories 4.9-4.14 request closure, when the packet is validated, then every invariant, design-digest reference, command/count, limitation, named reviewer decision, and exact EventStore source/artifact identity has one reproducible crosswalk and any omission keeps Story 4.15 non-done.
- Given a reviewed packet, when it is handed off, then it records EventStore platform completion against `5e8f175b2ced4715f7c6f765386812cc1001dbb4` and unchanged bound paths while release approval, Folders closure, package/pin authority, and consumer migration remain false.

### Review Findings

_Chunked code review, Group A of 3 (`tools/validate-oq8-platform-evidence.py` only; `Oq8PlatformClosureTests.cs` and the evidence/docs/sprint-status group are reviewed in follow-up chunks — see spec Verification/Code Map). Baseline `699ca712` → HEAD `83b32fcf`._

**⚠ Patch-application blocker discovered 2026-08-30:** `tools/validate-oq8-platform-evidence.py` is itself a sealed `gateInput` of the v2 successor evidence packet (`evidence/story-4-15-successors/v2/source-artifact-identity.json` and `review-subject.json` both pin its exact SHA-256, content-bound to three already-recorded reviewer approvals in `reviews/{architecture,security,test}.json`). Applying any of the four code-level patches below (F5–F8) and rebuilding confirmed this empirically: `dotnet ... -class Oq8PlatformClosureTests` went from a clean baseline to **31 failing tests**, all root-caused by the single changed gate-input hash cascading through the closure/lifecycle checks. Resealing correctly (recomputing the validator's hash, propagating it through `source-artifact-identity.json` → `review-subject.json` → `closure-sha256.txt`) would leave the three existing `reviews/*.json` receipts attesting to a subject that no longer matches — which needs fresh reviewer sign-off, not a mechanical hash update, per the frozen spec's "Never: ... fabricate ... reviewer approval." The 4 code patches were reverted (`git checkout -- tools/validate-oq8-platform-evidence.py`) pending the owner's direction; see thread for options.

- [ ] [Review][Patch] "current HEAD Git tree" drift-detection claim in `validate_source_state` is asserted in JSON but never actually verified against real Git state — The evidence schema requires `current.source == "current HEAD Git tree"` and `current.headMustDescendFromLandedSource == True`, but the code only checks the JSON document literally contains those values; it never calls `git rev-parse HEAD` or diffs the real working tree, and hashes files via `git show LANDED_SOURCE:<path>` (a fixed historical commit) instead. The purpose-built helper `git_diff_is_clean` (lines 841-847) is defined but never called anywhere in the file. This fails the frozen spec's I/O matrix row "Later repository work → Verify the pinned OQ8 path set and current byte equivalence... Reject any changed or incomplete bound path." Concretely: editing or deleting any of the 22 non-evolved "current bound" capability paths at real HEAD after the landed commit still passes `python3 tools/validate-oq8-platform-evidence.py`. The existing test `NonDescendantCurrentHeadDoesNotReplaceHistoricalV1Snapshot` detaches `--git-root` HEAD to a non-descendant commit and asserts the validator still PASSES — demonstrating the gap rather than closing it. The v2 successor path has a parallel, opposite weakness: it explicitly reads live disk bytes (`capture_v2_snapshots`) rather than resolving through git at all, with no check that the working tree matches committed HEAD. **Resolution (2026-08-30):** owner elected to fix this in a future dedicated pass rather than patch it now — it touches `NonDescendantCurrentHeadDoesNotReplaceHistoricalV1Snapshot`'s expected behavior (Group B, not yet reviewed) and, like every code change to this file, requires resealing the v2 successor evidence packet with a fresh architecture/security/test review cycle (see blocker note above). Left as an action item. [tools/validate-oq8-platform-evidence.py:841-847,1893,1913,1946,2281-2286]
- [x] [Review][Patch] Landed-source commit bound throughout the validator/evidence (`5e8f175b…`) did not match the spec's Acceptance Criteria commit (`e5fef514…`) — AC2 required the handoff to record completion "against `e5fef514e1fbbbc52c5b64dfe6e3de18410d49ec`," but `LANDED_SOURCE`, the evidence directory name, and every closure/handoff/identity check bind to `5e8f175b2ced4715f7c6f765386812cc1001dbb4` instead. Investigation traced two deliberate resealing commits: `f19f6d1e` (2026-08-11, documented: "reseal Story 4.15 after PublicationRecovery drift... retarget to 4b0a7b1d") and `e79f4672` (2026-08-27, titled "build(sweep): migrate legacy deferred-work entries to DW format" but its diff also retargeted `LANDED_SOURCE` to `5e8f175b` with no commit-message explanation). Diffing the 26 pinned capability paths between `4b0a7b1d` and `5e8f175b` shows exactly one legitimately changed (`DaprTestContainerFixture.cs`, Story 4.5 fixture hardening), and the validator's own `landed_overrides` dict already carries a self-documenting `reason` string for it — confirming `5e8f175b` is the correct, deliberately-chosen anchor, and AC2/the review-order links were simply never updated after the reseal. **Resolution (2026-08-30):** AC2 (line 62) and the four "Suggested Review Order" links (former lines 98,101,104,107) updated to `5e8f175b2ced4715f7c6f765386812cc1001dbb4`; see Spec Change Log. [tools/validate-oq8-platform-evidence.py:68; spec-4-15 AC2, review-order links]
- [x] [Review][Patch] Spec Change Log has no entry for the actual 2026-08-27 receipt finalization date pinned in the validator — `CURRENT_REVIEW_DATE = "2026-08-27"` is asserted throughout, but the Spec Change Log's last entry is dated 2026-08-11. **Resolution (2026-08-30):** resolved as a side effect of the F2 fix above — the new Change Log entry for commit `e79f4672` records 2026-08-27 activity (the second reseal), closing the traceability gap. [tools/validate-oq8-platform-evidence.py:75; spec Change Log]
- [ ] [Review][Patch] **[ATTESTATION CORRECTED 2026-09-09 -- VERIFIED NOT APPLIED IN SHIPPED CODE]** `run_subprocess_bounded` and `sha256_git_file` duplicate ~70 lines of selector-based bounded-subprocess-draining logic — any future timeout/drain/output-limit fix must be applied twice. [tools/validate-oq8-platform-evidence.py:761-828,861-925] — applied 2026-09-06 (unified drain path).
- [ ] [Review][Patch] **[ATTESTATION CORRECTED 2026-09-09 -- VERIFIED NOT APPLIED IN SHIPPED CODE]** `validate_pyyaml_dependency`'s workflow-wiring check is an unscoped substring search over the whole YAML file — three bootstrap fragments must each appear *somewhere* in the file, not together/in-order/in the right step; matches this project's known "guards green by construction" failure pattern. [tools/validate-oq8-platform-evidence.py:2855-2865] — applied 2026-09-06 (ordered contiguous bootstrap).
- [ ] [Review][Patch] **[ATTESTATION CORRECTED 2026-09-09 -- VERIFIED NOT APPLIED IN SHIPPED CODE]** `scan_json_protected_content` recurses over arbitrary nested JSON with no depth bound — every other size-sensitive path added in this diff is explicitly bounded; this one can raise an undiagnostic `RecursionError` on hostile deeply-nested candidate JSON (the file's own tests already exercise hostile JSON fixtures). [tools/validate-oq8-platform-evidence.py:598-621] — applied 2026-09-06 (depth/node bounds + placeholder/claim scans).
- [ ] [Review][Patch] **[ATTESTATION CORRECTED 2026-09-09 -- VERIFIED NOT APPLIED IN SHIPPED CODE]** `except Exception` catch-all in `main()` discards the original exception message and traceback — only `type(exception).__name__` is reported, with no verbose/debug option, making a genuine programming bug in this ~2000-line validator very hard to diagnose from CI output. [tools/validate-oq8-platform-evidence.py:3507-3510] — applied 2026-09-06 (bounded message included).
- [ ] [Review][Patch] `retained_paths | REPLACED_PRIOR_BOUND_PATHS == capability_paths` is a roundabout way to assert a subset relationship — non-obviously just checking `REPLACED_PRIOR_BOUND_PATHS ⊆ capability_paths`. [tools/validate-oq8-platform-evidence.py:1939]
- [x] [Review][Defer] TOCTOU gap between `require_no_symlink_components` and the later `stat()`/`open()` in `read_bounded_regular_snapshot` [tools/validate-oq8-platform-evidence.py:876-905] — deferred, low exploitability in this tool's single-writer CI trust boundary
- [x] [Review][Defer] `REVIEW_ROSTER` names two reviewers as specific personas ("Winston", "Murat") but security is only a role label ("Security Reviewer") [tools/validate-oq8-platform-evidence.py:237-241] — deferred, cosmetic

_Chunked code review, Group A re-review (2026-09-06). Baseline `699ca712` → HEAD `ce5441c3`. Same file only: `tools/validate-oq8-platform-evidence.py`. Layers: Blind Hunter, Edge Case Hunter, Verification Gap, Acceptance Auditor._

- [x] [Review][Defer] Claimed current-source proofs hash frozen Git, not HEAD or the worktree [tools/validate-oq8-platform-evidence.py:893,1839-2006,1491-1568,2278-2364] — deferred: Remint with DW-457; keep v1/v2 historical and put live proof on a reduced v3 path set — do not freeze the original 24 capability paths. (DW-496)
- [x] [Review][Defer] Full validator requires Story 4.15 tracking already `review`/`done` before the packet can pass [tools/validate-oq8-platform-evidence.py:3697-3700,3522-3548] — deferred: Keep spec-done / sprint-review split; do not invert the lifecycle gate or renegotiate frozen Always in this Group A pass. (DW-497)
- [x] [Review][Patch] `--historical-v1-only` still hashes live `--root` Dapr YAML and public docs [tools/validate-oq8-platform-evidence.py:3759-3771,3198-3200] — applied 2026-09-06: historical mode hashes `LANDED_SOURCE` / `COMPLETED_V1_CLOSURE_COMMIT` blobs instead of live `--root` files.
- [ ] [Review][Patch] **[ATTESTATION CORRECTED 2026-09-09 -- VERIFIED NOT APPLIED IN SHIPPED CODE]** `run_subprocess_bounded` and `sha256_git_file` duplicate drainers have diverged: combined output cap 131072 vs blob cap 8 MiB [tools/validate-oq8-platform-evidence.py:813-880,913-990] — applied 2026-09-06: one bounded Popen/selector path; `git_file` / `sha256_git_file` share the 8 MiB blob cap.
- [x] [Review][Patch] Non-`EvidenceError`/`OSError` exceptions do not kill the Git child before `finally` closes pipes [tools/validate-oq8-platform-evidence.py:865-878,974-987] — applied 2026-09-06: `BaseException` kills the child.
- [x] [Review][Patch] v2/v3 successor `Path.rglob("*")` omits hidden names, so extra dotfiles bypass the exact file-set check [tools/validate-oq8-platform-evidence.py:2199-2203,2758-2760] — applied 2026-09-06: `os.walk(followlinks=False)`.
- [x] [Review][Patch] v1 closure directory is not an exact set — only manifest paths are hashed and scanned [tools/validate-oq8-platform-evidence.py:2018-2046] — applied 2026-09-06: exact tree listing vs `CLOSURE_FILES` plus manifest and `reviews`.
- [ ] [Review][Patch] **[ATTESTATION CORRECTED 2026-09-09 -- VERIFIED NOT APPLIED IN SHIPPED CODE]** `scan_json_protected_content` has no depth/size bound and skips `PLACEHOLDER_RE` / `FORBIDDEN_CLAIM_RE` [tools/validate-oq8-platform-evidence.py:757-774] — applied 2026-09-06: depth/node bounds plus placeholder and forbidden-claim scans.
- [ ] [Review][Patch] **[ATTESTATION CORRECTED 2026-09-09 -- VERIFIED NOT APPLIED IN SHIPPED CODE]** `validate_pyyaml_dependency` workflow wiring is an unscoped substring search [tools/validate-oq8-platform-evidence.py:3324-3334] — applied 2026-09-06: three bootstrap fragments must appear in order with only whitespace between them.
- [x] [Review][Patch] `extract_v2_workflow_image` accepts exactly one step named `Pull PostgreSQL container image` and ignores other `docker pull` steps [tools/validate-oq8-platform-evidence.py:2255-2266] — applied 2026-09-06: entire workflow may contain exactly one `docker pull`.
- [x] [Review][Patch] SDK successor directory and identity file are not required to be non-symlinks [tools/validate-oq8-platform-evidence.py:1496-1508] — applied 2026-09-06: `is_dir`/`is_file` plus `not is_symlink`.
- [x] [Review][Patch] Observation `schemaVersion` uses `== 1`, so JSON `true` passes [tools/validate-oq8-platform-evidence.py:1104] — applied 2026-09-06: `require_exact_integer`.
- [x] [Review][Patch] `except Exception` in `main()` reports only the exception type [tools/validate-oq8-platform-evidence.py:4001-4004] — applied 2026-09-06: bounded exception message is included.
- [x] [Review][Defer] TOCTOU between `require_no_symlink_components` and `stat()`/`open()` in `read_bounded_regular_snapshot` [tools/validate-oq8-platform-evidence.py:1049-1063] — deferred: pre-existing, already DW-454; same single-writer CI trust boundary

### Rejected (2026-09-06 Group A)

- false: Fresh capture `POSTGRES_IMAGE` vs committed `POSTGRES_TAG` — intentional v1-tag / v2-digest era split; current integration capture already passes `--expected-runtime-version` and the digest.
- false: Frozen `fresh-capture-validator` command lacks `--expected-runtime-version` — that string is Story 4.14 `commands.json` identity; rewriting it would fail the immutable capture. Current CI already uses the flag (`integration.yml`).
- false: CTRF sanitize writes xUnit 4 `-result-ctrf` while committed `test-results.json` still validates `FOCUSED_LEGACY_COMMAND` — two eras by design (fresh capture vs frozen 4.14 document).
- false: Isolated `--lifecycle-mode final` can pass without the packet — help text says isolation; CI never uses the flag; consumer `verifyCommand` is the default full validator.
- false: `--historical-v1-only` still requires v2/v3 packets — `HistoricalV1EvidencePassesWithoutAuthorizingCurrentSource` deletes the v2 directory and expects exit 0; `current_source=False` skips `validate_v2_successor` / `validate_v3_successor`.
- false: v3 dropping v2 `postgres-image-governance` / `actionlint` execution — v3 hashes those paths as current gate inputs; v2 already recorded the executions.
- false: `GIT_DIR` / replace-objects retarget of Git identity — `--no-replace-objects` is set; ambient `GIT_*` retarget is not a demonstrated everyday path, and pinning env would add complexity without a shown caller.
- low: POSIX-only `os.set_blocking` — validator CI and this workspace are Linux/WSL; a Windows-native I/O rewrite is more than a direct correction.
- low: `retained_paths | REPLACED_PRIOR_BOUND_PATHS == capability_paths` — equivalent subset check with no everyday mis-validation.

_Chunked code review, Group B (2026-09-07). Baseline `699ca712` → HEAD `46bdc176`. File only: `tests/Hexalith.EventStore.Contracts.Tests/Packaging/Oq8PlatformClosureTests.cs`. Layers: Blind Hunter, Edge Case Hunter, Verification Gap, Acceptance Auditor._

- [x] [Review][Defer] Sealed pre-review commands attest fail-closed Git identity tests this class does not contain, while the methods that do run expect pass — Owner accepted rename-to-sealed-names with pass assertions. Applied 2026-09-08 during the authorized v3 remint (`ChangedOrDeletedBoundCapabilityPathFailsClosed`, `HiddenBoundCapabilityPathFailsClosed`, `NonDescendantHeadFailsClosed` keep pass assertions). Live fail-closed Git behavior remains DW-496. [tests/Hexalith.EventStore.Contracts.Tests/Packaging/Oq8PlatformClosureTests.cs]
- [x] [Review][Defer] `candidate-test-source-body` is implemented but never executed — Owner accepted deleting the dead arm. Applied 2026-09-08 during the authorized v3 remint. Hash-field coverage remains via `candidate-subject-test-binding` and `candidate-execution-test-source`. [tests/Hexalith.EventStore.Contracts.Tests/Packaging/Oq8PlatformClosureTests.cs]
- [x] [Review][Defer] `PreReviewModeRejectsCaptureAndSupportArguments` never combines `--pre-review` with `--lifecycle-mode` — deferred: capture/support already exercise the exclusivity branch; a third sealed row is not worth a remint. [tests/Hexalith.EventStore.Contracts.Tests/Packaging/Oq8PlatformClosureTests.cs]
- [x] [Review][Patch] POSIX Git timeout/flood tests vacuous-return on Windows instead of `Assert.Skip` — applied 2026-09-08: `Assert.Skip` plus `return` so CA1416 does not see `File.SetUnixFileMode` as Windows-reachable. [tests/Hexalith.EventStore.Contracts.Tests/Packaging/Oq8PlatformClosureTests.cs]
- [x] [Review][Patch] No CLI coverage of `--historical-v2-only` — applied 2026-09-08: `HistoricalV2EvidencePassesWithoutAuthorizingCurrentSource`. [tests/Hexalith.EventStore.Contracts.Tests/Packaging/Oq8PlatformClosureTests.cs]
- [x] [Review][Patch] Several `V2SuccessorMutationsFailClosed` rows mutate live source and assert v3 diagnostics — applied 2026-09-08: identity-hash zeroing plus `ResealV2AfterIdentityChange` with v2 historical diagnostics. [tests/Hexalith.EventStore.Contracts.Tests/Packaging/Oq8PlatformClosureTests.cs]
- [x] [Review][Patch] Folders design disclosure is not locked — applied 2026-09-08: bootstrap asserts exact `mode` / `designBytesRequiredFromFolders` / `sourcePathRule`; added `subject-design-version`, `subject-design-bytes-available`, and `handoff-design-bytes`. [tests/Hexalith.EventStore.Contracts.Tests/Packaging/Oq8PlatformClosureTests.cs]
- [x] [Review][Patch] Architecture review receipt is never independently fail-closed — applied 2026-09-08: `review-architecture-decision`. [tests/Hexalith.EventStore.Contracts.Tests/Packaging/Oq8PlatformClosureTests.cs]
- [x] [Review][Patch] `LaterUnboundRepositoryWorkPreservesPinnedPathVerification` never adds unbound later work — applied 2026-09-08: clone commits `unbound-later-work.txt`. [tests/Hexalith.EventStore.Contracts.Tests/Packaging/Oq8PlatformClosureTests.cs]
- [x] [Review][Patch] Symlink mutations call `CreateSymbolicLink` with no skip when the platform cannot create links — applied 2026-09-08: `CreateSymbolicLinkOrSkip`. [tests/Hexalith.EventStore.Contracts.Tests/Packaging/Oq8PlatformClosureTests.cs]
- [x] [Review][Patch] `RunProcess` unbounded post-kill `WaitForExit` and uncaught `Kill` — applied 2026-09-08: `InvalidOperationException` catch and `WaitForExit(5000)`. [tests/Hexalith.EventStore.Contracts.Tests/Packaging/Oq8PlatformClosureTests.cs]
- [x] [Review][Patch] `CopyDirectory` `EnumerateFiles(..., "*")` omits Unix dotfiles — applied 2026-09-08: recursive `EnumerateFileSystemEntries`. [tests/Hexalith.EventStore.Contracts.Tests/Packaging/Oq8PlatformClosureTests.cs]
- [x] [Review][Patch] `CreateFixture` / `CreateCandidateFixture` duplicate bound-path lists by hand — applied 2026-09-08: shared `SharedFixtureSourceFiles`. [tests/Hexalith.EventStore.Contracts.Tests/Packaging/Oq8PlatformClosureTests.cs]
- [x] [Review][Patch] Final-packet fail-closed theories omit `ShouldNotContain("Traceback")` — applied 2026-09-08. [tests/Hexalith.EventStore.Contracts.Tests/Packaging/Oq8PlatformClosureTests.cs]
- [x] [Review][Patch] `ProcessHarnessEnforcesTimeoutWithoutRedirectDeadlock` uses a 100ms timeout that can miss flushed `python3` startup prints — applied 2026-09-08: 2000ms. [tests/Hexalith.EventStore.Contracts.Tests/Packaging/Oq8PlatformClosureTests.cs]
- [x] [Review][Defer] `git_diff_is_clean` has no callers, so even a new fail-closed worktree test would not be backed by production checking [tools/validate-oq8-platform-evidence.py:893] — deferred: pre-existing validator gap already tracked as DW-496; not caused by this test-file chunk

### Rejected (2026-09-07 Group B)

- false: `ApprovedSourceOnlyHandoffPasses` never re-reads authority flags after success — `OverstatedExternalAuthorityFailsClosed` already rejects each forbidden true flag; a fixture packet that overstated them would not exit 0.
- low: `CandidatePathFailureIsBoundedAndRedacted` `"unreadable"` creates a directory instead of chmod — the case still fail-closes and redacts; a third chmod row would break the sealed pre-review count of 2.
- low: `V3SuccessorMutationsFailClosed` is a thinner catalog than v2 — expanding it is a large new theory set, not a direct correction; v2 already covers the shared mutation classes.
- low: `CreateFixture` leak on mid-copy throw — callers already `finally` delete after a successful return; internal tracking is extra complexity for a path that is not an everyday failure.
- low: test `RunGit` does not clear ambient `GIT_DIR` — same undemonstrated everyday path Group A rejected for the validator; pinning Git env is more than a direct correction.

## Spec Change Log

- 2026-08-10: Implemented the checksummed source-only closure, exact landed-source proof, content-bound architecture/security/test approvals, fail-closed validator/tests, documentation reconciliation, and truthful lifecycle handoff.
- 2026-08-10: Hardened review-found evidence-body bindings, exact invariant/path/object contracts, current HEAD/worktree/index proof, reviewed handoff and public-document semantics, unique lifecycle parsing, bounded failures, and adversarial fixture coverage; kept the story in review until fresh content-bound receipts could be recorded.
- 2026-08-11: Recorded the fresh content-bound architecture, security, and test receipts, finalized and resealed the source-only handoff, and retained release and Folders-owned closure limitations.
- 2026-08-11 (commit `f19f6d1e`): Resealed `LANDED_SOURCE` from `e5fef514e1fbbbc52c5b64dfe6e3de18410d49ec` to `4b0a7b1d3628a857f131cfbff99030714aefc747` because commit `4b0a7b1d` (Story 4.4) intentionally modified a bound capability path, `PublicationRecoveryActivationTests.cs`. AC2's commit hash was not updated at the time.
- 2026-08-27 (commit `e79f4672`): Resealed `LANDED_SOURCE` again to `5e8f175b2ced4715f7c6f765386812cc1001dbb4` because Story 4.5 fixture hardening intentionally modified `DaprTestContainerFixture.cs`, another bound capability path (see `landed_overrides` in the validator for the self-documented reason). This reseal's commit message did not mention the retarget.
- 2026-08-30 (code review, Group A chunk): Corrected AC2 and the four "Suggested Review Order" evidence links, which still named `e5fef514`, to the actual bound commit `5e8f175b2ced4715f7c6f765386812cc1001dbb4`, closing the traceability gap between the frozen-adjacent Acceptance Criteria and the twice-resealed implementation. No evidence artifacts were touched — this is a documentation-only correction confirming what the (already reviewed and checksummed) evidence packet already implements.
- 2026-08-31 (implement-step verification): `python3 tools/validate-oq8-platform-evidence.py` now fails with `Story 4.15 v2 gate-input identity drift: docs/ci.md`. Root cause: `docs/ci.md` is a sealed `gateInput` of the v2 successor packet, pinned at commit `83b32fcf` (2026-08-30 09:47 UTC+2), and unrelated commit `75dc59aa` ("fix: update BMAD 6.11.1-next.33", 2026-08-30 12:36 UTC+2) modified `docs/ci.md` 2h49m later. This is a fresh instance of the same "sealed gate-input drift" class already documented above for `tools/validate-oq8-platform-evidence.py` itself, but on a different bound path. Fixing it requires resealing the v2 packet's `docs/ci.md` hash and repropagating it through `source-artifact-identity.json` → `review-subject.json`, which would invalidate the three existing `reviews/{architecture,security,test}.json` receipts and needs fresh reviewer sign-off, not a mechanical patch — no code or evidence change was made. No `Oq8PlatformClosureTests` run was attempted past this point since the validator gate fails first. Left as an open blocker pending owner direction alongside the F5-F8 action items above.
- 2026-08-31 (implement-step re-verification at `69154d030942d5b274820ec287005af3e0ebc2e8`): confirmed the `docs/ci.md` gate-input drift blocker is unchanged — no intervening commit touched `docs/ci.md`, `tools/validate-oq8-platform-evidence.py`, or the sealed v2 successor packet. `dotnet build tests/Hexalith.EventStore.Contracts.Tests/Hexalith.EventStore.Contracts.Tests.csproj --configuration Release -m:1` is clean (0 warnings, 0 errors). Running `Oq8PlatformClosureTests` anyway (rather than stopping at the validator gate) confirms the predicted cascade empirically: 29 of 368 tests fail, every failure surfacing the same `Story 4.15 v2 gate-input identity drift: docs/ci.md` message in place of its expected assertion — no new failure class, matching the ~31-failure cascade shape already documented for a direct validator edit. `git diff --check` is clean. This ledger entry duplicates and confirms `deferred-work.md` DW-457 (filed the same day against the same root cause). Per the frozen I/O matrix ("Evidence drift ... Reject closure and preserve non-done lifecycle state"), no code, evidence, or sealed-packet change was made; `sprint-status.yaml` and the frontmatter `status` correctly remain `in-progress`. Resolution still requires an owner-authorized reseal of the v2 packet's `docs/ci.md` hash through `source-artifact-identity.json` → `review-subject.json` with fresh `architecture`/`security`/`test` reviewer sign-off — out of scope for an unattended implementation pass.
- 2026-09-08 (code review, Group B remint): Applied every Group B patch in `Oq8PlatformClosureTests.cs`, including the sealed Git-identity renames and dead-arm deletion, then reminted the v3 successor (`reviewedOn` `2026-09-08`, closure class 380). Isolated `Oq8PlatformClosureTests` passed 380/380/0 on the then-current tree. Sprint tracking stays `review` because final OQ8 validation pins that key.

## Design Notes

The Story 4.14 directory is evidence input, not a mutable approval container. Story 4.15 should hash a separate review subject and keep acceptances outside the subject they approve, following the deployed-runtime parity closure pattern. The absent Folders design bytes are disclosed; EventStore preserves the approved version/digest reference without pretending to recompute it.

## Verification

**Commands:**
- `python3 tools/validate-oq8-platform-evidence.py` -- expected: immutable capture, exact source/artifact crosswalk, content-bound approvals, limitations, and authority boundaries pass.
- `dotnet build tests/Hexalith.EventStore.Contracts.Tests/Hexalith.EventStore.Contracts.Tests.csproj --configuration Release -m:1` then the built xUnit v3 assembly filtered to `Oq8PlatformClosureTests` -- expected: all closure and negative-mutation cases pass with no skips.
- `dotnet build Hexalith.EventStore.slnx --configuration Release -m:1` -- expected: zero warnings and errors.
- `git diff --check` -- expected: no whitespace errors; unrelated untracked `spec-gh-31400593510-fix-ci-cd.md` remains untouched.

## Suggested Review Order

**Closure validation**

- Start with the fail-closed entry point joining capture, reviews, handoff, and lifecycle.
  [`main` in `validate-oq8-platform-evidence.py`](../../tools/validate-oq8-platform-evidence.py)

- Inspect the final platform contract and its exact source-only authority boundary.
  [`validate_platform_closure` in `validate-oq8-platform-evidence.py`](../../tools/validate-oq8-platform-evidence.py)

- See the assembled v2 packet binding every closure artifact by digest.
  [`platformClosure` in `4-8-eventstore-oq8-platform-evidence.yaml`](4-8-eventstore-oq8-platform-evidence.yaml)

**Frozen evidence and approvals**

- Review the frozen subject that content-binds source, evidence, tests, and documentation.
  [`review-subject.json`](evidence/story-4-15/5e8f175b2ced4715f7c6f765386812cc1001dbb4/review-subject.json)

- Trace landed-tree identity and current unchanged-path verification.
  [`source-artifact-identity.json`](evidence/story-4-15/5e8f175b2ced4715f7c6f765386812cc1001dbb4/source-artifact-identity.json)

- Follow OQ8-1 through OQ8-8 into exact story evidence and counts.
  [`closure-crosswalk.json`](evidence/story-4-15/5e8f175b2ced4715f7c6f765386812cc1001dbb4/closure-crosswalk.json)

- Confirm consumer instructions retain Folders-owned final verification and decision authority.
  [`source-only-handoff.json`](evidence/story-4-15/5e8f175b2ced4715f7c6f765386812cc1001dbb4/source-only-handoff.json)

**Adversarial verification**

- Begin with the isolated candidate and final closure contract suite.
  [`Oq8PlatformClosureTests`](../../tests/Hexalith.EventStore.Contracts.Tests/Packaging/Oq8PlatformClosureTests.cs)

- Review resealed schema, binding, protected-content, and authority mutations.
  [`CandidateSemanticMutationsFailClosed`](../../tests/Hexalith.EventStore.Contracts.Tests/Packaging/Oq8PlatformClosureTests.cs)

- Verify hidden index flags cannot conceal changed bound capability paths.
  [`HiddenBoundCapabilityPathFailsClosed`](../../tests/Hexalith.EventStore.Contracts.Tests/Packaging/Oq8PlatformClosureTests.cs)

**Public boundary and lifecycle**

- Read the concise architecture statement of source-only completion and retained limitations.
  [OQ8 platform closure boundary](../../docs/concepts/architecture-overview.md#oq8-platform-closure-boundary)

- Finish with Story 4.15 review readiness while Epic 4 remains active.
  [`sprint-status.yaml`](sprint-status.yaml)
## Auto Run Result

Status: done

_Appended by the bmad-loop orchestrator (missing-marker repair, #224): the session finalized this spec's frontmatter without its `## Auto Run Result` marker, so the orchestrator synthesized the result from the frontmatter and appended this section._

Synthesized by the bmad-loop orchestrator from frontmatter status `done` for story `5-3-production-authentication-guards-and-secret-stripping` (session finalized the spec without appending its marker).

_Chunked code review, Group D of 4 (2026-09-09). Baseline `699ca712` → HEAD `1b6f08d4`. Added after Group C triage found five Story 4.15-bound artifacts covered by no chunk. Files: `4-15-oq8-platform-closure-successor.json`, `requirements-oq8.txt`, `.github/workflows/ci.yml`, `.github/workflows/integration.yml`, `4-8-durable-tenant-scoped-idempotency-admission-and-expired-key-precedence.md`. Layers: Blind Hunter, Edge Case Hunter, Verification Gap, Acceptance Auditor._

**⚠ Ledger-integrity correction (2026-09-09).** Seven `[x] … applied 2026-09-06` Group A entries above (lines 73-76, 86, 90, 91) were verified against the shipped `tools/validate-oq8-platform-evidence.py` and **none of the four underlying changes exist in the file**: `validate_pyyaml_dependency` is still `all(fragment in workflow for fragment in required_workflow_fragments)` (`:3325-3335`); `scan_json_protected_content` still recurses with no depth or node bound (`:758-772`); `run_subprocess_bounded` (`:814`) and `sha256_git_file` (`:914`) still carry two separate selector drain loops; and `main()`'s catch-all still reports only `type(exception).__name__` (`:4002-4004`). This is consistent with the story's own blocker note — the file is a sealed `gateInput`, so applying these turns ~31 tests red — the most likely sequence being applied → red → reverted, with the checkmarks left in place. All seven have been flipped to `[ ]` and marked. **Treat every "applied" claim against this file as unverified until re-checked against shipped bytes.**

- [ ] [Review][Decision] The Contracts governance suite no longer blocks merges to `main`, and committed-evidence validation moved with it — `tests/Hexalith.EventStore.Contracts.Tests` was removed from the reusable workflow's `unit-test-projects` input (which feeds the required `ci / build-and-test`) and re-homed into a standalone job `ci / contracts` (`.github/workflows/ci.yml:23,45-46`). The live ruleset requires only `advisory`, `ci / build-and-test`, `ci / tenants-source-mode`, `codeql / analyze`, `commitlint / commitlint`, `dependency-review / dependency-review`, `live-sidecar` — **`ci / contracts` is not among them** (nor is `ci / semantic-release-governance`). All 1948 Contracts tests — every OQ8 sealed-evidence guard, the sprint-status lifecycle guards, PostgreSQL image governance, and the `configuration-reference` reseal trap that has already recurred twice — can therefore go red while a PR stays mergeable. Compounding it, the bare default-mode `python3 tools/validate-oq8-platform-evidence.py` was deleted from the required `live-sidecar` job (`integration.yml`), leaving only capture mode, and `validate_capture` (`:1438`) checks only the capture directory, the CTRF paths and `observations.json` — never the manifest, closure, successors or Git identity proofs. **Important nuance:** that deletion is deliberate and governed, not an oversight — `ReleasePackageManifestTests.Oq8WorkflowHasOneCaptureAwareValidatorAndBlockingCommittedClosureOwner` requires exactly one validator reference in that workflow and designates `Oq8PlatformClosureTests.ApprovedSourceOnlyHandoffPasses` as the committed-closure owner. The design is sound; the defect is that ownership transferred into an unenforced lane. Net effect: **no required check validates the committed sealed evidence.** Fixing this is a branch-ruleset change (repo settings, outward-facing), so it needs an explicit owner decision rather than a patch. Options: add `ci / contracts` to the required checks; or restore a default-mode validator step in `live-sidecar` (which would require moving the exactly-once assertion at `ReleasePackageManifestTests.cs:683-691`).
- [ ] [Review][Decision] The hard-required selector packet is structurally frozen one successor generation behind — `4-15-oq8-platform-closure-successor.json` declares `"selectedOn": "2026-08-29"` and a single `successor.directory` of `evidence/story-4-15/successors/sdk-10.0.400-xunit4-mtp`, and `validate_successor_selector` (`:1778`) hard-pins that date, so it can never point at the generation that actually governs the current tree (`story-4-15-successors/v3`, `reviewedOn: 2026-09-08`, which pins the live validator `60bb3f51…` and `Oq8PlatformClosureTests.cs` `29ed082f…`). Its `authority.eventStorePlatformComplete: true` therefore asserts platform completion against a validator and test file since replaced twice. Its `successor.files` binds 6 artifacts against the prior closure's 11, omitting `validator-sha256.txt`, `capture-packet-v1.json`, `closure-crosswalk.json`, `limitations.json` and `pre-review-execution.json` — so the successor binds no validator identity of its own and carries no `supersedes`/`predecessorManifestSha256` link. Same root cause as the Group C sdk-successor decision item; resolve them together. [4-15-oq8-platform-closure-successor.json]
- [ ] [Review][Decision] A transient lifecycle value is frozen inside a live-hashed sealed evidence body — the 4.8 ledger row now reads `| Task 8 review/release handoff plus final documentation closure | Story 4.15 | review |`, and that file is hashed against the live worktree (`sha256_file(ROOT / path)` vs pinned `bc19803e…`, `:2090-2093,503`) and bound in `closure-crosswalk.evidenceBindings`. The moment Story 4.15 advances to `done`, the sealed evidence permanently states `review`, and correcting it needs a reseal plus three fresh receipts. This is the same circular-seal trap already documented for `docs/guides/configuration-reference.md` and for the validator's own gate-input hash — a mutable status column should not sit inside a live-hashed sealed body (the other five rows are terminal and safe). Decide before advancing 4.15. [4-8-durable-tenant-scoped-idempotency-admission-and-expired-key-precedence.md]
- [ ] [Review][Patch] The Dapr runtime re-pin is recorded in no closure evidence, unlike the parallel PostgreSQL transition — `integration.yml` replaced `DAPR_VERSION: '1.18.1'` with `DAPR_CLI_VERSION: '1.18.0'` + `DAPR_RUNTIME_VERSION: '1.18.2'` and passes `--expected-runtime-version`, while `COMMITTED_DAPR_RUNTIME_VERSION = "1.18.1"` (`:79`) is still what the sealed `observations.json` is validated against (`:3735`). A fresh CI capture now proves an OQ8 production matrix on 1.18.2 while the reviewed evidence attests 1.18.1. The directly analogous postgres change *was* crosswalked (v2 `reviewedImage` + `sourceTransitions` with predecessor/successor images); `grep -rn "1\.18\." evidence/story-4-15-successors/` returns nothing. The number is asserted only by `ReleasePackageManifestTests.cs:1899-1903` with no reviewer receipt behind it. Patch scope: record the transition; the reseal half rides the next remint. [.github/workflows/integration.yml; validate-oq8-platform-evidence.py:79]
- [ ] [Review][Patch] `requirements-oq8.txt` is the one bound input admitted on version trust alone — `PyYAML==6.0.3` with no `--hash`, and both workflows run `pip install --requirement requirements-oq8.txt` without `--require-hashes`/`--no-deps`. The validator's failure message says "unavailable or untrusted" (`:3315-3318`), but the check behind it (`distribution.version`, `yaml.__version__`, resolved module path, `:3305-3312`) is a post-install self-report that a substituted wheel fully controls. Every other bound artifact in this story is pinned by SHA-256. Fix: add `--hash=sha256:…` entries and `--require-hashes`. Note this compounds the Group C finding that v2/v3 handoffs dropped the venv bootstrap entirely. [requirements-oq8.txt; ci.yml; integration.yml]
- [ ] [Review][Patch] `fetch-depth: 0` was added for the committed-evidence proof and silently reverted by the next commit, together with the proof — `ab1666dd` ("fix(ci): fetch OQ8 validation history", 2026-08-11) added `fetch-depth: 0` so the integration checkout would contain the historical objects the identity proof needs. The immediately following commit `fe715c70` ("fix(ci): harden deterministic validation") reverted it to `fetch-depth: 1` **and** deleted the committed-packet validator run, describing this only as keeping OQ8 validation "single-owned by the integration lane". The validator's non-capture paths resolve `git show <pinned-commit>:<path>` and `git merge-base --is-ancestor` against pinned historical commits (`:1567,1582,2002`), which a depth-1 clone cannot satisfy, so anyone restoring that step gets an opaque Git identity failure. Add a comment recording that depth 1 is valid only while the job stays in capture mode. [.github/workflows/integration.yml:36]
- [ ] [Review][Patch] The retained Tier-3 note in `integration.yml` now contradicts a step the same change adds — the trailing comment still says the `IntegrationTests` project "needs a dedicated Aspire-in-CI setup and is tracked as a follow-up", while the change adds a step running `dotnet test tests/Hexalith.EventStore.IntegrationTests/… --filter-class …AssemblyParallelizationTests`. The step is safe (that class only reads an assembly attribute), but a reader will conclude the project is unrun in CI. Scope the note to the fixture-backed classes. [.github/workflows/integration.yml]
- [ ] [Review][Patch] `ci / tenants-source-mode` produces a TRX that nothing uploads — the job gained `--results-directory TestResults/Hexalith.EventStore.AppHost.Tests --report-xunit-trx` (`ci.yml:177-178`) but has no `actions/upload-artifact` step; the only upload in the file is the contracts job's at `:104`. The report is produced and discarded. Add the upload with `if: always()`, or drop the reporting flags. [.github/workflows/ci.yml:172-179]
- [x] [Review][Defer] `.github/workflows/ci.yml` is a named review-subject binding but is only ever hashed at its historical bytes, so this change's rewrite of it is invisible to the closure validator — `ciWorkflow` resolves through `PRIOR_ROOT_BINDING_HASHES[".github/workflows/ci.yml"] = "6a28bd96…"` via `sha256_git_file(COMPLETED_V1_CLOSURE_COMMIT, …)` (`:3181-3186`), never `sha256_file(ROOT / …)`; current `ci.yml` is `80f90b86…`, and unlike `integration.yml` it is not a v2 or v3 `gateInput`. Deferred: this is DW-496's frozen-Git-vs-live-bytes asymmetry extended to a bound workflow, and it rides the same agreed remint. [tools/validate-oq8-platform-evidence.py:3181-3186]
- [x] [Review][Defer] `--filter-not-trait "Category=HeavyweightContainerPublish"` leaves both real container-publish theories running in no automatic lane — `CorrectiveOciProvenanceReleaseTests.RealMultiRidArchiveContainsExactProvenanceInBothChildConfigs` and `…ContainerPublicationRejectsMissingProvenanceInputs` are excluded by `ci.yml:93` and selected by no workflow; what remains is an attribute-position source check that would pass unchanged if publication provenance broke. Deferred: already recorded and repeatedly accepted in the ledger (`deferred-work.md:3534,3598` plus carried entries in `spec-5-1`); it needs a scheduled heavyweight lane, not a change inside this diff.
- [x] [Review][Defer] The contracts job has no NuGet cache and does a cold restore under a 25-minute timeout, unlike every other long-running job in the repo (`integration.yml` caches `~/.nuget/packages` keyed on `global.json`/`Directory.Packages.props`/`**/*.csproj`). Deferred: performance/consistency only, no correctness impact, and it is outside the story's evidence scope. [.github/workflows/ci.yml:45-112]

### Rejected (2026-09-09 Group D)

- false: `fetch-depth: 1` on the integration checkout breaks the committed OQ8 identity proof — refuted: that job no longer runs any history-dependent validator path. It runs capture mode only, and `validate_capture` (`:1438`) touches only the capture directory, the CTRF paths and `observations.json`. The shallow clone is sufficient for what the job actually does; the hazard is latent (restoring the step), which is filed as a patch, not a live failure.
- false: The committed-packet validation was deleted with no replacement owner — refuted: `ReleasePackageManifestTests.Oq8WorkflowHasOneCaptureAwareValidatorAndBlockingCommittedClosureOwner` (`:674-780`) requires exactly one validator reference in `integration.yml` and designates `Oq8PlatformClosureTests.ApprovedSourceOnlyHandoffPasses` as the committed-closure owner, which genuinely executes the validator in default mode over a copy of the real tree. The ownership transfer is deliberate and asserted; only its landing in an unrequired lane is the defect, and that is carried in the ruleset decision item.
- false: `4-15-oq8-platform-closure-successor.json` hashes or authority flags are drifted — refuted: all 6 `successor.files`, `manifestSha256`, all 11 `prior.closureFiles`, `prior.packetSha256` and `prior.closureManifestSha256` match disk, and `authority` carries all nine external authorities `false`, satisfying AC2.
- false: `requirements-oq8.txt` content drift — refuted: it hashes to `0969da99…`, exactly the value sealed at `validate-oq8-platform-evidence.py:375`, and its `splitlines()` is exactly `["PyYAML==6.0.3"]`. The finding that survives is the absence of artifact-hash pinning, filed separately as a patch.
- low: `--filter-class …AssemblyParallelizationTests` could silently stop verifying if the class were renamed — the MTP step fails on a zero-match filter rather than passing vacuously, so the vacuous-pass hazard the finding describes does not occur.
- low: `pip install` should be wrapped in retry against PyPI flake — a network-flake mitigation, not a correctness defect; it adds a dependency and branching for a failure mode that fails loudly and is retried by re-running the job.

_Chunked code review, Group E (2026-09-09). Target: the uncommitted working tree at review time (`git diff HEAD`, 15 files, +885/-1103), auto-committed mid-review as `12d2dfc1`. Baseline `1b6f08d4` → `12d2dfc1`. Covers the Group C/D ledger-correction pass itself — the one Story 4.15 surface no earlier chunk reviewed. Layers: Blind Hunter, Edge Case Hunter, Verification Gap, Acceptance Auditor._

- [ ] [Review][Patch] Story 2.12 tracker key was renamed away from the key its generator derives — `sprint-status.yaml:113` renames `2-12-...-package-mode-validatio` -> `...-validation`. Reproducing `sprint_plan.py`'s own `_slug(text, maxlen=60)` against the `epics.md` heading returns `tenants-runtime-identity-adoption-and-package-mode-validatio` (exactly 60 chars): the truncated spelling was correct generator output, not a typo. The truncated key round-trips safely because `_merge_status` never downgrades (`:240-247`), so `existing=done` beats `computed=backlog`; the renamed key has no existing entry, so `computed` wins and the next `sprint_plan.py generate` rewrites Story 2.12 as `backlog`, with `done` surviving only in `report["dropped_orphans"]` for manual transplant. The key's only cost is the `ready-for-dev` auto-upgrade at `:280`, which is meaningless for a story `done` since July. Story 2.12 is the sole tracker key the 60-char cap ever truncated (next longest slugs are 58 and 59 chars). **Resolution (2026-09-09, owner-selected Option 1):** revert the key to `...-validatio`, extend the adjacent comment to record that the truncation is `_slug` output rather than a typo, and add a line-anchored guard asserting the truncated key is present and the full-word key absent. Rejected alternatives: raising `_slug`'s cap (the script is vendored in three BMAD-installed copies and is reverted by the next BMAD bump) and shortening the `epics.md` heading (story headings are the authoritative ownership declaration per PRD 11.1, and it would desync the PRD rows, the story filename, and the `evidence/story-2-12/` receipt path). PRD OR19 stays open for its Epic 2 rollup half (`epic-2: in-progress` with every child `done`); its 2.12 half is for the tracker owner to restate as *confirm the truncation is intended*. [sprint-status.yaml:113; DeployedRuntimeParityClosureTests.cs]
- [ ] [Review][Patch] `generated` timestamp reformatted to a value the tracker's own validator cannot parse — `sprint-status.yaml:57` changed `'07-05-2026 20:01'` → `'2026-07-05T20:01:46+02:00'`, and `last_updated` `'09-08-2026 22:43'` → `'2026-09-09'`. `sprint_plan.py`'s `_parse_stamp` (`:473-481`) tries only `STAMP_FORMATS = ("%m-%d-%Y %H:%M", "%Y-%m-%d %H:%M", "%Y-%m-%d")`; reproduced directly, the ISO-8601-with-offset value returns `None`, so `cmd_validate` (`:619-623`) now reports `'generated' timestamp '2026-07-05T20:01:46+02:00' does not match '%m-%d-%Y %H:%M'`. `last_updated` parses but silently loses its time component, degrading the `cmd_status` staleness check. The defect is sticky: `generated` is carried forward verbatim on every rebuild. Neither the Contracts suite nor the OQ8 validator reads these keys, so both stay green. Restore `%m-%d-%Y %H:%M` for both, and add a line-anchored assertion in `DeployedRuntimeParityClosureTests` that the two stamps match that format. [sprint-status.yaml:57-58]
- [ ] [Review][Patch] The seven reopened ledger entries assert both states on the same line — each flipped entry now reads `- [ ] … **[ATTESTATION CORRECTED 2026-09-09 -- VERIFIED NOT APPLIED IN SHIPPED CODE]** … — applied 2026-09-06 (unified drain path).` The trailing "applied" clause survives intact on all seven, so the line simultaneously claims not-applied and applied, and any substring scan still reads "applied". The correction itself is factually right — re-verified against shipped bytes: `validate_pyyaml_dependency` is still `all(fragment in workflow …)`, `scan_json_protected_content` still recurses unbounded, two separate selector drain loops remain, and `main()` still reports only `type(exception).__name__`. Strike or requalify the trailing clause (e.g. "claimed applied 2026-09-06; refuted"). [spec-4-15…md:73-76,86,90,91]
- [ ] [Review][Patch] Action-item status `rejected` is outside the file's own declared enum — `sprint-status.yaml:577` sets `epic-3-retro-item-38-… status: rejected`, the only such value in the file (22 `done` / 16 `open`). The file's header declares `open | in-progress | done`, and both consumers hard-code `ACTION_STATUSES = ("open", "in-progress", "done")` (`sprint_plan.py:63`, `sprint_status.py:33`): `cmd_status` warns `action_items[i] has a missing or unknown status` and **skips the item** (`:533-536`), `cmd_validate` flags it (`:651`), and `bmad-retrospective --set-action-status` refuses the value. Express the same outcome as `status: done` and keep the existing SUPERSEDED note, which already records why rebinding was rejected. [sprint-status.yaml:577]
- [ ] [Review][Patch] The new lifecycle guard's comment miscounts what it asserts — the comment says "These **three** rows were each flipped to 'done' … and each was corrected back" and "re-flipping all **three**", but the body asserts **four** rows: `4-6-global-position-sharding-spec-renegotiation`, `3-15-corrected-deployed-runtime-parity-closure`, `5-3-production-authentication-guards-and-secret-stripping`, and `4-7-tenants-query-provenance-follow-up`. A reader trusts a re-flip demonstration that may never have covered the fourth row. In a repo that treats guard prose as the guard's rationale, state four and give 4-7's provenance. [tests/Hexalith.EventStore.Contracts.Tests/Packaging/DeployedRuntimeParityClosureTests.cs:4249-4271]
- [ ] [Review][Patch] The new guard pins four statuses with no documented release condition — `CorrectedLifecycleRowsRetainTheirCorrectedStatus` hard-asserts `4-6: awaiting-operator`, `3-15: in-progress`, `5-3: in-progress`, `4-7: in-progress`. Legitimate forward progress on any of them turns the Contracts suite red with no stated remedy, which invites the next session to delete the assertion rather than satisfy it — the exact failure mode the guard exists to prevent. Record the release condition in the comment (e.g. flip only in a commit that also moves the story's spec frontmatter). [DeployedRuntimeParityClosureTests.cs:4243-4272]
- [ ] [Review][Patch] The chunk ledger is internally inconsistent and Group C's findings were never recorded — the surviving Group A header still reads "Group A of 3" (`:66`) while the new header reads "Group D of 4" (`:214`), so one file states two chunk totals. More seriously, **no Group C findings section exists anywhere in this spec**: Group D's own preamble says it was "added after Group C triage" and item `:219` cites "the Group C sdk-successor decision item", but that item is recorded nowhere — Group C's dispositions survive only as inline comments in `sprint-status.yaml`. Reconcile the counts and append the Group C findings section it references. [spec-4-15…md:66,110,214,219,222]
- [ ] [Review][Patch] The three new deferred-work entries are outside both sanctioned ledger formats — `deferred-work.md:4005-4009` appends bare prose bullets with no `source_spec:` / `summary:` / `evidence:` keys and no `DW-###` id, unlike the entries immediately above them. Per `deferred-work-format.md`, entries are either canonical `### DW-n` blocks with a `status:` line or the flat `- source_spec:` blocks that attended sessions append; these are neither, so `bmad-loop-sweep` — for which this file is "the sole read surface" — cannot attribute or triage them, and having no status they can never be closed. Two of them also cite the ledger by line number (`deferred-work.md:3534,3598`), which appending to an append-only file makes fragile. Relatedly, the two Group D `[Review][Decision]` items requiring owner action outside the repo were written only to this spec — no `action_items` entry and no DW entry — so they surface in no owner queue. [deferred-work.md:4005-4009; spec-4-15…md:215-216]
- [ ] [Review][Patch] PRD §11.1 rows now contradict themselves and drop Story 1.21's only trace — the ownership table was rebuilt to single primary owners, which is a deliberate redefinition, but two rows were left half-edited: FR19 keeps the coverage text "Submodules under references layout; **root submodule refresh in Story 3.16**" while its owning column dropped 3.16 to leave `3.3`; FR21 keeps "latest-compatible catalog refresh in Story 3.16" against owning `3.5`. The two columns of one row disagree. Separately, FR36's coverage text was rewritten from "completed projection/query parity source/package closure in Story 1.20 and **frozen-evidence integrity repair in Story 1.21**; Epic 3 - … Story 3.13 … Story 3.14 …" down to "projection/query parity source/package closure; Epic 3 - deployed-runtime parity closure", so Story 1.21 now appears nowhere in the PRD — a gap the prior validation report had explicitly raised. [prd.md:231,233,460,462,477]
- [ ] [Review][Patch] The NFR7 rewrite deleted normative text and severed the append-fence ledger link — the new row (`prd.md:323`) states the five loss classes cleanly, but drops class (e)'s guarantee that "a consumed key cannot become executable fresh work because its replay result expired or storage became unreadable" (an explicit Epic 4 requirement: "every expired-key reuse fails identically before protected work"), and drops every per-class delivery-state annotation. `DW-326` went from 4 occurrences to 0 in `prd.md`: the old OR4 read "Give DW-326 an owner and a trigger… It is the only record of the unowned append fence," and the new blocking OR4 no longer names it, so nothing in the PRD points at the ledger entry that tracks class (c). Restore the class-(e) clause and re-cite DW-326 in OR4. [prd.md:323, OR4]
- [ ] [Review][Patch] The UX validation report gives a different count for a lens neither run modified — the new report tabulates "Architecture & readiness (existing evidence)" as `0 / 4 / 12 / 10`; the report it replaces states `0 / 4 / 12 / 7`. `review-architecture-readiness.md` is untouched by this change (last modified in `0f10d92c`), so one of the two counts is wrong. [ux-designs/ux-eventstore-2026-07-05/validation-report.md:18-22]
- [ ] [Review][Patch] The UX rubric's critical finding was downgraded with the underlying artifact unchanged and no stated basis — reported rubric counts move from `1 critical / 4 high / 9 medium / 14 low` to `0 / 3 / 10 / 6`, and the "Token completeness" category verdict moves from `broken` to `adequate`, while `DESIGN.md` is not modified in this change and the colour-token problem is still listed under Medium and Low. Either state what closed the critical, or restore the verdict. [ux-designs/ux-eventstore-2026-07-05/validation-report.md:18-30]
- [ ] [Review][Patch] Review lens files were overwritten in place while the sibling lane uses dated filenames — `review-rubric.md` and `review-accessibility-support-safety.md` are rewritten in place, discarding the 2026-07-19 rubric body that the previous report advertised under "Prior reviews preserved". The architecture lane in the same commit writes dated files (`review-validate-2026-09-09-*.md`), and the PRD lane writes both (`review-rubric-update-2026-09-09.md` **and** an overwritten `review-rubric.md`). Pick one convention; if a lens file is overwritten, keep a superseded copy. [prds/…/review-rubric.md; ux-designs/…/review-rubric.md; ux-designs/…/review-accessibility-support-safety.md]
- [ ] [Review][Patch] `.gitignore` OQ8 venv block introduces a stray double blank line — the new `.oq8-python/` block is preceded by two blank lines where every surrounding block is single-spaced. [.gitignore:494-495]
- [x] [Review][Defer] The new lifecycle guard runs only in `ci / contracts`, which is not a required status check — the live ruleset requires `advisory`, `ci / build-and-test`, `ci / tenants-source-mode`, `codeql / analyze`, `commitlint / commitlint`, `dependency-review / dependency-review`, `live-sidecar`; `ci / contracts` is absent. Re-flipping any of the four pinned rows fails only a non-blocking job while every required check — including the capture-mode OQ8 lane, whose `expected_statuses` excludes `4-6`, `4-7`, `3-15` and `5-3` — stays green and the PR stays mergeable. Deferred: promoting the check is a branch-ruleset change in repository settings, already carried as the Group D `[Review][Decision]` item; resolve them together rather than inside this diff.
- [x] [Review][Defer] Comment prose added inside `development_status` is destroyed by any tracker regeneration — `build_status` rebuilds `development_status` as a fresh `CommentedMap` and re-adds only a blank line before each epic key (`sprint_plan.py:290-296`), so the ~20 new rationale comment lines, and the guarded blocks themselves, do not survive a `sprint_plan.py generate`. Deferred: pre-existing fragility that the accepted `>>> GUARDED <<<` fence mitigation already acknowledges; this change adds weight to it but does not cause it.
- [x] [Review][Defer] `epic-3-retro-2026-09-07.md:680` still teaches the undercount this change corrects — the action-item text in `sprint-status.yaml` was corrected to record the five-line contiguous block, but the retro document it cites as `ref:` still says "the two sprint-status comment lines". Deferred: the retro file is not in this diff, and correcting an approved retrospective's recorded text is a separate authority.
- [x] [Review][Defer] The Acceptance Auditor's OQ8 identity findings restate open Group D items — successor selector `4-15-oq8-platform-closure-successor.json` frozen one generation behind while asserting `eventStorePlatformComplete: true`; `integration.yml` proving OQ8 on Dapr runtime `1.18.2` while `COMMITTED_DAPR_RUNTIME_VERSION = "1.18.1"` is what `observations.json` is validated against; and the 4.8 ledger's mutable `review` cell sealed inside a live-hashed body. Deferred: all three are already recorded as open `[Review][Decision]`/`[Review][Patch]` items by this very change; no new action, and each needs the v3 remint.

### Rejected (2026-09-09 Group E)

- false: Spec frontmatter stays `status: 'done'` while the change reopens 15 items, violating AC1's "any omission keeps Story 4.15 non-done" — refuted: `validate_status_and_documents(final=True)` **requires** the 4.15 frontmatter to be `done` and sprint tracking to be `review` (`tools/validate-oq8-platform-evidence.py:3532,3545-3547`). Flipping the frontmatter fails the final validator. The split is the deliberate, already-recorded DW-497 disposition ("Keep spec-done / sprint-review split").
- false: `# >>> END GUARDED COMMENTS <<<` is a dangling fence with no opener, implying everything from `1-19-` to `3-15-` is guarded — refuted: the opener is pre-existing at `sprint-status.yaml:131` (`>>> GUARDED COMMENTS -- DO NOT DELETE (see GUARDED COMMENT BLOCKS in the header) <<<`); the new END at `:152` closes exactly that block. The `1-19-` five-line block is separately fenced at `:85`/`:97`.
- false: The END fence is placed too early, leaving the Story 3.16 justification comments unfenced — refuted: nothing asserts the 3-16 comment prose (`grep` for `3-16-latest-compatible`, `epics.md:2849`, `spec-update-all-hexalith` across `tests/` returns no hits). Only the 3-15 prose is guard-bound, by `CorrectedDeployedRuntimeParityClosureTests.SubjectRestatingSurfacesNameTheCurrentSubject`, and the fence encloses exactly it.
- false: The PRD frontmatter's `implementation_readiness_result: reject` is unsupported by the report it cites — refuted: `validation-report.md:7` states `**Grade:** Poor` and `:13` states "the adversarial and brownfield reviews strengthen the rubric's **reject verdict**"; the new OR1 restates "the bound 2026-09-09 validation result is `Poor` / `Reject`".
- false: §12's closing note "the remaining numbering is unchanged" is no longer true after the OR table was split into five grouped tables — refuted: the split reordered rows but renumbered nothing. Every OR identifier still designates the same item, which is precisely what the sentence claims ("so existing reviews that cite an OR number keep pointing at the same item").
- false: The `references/Hexalith.Memories` gitlink bump may publish a dangling pointer — refuted: `e5eb0b654035848e1ff78f0bff2bd1b9d2f1effa` is contained in the submodule's `origin/main`.
- false: The new UX report removed its category verdicts and assigns no overall grade — refuted: it carries a full "Rubric category verdicts" table with a verdict and summary per category. The verdicts changed; they were not removed. The unexplained `broken` → `adequate` move is filed separately as a patch.
- false: `4-6`'s asserted `awaiting-operator` value does not fit the guard comment's "flipped to done and corrected back" narrative — refuted: a row flipped to `done` and corrected back to `awaiting-operator` fits that narrative exactly. The real defect is the three-versus-four count, filed as a patch.
- low: `SingleLineValue` returns the raw remainder without stripping inline `#` comments or surrounding quotes — the tracker is machine-written with bare scalars, no status row carries an inline comment, and the helper is pre-existing and shared; adding a parse step for an unreached shape is more than a direct correction.
- low: §1's new note that the 2026-09-08 proposal's "header does not state whether the proposal was approved" sits against §12 retiring OR2/OR3/OR12 on that proposal's authority — the document discloses precisely this tension deliberately; no named harm beyond the disclosure it already makes.
- low: The `ci / contracts` job does a cold `dotnet restore` with no NuGet cache — performance and consistency only, and already recorded in `deferred-work.md` by this same change.

_Chunked code review, Group F (2026-09-09). Baseline `12d2dfc1` → HEAD `e302432c` (the sole commit no earlier chunk covered). 17 files, +1445/-697, all planning artifacts: the architecture spine rewrite, seven new architecture review lenses, three UX lens rewrites, the UX validation report pair, `deferred-work.md`, and this spec's Group E section. Layers: Blind Hunter, Edge Case Hunter, Verification Gap, Acceptance Auditor._

**Scope note.** `python3 tools/validate-oq8-platform-evidence.py` passes on the shipped tree and no sealed OQ8 path is touched — `EXPECTED_DOCUMENT_HASHES` docs, `ci.yml`, `integration.yml`, `requirements-oq8.txt`, and every closure/crosswalk evidence body are outside this diff, and the `<frozen-after-approval>` block is unmodified. This is a Zone A change. Findings are governance-, traceability-, and evidence-integrity-level.

- [x] [Review][Decision] **RESOLVED 2026-09-09 (owner: flip to draft).** The architecture spine shipped `status: final` while all three reviews committed alongside it demand the opposite — `reconcile-update-2026-09-09.md:5` reads "**CHANGES REQUIRED before the reviewer gate** … The spine should remain `status: draft` until these are corrected" (4 High); `review-update-2026-09-09-rubric-walker.md:3` reads "**Gate verdict: CHANGES REQUIRED**" (5 High) and its rubric row 127 records "Prior adopted decisions not weakened | **Fail**"; `review-update-2026-09-09-adversarial-divergence.md:6` reads "**FAIL**" (1 Critical / 5 High). The diff changes only `updated: 2026-08-29` → `2026-09-09`; `architecture.md:8` is still `status: final` and no artifact records a re-reconciliation after R1-R8 landed. Every Edge Case Hunter ambiguity item this pass raised (AD-8 over-limit branch, AD-16 anonymous-endpoint catalog, AD-19 Indeterminate/cancelled rows, AD-27 culture-sensitive lowercasing, AD-31 retry-vs-quarantine terminal branch, AD-32 missing-correlation hop) is already one of those shipped open findings — they are evidence for this item, not separate defects. Downstream consumers read `status: final` as ratified build substrate, and the spine binds `FR1-FR37, NFR1-NFR19`. Options: flip to `draft` until the open Highs close, or record the acceptance that closes them. [architecture.md:8; reviews/reconcile-update-2026-09-09.md:5; reviews/review-update-2026-09-09-rubric-walker.md:3,127; reviews/review-update-2026-09-09-adversarial-divergence.md:6]
- [x] [Review][Decision] **RESOLVED 2026-09-09 (owner: restore the fail-closed subset, retire the rest explicitly).** The condensation (609 → 439 lines) deleted load-bearing normative clauses under a recorded claim that it preserved them — `.memlog.md:106` records "preserve AD-1 through AD-25 and add only new stable IDs" and `reconcile-update-2026-09-09.md:5` says the update "preserves `AD-1` through `AD-25`", but only the *ID register* survived; `review-update-2026-09-09-rubric-walker.md:127` scores the same property **Fail**. Verified absent from the shipped file (`grep` count 0 each): the AD-25 OQ8 ownership split naming `4-8-eventstore-oq8-platform-evidence.yaml` as the EventStore packet and `oq8-idempotency-evidence.yaml` as Folders-owned; the dated 2026-07-27 Story 2.12 and 2026-08-16 Story 3.13 AD-22 scoped exceptions and the `v3.94.1` rejected-non-authorizing disposition; the Design Paradigm rule "Planning or story status never authorizes release, deployment, consumer removal, or positive `v3.94.1` closure" together with the Epic 3 / Story 3.16 open-state paragraph; AD-11's 14-package release inventory, the Story 8.8 atomic 14→16 transition, and the failed-release immutability rule; AD-18 rule 6's disclosure that omitting `AddEventStoreDaprServiceInvocation` is **currently fail-open**; AD-16's `DevelopmentHealthResponseWriter` restriction and its falsifying evidence test; AD-17 rule 4 (no `Location` on mapped failures); AD-21's `event-store-admin` module identity and the FrontComposer catalog-variable rule; and the Stack rows for `Aspire.Hosting.Keycloak`/`Kubernetes` preview, `Microsoft.CodeAnalysis`, `Hexalith.Commons.UniqueIds`, and NBomber. The Capability To Architecture Map also dropped every FR/NFR identifier from its first column while the frontmatter still declares `binds: FR1-FR37`, so no FR now traces to an area. The AD-25 loss bears directly on this story: Story 4.15's Approach requires distinguishing "EventStore platform completion from release or Folders-owned final closure," and the deleted text was the spine's only statement of that split. Options: restore the clauses, or record each as deliberately retired in a named amendments file. [architecture.md; .memlog.md:106; reviews/review-update-2026-09-09-rubric-walker.md:127]
- [x] [Review][Decision] **RESOLVED 2026-09-09 (owner: mark AD-26 `[ASSUMPTION]`).** Reconciliation finding R9 was never dispositioned and the spine ships new inferred decisions as adopted — `reviews/reconcile-update-2026-09-09.md:110-114` asks that genuinely inferred choices be marked `[ASSUMPTION]` "at minimum AD-26 and any other non-ratified new target", and its Handoff (`:118`) reads "Apply R1-R8, **triage R9**". R1-R8 are all verifiably applied; R9 is the sole silent drop. The shipped spine contains zero `ASSUMPTION` occurrences and labels all of AD-26 through AD-33 `[ADOPTED]`, and no review, memlog entry, or spine note records a triage outcome. AD-26 in particular selects a new production target (self-managed Kubernetes, `state.postgresql` v1, `oq8-postgresql-v1`, OpenBao) that is a selection rather than delivered reality. Options: mark AD-26 `[ASSUMPTION]`, or record the acceptance that closes R9. [architecture.md:251; reviews/reconcile-update-2026-09-09.md:110-118]
- [x] [Review][Decision] **RESOLVED 2026-09-09 (owner: ratify in-place overwrite, port the dropped findings).** UX lens files were overwritten in place again, discarding the reproducible evidence base — this is Group E's already-filed patch ("Pick one convention; if a lens file is overwritten, keep a superseded copy", `spec:253`) re-done rather than applied. `review-architecture-readiness.md`, `review-accessibility-support-safety.md`, and `review-rubric.md` are rewritten wholesale while the architecture lane in the *same commit* writes seven dated `review-update-2026-09-09-*.md` files. The overwritten architecture-readiness body lost its `## Evidence` section (the enumerated 22-route `@page` inventory, nav counts, AppHost identity anchors, the FrontComposer catalog scan, vocabulary enumerations, `sha256sum` hash-pin verification, `git log` baseline) and its `## Resolved since 2026-07-05` history; the ~10 code-verified findings it carried (FluentBadge `Color=Neutral` invalid in V5, `FluentDrawer` absent from the rc.5 catalog, `FluentTabs` is neither router nor overflow solution, lifecycle colours conflicting with the shipped Tenants `ProjectionLifecycleBadge`, `/health` ownership split, the `epics.md:16-18` re-pin obligation, the legacy-token inventory, the wide-screen banner retirement) reappear in neither the new lens nor the report. The prior bodies remain recoverable from Git, so this is an audit-trail loss rather than data loss. Options: adopt dated filenames for the UX lane too, keep superseded copies, or ratify in-place overwrite and close the Group E item. [ux-designs/ux-eventstore-2026-07-05/review-{architecture-readiness,accessibility-support-safety,rubric}.md]
- [x] [Review][Decision] **RESOLVED 2026-09-09 -> DEFERRED (owner: accept this window's bump, fix the convention).** Commit `e302432c` is typed `feat:` for a documentation-only change — all 17 paths are markdown/HTML under `_bmad-output/`; no source, test, workflow, or configuration file is touched. `.releaserc.json` carries no path filter and uses default commit-analyzer rules, so this mints a **minor** version across the 14 packages in `tools/release-packages.json`. The project's own workflow rule is explicit: "Don't use `feat` for refactors (false minor bump + NuGet publish)"; `docs:` is the accurate type. Release is `workflow_dispatch`-only so nothing publishes automatically, making this version inflation rather than an unauthorized publication. The commit is already on `main`, so the correction is not mechanical. Options: accept the bump, or rewrite the type before the next Release dispatch. [commit e302432c]
- [x] [Review][Patch] **APPLIED 2026-09-09.** `review-update-2026-09-09-adversarial-closure.md` attests a frontmatter change that was never made, and contradicts its sibling review in the same commit — its M1 closure row (`:18`) reads "**Closed** | Frontmatter is now `altitude: platform` (`:5`)", but `architecture.md:5` is `altitude: feature` and the diff's frontmatter hunk never touches line 5. `review-update-2026-09-09-rubric-closure.md:23-29` files M-R1 arguing `platform` is outside the BMad taxonomy (only `initiative`, `feature`, `epic` exist) and must be `feature`. Two reviews shipped together assert opposite states of one line, and one closure record is therefore unverifiable. Correct the M1 row to record that `feature` was retained. [reviews/review-update-2026-09-09-adversarial-closure.md:18]
- [x] [Review][Patch] **APPLIED 2026-09-09 (partial re-anchor + disclosure).** Every line citation in the new architecture review files resolves to the wrong decision — the reviews cite `ARCHITECTURE-SPINE.md`, verified to be a symlink to `architecture.md` with identical bytes (`2678116099e3d1c1…`, 439 lines), so the anchors are checkable and they are wrong by roughly 32 lines throughout. AD-26 is at `:251`; `reconcile-update-2026-09-09.md:20` and `review-update-2026-09-09-rubric-walker.md:21,36,52` cite `:219-223`, which lands inside AD-22's consumer-removal rule, and `review-update-2026-09-09-adversarial-closure.md` cites `:241-247`. AD-8 is at `:126` (cited `:105-109`), AD-16 at `:180` (cited `:155-159`), AD-24 at `:203-207` in the citations versus its shipped position, AD-33 at `:295` (cited `:285-291`), and the gates table at `:419-439` (cited `:374-390`/`:409-429`). The citations were computed against pre-final drafts and never re-anchored, so no closure disposition can be checked against what shipped. Re-anchor them, or cite AD identifiers, which are stable. [reviews/reconcile-update-2026-09-09.md; reviews/review-update-2026-09-09-*.md]
- [x] [Review][Patch] **APPLIED 2026-09-09.** The spine rewrite breaks the declared content digest and the diff does not re-pin it — `epics.md:15` pins `_bmad-output/planning-artifacts/architecture.md: 623bc23e453aba5a703c5aa1b208bf9f985f5937f5900f5e665f5cb5abe5ca94` under `inputDocumentDigests`; the rewritten file hashes `2678116099e3d1c1f68ee38ef344b9bef5a58a82062a800e5a89b8b0f5774395` (`sha256sum`). `epics.md` is not in this diff. No test or tool recomputes these digests, so the drift is invisible to every check — this is the recurrence the 2026-08-29 sprint-change proposal §4.5 was raised to repair. Re-pin `architecture.md` only; the `prd.md` pin is separately stale and pre-existing. [epics.md:15]
- [x] [Review][Patch] **APPLIED 2026-09-09.** `index.md` was not regenerated alongside the five artifacts it indexes — it still reads `Updated: 2026-08-01` (`:4`) and lists `validation-report.md`, `validation-report.html`, and all three lens reviews with no dates, though all five were regenerated on 2026-09-09. The new `validation-report.md` itself says `index.md` "should continue to identify the current review date", and no such date exists in the file. Add per-artifact dates and bump `Updated:`. [ux-designs/ux-eventstore-2026-07-05/index.md:4,26-30]
- [x] [Review][Patch] **APPLIED 2026-09-09.** The reconciliation's M12 continuity attestation states a line count that does not match the file — `reviews/reconcile-update-2026-09-09.md:47` reads "Resolved: append-only memlog grew from 105 to 127 lines; no historical rewrite was used." The diff hunk is `@@ -103,3 +103,31 @@` and `wc -l .memlog.md` returns **133**. The append-only conclusion is correct; the arithmetic backing it is not, which is exactly the kind of unchecked attestation this story's ledger corrections exist to catch. [reviews/reconcile-update-2026-09-09.md:47]
- [x] [Review][Patch] **APPLIED 2026-09-09.** The regenerated UX validation report dropped the baseline it validated — the predecessor header carried `- **Spines validated:** `status: final`, updated 2026-08-01` and `- **Additional evidence included:** pre-existing architecture & readiness review dated 2026-09-08…`; the new header carries neither. The report now grades DESIGN.md/EXPERIENCE.md without recording which revision it graded, while its own Critical and High findings are about those spines. Restore the validated-baseline line. [ux-designs/ux-eventstore-2026-07-05/validation-report.md:1-6]
- [x] [Review][Patch] **APPLIED 2026-09-09.** `sources:` omits the approved proposal whose content the update ratifies — AD-10's new JWT contract clause encodes `JwtBearerAuthenticationContract`, the subject of `sprint-change-proposal-2026-09-08-nfr3-nfr4-authentication-ratification.md`, which exists on disk and is absent from `sources:`. The same hunk also removes `implementation-readiness-report-2026-07-05.md`, `-2026-07-15.md`, `story-id-migration-2026-08-01.md`, and the three `sprint-change-proposal-2026-07-18-*` files with no artifact recording why they ceased to be inputs (rubric-walker M4 asked for that restoration). Add the 2026-09-08 authentication proposal at minimum. [architecture.md:87-104]
- [x] [Review][Defer] The spine now asserts DAPR runtime `1.18.2` while sealed OQ8 evidence attests `1.18.1` — `architecture.md:330` records "CI `1.18.2`; deployment examples `1.18.0`" and `.memlog.md` repeats it, but `COMMITTED_DAPR_RUNTIME_VERSION = "1.18.1"` (`tools/validate-oq8-platform-evidence.py:79`) is what `observations.json` is validated against, with no crosswalk of the transition (the analogous PostgreSQL change was crosswalked). Deferred: this is the open Group D patch (`spec:221`) widened to a new surface; it rides the same agreed v3 remint rather than opening Zone B here.
- [x] [Review][Defer] `prd.md`'s `inputDocumentDigests` pin is stale (`8f9c88e8…` pinned vs `b99effdb4142…` actual) — deferred: pre-existing, introduced by `12d2dfc1`, not by this change; fold into the same re-pin pass as the architecture digest.
- [x] [Review][Defer] The Group D deferred-work block uses bare prose bullets outside both sanctioned ledger formats (`deferred-work.md`, "Deferred from: code review of story-4.15 Group D") — deferred: pre-existing from `12d2dfc1` and already filed as an open Group E patch (`spec:248`); the file is append-only so the malformed entries are permanent, and the Group F/E entries appended after them do use the sanctioned flat `- source_spec:` form.
- [x] [Review][Defer] The fourth Group E deferred-work entry bundles three distinct issues (successor-selector freeze, the Dapr `1.18.2`/`1.18.1` gap, the 4.8 live-hashed `review` cell) under one un-idded block, so `bmad-loop-sweep` cannot triage or close them individually — deferred: the flat block form itself is sanctioned; splitting is housekeeping with no correctness impact.

### Rejected (2026-09-09 Group F)

- rejected (fix edits the spec under review): No Spec Change Log entry records the 2026-09-09 work — real, and the Change Log does end at 2026-09-08, but the fix is an edit to this spec.
- rejected (fix edits the spec under review): The chunk ledger states three different totals ("Group A of 3" `:66`, "Group D of 4" `:214`, bare "Group E" `:239`) and no Group C findings section exists though `:219` cites one — already filed as an open Group E patch (`spec:247`); the fix is an edit to this spec.
- rejected (fix edits the spec under review): The Group E ledger records as open `[ ]` two items this same commit resolved — `spec:250` states the UX architecture-readiness counts are `0/4/12/10` and the lens is "untouched by this change", but the diff rewrites that lens (`1/6/5/2`, matching `validation-report.md:20`); `spec:251` states Token completeness was downgraded `broken` → `adequate` with counts `0/3/10/6`, but the shipped report reads **broken** with rubric counts `1/4/9/6`. The new counts reconcile exactly against the three lens files (`1/4/9/6` + `1/10/7/1` + `1/6/5/2` = `3/20/21/9`). The ledger now asserts a false open state, but the fix is an edit to this spec.
- rejected (fix edits the spec under review): The seven reopened Group A entries still carry a trailing "— applied 2026-09-06" clause alongside the "VERIFIED NOT APPLIED" correction — already filed as an open Group E patch (`spec:245`).
- false: The new Group E deferred-work entries are outside both sanctioned ledger formats — refuted: all four use the flat `- source_spec:` / `summary:` / `evidence:` block form that `deferred-work-format.md` sanctions for attended sessions. The malformed bare-prose bullets are the *Group D* block from the prior commit, deferred separately above.
- low: `event-store-admin` module identity and `eventstore-admin-ui` resource identity survive only as a mermaid node — the substantive loss is already carried as a member of the AD-21 condensation decision item above; filing it twice adds nothing.
- low: Individual Edge Case Hunter ambiguity items against AD-8, AD-16, AD-17, AD-19, AD-24, AD-27, AD-31, and AD-32 — each is already one of the open High findings shipped in this very commit's own reviews (`rubric-walker` 5 High, `adversarial-divergence` 1 Critical / 5 High, `reconcile-update` 4 High). They are recorded as evidence under the `status: final` decision item rather than re-filed as new defects.

**Decision resolutions applied 2026-09-09 (Group F).** All five `[Review][Decision]` items above were resolved by the owner in one pass, all Zone A (no sealed OQ8 path touched, no reseal, no receipt cascade):

- `architecture.md` frontmatter `status: final` -> `status: draft`, matching the three reviews committed with it.
- AD-26 relabelled `[ADOPTED]` -> `[ASSUMPTION]`, closing reconciliation finding R9 without authoring an acceptance that was never granted. AD-27 through AD-33 describe shipped seams and remain `[ADOPTED]`.
- Restored to the spine: AD-25 OQ8 evidence-ownership split (`4-8-eventstore-oq8-platform-evidence.yaml` EventStore-owned vs Folders-owned `oq8-idempotency-evidence.yaml`, source-only handoff, external authorities false) -- the clause this story's Approach and AC2 depend on; AD-18 rule 6 fail-open disclosure plus the innermost-registration requirement; AD-11 14-package inventory, the Story 8.8 atomic 14->16 transition, package-intent-in-every-configuration, and failed-release tag immutability naming `v3.75.0`/`v3.94.1`; the Design Paradigm rule that planning or story status never authorizes release, deployment, consumer removal, or positive `v3.94.1` closure, with the Epic 3 story dispositions; and FR/NFR identifiers in the Capability To Architecture Map's first column.
- Retired clauses recorded in `_bmad-output/planning-artifacts/sprint-change-proposal-2026-09-09-architecture-condensation.md` (new, added to the spine's `sources:`), so no clause is dropped without a record.
- `.memlog.md` corrected by append: the "preserve AD-1 through AD-25" claim was false, only the ID register survived.
- The ~10 code-verified findings lost in the UX lens overwrite were ported into `review-architecture-readiness.md` under "Carried forward from the 2026-09-08 lens" (FluentBadge `Color=Neutral`, `FluentDrawer` absent from rc.5, `FluentTabs` routing/overflow, Tenants lifecycle-colour conflict, `MessageId`/`CorrelationId` and the missing AD-17 row, `/health` ownership split, host title/breadcrumb/role switcher, wide-screen banner retirement, legacy token inventory, FR36-vs-FR4 authority label, and the `epics.md:16-18` re-pin obligation).
- Commit-type inflation deferred: five `feat:` commits already sit between `v3.103.0` and HEAD, three of them docs-only, so the minor bump is locked in regardless and retyping one commit would mean force-pushing `main` past its ruleset.

Note that `epics.md:15` now needs re-pinning against the edited `architecture.md`; it is carried as an open patch above and its hash must be recomputed after these edits, not before.

**Patches applied 2026-09-09 (Group F).** All seven, Zone A, OQ8 validator re-verified `passed` after each pass:

- `review-update-2026-09-09-adversarial-closure.md` M1 row corrected from **Closed / "now `altitude: platform`"** to **Not closed / `altitude: feature` retained**, resolving the contradiction with `review-update-2026-09-09-rubric-closure.md` M-R1.
- Spine citations re-anchored to stable AD identifiers where the target was unambiguous (19 distinct ranges across 5 files), stale `Deferred` section names updated to `Implementation Status And Production Gates`, and an **anchor note** added to each citing file. The remaining ~35 bare `:NNN` references were **not** invented into mappings: the draft-to-final offset is non-uniform (AD-8 +21, AD-16 +25, AD-26 +32), so the note states plainly that residual numbers are unreliable and the AD named in the surrounding prose governs.
- `epics.md:15` re-pinned to `7e3dbc7bd335034bd9b98cadfed8b14650b7d811321b326b8f32e1b280960d51`, computed after all Group F spine edits and verified byte-equal.
- `index.md` artifact list given per-artifact regeneration dates and an explicit in-place-regeneration note.
- `reconcile-update-2026-09-09.md` M12 line-count claim corrected (said "105 to 127"; the file was 133). The append-only conclusion stands.
- `validation-report.md` regained its `**Spines validated:**` baseline line, now stating that DESIGN.md/EXPERIENCE.md were not modified by that run.
- `sources:` gained the 2026-09-08 authentication ratification proposal (applied earlier with the D2 restorations).

**⚠ Concurrent-session warning.** `main` advanced from `e302432c` to `0825f0dc` *during* this review, and the UX working files and mockups were still being modified at completion. `0825f0dc` rewrote `DESIGN.md`, `EXPERIENCE.md`, `ux.md`, and `index.md`. Verified before finishing: the Group F working-tree edits are pure additions over the new HEAD (`git diff HEAD` shows **zero** deleted lines in `deferred-work.md` and this spec), so no concurrent work is reverted. Two consequences: (1) four of the five `epics.md` digest pins are stale from that commit and are recorded as deferred rather than re-pinned, because the UX artifacts were still in flight; (2) the `architecture.md` pin above holds only while the spine is not edited again — recompute it before committing if another session has touched it.

### Review Findings

_Chunked code review, Group G (2026-09-09). Baseline `699ca712` → HEAD `23a722a1`. Files: the top-level successor selector plus the SDK, v2, and v3 successor evidence chains (28 files, +1052/-0). Layers: Blind Hunter, Edge Case Hunter, Verification Gap, Acceptance Auditor._

- [x] [Review][Patch] **RESOLVED 2026-09-09 (owner: unify on v3).** Make v3 the single canonical active successor lineage: evolve the top-level selector from the 2026-08-29 SDK successor to v3, incorporate the intended SDK/current-source path coverage, and bind the landed commit, tree, and HEAD ancestry before claiming current-source closure. Preserve prior generations as immutable historical evidence and remint the content-bound v3 subject, receipts, handoff, and manifest in dependency order. [`4-15-oq8-platform-closure-successor.json:3`; `story-4-15-successors/v3/source-artifact-identity.json:5,24`; `tools/validate-oq8-platform-evidence.py:1492,1761,2818`]
- [x] [Review][Patch] **RESOLVED 2026-09-09 (owner: obtain a fresh independent security review).** Do not carry forward the existing self-described non-independent v3 security receipt as approval. After the unified v3 candidate and review subject are frozen, obtain a genuinely independent security review of that exact subject and bind its actual decision into the handoff and manifest; closure remains incomplete unless it approves. [`story-4-15-successors/v3/reviews/security.json:4-12`]
- [x] [Review][Patch] **RESOLVED 2026-09-09 (owner: bootstrap the active lineage and preserve historical bytes).** The active v3 handoff now creates `.oq8-python`, installs the pinned requirements with that interpreter, and verifies with the same interpreter. The SDK and v2 handoffs remain byte-immutable historical evidence under the unified-v3 decision rather than active consumer instructions. [`story-4-15-successors/v3/source-only-handoff.json:17`]
- [x] [Review][Patch] **APPLIED 2026-09-09.** Extend `V3SuccessorMutationsFailClosed` with rejected-review and missing/failed/skipped test-verification mutations, mirroring the already-present v2 coverage. [`tests/Hexalith.EventStore.Contracts.Tests/Packaging/Oq8PlatformClosureTests.cs:326`]

#### Rejected (2026-09-09 Group G)

- false: Review receipts need authenticated account identifiers or cryptographic signatures — the governing contracts require named, content-bound receipts; external identity signatures are not part of the approved evidence schema.
- false: The v3 test receipt must bind raw TRX/CTRF output — the approved v3 contract deliberately defines the receipt's exact command/count/result fields as the evidence record and does not require raw runner artifacts.
- false: The v3 `--no-build` command can silently test stale binaries — the bound pre-review sequence restores and builds before subject freeze, and the later test command and current source hashes are exact; absence of an assembly digest alone does not demonstrate stale execution.
- false: The PostgreSQL index/amd64 relationship is merely an unsupported declaration — the governing image-governance spec records the 2026-08-27 registry inspection and exact returned index/child digests; binding raw registry output was not required.
- false: The verification-evidence finding independently demonstrates fabricated test counts — it restates the rejected raw-output/assembly requirement; under the approved contract, the named test receipt is the attestation and its exact values are validator-bound.
- false: Editing v3 review prose while resealing the handoff and manifest is undetected receipt drift — an isolated receipt edit fails both bindings; a coherent reseal is visible repository history and is the explicit successor mechanism, not silent drift.
- low: A filesystem actor can swap a symlink between component checks and `read_bytes()` — this requires a concurrent hostile writer inside the single-writer CI trust boundary, is already tracked as DW-454, and an fd-based cross-platform rewrite is disproportionate here.
- false: The v3 subject omits the design, invariant crosswalk, approved test seams, and sanitized-state limitation — its validated v3 → v2 → v1 predecessor hashes transitively bind the v1 subject that contains those exact items; the additive successor design intentionally avoids duplicating immutable predecessor content.

**Validation note (outside this chunk):** `PYTHONDONTWRITEBYTECODE=1 python3 tools/validate-oq8-platform-evidence.py` currently exits non-zero with `Reviewed public document body drift: docs/guides/configuration-reference.md`. All six v3 live gate-input hashes still match, and HEAD descends from the declared completed-v2 commit; the red public-document gate belongs to the later v1/public-doc/lifecycle review group.

## Review Triage Log

_BMad Build review of the baseline-wide diff on 2026-09-09. Routes are recorded before survivor grouping; `defer` entries below are not caused by the Group G v3 closure implementation._

| Finding | Verdict | Route | Evidence |
| --- | --- | --- | --- |
| blind-01 | high | defer | `release.yml` deliberately lets the break-glass `bypass-validation=true` path substitute commitlint for the normal CI conclusion, so a protected-environment approval can release source whose build/tests did not run. This is an existing, owner-ratified release policy rather than a Story 4.15 change. |
| blind-02 | medium | defer | The legacy release job grants write-scoped `id-token` and attestation permissions that its steps do not consume, increasing impact if a release dependency is compromised. The permissions predate this story. |
| blind-03 | false | reject | `governed-release: false` and `require-publication-authority: false` are the approved ordinary-release contract; the protected production environment is intentionally the authority boundary, while the one-use corrective gate is opt-in. The cited values therefore do not bypass the ratified design. |
| blind-04 | medium | defer | The required live-sidecar lane captures OQ8 evidence but does not validate a committed closure packet, while `ci / contracts`, which performs that validation, is not a required context. This repository-settings/CI ownership gap predates Group G. |
| blind-05 | high | defer | The Operations authorization helper checks the configured Dapr caller ID and only bearer non-emptiness, not token validity, so its bearer check adds no authentication assurance. Actor routes also need a coherent caller-authorization boundary; this is unrelated Operations work. |
| blind-06 | medium | defer | `DeadLetterDrainActor` retains record bodies and its global index without a retention or compaction policy, so long-running tenants can accumulate unbounded state and retained payload data. |
| blind-07 | medium | defer | Capture rewrites the full dead-letter index and backlog observation scans it, making sustained growth cumulative quadratic work and making list/activation operations scale with all historical entries. |
| blind-08 | medium | defer | `RetryAsync` persists replay intent and arms the reminder but then awaits `DrainReplayRequestsAsync`, so an operator HTTP call can remain open for the entire sequential drain and time out despite accepted intent. |
| blind-09 | medium | defer | Replay treats permanent HTTP 400/401/403/404 failures like transient failures until the attempt budget is exhausted, causing avoidable repeated delivery and delayed terminal disposition. |
| blind-10 | high | defer | Oversize, empty, hash-conflicting, or otherwise unretainable captures are acknowledged with telemetry only; no durable terminal record remains, so an acknowledged dead letter can be irrecoverably lost. |
| blind-11 | medium | defer | Startup backlog reconciliation stops after its bounded retries without a durable failure signal, log, metric, or unhealthy state, allowing gauges to remain falsely clear after reconciliation failed. |
| blind-12 | medium | defer | `requirements-oq8.txt` pins only a PyYAML version and both workflows install without artifact hashes, so the security-evidence bootstrap is reproducible by version but not content-addressed. This limitation was already open before Group G. |
| blind-13 | medium | defer | Hundreds of tracked `.playwright-cli` artifacts include browser network traces with antiforgery cookies and ephemeral SignalR connection tokens; even when short-lived, this exposes session material and adds generated trace bulk to source control. |
| blind-14 | medium | defer | `references/Hexalith.Tenants` is a dirty nested worktree; the superproject diff can identify the committed gitlink but cannot reproduce the uncommitted submodule bytes used by local validation. Baseline rules prohibit changing that submodule here. |
| edge-01 | medium | defer | The historical v1 current-source routine checks frozen Git identities, while active v3 intentionally binds only its reduced current gate-input set. A change to an older capability path outside that set can coexist with a passing current v3 closure; this is the accepted DW-496 ownership boundary, not introduced by Group G. |
| edge-02 | false | reject | Rewriting an SDK predecessor and coherently resealing v3 necessarily changes the v3 subject, receipts, handoff, manifest, selector identity, and visible Git history. Hard-coding the predecessor manifest in the same mutable validator would not add an independent trust root, so the claimed undetected rewrite does not occur under the approved repository trust model. |
| edge-03 | false | reject | Candidate artifacts are read through a one-MiB cap and unexpected recursion failures are caught as a non-zero, fail-closed validator result. Deep malformed JSON can be rejected, but it cannot make malformed evidence validate or bypass the closure. |
| edge-04 | low | reject | A hostile same-user process could pre-create the fixed `.tmp` output as a symlink before `write_json` opens it, but support/capture output runs in the single-writer CI boundary and an exclusive no-follow rewrite is disproportionate for this unlikely local race. |
| edge-05 | low | reject | An ancestor can be swapped after component checks and before open, but exploitation requires a concurrent hostile workspace writer already able to edit the evidence and validator. The fd-based cross-platform repair is disproportionate and the residual is already documented as DW-454. |
| edge-06 | low | reject | Each Git query owns a timeout instead of sharing an overall deadline, so a pathological repository can multiply wait time, but every call remains bounded and the situation is unlikely in ordinary validation; adding deadline plumbing is not a direct correction. |
| edge-07 | false | reject | The approved trust model requires named, content-bound receipts in reviewed Git history, not cryptographic reviewer signatures. Adding signatures would introduce an authority contract the frozen intent does not require and would not refute any isolated-binding check. |
| edge-08 | medium | defer | PyYAML bootstrap validation searches for three unscoped workflow substrings, so comments or unrelated steps could retain those fragments while the actual setup stops creating and using the pinned environment. |
| edge-09 | medium | defer | `parse_unique_frontmatter_status` recognizes only one exact unquoted `status:` spelling; an additional YAML-equivalent quoted or space-before-colon key can evade the ambiguity count and let lifecycle consumers disagree. |
| edge-10 | low | reject | Unexpected exceptions report only their type, reducing debugging detail, but they still fail closed and sanitized message disclosure would require new redaction rules for a rare path rather than a direct safe fix. |
| edge-11 | medium | defer | The workflow guard forbids one literal `dotnet test tests/Hexalith.EventStore.Server.Tests/` form, so equivalent path or solution invocations can accidentally run the prohibited suite without tripping the contract test. |
| edge-12 | medium | defer | Dapr pinning guards recognize a narrow quoting/location form and do not cover equivalent double-quoted or job-level overrides, allowing the effective runtime value to drift while the expected literal remains elsewhere. |
| edge-13 | medium | defer | An invalid dead-letter continuation token silently becomes offset zero, returning duplicate first-page results instead of rejecting malformed pagination input. |
| edge-14 | medium | defer | Tenant identifiers are not validated at the Operations HTTP boundary, so malformed input reaches actor construction and can surface as a server error instead of a bounded client error. |
| edge-15 | high | defer | Dapr actor endpoints do not enforce the Operations caller policy themselves; the target sidecar supplies its app-channel token, so another reachable workload can invoke actor methods without the intended admin caller check. |
| edge-16 | false | reject | `UnpublishedPublicationIndex.Normalize` intentionally fails closed on conflicting valid duplicate message IDs, as required by Story 5.1. Turning the conflict into silent selection would violate that approved invariant. |
| edge-17 | medium | defer | Blank malformed publication-index entries do not consume the activation probe budget, so a corrupted index can force unbounded scanning and repeated logging before valid work is reached. |
| verification-01 | medium | defer | The credential-injecting Pact verifier and auth handler are present, but the only real-Kestrel interaction is unauthorized and succeeds regardless of the generated credential. No test proves an authorized interaction reaches the provider with the matching token. |
| verification-02 | medium | defer | Aspire authentication tests cover a valid audience pair and an invalid token endpoint, but do not exercise a missing half of the audience pair or an unsupported parameter name, leaving bounded pre-mutation validation unproved. |

### Completion Verification (2026-09-09)

- Story-isolated final validator: passed (`OQ8 platform evidence validation passed`).
- Story-isolated `Oq8PlatformClosureTests`: 384 passed, 0 failed, 0 skipped.
- Story-isolated `dotnet build Hexalith.EventStore.slnx --configuration Release -m:1`: passed with 0 warnings and 0 errors.
- Live-tree Contracts project build: passed with 0 warnings and 0 errors.
- Live-tree final validator and focused closure lane are blocked by the unrelated dirty `docs/guides/configuration-reference.md` sealed-body drift; the focused lane reports 259/384 failures, all cascading from that first gate.
- Live-tree full solution build is additionally blocked by concurrent edits: `PactInteractionVerifier.cs(256,5)` CS1519 and `SecretsProtectionTests.cs(637,36)` CS1503.
- `git diff --check`: passed; Git emitted only the existing CRLF-to-LF warning for `ApplicationRuntimeProofEndpointTests.cs`.

### Review Findings

_Chunked code review, Group H (2026-09-09). Target: the uncommitted Group G remint in the working tree (`git diff HEAD`, 14 Story 4.15-bound files, +495/-161): `tools/validate-oq8-platform-evidence.py`, `Oq8PlatformClosureTests.cs`, `4-15-oq8-platform-closure-successor.json`, the `story-4-15-successors/v3/` chain, this spec's Group G section, and the Group G `deferred-work.md` block. Layers: Blind Hunter, Edge Case Hunter, Verification Gap, Acceptance Auditor._

**Reseal note.** `tools/validate-oq8-platform-evidence.py` and `Oq8PlatformClosureTests.cs` are both live v3 gate inputs (`sourceTransitions` + `gateInputs`), so every patch below that touches either file changes a sealed hash and requires one coherent v3 reseal (identity → pre-review → subject → receipts → handoff → manifest → selector) with fresh receipts. The remint is still uncommitted, so applying the patches before the first commit costs one reseal rather than two.

- [x] [Review][Decision] The v3 security receipt now asserts an independent review with no recorded provenance, and its findings text contradicts the diff it approves — `reviews/security.json` replaces the prior honest disclosure ("does not claim a new independent security-review conversation") with "An independent security review reproduced frozen subject cd29ee74…", but the receipt records nothing distinguishing it from the previous scripted remint: `issuedAt` values run 15:10:45 / 15:10:50 / 15:10:55Z with `assembledAt` 15:11:00Z, the same 5-second cadence as the 2026-09-08 mint (06:00:10/15/20/25Z), the reviewer is the validator-pinned role label `Security Reviewer`, and the findings are subject-derivable hashes. Its third finding states "Validator and production semantics are unchanged", which is false on the face of this diff: selector validation was removed from `--pre-review` and both `--historical-*` modes, and `landedSource` / `headAncestry` / `merge-base --is-ancestor` checks were added. The Group G resolution above says "closure remains incomplete unless [a genuinely independent review] approves" (frozen Never: "Fabricate … reviewer approval"). Only the owner can state whether a distinct reviewer examined subject `cd29ee74…`. Options: (1) confirm the review occurred and record its provenance (who, how, when) in the receipt findings and correct the "unchanged" sentence, then reseal; (2) revert the wording to a non-independent disclosure and leave closure incomplete until an independent review is obtained; (3) confirm no independent review occurred and set `decision: rejected`. [evidence/story-4-15-successors/v3/reviews/security.json:5-13]
- [x] [Review][Patch] New v3 and selector bindings shipped without fail-closed mutation coverage — the remint adds `historicalSdkSuccessor`, `landedSource`, `headAncestry` (identity, pre-review `candidateInputs`, subject), the live `merge-base --is-ancestor LANDED_SOURCE HEAD` proof, the selector v2 `successor` pointer (`directory`, `manifestSha256`, `files`, three hashes), and seven SDK-only paths newly current-bound through `V3_GATE_INPUT_PATHS`, but `V3SuccessorMutationsFailClosed` gains only `receipt-rejected` (security only) and three `test-verification-*` rows on `oq8-platform-closure`. Grep of the test file for `landedSource` (v3), `headAncestry`, `historicalSdkSuccessor`, `successor selection drift`, `global.json` mutation: none. `NonDescendantHeadFailsClosed` runs `--pre-review` and asserts exit 0, so no test shows a non-descendant HEAD fails the final gate — yet `LaterUnboundRepositoryWorkPreservesPinnedPathVerification` already runs full mode against a `CreateGitFixture` clone, so detaching that clone to `e60a3777` (an ancestor of `5e8f175b`) gives the fixture for free. Weakening `:2850-2853` to `isinstance(..., dict)`, deleting `:2862`, loosening the selector `successor` compare to `directory` only, or reverting `:262` to the v2 set all leave 384/384 green. Spec task: "mutate each critical field to demonstrate rejection." Add rows: `landed-source-drift`, `head-ancestry-drift`, `sdk-link-drift` (with a `ResealV3AfterIdentityChange` helper propagating identity → `candidateInputs` → subject fields + `bindings.sourceIdentity.sha256`), `sdk-gate-input-drift` (append to `global.json`), `successor-selection` (zero `selector.successor.handoffSha256`), `contracts-full-failed`, `consumer-install-command-drift`, and a full-mode `NonDescendantHeadFailsFinalClosure`. [tools/validate-oq8-platform-evidence.py:262,2842-2862,1800-1811; Oq8PlatformClosureTests.cs:325-357,363-469]
- [x] [Review][Patch] Selector validation was silently removed from `--pre-review` and both `--historical-*` modes — at HEAD, `validate_successor_source_identity` (called from `validate_pre_review_candidate:3632` and `validate_successor_closure`) required the selector to be a regular non-symlink file with schema v1, the SDK directory, and a matching `sourceIdentitySha256`, and `validate_successor_closure` then ran full `validate_successor_selector` in every mode. The diff deletes both and invokes `validate_successor_selector` only under `if current_source:` in `validate_platform_closure`. `--historical-v1-only` / `--historical-v2-only` (the two commands sealed in `V3_PRE_REVIEW_COMMANDS`) and `--pre-review` now pass with a missing, symlinked, or schema-drifted `4-15-oq8-platform-closure-successor.json`, and `HistoricalV2EvidencePassesWithoutAuthorizingCurrentSource` cannot detect a corrupted `historical` block. The `historical` block, `schema`, field set, `selectedOn`, `reason`, and `authority` are verifiable without current source. Split `validate_successor_selector` into a historical part (run in every mode from `validate_successor_closure`) and a `successor`-pointer part (current-source only), and add a historical-mode selector mutation test. [tools/validate-oq8-platform-evidence.py:1500-1510,1817-1835,3756-3771]
- [x] [Review][Patch] The lineage-model change was not propagated to the prose that describes it — `bindingRule`, `V3_SELECTION_REASON`, and all three receipts now name the SDK successor as a third historical generation and add landed-commit/tree and HEAD-ancestry binding, but (a) `limitations.json` / `V3_LIMITATIONS` is byte-unchanged (`d298f5dd…`) and still says only "v1 and v2 remain immutable historical evidence" with no disclosure that the ancestry proof needs full Git history; (b) `source-only-handoff.json.consumerInstructions.historicalRule` still reads "Validate Story 4.15 v1 and v2 only …"; (c) `docs/ci.md` — a bound `sourceTransitions` path and gate input whose hash `80b09abe…` was carried forward — still says v3 "requires valid historical v1/v2 evidence" (`:119-124`), tells operators to run bare `python3 tools/validate-oq8-platform-evidence.py` (`:103`) while the handoff mandates `.oq8-python/bin/python`, and mentions neither selector schema v2, the SDK successor's demotion, nor that `merge-base --is-ancestor` cannot run on a `fetch-depth: 1` clone (`:69-70` documents depth 1 as sufficient). A developer restoring the committed-closure step in CI from `docs/ci.md` gets an opaque Git identity failure. Update the three surfaces together and reseal. [tools/validate-oq8-platform-evidence.py:278-283; v3/source-only-handoff.json:19; docs/ci.md:69-70,103,119-124]
- [x] [Review][Patch] Selector is loaded before its regular-file guard — `validate_platform_closure` calls `load_candidate_json(SUCCESSOR_SELECTOR)` as an argument and only then does `validate_successor_selector` check `is_file() and not is_symlink()`. At HEAD the guard preceded the load. A symlinked selector is read through the link (unbounded `load_json`, no `read_bounded_regular_snapshot`) before rejection, and a missing selector reports the generic `Cannot load JSON evidence …` instead of the selector-specific message. Move the guard ahead of the load (load inside `validate_successor_selector` after the `require`, or read via `read_bounded_regular_snapshot`). [tools/validate-oq8-platform-evidence.py:3763-3771,1822-1825]
- [x] [Review][Patch] `symlinked-source-ancestor` expectation weakened to a layer-agnostic substring — the row went from the full `Story 4.15 v3 bound source .github/workflows/integration.yml has a symlinked path component` to bare `has a symlinked path component`, because `V3_GATE_INPUT_PATHS` is an unordered set now containing both `ci.yml` and `integration.yml` under the same symlinked ancestor. That substring is emitted by the shared `require_no_symlink_components` for every label, so the case no longer proves the v3 routine rejected the symlink (the security receipt explicitly accepted this). Iterate `sorted(V3_GATE_INPUT_PATHS)` in `capture_v3_snapshots` (deterministic diagnostics for every caller) and restore the v3-prefixed expectation, or assert a `Story 4.15 v3 bound source \.github/workflows/(ci|integration)\.yml has a symlinked path component` regex. [Oq8PlatformClosureTests.cs:329; tools/validate-oq8-platform-evidence.py:2776]
- [x] [Review][Patch] Test reseal helpers were not updated for the selector v2 schema, so fixture selectors are coherent only by validation order — `ResealSuccessorArtifacts` (`:3223-3234`) still writes the SDK `successor-sha256.txt` hash, six SDK file hashes, and SDK identity/subject/handoff hashes into `selector["successor"]`, which is now the v3 pointer; it should update `historical.sdkSuccessor.manifestSha256` / `.files`. The three SDK mutations that call it (`reviewed-base-wording`, `reviewed-resolver-count`, `review-external-authority`) pass only because their diagnostics fire in `validate_successor_closure` before `validate_successor_selector`. Likewise `ResealV3Manifest` / `ResealV3Receipt` never refresh `selector.successor.{files,manifestSha256,handoffSha256}`, so every v3 mutation leaves a stale selector; reorder validation and the whole matrix reports `successor selection drift` instead of what it claims to test. [Oq8PlatformClosureTests.cs:3183-3235,3505-3600]
- [x] [Review][Patch] The Group G `deferred-work.md` block commits 19 absolute private paths — every new `source_spec:` reads `` `/home/administrator/projects/hexalith/eventstore/_bmad-output/...` `` where the repo convention is the relative `_bmad-output/implementation-artifacts/spec-4-15-…` (311 relative vs 83 absolute before this change). Frozen Never: "commit … private paths". Replace the prefix. [_bmad-output/implementation-artifacts/deferred-work.md, Group G block]
- [x] [Review][Defer] The v3 test receipt's 384 / 1952 counts are not reproducible from any recorded execution — `reviews/test.json` attests `contracts-full` 1952/1952 and `V3_FULL_CONTRACTS_TEST_COUNT = 1952`, but the Completion Verification above records an isolated `Oq8PlatformClosureTests` run (384), a solution build, and a Contracts *build* — no full Contracts test execution — and states the live tree reports 259/384 focused failures. The +4 (380→384) matches the four new `InlineData` rows; the +51 full-lane delta is plausible from the uncommitted Group E guards but unverified. Deferred (unverified; would be high if false): settle by running `V3_CONTRACTS_TEST_COMMAND` on a story-isolated worktree of this exact remint and recording the 1952/1952 result (TRX path or tree hash) in `pre-review-execution.json` or the receipt findings. [v3/reviews/test.json:27-35; tools/validate-oq8-platform-evidence.py:293-295]

#### Resolution and Completion Verification (2026-09-09 Group H)

- Decision resolved as option (1): the owner confirmed an independent security review of predecessor subject `cd29ee74…` occurred in a separate LLM agent session using a different model, run by Administrator on 2026-09-09; `reviews/security.json` now records that provenance, states that the receipt re-issues the approval against the resealed subject, and replaces the false "semantics are unchanged" sentence with the actual validator-semantics delta.
- All seven patches applied, then one coherent v3 reseal (validator record → identity → pre-review execution → subject → receipts → handoff → manifest → selector). The pre-review commands (`validator-syntax`, `historical-v1-validation`, `historical-v2-validation`, `contracts-restore`, `contracts-build`) were executed for real on the story-isolated worktree and their exit codes recorded in `pre-review-execution.json`.
- Validator: `load_successor_selector` (regular-file guard before any read, bounded read, schema/field-set/date/reason), `validate_successor_selector_historical` (every mode), `validate_successor_selector` (v3 pointer, current-source only), `validate_successor_selector_sdk_link` (pre-review); `capture_v3_snapshots` iterates `sorted(V3_GATE_INPUT_PATHS)`; `V3_LIMITATIONS` and the handoff `historicalRule` name the SDK successor as historical and disclose the full-history Git ancestry requirement; `V3_FINAL_CLOSURE_TEST_COUNT = 398`, `V3_FULL_CONTRACTS_TEST_COUNT = 1966`.
- Tests: `V3SuccessorMutationsFailClosed` +7 rows (`sdk-gate-input-drift`, `landed-source-drift`, `head-ancestry-drift`, `sdk-link-drift`, `contracts-full-failed`, `consumer-install-command-drift`, `successor-selection-drift`) with `MutateV3Identity` / `ResealV3AfterIdentityChange` helpers and the v3-prefixed `symlinked-source-ancestor` expectation restored; new `NonDescendantHeadFailsFinalClosure` (full mode, exit 1), `HistoricalModesRejectCorruptedSelector` (5 rows across `--historical-v1-only` / `--historical-v2-only`), `PreReviewRejectsSelectorSdkLinkDrift`; `ResealSuccessorArtifacts` now targets `historical.sdkSuccessor`; `MutateV3TestVerification` takes the command name. Sealed v1 per-method counts are untouched (no rows added to v1-pinned methods).
- Prose: `limitations.json`, `source-only-handoff.json.consumerInstructions.historicalRule`, and `docs/ci.md` (fetch-depth requirement for the committed-closure step, `.oq8-python` invocation, v1/SDK/v2 historical + v3 active lineage) updated together; the Group G `deferred-work.md` block uses relative `source_spec` paths.
- Story-isolated worktree (`HEAD 0994c378` + the Story 4.15 files, `docs/ci.md`, and the two in-flight gate inputs `ReleasePackageManifestTests.cs` / `Oq8PostgresqlFixture.cs` that the v3 identity already sealed): `python3 tools/validate-oq8-platform-evidence.py` passed; `--historical-v2-only` passed; `--lifecycle-mode final` passed; `Oq8PlatformClosureTests` 398 passed / 0 failed / 0 skipped; `V3_CONTRACTS_TEST_COMMAND` 1966 passed / 0 failed / 0 skipped (settles the Group H defer item); Contracts build 0 warnings / 0 errors.
- Live tree: `--lifecycle-mode final` passed; `git diff --check` passed on the Story 4.15 files; the full validator remains blocked by the unrelated in-flight `docs/guides/configuration-reference.md` sealed-body drift (same blocker as the Group G verification above).
- Note for CI: the isolated full-lane run required the root `references/` submodules to be present (two Contracts tests read `references/Hexalith.AI.Tools` and `references/Hexalith.Tenants`), which the CI checkout already provides.

#### Rejected (2026-09-09 Group H)

- false: `historical.v2Successor` "discards `validate_v2_successor()`'s return value" — that function returns `None`; the v2 manifest hash is asserted inside it against `V2_CLOSURE_MANIFEST_SHA256` (`:2714-2718`), which is the same constant the selector is compared to. The `files`-less shape is a design asymmetry with no named mis-validation.
- false: The prior v3 generation (2026-09-08) was "overwritten in place rather than preserved", violating frozen Never "rewrite … pending-review history" — the active v3 lineage is reminted by design (Group B did the same on 2026-09-08; Group G rejected "coherent reseal is undetected drift"); the frozen clause protects the Story 4.14 captured observations and pending-review records, which are untouched, and the prior v3 bytes remain at `f27826af`.
- false: Selector schema `…/v2` versus `V3_SELECTION_*` constants and a `landedSource` that "points at the v1 commit" are misbindings — the selector's schema version is independent of the successor generation, and `landedSource` is the story's canonical AC2 commit `5e8f175b` by definition.
- rejected (fix edits the spec under review): the Group G line "mirroring the already-present v2 coverage" overstates what was added (v2 also mutates name, command, count, float, boolean); the missing rows are carried in the mutation-coverage patch above.
- low: `run_git("rev-parse", "--verify", f"{LANDED_SOURCE}^{{commit}}")` followed by `require(landed_commit == LANDED_SOURCE)` is tautological — it still proves the object exists; the tree comparison carries the information. Deleting it changes nothing.
- low: Ancestry failure surfaces as the generic `Git identity proof failed for merge-base --is-ancestor` rather than a `Story 4.15 v3 …` message — the command is named; wrapping it adds a branch for a rare path.
- low: `V3_PRE_REVIEW_COMMANDS` still run bare `python3` while the handoff mandates `.oq8-python/bin/python` — no harm named: `validate_pyyaml_dependency` would have failed the pre-review sequence had that interpreter lacked PyYAML.
- low: `sha256_file(SUCCESSOR / "successor-sha256.txt")` follows a symlinked SDK manifest because `validate_successor_manifest` checks `is_file()` only for the manifest itself — pre-existing, requires a hostile same-user writer, and the fix adds a guard (DW-454 class).
- low: Selector `manifestSha256` is compared to live-disk `sha256_file(V3_SUCCESSOR / "closure-sha256.txt")` while v3 uses snapshots — a concurrent-writer window inside the single-writer CI boundary (DW-454 class).
- low: Test case `prior-selection` keeps its stale name after the field became `historical` — cosmetic, and renaming forces a reseal of a sealed gate input for zero behaviour change.
- low: The new deferred-work entries restate DW-420 / DW-364 (unused `id-token` / attestation writes) and the `ci / contracts` not-required item already at `deferred-work.md:4014` without cross-references — the flat form is sanctioned and `bmad-loop-sweep` verifies against the tree; adding "already tracked as" notes is housekeeping.

### Review Findings

_Chunked code review, Group I (2026-09-09). Target: the Group H remint committed in `df382874` (4.15-bound paths vs `0994c378`, 16 files, +823/−203). Layers: Blind Hunter, Edge Case Hunter, Verification Gap, Acceptance Auditor._

- [x] [Review][Decision] The v3 security receipt still binds `decision: approved` to frozen subject `84fdda80…` while disclosing that the independent review examined predecessor `cd29ee74…`, and the architecture and test receipts add no independent provenance of the resealed subject — `reviews/security.json` findings[0] states the independent review was of `cd29ee7408c988d1af2480ec862d985295e1ad81f68d81184565f2c4f8f851ac` and that “this receipt re-issues that approval against resealed subject 84fdda… after the review-driven patches were applied and re-verified in the sealing session.” Architecture and test receipts for the same subject carry no equivalent provenance; architecture findings remain two lineage sentences and do not inspect the Group H patches they approve (selector load-before-read, historical/current split, ancestry proof, mutation matrix). Receipt `issuedAt` values remain a 1–2s mechanical cadence (`17:30:37Z` / `17:30:39Z` / `17:30:40Z`, handoff `assembledAt` `17:30:41Z`). Frozen Always requires every approval bound to one frozen subject; frozen Never forbids fabricating reviewer approval; Group G still requires an independent review of that exact subject. Group H recorded owner option (1) for the predecessor review; this remint did not obtain a distinct review of `84fdda80…`. Options: (1) obtain a genuinely independent review of subject `84fdda80…` and bind those receipts; (2) human-renegotiate frozen Always/Never to allow predecessor-review plus owner-attested reseal, and record that contract on every receipt; (3) set `decision: rejected` until an independent review of the current subject exists. [`evidence/story-4-15-successors/v3/reviews/security.json:5-13`; `reviews/architecture.json:10-12`; `reviews/test.json:10-12`]
- [x] [Review][Patch] `--pre-review` still does not pin selector authority or the historical v1/v2/SDK hashes beyond `sdkSuccessor.files["source-artifact-identity.json"]` — `validate_pre_review_candidate` calls `validate_successor_selector_sdk_link(load_successor_selector())`, which checks schema/field-set/date/reason plus that one identity hash. `validate_successor_selector_historical` (which compares the full `historical` object, including `v1Closure.packetSha256`, `v2Successor.manifestSha256`, `sdkSuccessor.manifestSha256`/`files`, and `validate_authority`) runs only from `validate_platform_closure`. A selector with `releaseApproved: true` or a zeroed `historical.v1Closure.packetSha256` therefore still passes `--pre-review` and can be frozen. Add those pins to the pre-review path (or call the historical helper with the already-known constants) and add pre-review mutations. [tools/validate-oq8-platform-evidence.py:3648-3664]
- [x] [Review][Patch] The v3 test receipt claims skipped verification coverage for both commands; the matrix only skips the focused command — `reviews/test.json` findings[1] says the matrix “rejects a rejected review and missing, failed, or skipped test-verification evidence for both verification commands.” `V3SuccessorMutationsFailClosed` has `test-verification-skipped` only against `oq8-platform-closure` and `contracts-full-failed` only for `failed` on `contracts-full`; there is no `contracts-full-skipped` row. Add that row (or correct the receipt). [Oq8PlatformClosureTests.cs:339-343,3617-3625; reviews/test.json:12]
- [x] [Review][Patch] `--pre-review` never asserts the selector regular-file guard this remint added — `load_successor_selector` now requires a regular non-symlink file before any parse, and `--pre-review` does call it, but no pre-review test deletes or symlinks the selector. `HistoricalModesRejectCorruptedSelector` and `SuccessorSealMutationsFailClosed` `selector-symlink` cover historical and default modes only; `PreReviewRejectsSelectorSdkLinkDrift` writes a regular file and zeros one hash. Add a `--pre-review` symlink (and missing-file) case on `CreateCandidateFixture` asserting `Story 4.15 successor selector must be a regular non-symlink file`. [Oq8PlatformClosureTests.cs:430-451; tools/validate-oq8-platform-evidence.py:1755-1759,3649]
- [x] [Review][Patch] The v3 handoff still omits the full-history checkout requirement that limitations and `docs/ci.md` now state — `limitations.json` and `V3_LIMITATIONS` say a shallow clone that lacks `5e8f175b…` fails closed, and `docs/ci.md` says any lane running committed-closure needs `fetch-depth: 0`, but `source-only-handoff.json.consumerInstructions.historicalRule` / `currentRule` only mention immutable historical artifacts and current candidate bytes. A consumer following only the handoff JSON will run `.oq8-python/bin/python tools/validate-oq8-platform-evidence.py` on a shallow clone and get an opaque Git identity failure. Put the object-store / `fetch-depth: 0` requirement in `consumerInstructions` next to `installCommand` / `verifyCommand`. [v3/source-only-handoff.json:17-22; tools/validate-oq8-platform-evidence.py:278-283,3140-3147]
- [x] [Review][Defer] The Group H deferred-work row still summarizes the 384/1952 reproducibility gap as open while its evidence paragraph says it was settled — `deferred-work.md` Group H `summary` reads “The v3 test receipt's 384 / 1952 test counts are not reproducible from any recorded execution,” and the `evidence` field then records the 1966/1966 isolated run that settled it. The append-only ledger cannot rewrite the summary; `bmad-loop-sweep` will keep seeing an open unreproducible-count claim. Deferred: pre-existing append-only shape; settlement is already in the evidence paragraph and the spec's Group H Completion Verification. [_bmad-output/implementation-artifacts/deferred-work.md:4114-4116]

#### Rejected (2026-09-09 Group I)

- false: `requirements-oq8.txt` is unnamed by current-source closure because it is absent from `V3_GATE_INPUT_PATHS` — live content is exact-matched by `validate_pyyaml_dependency` (`splitlines() == [PINNED_PYYAML_REQUIREMENT]`) and the file is historically hashed in `PRIOR_ROOT_BINDING_HASHES`; changing the bootstrap file fails validation. Artifact-hash pinning of the PyYAML wheel remains the open Group D patch.
- false: `NonDescendantHeadFailsClosed` must be renamed because it still expects `--pre-review` exit 0 — the method name is a sealed v1 pre-review command identity (`PRE_REVIEW_COMMAND_RESULTS` / `NonDescendantHeadFailsClosed`); the XML docs already state that, and `NonDescendantHeadFailsFinalClosure` covers default-mode ancestry rejection.
- false: The commit claims a source-safety UX validation report, `LocalAuthenticationTestInvocation`, and `ProviderVerificationCredential` that are missing from this diff — those paths were excluded from this Group I slice by design; they are the rest of mixed commit `df382874`, not missing 4.15 behavior.
- false: The security receipt's “historical block are validated in every mode” is a lie about `--pre-review` — the same finding sentence separately says the historical SDK link is checked in pre-review mode; `validate_successor_selector_historical` does run for `--historical-v1-only`, `--historical-v2-only`, and default `validate_platform_closure`. The remaining pre-review pin hole is filed as a patch above.
- rejected (fix edits the spec under review): Binding live `ci.yml` as a v3 gate input and changing `selectedOn` to `2026-09-09` leaves Group D/E ledger rows stale — those rows are in this spec; correcting them is a spec edit.
- low: Limitations claim a missing landed commit fail-closes, but there is no depth-1 / missing-object fixture — `merge-base --is-ancestor` already fail-closes when Git lacks `5e8f175b…`; a new shallow-clone fixture is more than a direct correction, and `ci / contracts` already checks out with `fetch-depth: 0`.
- low: `docs/ci.md` still explains fetch-depth under Integration Tests and never names `ci / contracts` — the paragraph already states depth 1 is valid only in capture mode and that any committed-closure lane needs `fetch-depth: 0`; relocating it and listing all 13 gate paths is operator-doc polish on a sealed gate input.
- low: `ResealV3Manifest` / `ResealV3Receipt` still do not refresh `selector.successor` hashes — the current matrix fails inside `validate_v3_successor` before the selector pointer is compared; everyday use does not reorder those checks.
- low: `SourceOnlyConsumerBootstrapInstructionsAreExactAndOrdered` / `DOCUMENT_REQUIRED_TEXT` do not lock the new `docs/ci.md` fetch-depth sentences — `docs/ci.md` is already a live v3 gate input whose hash pins those bytes; adding a parallel semantic document surface duplicates the handoff-instruction patch above.
- low: Sealed `V3_PRE_REVIEW_COMMANDS` still record bare `python3` rather than `.oq8-python/bin/python` — already rejected in Group H; `validate_pyyaml_dependency` still fail-closes if that interpreter lacks the pin.

#### Resolution and Completion Verification (2026-09-09 Group I)

- Decision resolved as sequenced option (1): do not independently review predecessor subject `84fdda80…`; apply the four Group I patches; freeze a new subject; obtain one independent security review of that exact subject. Architecture and test receipts are owner-attested. Do not set `decision: rejected` on the old receipt. Sprint status remains `review`; Group J is not started from this remint.
- All four patches applied, then one coherent v3 reseal (validator record → identity → pre-review execution → subject → receipts → handoff → manifest → selector). A first freeze at subject `90803fdf…` was discarded after the isolated full Contracts lane reported 1970 rather than the arithmetic 1966+6=1972 (`62b2a630` is two tests below the Group H tree). The bound subject is `520d8569b272b2a22a63264ab8a8d77edb5ae202734a99d63999b0f5918a2609`.
- Validator: `validate_successor_selector_sdk_link` now calls `validate_authority` and pins `historical.v1Closure.packetSha256` / `historical.v2Successor.manifestSha256`; `V3_CONSUMER_HISTORICAL_RULE` requires a full Git object store (`fetch-depth: 0`) because a shallow clone lacking `5e8f175b2ced4715f7c6f765386812cc1001dbb4` fails closed; `V3_FINAL_CLOSURE_TEST_COUNT = 404`, `V3_FULL_CONTRACTS_TEST_COUNT = 1970`.
- Tests: `V3SuccessorMutationsFailClosed` adds `contracts-full-skipped`; `PreReviewRejectsCorruptedSelector` covers missing, symlink, `releaseApproved` overstatement, v1-packet, and v2-manifest drift on `--pre-review`.
- Independent security review: a separate Claude Opus 5 session hashed subject `520d8569…` in the story-isolated worktree, re-executed `validator-syntax` / `--historical-v1-only` / `--historical-v2-only` there, and returned `decision: approved`. The same reviewer first rejected the live working tree because HEAD public documents drift the immutable v1 pins; that failure is the later public-document group, not this remint. Architecture and test receipts disclose owner attestation; security `issuedAt` is `2026-09-09T20:58:31Z` (not a 1–2s cadence with the other receipts).
- Story-isolated worktree (`HEAD 62b2a630` + the Story 4.15 files, with `docs/guides/configuration-reference.md` and `docs/reference/command-api.md` restored from `0994c378`): `python3 tools/validate-oq8-platform-evidence.py` passed; `--historical-v1-only` passed; `--historical-v2-only` passed; `Oq8PlatformClosureTests` 404 passed / 0 failed / 0 skipped; `V3_CONTRACTS_TEST_COMMAND` 1970 passed / 0 failed / 0 skipped.
- Live tree: the full validator remains blocked by the unrelated in-flight `docs/guides/configuration-reference.md` sealed-body drift (same blocker as Group G/H). The isolated public-document overlays were not copied into the live tree.
- Group J should not start unless a later independent review of this bound subject finds a must-fix.

### Review Findings

_Chunked code review, Group J (2026-09-09). Target: the uncommitted Group I remint vs HEAD `62b2a630` (14 files at review, +173/−115; two public-doc overlays then restored). Layers: Blind Hunter, Edge Case Hunter, Verification Gap (empty — treated as failed), Acceptance Auditor._

- [x] [Review][Decision] **RESOLVED 2026-09-10 (owner: omit overlay from remint).** This remint copies the `0994c378` public-document overlay into the working tree, restoring operator docs the product no longer implements and re-introducing example secrets — Group I recorded that isolated overlays were not copied into the live tree and that HEAD public-document drift belongs to a later group. This remint still restores `docs/guides/configuration-reference.md` and `docs/reference/command-api.md` from `0994c378` so v1 `EXPECTED_DOCUMENT_HASHES` live-file checks pass. Those bytes delete `ValidAudiences`, `AllowedAlgorithms`, XOR `Authority`/`SigningKey`, UTF-8 32-byte `SigningKey`, Production symmetric rejection, HTTPS URI rules, published UI grant tables, and `aspire start` / `aspire publish`; they describe `EnableKeycloak=false` as "testing without authentication" while AppHost still uses a per-run symmetric key; they add `admin-user`/`admin-pass`, `Password=secret`, `amqp://user:pass@…`, and `https://localhost:5001`. Frozen Never forbids committing secrets; Tasks require truthful final-behavior docs; Ask First covers public contracts. The v3 security receipt approved subject `520d8569…` after that overlay and states the two paths are outside every v3 gateInput, so v3 approval does not authorize shipping those bytes. [docs/guides/configuration-reference.md; docs/reference/command-api.md; reviews/security.json:10-12]
- [x] [Review][Defer] v1 public-document proofs hash live working-tree files rather than the v1 Git snapshot [tools/validate-oq8-platform-evidence.py:3282-3284] — deferred: pre-existing. Other historical bindings use `sha256_git_file(COMPLETED_V1_CLOSURE_COMMIT, …)`; public docs use `sha256_file(ROOT / relative)` against `EXPECTED_DOCUMENT_HASHES`. That is why later JWT-doc work breaks Story 4.15 without a v3 gate-input change, and why this remint reached for the overlay. Belongs to the later public-document group, not this Group I remint.
- [x] [Review][Defer] `--pre-review` still does not pin the full selector `historical` object [tools/validate-oq8-platform-evidence.py:3658-3682] — deferred: leftover of the accepted Group I partial pin, not introduced here. `validate_successor_selector_sdk_link` now checks authority, v1 `packetSha256`, v2 `manifestSha256`, SDK directory, and one SDK file hash. `validate_successor_selector_historical` also pins v1 `packetPath` / directory / `manifestSha256` / `files`, v2 directory, and the SDK manifest. Pre-review can still freeze a selector that final closure would reject.

#### Rejected (2026-09-09 Group J)

- false: Architecture and test receipts being owner-attested is a new defect — Group I resolved those two lanes as owner-attested; only security required an independent review of subject `520d8569…`.
- false: The architecture receipt must record the public-doc overlay — those two paths are outside the v3 subject; the security receipt already discloses the isolated `0994c378` restore.
- false: Focused 404 versus full-lane 1970 means two tests are missing — Group I discarded the 1966+6=1972 arithmetic because HEAD `62b2a630` is two tests below the Group H tree; the isolated run measured 1970/1970.
- false: Putting `fetch-depth: 0` on `historicalRule` contradicts `bindingRule` — this remint is the Group I handoff patch; `limitations.json` already states the Git object-store rule; `V3_BINDING_RULE` is about source-path resolution, not clone depth.
- false: `PreReviewRejectsCorruptedSelector` must include `field-set` — `load_successor_selector` already requires that field set on `--pre-review`.
- false: Historical modes lack `missing` and `authority` coverage, so those drifts can pass — `load_successor_selector` and `validate_successor_selector_historical` already fail closed; `HistoricalModesRejectCorruptedSelector` already covers symlink, historical-hash, and field-set.
- false: Security said it did not review the overlay — the receipt records that the independent reviewer rejected the live tree for v1 public-doc drift, then approved the isolated tree after the `0994c378` restore, and that those paths are outside v3 gateInputs. The remaining issue is shipping the overlay with the remint, filed as the decision item.
- low: `v1-packet` and `historical` share one diagnostic — both fail closed; splitting the message reseals a live gate input for no everyday behavior change.
- low: There is no shallow-clone mutation fixture — `merge-base --is-ancestor` already fail-closes when Git lacks `5e8f175b…`; Group I rejected a new fixture, and `ci / contracts` already checks out with `fetch-depth: 0`.

#### Resolution and Completion Verification (2026-09-10 Group J)

- Decision resolved as option (1): omit the two public-document overlays from this remint. `docs/guides/configuration-reference.md` and `docs/reference/command-api.md` were restored from HEAD `62b2a630`. Current JWT/Aspire/auth docs remain; example secrets are not in the remint. Isolated validation may still overlay `0994c378` locally; that overlay stays uncommitted.
- Remaining dirty 4.15 surface is the Group I v3 remint (12 files): validator, `Oq8PlatformClosureTests.cs`, selector, and the v3 successor chain. Full live-tree validator stays red on v1 public-document drift until the later public-document group.
- Sprint tracking stays `review` (OQ8 lifecycle gate; same as Group I). Do not flip Story 4.15 to `done` from this review.

### Review Findings

_Chunked code review, Group K (2026-09-10). Target: the uncommitted Group I remint vs HEAD `62b2a630` after Group J omitted the public-document overlays (12 files, +138/−65). Layers: Blind Hunter, Edge Case Hunter, Verification Gap (empty — treated as failed), Acceptance Auditor._

- [ ] [Review][Patch] Clone-depth disclosure is attached to `historicalRule`, not the current-source verify path — `V3_CONSUMER_HISTORICAL_RULE` / `source-only-handoff.json` `historicalRule` now say a shallow clone lacking `5e8f175b…` fails closed, using GitHub Actions `fetch-depth: 0` jargon. `currentRule` and `verifyCommand` are unchanged. `V3_LIMITATIONS` / `limitations.json` (`b82bfd65…`, unchanged) locate that failure on v3 landed-commit / tree / HEAD-ancestry proofs, which run on the default full validator, not on historical v1/SDK/v2 snapshot validation. Group I asked for the object-store rule next to `installCommand` / `verifyCommand`; it landed on the wrong sibling field. Move the sentence onto `currentRule` (keep `historicalRule` as historical-only) and reseal. [tools/validate-oq8-platform-evidence.py:298-302,3147-3151; v3/source-only-handoff.json:19-20]
- [x] [Review][Defer] `--pre-review` still does not pin the full selector `historical` object [tools/validate-oq8-platform-evidence.py:3658-3682] — deferred: leftover of the accepted Group I partial pin, already recorded by Group J. `validate_successor_selector_sdk_link` checks authority, v1 `packetSha256`, v2 `manifestSha256`, SDK directory, and `files["source-artifact-identity.json"]`. It does not pin v1 `packetPath` / directory / `manifestSha256` / `files`, v2 `directory`, SDK `manifestSha256` / remaining files, or `set(historical)`. `PreReviewRejectsCorruptedSelector` never mutates those holes. A selector that final closure would reject can still be frozen.
- [x] [Review][Defer] Sealed 404/1970 counts and the new `--pre-review` tests are not reproducible on this remint tree [reviews/test.json:17-35; Oq8PlatformClosureTests.cs:466-474; validate-oq8-platform-evidence.py:3282-3284] — deferred: same live-hash public-document gate Group J already deferred to the later public-document group. `validate_review_subject` still hashes live `docs/guides/configuration-reference.md` (`edd38e7c…` vs pin `e2fde4db…`) and `docs/reference/command-api.md` (`1f19e75b…` vs `6b0bfd40…`). `CreateCandidateFixture` copies those live files; `--pre-review` calls `validate_review_subject` before the new selector checks. Group J omitted the `0994c378` overlay; isolated validation may still overlay locally.

#### Rejected (2026-09-10 Group K)

- false: `SourceOnlyConsumerBootstrapInstructionsAreExactAndOrdered` must lock the new `historicalRule` text — `validate_v3_handoff` already exact-matches `consumerInstructions.historicalRule` to `V3_CONSUMER_HISTORICAL_RULE`; a parallel C# pin would duplicate a sealed gate input.
- false: `V3_REVIEW_SCOPES` must name pre-review selector pins and clone-depth disclosure — `acceptedScope` is a coarse constant; findings are allowed to be more specific, and the validator does not require findings ⊆ scope.
- false: The security receipt's "shallow-clone failure behavior are enforced" is only new handoff prose — `merge-base --is-ancestor` already fail-closes when Git lacks `5e8f175b…`; the receipt does not claim a new fixture. Group I/J already rejected adding one.
- false: `docs/ci.md` must describe the new `--pre-review` authority / v1 / v2 pins — that file is unchanged in this remint and describes CI capture/committed-closure, not candidate freeze. `--pre-review` is pinned by the validator and `PreReviewRejectsCorruptedSelector`.
- false: `validate_successor_selector_sdk_link` must be renamed after growing into an authority / v1 / v2 checker — cosmetic; renaming a sealed gate-input symbol reseals for no behavior change.
- false: Architecture findings must record live-tree public-document drift — those two paths are outside every v3 gateInput; the security receipt already discloses the isolated `0994c378` restore. Group J rejected the same demand.
- false: Restoring public docs from HEAD `62b2a630` names the wrong public-doc baseline — working-tree bytes match HEAD `62b2a630`; that restore is Group J's omit-overlay decision. The v1 pin baseline remains `0994c378` and is the deferred public-document group's subject, not a claims lie.

#### Actions (2026-09-10 Group K)

- Owner left the one patch as an action item (do not apply, do not reseal from this review).
- Sprint tracking stays `review`; spec frontmatter stays `done` (DW-497 / OQ8 lifecycle gate). Do not flip Story 4.15 to `in-progress` or `done` from this review.

### Review Findings

_Chunked code review, Group L (2026-09-10). Target: Story 4.15 v1 public documents, `699ca712` → `62b2a630`. Files: `docs/guides/configuration-reference.md`, `docs/reference/command-api.md`, `docs/concepts/command-lifecycle.md`, `docs/concepts/architecture-overview.md`. Layers: Blind Hunter, Edge Case Hunter, Verification Gap (empty — treated as failed), Acceptance Auditor._

- [x] [Review][Decision] **RESOLVED 2026-09-10 (owner: hash the v1 Git snapshot).** Identity uses `sha256_git_file(COMPLETED_V1_CLOSURE_COMMIT, relative)`; `17e47a39` already matches `EXPECTED_DOCUMENT_HASHES`. Keep `validate_document_semantics` on live files. Overlay restore remains rejected. Validator edit is the patch below; v2/v3 remint follows because the validator is a sealed gate input. [tools/validate-oq8-platform-evidence.py:3282-3284]
- [ ] [Review][Patch] Public-document identity hashes live files instead of the v1 Git snapshot — Switch the `EXPECTED_DOCUMENT_HASHES` loop in `validate_review_subject` to `sha256_git_file(COMPLETED_V1_CLOSURE_COMMIT, relative)`. Leave the pin map and live `validate_document_semantics` unchanged. [tools/validate-oq8-platform-evidence.py:3282-3284]
- [ ] [Review][Patch] Present-tense "live closure validator passes" overclaims completion — The four new OQ8 blocks assert that the live validator already passes. `DOCUMENT_REQUIRED_TEXT` does not include that clause; the required consumer gate is the following "only while" sentence. Live hashes already fail on two of these files. [docs/guides/configuration-reference.md:808; docs/reference/command-api.md:558; docs/concepts/architecture-overview.md:282; docs/concepts/command-lifecycle.md:270]
- [ ] [Review][Patch] Authentication docs contradict `JwtBearerAuthenticationContract` — Intro and the Audience row still say Audience is always required; the new `ValidAudiences` row says an empty Audience is allowed and that the first non-blank entry becomes primary, while `ResolveAudiences` throws on any blank entry. The summary still has Audience as a non-empty string and SigningKey as character `length >= 32`. `AllowInsecureSymmetricKey` and symmetric-mode `AllowedAlgorithms` (empty or HS256 only) are omitted. The new URI bullet forbids query on token endpoints (product allows a fixed query), implies Development Authority may carry userinfo/query/fragment (code rejects always), and does not say HTTP Authority in Development requires `RequireHttpsMetadata=false` or that a discovered token endpoint off the Authority origin needs an explicit `TokenEndpoint`. [docs/guides/configuration-reference.md:383-420,434,766-771]
- [ ] [Review][Patch] `aspire publish` uses unsupported `--project` — The same hunk correctly switches start to `--apphost`; the publish one-liner still uses `--project`, which pinned Aspire 13.4 rejects. Tracked publish examples use `--apphost`. [docs/guides/configuration-reference.md:663]
- [ ] [Review][Patch] Complete Flow polls `https://localhost:5001` after submit uses `EVENTSTORE_URL` — Steps 1–2 take the live `eventstore` HTTPS URL; step 3 still hits port 5001. [docs/reference/command-api.md:535]
- [ ] [Review][Patch] Token-acquisition prose denies credential exposure the AppHost still publishes — "neither exposes its generated realm-user credentials" is false for non-secret `local-auth-admin-username` and `local-auth-sample-username` (`"tenant-a-user"`). Passwords are secret parameters; `HEXALITH_ADMIN_*` are correctly not env inputs. [docs/reference/command-api.md:26]
- [x] [Review][Defer] Remaining `https://localhost:5001` curl/Location examples on the Command API page were not part of this hunk — deferred: pre-existing. Only Complete Flow submit was switched to `EVENTSTORE_URL`. [docs/reference/command-api.md:99,116,132,153,246,302,390]
- [x] [Review][Defer] Sibling public docs still teach `dotnet run --project` AppHost and `localhost:8180` token recipes — deferred: pre-existing, outside this four-file chunk (`docs/guides/troubleshooting.md`, `docs/assets/regenerate-demo-checklist.md`).

#### Rejected (2026-09-10 Group L)

- false: Authority JSON sample must include `ValidAudiences` — the sample already sets `Audience`; additional audiences are optional.
- false: Empty `AllowedAlgorithms` `[]` is undocumented — the new table and validation rules already require a non-empty authority-mode allow-list.
- false: Deleting the Keycloak 401 re-acquire tip leaves callers without 401 recovery — the status tables still document 401 as a missing or invalid JWT; the deleted note was the 5-minute Keycloak recipe Group J forbade.
- false: The `EnableKeycloak=false` row is still "testing without authentication" — this hunk already retargets it to per-run symmetric mode; Production rejection is in the JWT rules.
- low: OQ8 handoff prose sits under Next Steps — the marker and required strings can live in that section; moving them reseals for navigation only.
- low: `TokenEndpoint` / audience-parameter rows are missing from the main JWT table and summary — they are in the new Published UI table.
- low: Authority algorithm set is not enumerated — the table already examples `RS256` and says "supported asymmetric algorithms".
- low: No SigningKey-mode JSON example — the validation rules already describe that mode; a second sample is extra surface.
- low: One-sided `AudienceParameterName` / `Value` is not spelled "both or neither" — the new row already says optional pair and "omit both".
- low: Empty `EVENTSTORE_URL` can make curl post a relative path — the recipe assumes a running AppHost; an extra abort guard is not an everyday correction.
- low: OQ8 consumer steps are Unix-only `.oq8-python/bin/python` — `DOCUMENT_REQUIRED_TEXT` pins those exact strings; a Windows stanza is extra and this workspace's OQ8 path is Linux/WSL.

#### Actions (2026-09-10 Group L)

- Owner chose option 2 (hash the v1 Git snapshot). That decision is a patch; all six patches were left as action items (do not apply, do not reseal from this review).
- Sprint tracking stays `review`; spec frontmatter stays `done` (DW-497 / OQ8 lifecycle gate). Do not flip Story 4.15 to `in-progress` or `done` from this review.

_Chunked code review, Group B re-review (2026-09-10). Baseline `699ca712` → HEAD `62b2a630`. File only: `tests/Hexalith.EventStore.Contracts.Tests/Packaging/Oq8PlatformClosureTests.cs` (committed; uncommitted overlay excluded). Layers: Blind Hunter, Edge Case Hunter, Verification Gap, Acceptance Auditor._

- [ ] [Review][Patch] Semantic Postgres image-tag rows never observe validator image extraction — `semantic-workflow-tag` / `semantic-fixture-tag` rewrite fixture bytes then point v2 `successorSha256` at those bytes, so default validation dies on `Story 4.15 v2 historical source identity drift` against frozen git. `extract_v2_workflow_image` / `extract_v2_fixture_image` and `Story 4.15 v3 current source PostgreSQL image drift` are unasserted. Add v3 rows that swap the digest, reseal matching `gateInputs`, and assert the image-drift diagnostic; retarget or drop the v2 `semantic-*` rows so they are not a second hash test. [tests/Hexalith.EventStore.Contracts.Tests/Packaging/Oq8PlatformClosureTests.cs:182-183,347,3919-3931]
- [ ] [Review][Patch] Named test-reviewer `decision` is never independently rejected — v1 `review-decision` / `review-architecture-decision` and v3 `receipt-rejected` mutate security or architecture; `reviews/test.json` is only hash- or count-mutated (`review-subject`, `test-verification-*`). Add a `decision = "rejected"` row for the test receipt with intact verification counts. [tests/Hexalith.EventStore.Contracts.Tests/Packaging/Oq8PlatformClosureTests.cs:2926-2945,3632-3639]
- [ ] [Review][Patch] No fail-closed row sets `eventStorePlatformComplete` to false — `ApprovedSourceOnlyHandoffPasses` only checks exit 0; `OverstatedExternalAuthorityFailsClosed` only sets forbidden flags true. Add a mutation that clears platform completion and asserts rejection. [tests/Hexalith.EventStore.Contracts.Tests/Packaging/Oq8PlatformClosureTests.cs:74-84,1133-1152]
- [ ] [Review][Patch] Story 4.14 capture immutability is only an `observations.json` checksum poke — no `review-records.json` pending-history mutation, extra eighth capture file, or rewrite of pending-review fields. Add those fail-closed rows against the fixture copy of the 4.14 directory. [tests/Hexalith.EventStore.Contracts.Tests/Packaging/Oq8PlatformClosureTests.cs:2791-2793,2024]
- [ ] [Review][Patch] Public-document semantic fail-closed covers only `command-api.md` — `document-semantics`, `candidate-document-semantics`, and `candidate-document-stale-state` edit that one file. Extend the same authority-wording mutation to the other three Code Map public docs. [tests/Hexalith.EventStore.Contracts.Tests/Packaging/Oq8PlatformClosureTests.cs:3125-3137,2382-2392]
- [ ] [Review][Patch] Approved limitation strings are never golden-locked — `EveryLimitationTextIsExact` / `CandidateLimitationTextIsExact` replace indexes 0–5 with `"Mutated limitation {index}."` and only assert drift. Assert the six reviewed texts (test seams, sanitized structural-state, Folders-bytes disclosure) before mutating. [tests/Hexalith.EventStore.Contracts.Tests/Packaging/Oq8PlatformClosureTests.cs:1172-1198,1383-1405]
- [ ] [Review][Patch] v3 consumer-instruction lock omits `historicalRule` and `currentRule` — `SourceOnlyConsumerBootstrapInstructionsAreExactAndOrdered` pins v1 field set and only `installCommand` / `verifyCommand` on the v3 handoff. Lock the v3 object keys and those two extra strings. [tests/Hexalith.EventStore.Contracts.Tests/Packaging/Oq8PlatformClosureTests.cs:602-638]
- [ ] [Review][Patch] `CandidateExternalAuthorityFailsClosed` omits `ShouldNotContain("Traceback")` — sibling fail-closed theories require it; a stack leak on this path would still pass. [tests/Hexalith.EventStore.Contracts.Tests/Packaging/Oq8PlatformClosureTests.cs:1427-1442]
- [ ] [Review][Patch] `InvalidPyYamlDependenciesFailClosed("missing")` uses `python3 -S` but does not clear `PYTHONPATH` — ambient `PYTHONPATH` can still import PyYAML, so the missing-dependency row is not isolated. Set `PYTHONPATH` empty when `disableSitePackages` is true. [tests/Hexalith.EventStore.Contracts.Tests/Packaging/Oq8PlatformClosureTests.cs:668-695,4839-4880]
- [ ] [Review][Patch] Later-unbound fixture commit does not disable commit-msg hooks — `LaterUnboundRepositoryWorkPreservesPinnedPathVerification` sets `user.*` and `commit.gpgsign=false` but not `core.hooksPath`. Add `-c core.hooksPath=/dev/null` so global/husky hooks cannot reject the fixture commit. [tests/Hexalith.EventStore.Contracts.Tests/Packaging/Oq8PlatformClosureTests.cs:947-960]
- [x] [Review][Defer] Bound-path Git tests named fail-closed still expect pass — deferred: already DW-496 / Group B owner acceptance; `ChangedOrDeletedBoundCapabilityPathFailsClosed`, `HiddenBoundCapabilityPathFailsClosed`, and `NonDescendantHeadFailsClosed` remain sealed pre-review pass assertions. Live current-source Git rejection is not this test-file chunk. [tests/Hexalith.EventStore.Contracts.Tests/Packaging/Oq8PlatformClosureTests.cs:1890-1968]

#### Rejected (2026-09-10 Group B re-review)

- false: `--historical-v1-only` plus selector `field-set` is untested — `load_successor_selector` is the same function already failed closed by `--historical-v2-only` `field-set`.
- false: `spec_from_file_location` result is unchecked — the probe always passes the real validator path; a missing loader would fail the test, not green-wash production.
- false: Float-integer mutations can serialize as ints and become no-ops — the rows currently fail closed, so `WriteObject` preserves the `18.0` / `368.0` tokens.
- false: Isolated `--lifecycle-mode final` can pass without the packet — intentional isolation; already rejected in Group A/B; default `ApprovedSourceOnlyHandoffPasses` is the packet gate.
- false: Bootstrap test never runs `installCommand` / `verifyCommand` and uses `ignoreOrder: true` — v1 subject vs handoff `ToJsonString()` locks field order; executing the venv is outside this class; `docs/ci.md` is a hash gate-input, not a bootstrap doc.
- false: `ApprovedSourceOnlyHandoffPasses` never re-reads overstated authority flags — already rejected in Group B; `OverstatedExternalAuthorityFailsClosed` covers each forbidden `true`.
- low: Extra CLI exclusivity rows (`--pre-review --lifecycle-mode`, historical combined with capture) — Group B already deferred the lifecycle pair; remaining combos are not everyday.
- low: Pre-review selector mutations beyond SDK link — committed `HistoricalModesRejectCorruptedSelector` / `SuccessorSealMutationsFailClosed` already hit the shared loader; validator-side incomplete pre-review pin is DW (Groups I–K).
- low: `V3SuccessorMutationsFailClosed` is a thinner catalog than v2 — already rejected in Group B as a large new theory set; the image-extract gap is tracked as a patch above.
- low: `HistoricalV1EvidencePassesWithoutAuthorizingCurrentSource` does not mutate current source — v1 historical does not hash current source; v2 historical already appends `docs/ci.md`.
- low: `RunValidator` defaults `--git-root` to the live repository — forcing `CreateGitFixture` on every theory is extra complexity; `HEAD == LandedSource` is not everyday CI.
- low: `"unreadable"` creates a directory instead of chmod — already rejected in Group B; the case still fail-closes and redacts.
- low: Support CTRF record count is hardcoded `33` — matches `SUPPORT_CASE_TOTAL`; drift fails the theory loudly.
- low: `CreateSymbolicLinkOrSkip` and Windows `Assert.Skip` on Git timeout/flood — Group B applied those skips; required CI is Linux/WSL and does not skip.
- low: `CopyDirectory` `File.Copy`s directory reparse points — checked-in evidence trees have no directory symlinks; the copy would throw, not green-wash.
- low: `Process.Kill` only catches `InvalidOperationException` — Group B already bounded kill; extra Win32/NotSupported paths are not everyday on Linux CI.
- low: `CreateFixture` leak on mid-copy throw — already rejected in Group B.
- low: Full `git clone --shared` per Git case and `Directory.Delete` without ignore — not an everyday functional miss; adding chmod/`IDisposable` is extra harness complexity.

#### Actions (2026-09-10 Group B re-review)

- Owner chose option 2 (leave as action items). The ten patches stay unchecked in this section; they were not applied.
- Sprint tracking stays `review`; spec frontmatter stays `done` (DW-497 / OQ8 lifecycle gate). Do not flip Story 4.15 to `in-progress` or `done` from this review.

### Review Findings

_Chunked code review, Group M (2026-09-10). Target: v1 frozen packet, `699ca712` → `62b2a630`. Files: `4-8-eventstore-oq8-platform-evidence.yaml` plus `evidence/story-4-15/5e8f175b2ced4715f7c6f765386812cc1001dbb4/` (13 files, +992/−30). Layers: Blind Hunter, Edge Case Hunter (empty — treated as failed), Verification Gap, Acceptance Auditor._

- [ ] [Review][Decision] The sealed test receipt attests workflow and Tenants reruns that the bound execution log does not contain — `reviews/test.json` finding 4 says “fresh review reruns passed … workflow guards, and Tenants skip and token guards.” `pre-review-execution.json` records 32 commands / 237 tests and has no `ReleasePackageManifestTests` or Tenants command. The subject binds `ReleasePackageManifestTests.cs` as `workflowGuardrailTests`, but that is identity, not a recorded run. Editing the receipt changes its SHA-256 and the v1 `closureFiles` / handoff receipt map; that needs a fresh Murat receipt, not a mechanical hash update.
- [x] [Review][Defer] Bound-path `currentVerification` is JSON-pinned, not Git-checked against HEAD — deferred: accepted DW-496 split. `source-artifact-identity.json` `currentVerification.source` is `"current HEAD Git tree"` and receipts claim 24-path HEAD/worktree equivalence, but `validate_source_state` hashes `LANDED_SOURCE` / `COMPLETED_V1_CLOSURE_COMMIT` and never calls `git_diff_is_clean`. The packet records no `reviewedAtCommit`. Keep v1 historical; live proof stays on the reduced v3 gate-input set. [`source-artifact-identity.json:42-72`; `tools/validate-oq8-platform-evidence.py:1992-2025,910`]
- [x] [Review][Defer] Sealing validator bytes are not the landed-commit blob — deferred: `validator-sha256.txt` / `PRIOR_VALIDATOR_SHA256` are `96520190…`; `git show 5e8f175b:tools/validate-oq8-platform-evidence.py` is `585e4d86…`. `landedGitByteOverrides` and `closureEvolvedPaths` already state that split. `sourcePathRule` orders validator-then-landed-source; reminting v1 limitations/handoff to repeat it is the DW-496 “keep v1 historical” path. Live consumer instructions are the v3 handoff. [`validator-sha256.txt`; `source-artifact-identity.json:74-76,129-133`; `source-only-handoff.json:12-17`]
- [x] [Review][Defer] The tests that execute this packet are not a required merge check — deferred: not caused by these 13 files; already the Group D ruleset decision. `ApprovedSourceOnlyHandoffPasses` lives in `ci / contracts`, which is not in the Protect required set; `live-sidecar` runs the validator only with `--capture-directory`.

#### Rejected (2026-09-10 Group M)

- false: `finalValidation.status` `not-run-pre-review` contradicts `platformClosure.status: complete` — the execution artifact is scoped `receipt-independent-isolated-candidate` and its `reason` states the class-level packet tests require the three receipts. Those receipts cannot hash a log of the run that requires them. Outer `complete` is the post-receipt authority object; the live gate is `ApprovedSourceOnlyHandoffPasses` (outside this chunk).
- false: `source-artifact-identity.json` needs a full `landedGitPaths` map — `validate_source_state` uses capture SHA-256 as landed SHA-256 unless an override exists. Unmodified `deploy/dapr/resiliency.yaml` is `115bdb0d…` at `5e8f175b`. The five `landedGitByteOverrides` are the complete difference set.
- false: `limitations.json` conceals capture-versus-landed byte gaps for `DaprTestContainerFixture.cs`, `PublicationRecoveryActivationTests.cs`, and `Program.cs` — the same frozen subject binds `landedGitByteOverrides` with those reasons; architecture/security/test receipts already attest the five overrides. “Same 26 paths” is the path set, not a byte-identity claim.
- false: OQ8-8 must name `test-results.json` and `observations.json` — it binds the seven-file manifest `evidence-sha256.txt` (`02c3f507…`). `observations.json` is already on OQ8-2–OQ8-6; `verification` binds focused-production counts; `capture-packet-v1.json` `evidenceFiles` lists `test-results.json`.
- false: The packet must map 4.14 pending Murat/Security records onto 4.15 receipts — spec Never rewrite pending-review history; `validate_capture` pins those two records at `performed: false`. The 4.15 `reviews/{test,security}.json` receipts are the named decisions. A discharge table would mutate 4.14 or duplicate owner/role already on both sides.
- false: `source-only-handoff.json` omits design digest, landed tree, validator digest, and `git checkout 5e8f175b` — `landedSourceCommit` and `reviewSubjectSha256` are on the handoff; design `1.0.0` / `1a55b030…`, tree `96fdfbba…`, and validator `96520190…` live on the hashed subject. `EXPECTED_CONSUMER_INSTRUCTIONS` pins the five-field instruction object.
- false: `closure-crosswalk.json` must add a 4.15 `storyEvidence` row and lift tree/receipts onto `platformClosure` — `storyEvidence` is the 4.9–4.14 child chain; 4.15 is the closing `story` field. Receipts are `closureFiles`; tree is in bound identity. `validate_platform_closure` pins the outer field set; adding keys fail-closes without a validator remint (outside this chunk).
- false: Nested `capture.story415Status: backlog` beside `platformClosure.status: complete` is an unlabeled snapshot, and the packet should be renamed off `.yaml` — `load_json` reads the historical 4.8 path; the validator requires the inner object to stay byte-identical to `capture-packet-v1.json` including `story415Status: backlog`. Status is `platformClosure.status`.
- low: Date-only `2026-08-27` stamps cannot reconstruct execution → subject → receipt order — later successors use UTC seconds; adding them here reseals the v1 packet for forensic ordering that no everyday consumer needs.

#### Actions (2026-09-10 Group M)

- Decision item below. Sprint tracking stays `review`; spec frontmatter stays `done` (DW-497 / OQ8 lifecycle gate). Do not flip Story 4.15 to `in-progress` or `done` from this review.
