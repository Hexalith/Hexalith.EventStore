diff --git a/src/Hexalith.EventStore.Admin.UI/Components/StateInspectorModal.razor b/src/Hexalith.EventStore.Admin.UI/Components/StateInspectorModal.razor
index 865ad7bf..953e5b37 100644
--- a/src/Hexalith.EventStore.Admin.UI/Components/StateInspectorModal.razor
+++ b/src/Hexalith.EventStore.Admin.UI/Components/StateInspectorModal.razor
@@ -10,7 +10,7 @@
               aria-label="Aggregate state inspector"
               OnStateChange="OnDialogStateChangeAsync">
     <FluentDialogBody>
-        <TitleTemplate>
+        <div class="state-inspector-modal-body" style="min-width: 500px; max-width: 800px; max-height: 70vh; overflow: auto;">
             <FluentStack Orientation="Orientation.Horizontal" VerticalAlignment="VerticalAlignment.Center" HorizontalGap="8">
                 <h3 style="margin: 0; flex: 1;">State Inspector</h3>
                 <FluentButton Appearance="ButtonAppearance.Transparent"
@@ -20,120 +20,117 @@
                     &#x2716;
                 </FluentButton>
             </FluentStack>
-        </TitleTemplate>
-        <ChildContent>
-            <div class="state-inspector-modal-body" style="min-width: 500px; max-width: 800px; max-height: 70vh; overflow: auto;">
-                <FluentStack Orientation="Orientation.Vertical" VerticalGap="12">
-                    <div class="stream-identity"
-                         style="font-size: 12px; color: var(--colorNeutralForeground3); padding: 4px 0; border-bottom: 1px solid var(--colorNeutralStroke2);"
-                         aria-label="Stream identity">
-                        <span class="monospace"><strong>Tenant:</strong> @TenantId</span>
-                        <span style="margin: 0 8px;">·</span>
-                        <span class="monospace"><strong>Domain:</strong> @Domain</span>
+
+            <FluentStack Orientation="Orientation.Vertical" VerticalGap="12">
+                <div class="stream-identity"
+                     style="font-size: 12px; color: var(--colorNeutralForeground3); padding: 4px 0; border-bottom: 1px solid var(--colorNeutralStroke2);"
+                     aria-label="Stream identity">
+                    <span class="monospace"><strong>Tenant:</strong> @TenantId</span>
+                    <span style="margin: 0 8px;">·</span>
+                    <span class="monospace"><strong>Domain:</strong> @Domain</span>
+                    <span style="margin: 0 8px;">·</span>
+                    <span class="monospace"><strong>Aggregate:</strong> @AggregateId</span>
+                    @if (_useTimestampMode)
+                    {
                         <span style="margin: 0 8px;">·</span>
-                        <span class="monospace"><strong>Aggregate:</strong> @AggregateId</span>
-                        @if (_useTimestampMode)
+                        <span class="monospace"><strong>Mode:</strong> timestamp</span>
+                        @if (!string.IsNullOrWhiteSpace(_timestampInput))
                         {
                             <span style="margin: 0 8px;">·</span>
-                            <span class="monospace"><strong>Mode:</strong> timestamp</span>
-                            @if (!string.IsNullOrWhiteSpace(_timestampInput))
-                            {
-                                <span style="margin: 0 8px;">·</span>
-                                <span class="monospace"><strong>At:</strong> @_timestampInput</span>
-                            }
+                            <span class="monospace"><strong>At:</strong> @_timestampInput</span>
                         }
-                        else if (_sequenceInput.HasValue)
-                        {
-                            <span style="margin: 0 8px;">·</span>
-                            <span class="monospace"><strong>Sequence:</strong> #@_sequenceInput.Value</span>
-                        }
-                    </div>
-
-                    <FluentStack Orientation="Orientation.Horizontal" HorizontalGap="8" VerticalAlignment="VerticalAlignment.Center">
-                        <span style="font-weight: 600;">Mode:</span>
-                        <FluentSwitch @bind-Value="_useTimestampMode"
-                                      Label="By Timestamp"
-                                      aria-label="Toggle between sequence and timestamp mode" />
-                    </FluentStack>
-
-                    @if (!_useTimestampMode)
-                    {
-                        <FluentStack Orientation="Orientation.Horizontal" HorizontalGap="8" VerticalAlignment="VerticalAlignment.Center">
-                            <FluentButton Appearance="ButtonAppearance.Outline"
-                                          OnClick="DecrementSequence"
-                                          Disabled="@(_sequenceInput is null or <= 0 || _fetching)"
-                                          aria-label="Previous sequence"
-                                          Title="Previous position">
-                                -1
-                            </FluentButton>
-                            <NumericInput TValue="long" @bind-Value="_sequenceInput"
-                                          Min="0"
-                                          Label="Sequence Number"
-                                          aria-label="Sequence number to inspect"
-                                          Style="flex: 1;"
-                                          Autofocus="true" />
-                            <FluentButton Appearance="ButtonAppearance.Outline"
-                                          OnClick="IncrementSequence"
-                                          Disabled="@_fetching"
-                                          aria-label="Next sequence"
-                                          Title="Next position">
-                                +1
-                            </FluentButton>
-                        </FluentStack>
                     }
-                    else
+                    else if (_sequenceInput.HasValue)
                     {
-                        <FluentTextInput @bind-Value="_timestampInput"
-                                         Label="Timestamp (ISO 8601)"
-                                         Placeholder="2026-03-23T12:00:00Z"
-                                         aria-label="ISO 8601 timestamp to inspect"
-                                         Title="Enter ISO 8601 format: YYYY-MM-DDThh:mm:ssZ" />
-                        <span style="font-size: 12px; color: var(--colorNeutralForeground3);">
-                            Current time: @DateTimeOffset.UtcNow.ToString("yyyy-MM-ddTHH:mm:ssZ")
-                        </span>
-                        @if (_timestampError is not null)
-                        {
-                            <span style="color: var(--hexalith-status-error); font-size: 13px;" role="alert">@_timestampError</span>
-                        }
+                        <span style="margin: 0 8px;">·</span>
+                        <span class="monospace"><strong>Sequence:</strong> #@_sequenceInput.Value</span>
                     }
+                </div>
 
-                    <FluentButton Appearance="ButtonAppearance.Primary"
-                                  OnClick="FetchState"
-                                  Disabled="@_fetching"
-                                  Style="align-self: flex-start;">
-                        @(_fetching ? "Loading..." : "Inspect")
-                    </FluentButton>
+                <FluentStack Orientation="Orientation.Horizontal" HorizontalGap="8" VerticalAlignment="VerticalAlignment.Center">
+                    <span style="font-weight: 600;">Mode:</span>
+                    <FluentSwitch @bind-Value="_useTimestampMode"
+                                  Label="By Timestamp"
+                                  aria-label="Toggle between sequence and timestamp mode" />
+                </FluentStack>
 
-                    @if (_fetching)
-                    {
-                        <SkeletonCard />
-                    }
-                    else if (_fetched && _snapshot is not null)
-                    {
-                        <div>
-                            <h4 style="margin: 8px 0;">State at Sequence #@_snapshot.SequenceNumber</h4>
-                            <span class="monospace" style="font-size: 12px; color: var(--colorNeutralForeground3);">
-                                @_snapshot.Timestamp.ToString("yyyy-MM-dd HH:mm:ss.fff zzz")
-                            </span>
-                            <JsonViewer Json="@_snapshot.StateJson"
-                                        AriaLabel="@($"Aggregate state at sequence {_snapshot.SequenceNumber}")" />
-                        </div>
-                    }
-                    else if (_fetched && _fetchError is not null)
-                    {
-                        <div class="state-inspector-error" role="alert"
-                             style="color: var(--hexalith-status-error); padding: 8px;">
-                            @_fetchError
-                        </div>
-                    }
-                    else if (_fetched && _snapshot is null)
+                @if (!_useTimestampMode)
+                {
+                    <FluentStack Orientation="Orientation.Horizontal" HorizontalGap="8" VerticalAlignment="VerticalAlignment.Center">
+                        <FluentButton Appearance="ButtonAppearance.Outline"
+                                      OnClick="DecrementSequence"
+                                      Disabled="@(_sequenceInput is null or <= 0 || _fetching)"
+                                      aria-label="Previous sequence"
+                                      Title="Previous position">
+                            -1
+                        </FluentButton>
+                        <NumericInput TValue="long" @bind-Value="_sequenceInput"
+                                      Min="0"
+                                      Label="Sequence Number"
+                                      aria-label="Sequence number to inspect"
+                                      Style="flex: 1;"
+                                      Autofocus="true" />
+                        <FluentButton Appearance="ButtonAppearance.Outline"
+                                      OnClick="IncrementSequence"
+                                      Disabled="@_fetching"
+                                      aria-label="Next sequence"
+                                      Title="Next position">
+                            +1
+                        </FluentButton>
+                    </FluentStack>
+                }
+                else
+                {
+                    <FluentTextInput @bind-Value="_timestampInput"
+                                     Label="Timestamp (ISO 8601)"
+                                     Placeholder="2026-03-23T12:00:00Z"
+                                     aria-label="ISO 8601 timestamp to inspect"
+                                     Title="Enter ISO 8601 format: YYYY-MM-DDThh:mm:ssZ" />
+                    <span style="font-size: 12px; color: var(--colorNeutralForeground3);">
+                        Current time: @DateTimeOffset.UtcNow.ToString("yyyy-MM-ddTHH:mm:ssZ")
+                    </span>
+                    @if (_timestampError is not null)
                     {
-                        <EmptyState Title="No state available at this position"
-                                    Description="@(_capExceeded ? "Timestamp too far in history — use sequence number instead." : "The aggregate has no state at the requested position.")" />
+                        <span style="color: var(--hexalith-status-error); font-size: 13px;" role="alert">@_timestampError</span>
                     }
-                </FluentStack>
-            </div>
-        </ChildContent>
+                }
+
+                <FluentButton Appearance="ButtonAppearance.Primary"
+                              OnClick="FetchState"
+                              Disabled="@_fetching"
+                              Style="align-self: flex-start;">
+                    @(_fetching ? "Loading..." : "Inspect")
+                </FluentButton>
+
+                @if (_fetching)
+                {
+                    <SkeletonCard />
+                }
+                else if (_fetched && _snapshot is not null)
+                {
+                    <div>
+                        <h4 style="margin: 8px 0;">State at Sequence #@_snapshot.SequenceNumber</h4>
+                        <span class="monospace" style="font-size: 12px; color: var(--colorNeutralForeground3);">
+                            @_snapshot.Timestamp.ToString("yyyy-MM-dd HH:mm:ss.fff zzz")
+                        </span>
+                        <JsonViewer Json="@_snapshot.StateJson"
+                                    AriaLabel="@($"Aggregate state at sequence {_snapshot.SequenceNumber}")" />
+                    </div>
+                }
+                else if (_fetched && _fetchError is not null)
+                {
+                    <div class="state-inspector-error" role="alert"
+                         style="color: var(--hexalith-status-error); padding: 8px;">
+                        @_fetchError
+                    </div>
+                }
+                else if (_fetched && _snapshot is null)
+                {
+                    <EmptyState Title="No state available at this position"
+                                Description="@(_capExceeded ? "Timestamp too far in history — use sequence number instead." : "The aggregate has no state at the requested position.")" />
+                }
+            </FluentStack>
+        </div>
     </FluentDialogBody>
 </FluentDialog>
 
diff --git a/tests/Hexalith.EventStore.Admin.UI.Tests/Components/StateInspectorModalTests.cs b/tests/Hexalith.EventStore.Admin.UI.Tests/Components/StateInspectorModalTests.cs
index 4e90a1a5..e49cad3c 100644
--- a/tests/Hexalith.EventStore.Admin.UI.Tests/Components/StateInspectorModalTests.cs
+++ b/tests/Hexalith.EventStore.Admin.UI.Tests/Components/StateInspectorModalTests.cs
@@ -39,6 +39,19 @@ public class StateInspectorModalTests : AdminUITestContext {
         markup.ShouldContain("Sequence Number");
     }
 
+    [Fact]
+    public void StateInspectorModal_DialogBodyUsesFlatV5Content() {
+        // Arrange & Act
+        IRenderedComponent<StateInspectorModal> cut = RenderInspector(42L);
+
+        // Assert
+        string markup = cut.Markup;
+        markup.ShouldContain("fluent-dialog-body");
+        markup.ShouldContain("state-inspector-modal-body");
+        markup.ShouldNotContain("<titletemplate");
+        markup.ShouldNotContain("<childcontent");
+    }
+
     [Fact]
     public void StateInspectorModal_FetchesStateOnSubmit() {
         // Arrange
diff --git a/tests/Hexalith.EventStore.Admin.UI.Tests/Layout/Dw5SidebarShortcutAtddTests.cs b/tests/Hexalith.EventStore.Admin.UI.Tests/Layout/Dw5SidebarShortcutAtddTests.cs
index 3671d0f5..3e2ad594 100644
--- a/tests/Hexalith.EventStore.Admin.UI.Tests/Layout/Dw5SidebarShortcutAtddTests.cs
+++ b/tests/Hexalith.EventStore.Admin.UI.Tests/Layout/Dw5SidebarShortcutAtddTests.cs
@@ -32,7 +32,7 @@ public class Dw5SidebarShortcutAtddTests : AdminUITestContext {
         // the new value to local storage via hexalithAdmin.setLocalStorage, and (c)
         // trigger a rerender on the Blazor synchronization context.
         // bUnit captures (a) and (b) deterministically; (c) is observable by asserting
-        // the layout markup width transitions between "220px" and "48px" without throwing
+        // the layout markup width transitions between "220px" and "140px" without throwing
         // InvalidOperationException("StateHasChanged was called outside of the renderer
         // synchronization context").
         IRenderedComponent<Hexalith.EventStore.Admin.UI.Layout.MainLayout> cut = Render<Hexalith.EventStore.Admin.UI.Layout.MainLayout>(
@@ -45,7 +45,7 @@ public class Dw5SidebarShortcutAtddTests : AdminUITestContext {
         await cut.InvokeAsync(() => cut.Instance.OnToggleSidebarShortcut());
 
         cut.WaitForAssertion(
-            () => cut.Markup.ShouldContain("48px"),
+            () => cut.Markup.ShouldContain("140px"),
             TimeSpan.FromSeconds(5));
     }
 
@@ -63,7 +63,7 @@ public class Dw5SidebarShortcutAtddTests : AdminUITestContext {
         int setCallsBefore = JSInterop.Invocations.Count(i => i.Identifier == "hexalithAdmin.setLocalStorage");
 
         await cut.InvokeAsync(() => cut.Instance.OnToggleSidebarShortcut());
-        cut.WaitForAssertion(() => cut.Markup.ShouldContain("48px"), TimeSpan.FromSeconds(5));
+        cut.WaitForAssertion(() => cut.Markup.ShouldContain("140px"), TimeSpan.FromSeconds(5));
 
         await cut.InvokeAsync(() => cut.Instance.OnToggleSidebarShortcut());
         cut.WaitForAssertion(() => cut.Markup.ShouldContain("220px"), TimeSpan.FromSeconds(5));
@@ -111,7 +111,7 @@ public class Dw5SidebarShortcutAtddTests : AdminUITestContext {
 
         // Capture initial collapsed state for the tier (compact/minimum default to collapsed
         // when no saved state exists; optimal/standard default to expanded).
-        bool initialCollapsed = cut.Markup.Contains("48px", StringComparison.Ordinal);
+        bool initialCollapsed = cut.Markup.Contains("140px", StringComparison.Ordinal);
         string expectedValue = (!initialCollapsed).ToString().ToLowerInvariant();
 
         int setCallsBefore = JSInterop.Invocations.Count(i => i.Identifier == "hexalithAdmin.setLocalStorage");
@@ -144,8 +144,8 @@ public class Dw5SidebarShortcutAtddTests : AdminUITestContext {
             parameters => parameters.Add(p => p.Body, builder => builder.AddContent(0, "Body")));
 
         cut.WaitForAssertion(
-            () => cut.Markup.ShouldContain("48px",
-                customMessage: $"DW5 AC#6: {tier} viewport ({viewportWidth}px) must collapse to 48px width when no saved state."),
+            () => cut.Markup.ShouldContain("140px",
+                customMessage: $"DW5 AC#6: {tier} viewport ({viewportWidth}px) must collapse to 140px width when no saved state."),
             TimeSpan.FromSeconds(5));
     }
 
diff --git a/tests/Hexalith.EventStore.Contracts.Tests/Queries/SubmitQueryResponseTests.cs b/tests/Hexalith.EventStore.Contracts.Tests/Queries/SubmitQueryResponseTests.cs
index da4847c7..ec7045f9 100644
--- a/tests/Hexalith.EventStore.Contracts.Tests/Queries/SubmitQueryResponseTests.cs
+++ b/tests/Hexalith.EventStore.Contracts.Tests/Queries/SubmitQueryResponseTests.cs
@@ -25,4 +25,26 @@ public class SubmitQueryResponseTests {
 
         response2.ShouldBe(response1);
     }
+
+    [Fact]
+    public void JsonRoundTrip_CamelCaseWireContract_PreservesFailureEnvelope() {
+        JsonSerializerOptions options = new(JsonSerializerDefaults.Web);
+        JsonElement payload = JsonDocument.Parse("{\"reason\":\"denied\"}").RootElement;
+        var response = new SubmitQueryResponse(
+            "corr-401",
+            payload,
+            Success: false,
+            ErrorMessage: "Forbidden");
+
+        string json = JsonSerializer.Serialize(response, options);
+        SubmitQueryResponse? roundTripped = JsonSerializer.Deserialize<SubmitQueryResponse>(json, options);
+
+        json.ShouldContain("\"correlationId\":\"corr-401\"");
+        json.ShouldContain("\"success\":false");
+        roundTripped.ShouldNotBeNull();
+        roundTripped.CorrelationId.ShouldBe("corr-401");
+        roundTripped.Success.ShouldBeFalse();
+        roundTripped.ErrorMessage.ShouldBe("Forbidden");
+        roundTripped.Payload.GetProperty("reason").GetString().ShouldBe("denied");
+    }
 }
