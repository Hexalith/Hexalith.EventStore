---
title: 'Story 5.3: Production Authentication Guards And Secret Stripping'
type: 'feature'
created: '2026-09-07'
status: 'in-progress'
review_loop_iteration: 1
followup_review_recommended: false
baseline_revision: '3c6a5e33f9fbaf8469047ba3de72f70ab4425e66'
baseline_commit: '3c6a5e33f9fbaf8469047ba3de72f70ab4425e66'
context:
  - '{project-root}/_bmad-output/implementation-artifacts/epic-5-context.md'
  - '{project-root}/references/Hexalith.AI.Tools/hexalith-ux-instructions.md'
warnings: [oversized]
deferred: []
---

<intent-contract>

## Intent

**Problem:** EventStore, Admin.Server, and the external sample API accept incomplete or unsafe non-Development JWT configuration, do not constrain accepted signing algorithms, and disagree about multi-audience configuration. Committed development settings, Keycloak users, fixtures, scripts, and documentation also publish reusable material capable of minting privileged tokens.

**Approach:** Establish one fail-closed JWT configuration contract across public hosts, wire AppHost run/publish models through it, generate local credentials per run, and enforce a repository-level tracked-content guard that permits inert placeholders but rejects forgeable credentials and signing material.

## Boundaries & Constraints

**Always:** Require exactly one of Authority or SigningKey; require non-blank issuer and at least one non-blank audience; validate issuer, all configured audiences, signature, expiration, lifetime, and an explicit algorithm allow-list; require an absolute authority without userinfo/query/fragment and HTTPS metadata outside Development; generate local signing keys and Keycloak passwords cryptographically per run; log an explicit support-safe warning when a permitted non-Production symmetric exception is active; keep real OIDC and probe behavior testable.

**Never:** Permit symmetric JWT validation in Production even when the legacy override is set; allow a non-Development HTTP authority or disabled HTTPS metadata; silently prefer Authority when SigningKey is also present; default issuer/audience, privileged subject, password, or administrator claims outside Development; commit usable keys, passwords, tokens, decoded JWT payloads, or privileged fixture credentials; expose generated secret values in logs/evidence; implement Story 5.2 authorization, Story 5.4 Admin hygiene/OpenAPI gating, Story 5.5 internal trust, general Story 5.6 topology work, Story 5.9 deployment alignment, Story 7.6 OpenBao retrieval, or modify `sprint-status.yaml`.

## I/O & Edge-Case Matrix

| Scenario | Input / State | Expected Output / Behavior | Error Handling |
|----------|---------------|---------------------------|----------------|
| Production OIDC | One absolute HTTPS Authority, HTTPS metadata, issuer, audiences, no key | Host starts and accepts only configured issuer/audience/algorithms | Invalid or incomplete configuration fails during startup before serving |
| Unsafe authority | Blank/relative/HTTP/userinfo/query/fragment Authority, or HTTPS metadata disabled outside Development | Configuration is rejected | Failure names the setting, never a secret value |
| Ambiguous mode | Authority and SigningKey both configured | Configuration is rejected | No precedence fallback |
| Symmetric mode | Development, or explicit non-Production exception, with a random 32-byte-or-stronger key | HS256 only; exception emits a redacted warning | Production and unapproved non-Development use fail startup |
| Token validation | Missing expiry/signature, wrong issuer/audience/key, or algorithm outside the mode allow-list | Authentication fails before protected work | Existing bounded/redacted 401 contract remains |
| Local topology | Keycloak enabled or disabled during `aspire run` | Per-run credentials are shared only through resource environment; no committed fallback | Missing runtime inputs fail closed without printing values |
| Repository guard | Tracked source/config/docs/scripts/fixtures contain real secrets versus documented placeholder syntax | Placeholders pass; usable material fails with path/line only | Generated/build/VCS content is excluded explicitly, never by broad test-folder exemption |

</intent-contract>

## Code Map

- `src/Hexalith.EventStore.ServiceDefaults/Authentication/` -- new shared options validation and token-parameter construction seam; authority mode consumes a nonempty explicitly configured `AllowedAlgorithms` collection containing only supported asymmetric algorithms and has no implicit production default, while Development/break-glass symmetric mode pins HS256.
- `src/Hexalith.EventStore.ServiceDefaults/Extensions.cs` -- attach explicit anonymous metadata to `/health`, `/alive`, and `/ready` while preserving the bounded non-Development response writer.
- `src/Hexalith.EventStore/Authentication/EventStoreAuthenticationOptions.cs` and `ConfigureJwtBearerOptions.cs` -- current gateway startup validation and bearer configuration; preserve its bounded challenge behavior while adopting the shared mode/audience/algorithm contract.
- `src/Hexalith.EventStore/Extensions/ServiceCollectionExtensions.cs` -- existing `ValidateOnStart` registration and named JWT options seam.
- `src/Hexalith.EventStore.Admin.Server.Host/Authentication/` and `Program.cs` -- duplicate Admin host options/configurer and startup registration to align with the same contract.
- `samples/Hexalith.EventStore.Sample.Api/Program.cs` -- inline JWT setup currently defaults production issuer/audience and validates too late; bind validated options at startup instead.
- `src/Hexalith.EventStore.Admin.UI/Services/AdminApiAccessTokenProvider.cs` and `samples/Hexalith.EventStore.Sample.BlazorUI/Services/EventStoreApiAccessTokenProvider.cs` -- authority-or-local token acquisition; constrain local minting to Development and validate token endpoint authority before network use.
- `src/Hexalith.EventStore.Aspire/HexalithEventStoreSecurityExtensions.cs` and `HexalithEventStoreJwtAuthenticationOptions.cs` -- reuse `WithEventStoreJwtAuthentication`; align emitted multi-audience keys with runtime binding and remove default client credentials.
- `src/Hexalith.EventStore.AppHost/Program.cs`, `KeycloakRealms/hexalith-realm.json`, and new AppHost-local credential/template helpers -- replace legacy auth wiring, deterministic bootstrap identity/passwords, and checked-in realm credentials with one per-run secret set shared by Keycloak, hosts, UIs, and Tenants bootstrap; create temporary secret-bearing directories/files with restrictive permissions before content is written and guarantee cleanup on render/setup failure and normal shutdown.
- `src/Hexalith.EventStore*/appsettings*.json` and `samples/**/appsettings*.json` -- remove fixed signing keys, passwords, privileged subjects/claims, and all-environment symmetric fallbacks; retain non-secret local defaults only.
- `tests/Hexalith.EventStore.Server.Tests/Authentication/`, `tests/Hexalith.EventStore.Admin.Server.Host.Tests/`, and `tests/Hexalith.EventStore.Sample.Tests/` -- startup matrix and real token validation evidence for every public host.
- `tests/Hexalith.EventStore.AppHost.Tests/Configuration/HexalithEventStoreJwtAuthenticationTests.cs` and integration Keycloak fixtures/tests -- assert run/publish environment models and acquire runtime-generated credentials without fixed literals.
- `tests/Hexalith.EventStore.Server.Tests/Security/SecretsProtectionTests.cs` -- replace broad exclusions with a Git-tracked reusable-content scan that covers workflows, root/build configuration, source, docs, deployment files, extensionless configuration, and credential-bearing file types; exemptions recognize only exact inert placeholder/source-expression forms rather than length, prefixes, or punctuation heuristics.
- `tests/**`, `perf/Hexalith.EventStore.LoadTests/`, and `scripts/generated-api-smoke-preflight.sh` -- replace reusable static keys/tokens/passwords with per-process/per-run generation while preserving deterministic assertions that do not depend on their values.
- `samples/dapr-components/postgresql/statestore.yaml` and authentication sections in `docs/getting-started/`, `docs/reference/`, `docs/guides/security-model.md`, `docs/guides/configuration-reference.md`, and `deploy/README.md` -- strip literal credentials and document environment/user-secret/ephemeral inputs plus the exact production guard.

## Tasks & Acceptance

**Execution:**
- [ ] `src/Hexalith.EventStore.ServiceDefaults/Authentication/`, `src/Hexalith.EventStore/Authentication/`, `src/Hexalith.EventStore.Admin.Server.Host/Authentication/`, and `samples/Hexalith.EventStore.Sample.Api/Program.cs` -- centralize and apply exclusive-mode, URI/HTTPS, issuer/audience, key-strength, lifetime/signature, and algorithm validation with startup failure in every public host. Authority mode must bind a nonempty explicit `Authentication:JwtBearer:AllowedAlgorithms` list, reject blank/unknown/symmetric entries, and never supply a production default; Development and permitted break-glass symmetric mode accept HS256 only.
- [ ] `src/Hexalith.EventStore.ServiceDefaults/Extensions.cs` and real-host tests -- attach explicit `AllowAnonymous` metadata (or a proven equivalent) independently to `/health`, `/alive`, and `/ready`, preserve any fail-closed fallback/default policy, and prove Production probes expose only bounded status while a representative Admin endpoint still challenges.
- [ ] `src/Hexalith.EventStore.Aspire/`, `src/Hexalith.EventStore.AppHost/Program.cs`, and `src/Hexalith.EventStore.AppHost/KeycloakRealms/hexalith-realm.json` -- use the mode-aware auth composition seam; propagate the configured primary plus additional audiences and explicit allowed algorithms; provision one per-run credential set; share the exact no-Keycloak signing key with integration issuers; give the sample UI the tenant-A identity rather than the administrator identity; reject whitespace-padded persistent mode; and remove default credentials/deterministic privileged principals. Publish output and documentation must consistently use an external HTTPS IdP, provide explicit non-secret OIDC/client parameter references to accepting hosts and UIs, and must not claim a run-only Keycloak resource is published.
- [ ] AppHost realm rendering -- create the parent/run directory as `0700` and the realm file atomically as `0600` before secret bytes become visible on Unix, clean partial output on every render/setup exception, dispose from the point rendering succeeds, and safely remove stale owned run directories without traversing unrelated paths.
- [ ] `src/Hexalith.EventStore.Admin.UI/Services/AdminApiAccessTokenProvider.cs`, `samples/Hexalith.EventStore.Sample.BlazorUI/Services/EventStoreApiAccessTokenProvider.cs`, and their registrations/tests -- forbid non-Development local token minting, validate configured authority endpoints, and require explicit privileged claims/credentials rather than production defaults.
- [ ] All tracked `appsettings*.json`, reusable test/perf helpers, Keycloak integration fixtures, `scripts/generated-api-smoke-preflight.sh`, and `samples/dapr-components/postgresql/statestore.yaml` -- replace usable literals with user-secret/environment inputs or cryptographically random per-run values propagated to both issuer and validator.
- [ ] `tests/Hexalith.EventStore.Server.Tests/Security/SecretsProtectionTests.cs`, `docs/getting-started/`, `docs/reference/`, `docs/guides/security-model.md`, `docs/guides/configuration-reference.md`, and `deploy/README.md` -- scan all relevant Git-tracked reusable text/configuration without broad directory, extension, test, value-length, prefix, punctuation, or assignment-name blind spots; recognize compact and decoded JWT material, private keys, connection strings, credentials/tokens, multiline assignments, and realm passwords; allow only exact inert placeholder/source-expression forms; report path/line without values; and synchronize commands with dynamically described local endpoints and explicit publish inputs.
- [ ] `tests/Hexalith.EventStore.Server.Tests/Authentication/`, `tests/Hexalith.EventStore.Admin.Server.Host.Tests/`, `tests/Hexalith.EventStore.AppHost.Tests/Configuration/`, `tests/Hexalith.EventStore.Sample.Tests/`, `tests/Hexalith.EventStore.Admin.UI.Tests/`, `tests/Hexalith.EventStore.IntegrationTests/`, and `scripts/tests/generated-api-smoke-preflight.test.sh` -- cover every matrix lane, production startup timing, explicit/missing/empty/invalid algorithm lists, token acceptance through additional audiences, every invalid validation dimension against authority-mode configuration for each public host, successful UI authority token acquisition and caching, complete local-token claims/signature, generated credential propagation, missing/weak smoke/load keys, rendered-template parsing, and retained real-Keycloak/probe behavior.

**Acceptance Criteria:**
- Given EventStore, Admin.Server, or the external sample API runs outside Development, when authentication configuration is missing, ambiguous, non-HTTPS, disables metadata HTTPS, or selects an unapproved mode, then startup fails before any endpoint serves and no secret value is disclosed.
- Given a valid OIDC configuration with a nonempty explicitly configured `Authentication:JwtBearer:AllowedAlgorithms` list, when signed, unsigned, expired, wrong-issuer, wrong-audience, wrong-family, non-allowlisted-algorithm, or no-algorithm tokens reach each public host, then only a currently valid token using a configured supported asymmetric algorithm authenticates, additional configured audiences work at token level, and every validation dimension remains enabled; missing/empty/blank/unknown/symmetric authority-mode lists fail before serving.
- Given Development or an explicitly approved non-Production symmetric exception, when the host validates tokens, then only HS256 with the per-run key succeeds; Production rejects the override and the exception produces a redacted audit warning.
- Given AppHost run and publish models are inspected, when auth environment is rendered, then every accepting host consumes the same configured primary/additional audience and algorithm contract, published UIs receive explicit external token-acquisition settings without literal credentials, publish mode contains only external HTTPS OIDC and does not promise a run-only Keycloak service, and run-mode credentials are runtime-generated rather than repository literals. The sample UI uses a tenant identity; the Admin UI alone uses the generated administrator identity.
- Given the local Keycloak realm is rendered, when permissions, write failures, model-construction failures, normal shutdown, and stale-owned-run cleanup are exercised, then no secret bytes are ever exposed through permissive Unix modes and no generated realm is left behind by an agent-controllable failure path.
- Given the tracked repository content is scanned, when root/build configuration, workflows, deployment/config files (including extensionless and multiline forms), realm JSON, source, tests, scripts, samples, performance fixtures, and authentication documentation are evaluated, then no forgeable administrator credential, usable signing/private key/token/password/connection string, compact or decoded JWT data remains. Short values and values beginning with code-like/test prefixes are not implicitly safe; only exact inert placeholders and structurally recognized nonliteral source expressions pass.
- Given `ServiceDefaults.MapDefaultEndpoints` maps `/health`, `/alive`, and `/ready`, when endpoint metadata and the real Admin Server Host Production pipeline are inspected, then all three probes carry explicit anonymous metadata, return their actual status with bounded status-only bodies, and a representative protected Admin endpoint challenges without weakening any fallback/default policy.
- Given all authentication hardening is applied, when focused projects, real-host startup/token tests, retained Keycloak E2E evidence where infrastructure is available, and the Release solution build run, then they pass without warnings or regressions to the anonymous health probes.

## Spec Change Log

### 2026-09-07 — Review repair 1

- Trigger: the first independent review proved that the draft replaced the authoritative Story 5.3 requirement for a nonempty explicitly configured authority-mode `AllowedAlgorithms` list with an implicit built-in list, under-specified the mandatory anonymous probe metadata/Production-pipeline proof, and left security-sensitive scanner, temporary-file, UI, and publish invariants too loose to prevent the observed implementation gaps.
- Amended: the code map, execution tasks, acceptance criteria, design notes, and verification contract now state the exact configured-algorithm, probe, audience, publish/UI, per-run credential propagation, protected realm lifecycle, comprehensive tracked-content scanning, and authority-mode evidence requirements. The `<intent-contract>` block is unchanged.
- Known-bad state avoided: no implicit production algorithm default; no heuristic secret bypass by path/extension/length/prefix/punctuation; no permissive or orphaned realm file; no independent local issuer/validator keys; no published UI without an authentication contract; no documentation that advertises a run-only Keycloak service or assumes the preferred port is always free.
- KEEP: retain one shared fail-closed JWT contract; exclusive authority/key modes; HTTPS and URI validation; Production symmetric rejection; HS256-only Development/break-glass with redacted warning; 60-second skew; per-run CSPRNG credentials and inert realm placeholders; non-Development local-minting prohibition; successful live Keycloak topology; removal of reusable literals; support-safe diagnostics; proof-packet compatibility adjustments; zero `sprint-status.yaml` changes.

## Review Triage Log

### 2026-09-07 — Review pass

- verdicts: 40 findings — high 18, medium 17, low 2, false 3, maybe-false 0
- findings:
  - `[medium]` `[patch]` No-Keycloak Aspire fixtures mint with a key different from the AppHost-generated validator key — The cited fixtures set only `EnableKeycloak=false`; re-derivation now requires propagating `TestJwtTokenGenerator.SigningKey` and restoring prior process state.
  - `[high]` `[patch]` Publish hardcodes the primary audience and ignores `Authentication:JwtBearer:Audience` — `Program.cs` fixes `PrimaryAudience` to the default while reading only additional audiences; re-derivation must propagate the configured primary value.
  - `[medium]` `[patch]` Documented publish commands omit the newly mandatory authority and issuer — The commands reach publish model construction without those values and fail before output; re-derivation must make the explicit inputs and examples consistent.
  - `[medium]` `[patch]` Docker documentation still promises a published Keycloak service although Keycloak is run-mode only — The `security` resource is guarded by `IsRunMode`; the documentation constraint now requires an external IdP and removes the contradictory generated-service claim.
  - `[high]` `[bad_spec]` Published Admin and sample UIs receive no production token-acquisition contract — The authority/client branch is not configured for either UI and local minting throws outside Development; the amended spec requires explicit external UI parameter references and successful authority-path tests.
  - `[high]` `[patch]` The sample UI receives the generated global-administrator credentials — `Program.cs` passes `localAdminUsername` and `localAdminPassword` to both UIs despite a tenant-A realm identity; re-derivation must use the least-privileged tenant identity for the sample.
  - `[high]` `[bad_spec]` Secret-bearing realm bytes are written before Unix permissions are restricted — `Directory.CreateDirectory` and `File.WriteAllText` precede both chmod calls; the amended spec requires 0700/0600 at creation before content becomes visible.
  - `[medium]` `[bad_spec]` Realm cleanup starts too late and misses model-construction or abrupt setup failures — Rendering occurs well before the sole `try/finally`; the amended lifecycle requirement moves ownership/cleanup to the render boundary and covers partial setup.
  - `[high]` `[bad_spec]` Authority mode has an implicit broad algorithm list instead of mandatory configured `AllowedAlgorithms` — The authoritative Epic 5.3 AC and NFR3 require a nonempty explicit asymmetric list with no production default; this triggered the spec repair loop.
  - `[high]` `[bad_spec]` The secret guard scans only selected roots and extensions — `.github`, build/config file types, and extensionless reusable configuration are outside `ShouldScan`; the amended scan contract enumerates these surfaces without broad exclusions.
  - `[high]` `[bad_spec]` The secret guard treats every value shorter than eight characters as inert — `IsUsableLiteral` returns false solely on length, allowing usable short credentials; the amended contract forbids length-based exemption.
  - `[high]` `[bad_spec]` Broad value-prefix and punctuation bypasses admit reusable secrets — Values starting with `Test`, `Authentication`, or `s_`, containing `(`, or starting `[` are unconditionally exempted; the amended contract permits only exact structural nonliteral forms.
  - `[high]` `[bad_spec]` Common token/credential/private-key/connection-string names and multiline assignments are not covered — The assignment regex is line-scoped and narrowly named; the amended scanner requirement covers these credential families and multiline forms.
  - `[high]` `[bad_spec]` Invalid-token evidence is symmetric-only rather than authority/OIDC-mode evidence for each public host — Existing Keycloak tests prove valid acquisition but not every required negative token dimension; the amended verification contract now requires authority-mode negative evidence per host.
  - `[medium]` `[patch]` The smoke script places the ephemeral HMAC key in the `openssl` argv — `openssl ... -hmac "${DEV_SIGNING_KEY}"` is observable in process listings; re-derivation must pass the key without exposing it in argv.
  - `[medium]` `[patch]` Local token documentation assumes preferred port 8180 even though AppHost selects the next free port — Multiple instructions hardcode 8180 while dynamic port allocation is intentional; examples must derive the endpoint from Aspire topology output.
  - `[medium]` `[patch]` Whitespace-padded `KeycloakPersistent=true` bypasses the AppHost rejection but is accepted downstream — The first check does not trim while `AddHexalithEventStoreSecurity` does; re-derivation must parse once or trim consistently.
  - `[medium]` `[bad_spec]` Resource setup exceptions after rendering can orphan the realm directory — `try/finally` begins only around `Build().RunAsync()`; this is the same under-specified realm-lifecycle root cause and is covered by the amendment.
  - `[medium]` `[bad_spec]` A write or permission failure inside rendering leaves a partial directory — `Render` has no exception cleanup after directory creation; the amended lifecycle contract requires deletion on every partial failure.
  - `[high]` `[bad_spec]` A permissive umask exposes realm content before post-write chmod — The file is created with ambient permissions and hardened later; the amended contract requires restrictive creation modes atomically.
  - `[medium]` `[patch]` A consumer can mutate the shared process-wide algorithm arrays through `ValidAlgorithms` — `TokenValidationParameters` receives the private static arrays by reference; re-derivation must assign a defensive copy.
  - `[low]` `[patch]` Admin UI local tokens preserve surrounding whitespace in issuer/audience while validators trim — `RequireConfiguration` returns the original string, so a plausible local setting yields rejection; trimming is a direct correction included in re-derivation.
  - `[low]` `[patch]` Sample UI local tokens preserve surrounding whitespace in issuer/audience while validators trim — The same direct mismatch exists in the sample provider and is included in re-derivation.
  - `[medium]` `[patch]` Smoke signing keys are not rejected when whitespace-only or shorter than 32 bytes — `mint_dev_jwt` tests only shell emptiness; re-derivation must validate nonblank UTF-8 length before signing.
  - `[medium]` `[patch]` Load-test signing keys are not rejected when blank or shorter than 32 bytes — The static initializer checks only null, allowing tokens the hardened host cannot accept; re-derivation must fail early with a support-safe message.
  - `[high]` `[patch]` AppHost publish does not honor the configured primary audience — Direct inspection confirms only the default primary plus `ValidAudiences` are propagated; this duplicates the earlier verified publish-audience defect.
  - `[high]` `[bad_spec]` The guard's all-tracked-content claim exceeds its selected roots/extensions — Direct inspection confirms relevant workflows/build/config types are skipped; this duplicates the scanner-scope defect addressed by the amendment.
  - `[high]` `[bad_spec]` Short and bracket-prefixed reusable secrets bypass the guard — `candidate.Length < 8` and `candidate[0] == '['` return nonviolations; the amended exact-placeholder contract removes both heuristics.
  - `[high]` `[bad_spec]` Decoded JWT payloads are not recognized by the tracked-content guard — Only compact JWTs, private-key blocks, assignments, connection passwords, and realm-password objects are scanned; the amended requirement explicitly includes decoded JWT claim material.
  - `[medium]` `[patch]` Additional configured audiences lack a token-level acceptance regression — Existing tests prove binding/emission only; re-derivation must validate a signed token whose audience matches only an additional entry.
  - `[high]` `[bad_spec]` Successful authority-mode acquisition and caching are untested in both UI token providers — Provider tests bypass the HTTP branch used outside Development; the amended verification contract requires endpoint, form, response, and cache assertions for both providers.
  - `[medium]` `[patch]` The sample local-token test checks only three JWT segments — It does not validate signature, algorithm, issuer, audience, subject, tenants, domains, or permissions; re-derivation must cryptographically validate and assert the claims.
  - `[high]` `[bad_spec]` The secret guard tests do not challenge its broad literal exemptions — A test-prefixed signing key demonstrably passes; the amended scanner contract and adversarial cases must remove that blind spot.
  - `[medium]` `[patch]` Missing smoke signing-key behavior is not tested — The shell test always exports a generated key, so the intended blocked-environment branch can regress unnoticed; re-derivation must add an unset-key subprocess case.
  - `[medium]` `[patch]` No-Keycloak protected activation uses an independently generated test key — Caller tracing confirms the AppHost receives no matching `LocalAuthentication__SigningKey`; this duplicates the first fixture-propagation defect.
  - `[medium]` `[patch]` Static realm configuration tests still search for literal `admin-user` after templating the username — `FindUser("admin-user")` cannot find `__HEXALITH_ADMIN_USERNAME__`; re-derivation must test the inert template or rendered realm deliberately.
  - `[false]` `[reject]` Workflow-level commit and final-result requirements are not demonstrated by the review diff — Review intentionally precedes commit/finalization, and those execution/conversation surfaces cannot be expected inside the implementation diff.
  - `[false]` `[reject]` The functional scope is an unjustified expansion of the slug alone — The slug resolves the repository's authoritative, fully specified Epic 5.3 story, so implementing those tracked requirements is the defensible functional reading.
  - `[false]` `[reject]` The broad operator-handoff interpretation requires `awaiting-operator` — No acceptance criterion requires an irreducibly human external action; live Keycloak evidence is agent-executable and passed, so the conditional handoff does not activate.
  - `[high]` `[bad_spec]` The diff's tests exercise a narrower surface than the authority/OIDC contract asserted by the story — The invalid-token suites use symmetric mode and the UI authority branches are untested; this matches the verified evidence gaps and the amendment now requires authority-mode proof.

## Design Notes

The shared contract belongs in ServiceDefaults because EventStore.Gateway, Admin.Server.Host, and the external sample API already depend on it. Host-specific wrappers may preserve existing option names, logging, and challenge behavior, but they must delegate security decisions to one implementation. Runtime audience binding must use a first-class `ValidAudiences` collection rather than environment keys targeting a `TokenValidationParameters` instance that the configurers replace. `AllowedAlgorithms` is a first-class configuration collection: authority mode requires a nonempty caller-supplied subset of supported asymmetric algorithms and never invents a default; symmetric modes ignore/reject asymmetric configuration and accept HS256 only. Assign defensive copies to framework option objects.

Local Keycloak realm content may retain non-secret claims needed for authorization tests, but password and subject/bootstrap identifiers are template inputs resolved once per AppHost run. The no-Keycloak development path uses the same generated HS256 key for token issuers and validators, including Aspire integration fixtures. Secret-bearing directories/files are created with their final restrictive modes before bytes are written and are owned by a cleanup scope that begins immediately after render. Repository scanning is Git-tracked-content and syntax aware: configuration key names, `${NAME}`, `{env:NAME}`, `<token>`, and deliberately malformed sentinels are inert only when they match exact recognized forms; broad path/extension exclusions, minimum-secret lengths, value-prefix shortcuts, punctuation shortcuts, and fixed “dev-only” secret exceptions are not.

Publish mode represents external production identity only. It propagates the configured primary/additional audiences and explicit algorithm list to accepting hosts, emits explicit external client settings for both UIs without embedding credential values, and documents those inputs on every publish command. Local run mode may use dynamic Keycloak ports and documentation must show how to discover the actual endpoint. All three default probes carry explicit anonymous metadata even if a fallback policy is later introduced; Production real-host evidence couples their bounded responses with a protected Admin negative control.

## Verification

**Commands:**
- `dotnet restore Hexalith.EventStore.slnx` -- restore succeeds.
- `dotnet build tests/Hexalith.EventStore.Server.Tests/Hexalith.EventStore.Server.Tests.csproj --configuration Release -m:1` and direct `-class` runs for authentication and secret protection -- shared validation, token, and repository guards pass.
- Direct ServiceDefaults/default-endpoint and real Admin Production-host tests -- `/health`, `/alive`, and `/ready` have explicit anonymous metadata and bounded output while the protected endpoint challenges.
- `dotnet build tests/Hexalith.EventStore.Admin.Server.Host.Tests/Hexalith.EventStore.Admin.Server.Host.Tests.csproj --configuration Release -m:1` and direct authentication/startup classes -- Admin startup matrix passes.
- `dotnet build tests/Hexalith.EventStore.AppHost.Tests/Hexalith.EventStore.AppHost.Tests.csproj --configuration Release -m:1` and direct auth-composition classes -- run/publish models contain no reusable credentials.
- `dotnet build tests/Hexalith.EventStore.Sample.Tests/Hexalith.EventStore.Sample.Tests.csproj --configuration Release -m:1` plus affected Admin UI and integration classes -- successful UI authority acquisition/caching, complete local-token validation, per-host authority-mode negatives, additional-audience acceptance, template rendering/cleanup, no-Keycloak shared-key behavior, and real OIDC fixtures pass.
- `bash scripts/tests/generated-api-smoke-preflight.test.sh` -- runtime-generated script credentials preserve smoke behavior.
- `dotnet build Hexalith.EventStore.slnx --configuration Release` -- zero warnings and errors.
- `git diff --check` -- no whitespace errors.
