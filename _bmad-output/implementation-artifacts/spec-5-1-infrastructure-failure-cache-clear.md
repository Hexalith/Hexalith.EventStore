---
title: 'Story 5.1: Infrastructure Failure Cache Clear'
type: 'bugfix'
created: '2026-09-05'
status: 'in-review'
route: 'dispatch'
review_loop_iteration: 7
baseline_commit: 'b43d64f906665e2bf3015eb2d3f16b771598d352'
context:
  - '{project-root}/_bmad-output/implementation-artifacts/epic-5-context.md'
---

<frozen-after-approval reason="human-owned intent — do not modify unless human renegotiates">

## Intent

**Problem:** Infrastructure and persistence-conflict rejection paths clear staged actor state on their successful paths, but remediation failures and the unconditional pending-count finalizer can still expose a later save to residual event, metadata, snapshot, publication, pipeline, or pending mutations. Existing tests prove calls and retryability rather than the committed end state.

**Approach:** Harden the `AggregateActor` failure boundary so every retry or rejection clears failed-attempt state before restaging or saving, preserves the earliest causal failure when remediation also fails, and never claims safe discard without durable evidence. Add deterministic fault-injection tests that stage concrete values and inspect committed state after each terminal path.

## Boundaries & Constraints

**Always:** Keep `AggregateActor` as the sole durable event-mutation coordinator; use clear-before-restage/save ordering; preserve support-safe redaction; retain retryable infrastructure/conflict semantics; apply `ConfigureAwait(false)` to every production await; verify exact committed keys and values after failures.

**Never:** Change retry counts, event identity, append fencing, idempotency rules, publication recovery, projection triggering, public contracts, packages, UI, topology, or dead-letter retry policy. Do not treat call counts, HTTP results, or advisory status as durable-state proof, and do not modify `references/` content or unrelated worktree changes.

## I/O & Edge-Case Matrix

| Scenario | Input / State | Expected Output / Behavior | Error Handling |
|----------|---------------|----------------------------|----------------|
| Infrastructure rejection | Concrete event, metadata, snapshot, or publication state is staged before a pre-terminal failure | Clear completes before rejection/cleanup state is staged and only the permitted cleanup is committed | Return a bounded, redacted rejection; retain transient retryability |
| Conflict retry | A failed attempt staged state and a concurrent winner changed durable state | Clear every failed attempt, rehydrate the winning state, and commit no stale attempt value | Exhaust only the configured budget |
| Conflict exhaustion | Final failed attempt contains staged values | Clear before cleanup/save; leave stream data unchanged and no terminal idempotency/publication residue | Return the existing redacted concurrency rejection |
| Advisory dead-letter failure | Dead-letter returns failure or throws after the primary failure | Continue only where the bounded contract permits; never restore discarded staged state | Preserve and classify the primary cause plus the advisory failure without payload disclosure |
| Remediation failure | Clear, checkpoint, cleanup, rejection save, or pending final save fails | Prevent a later save from silently committing residual state and report the actual durable consequence | Never claim staged state was discarded unless inspection proves it |

</frozen-after-approval>

## Code Map

- `src/Hexalith.EventStore.Server/Actors/AggregateActor.cs` -- every actor entry point that reads or writes state, every command-phase save, activation recovery, infrastructure/conflict cleanup, and pending-count finalization participate in the same cache-safety boundary.
- `src/Hexalith.EventStore.Server/Actors/ActorStateRemediationException.cs` -- support-safe secondary-failure classification; retain stage/type/discard facts without exposing exception messages.
- `src/Hexalith.EventStore.Server/Actors/IdempotencyChecker.cs` -- legacy-redirect persistence currently converts save failure to `Unavailable`; its caller must not let that conversion hide cached staged state.
- `src/Hexalith.EventStore.Server/Actors/UnpublishedPublicationIndex.cs` -- its committed entry count is the authoritative idle-activation count of commands still awaiting publication recovery.
- `src/Hexalith.EventStore.Server/Events/IDeadLetterPublisher.cs` -- cancellation contract is authoritative: requested cancellation propagates and is never converted into advisory publication failure.
- `src/Hexalith.EventStore.Server/Events/EventPersister.cs` -- stages write-once event keys and aggregate metadata without saving; reuse as the concrete leak seam and do not move commit ownership.
- `src/Hexalith.EventStore.Server/Events/SnapshotManager.cs` -- stages snapshots/removals; preserve advisory snapshot behavior.
- `src/Hexalith.EventStore.Server/Actors/ActorStateMachine.cs` -- stages checkpoints and pipeline cleanup without saving.
- `src/Hexalith.EventStore.Testing/Fakes/InMemoryStateManager.cs` -- models pending versus committed state; reuse its `CommittedState`, clear, and save semantics unchanged.
- `tests/Hexalith.EventStore.Server.Tests/Actors/AggregateActorTestHelper.cs` -- central actor construction seam for injecting a recording/faulting state manager.
- `tests/Hexalith.EventStore.Server.Tests/Actors/PublicationRecoveryActivationTests.cs` -- activation entry seam for proving pending-count reconciliation survives deactivation without widening production visibility.
- `tests/Hexalith.EventStore.Server.Tests/Actors/EventDrainRecoveryTests.cs` -- contains the existing stage→clear ordering pattern; reuse the pattern without folding publication-recovery behavior into scope.
- `tests/Hexalith.EventStore.Server.Tests/Actors/DeadLetterRoutingTests.cs` and `AggregateActorDomainResultTests.cs` -- preserve existing retryability, redaction, status, and dead-letter contracts; their call assertions are not sufficient completion evidence.

## Tasks & Acceptance

**Execution:**
- [x] `src/Hexalith.EventStore.Server/Actors/AggregateActor.cs` -- establish one fail-closed cache boundary for every state-reading or state-writing actor turn and every command-phase batch, including legacy migration/redirect, stale checkpoint handoff, initial and stale-slot Processing admission, manual snapshot, resume terminal/PublishFailed recovery, infrastructure/conflict cleanup, reminder retry/stamp/success/exhaustion, and finalization. Guard the whole atomic batch from its first state read or mutation through `SaveStateAsync`; if any intermediate state operation fails after an earlier mutation staged, complete non-cancelable discard/poison handling before returning, retrying, or saving any replacement batch. Classify pre- versus post-commit outcomes from fresh exact durable witnesses rather than booleans set before a save returns. Caller cancellation during any staged batch must complete state-safety cleanup and then propagate even when fresh inspection proves the batch committed; do not continue the incoming command after cancellation.
- [x] `src/Hexalith.EventStore.Server/Actors/AggregateActor.cs` -- reconcile `pending_command_count` after activation against the committed, normalized set of distinct publication-recovery owners; retry that reconciliation at the first later barrier if activation recovery degraded; and never admit/read state while reconciliation or cache discard remains unproved. Cache-unsafety, finalizer recovery, and general counter reconciliation are independent obligations: satisfying one must not clear another. When stale `Processing` admission reuses a proven excess slot, retain that ownership until replacement commit is proved; a pre-commit replacement failure must release/reconcile the orphan instead of resetting the finalizer guard and stranding it.
- [x] `src/Hexalith.EventStore.Server/Actors/AggregateActor.cs` and `UnpublishedPublicationIndex.cs` -- maintain the counter/index ownership invariant whenever first-pass, resume, handoff, or terminal cleanup adds/removes a recovery owner. `EventsStored` resume must derive the post-terminal count from the normalized post-removal owner set rather than blindly decrement; resumed `PublishFailed` must atomically persist any newly added owner's count. A stale handoff commits the normalized owner count atomically, including zero-event cleanup. Normalize by message ID while preferring the well-formed entry, remove all same-ID remnants with an owner, and apply capacity to well-formed distinct owners so malformed retained entries never consume capacity. Two well-formed entries for one message ID with different correlations are corrupt and must fail closed without silently selecting or pruning either recovery path. Bound activation work for arbitrarily large malformed persisted indexes: every malformed cleanup that performs state I/O consumes a finite activation budget and unprocessed entries remain durable. A failed pending-count read never persists a guessed zero/one over existing owners, and every reported backpressure count uses the same normalized `OwnerCount` authority as capacity and reconciliation.
- [x] `src/Hexalith.EventStore.Server/Actors/AggregateActor.cs` -- after ambiguous reminder saves, complete the correct post-commit side effects without resurrecting state or adding a redundant save: an observed successful drain cleanup still unregisters its reminder and writes terminal advisory status, while an already-observed retry increment proceeds without restaging the same record. Drain-success, retry, marker, and exhaustion cleanup each guard their entire staging batch; an outer failure path must clear a partial cleanup batch before staging or saving a retry.
- [x] `src/Hexalith.EventStore.Server/Actors/AggregateActor.cs` -- make every ambiguity inspector accept only the exact attempted durable postcondition. Manual snapshot success requires inspection of the exact snapshot staged by this call and only after a save was attempted; legacy redirect success requires the attempted target/digest; event-bearing stale handoff requires the complete expected drain record, exact owner, absent original pipeline, and normalized owner count. An absent pipeline without the remaining exact witnesses is ambiguous and poisons the activation. Convert `InvalidOperationException` to `ConcurrencyConflictException` only when it came from the actual state save, never from a preceding checkpoint, idempotency, index, counter, or other mutator.
- [x] `src/Hexalith.EventStore.Server/Actors/ActorStateRemediationException.cs` -- carry support-safe primary, remediation, discard-failure, and durable-observation classification without raw exception messages or ambiguous claims about later cached state. Track barrier discard separately from later recovery: a completed clear remains reported as a completed discard even if finalizer repair, reconciliation, or durable inspection subsequently fails. Keep remediation operation/type pairs internally consistent: a `Discard...` operation reports the clear failure type, while the original save/mutator remains the primary type and the discard-specific field independently agrees.
- [x] `src/Hexalith.EventStore.Server/Actors/IdempotencyChecker.cs` and its aggregate-actor caller -- preserve the existing legacy redirect result contract while ensuring `Unavailable` cannot retain an uncommitted redirect for another turn to save. The caller owns discard/poison handling for the entire legacy migration sequence, including a failure between staging the message key and removing the legacy key; after an ambiguous save, accept only the exact durable `Terminal` or `Recoverable` witness that matches the migrated command, or the exact redirect record/digest that matches the requested target.
- [x] `tests/Hexalith.EventStore.Server.Tests/Actors/FaultInjectingActorStateManager.cs` -- add a test-local wrapper over the real pending/committed model with ordered tracing, before-delegate and commit-then-throw faults, repeated/double failures, committed snapshots, concurrent-winner injection, and fresh-activation reuse of the same durable manager. It must fault state reads and individual mutators after an earlier mutation has staged, not only `SaveStateAsync`.
- [x] `tests/Hexalith.EventStore.Server.Tests/Actors/AggregateActorTestHelper.cs`, `AggregateActorInfrastructureFailureTests.cs`, `EventDrainRecoveryTests.cs`, `PublicationRecoveryActivationTests.cs`, `AggregateActorManualSnapshotTests.cs`, and `AggregateActorFencingTests.cs` -- prove all frozen matrix lanes plus initial/stale-slot admission and PublishFailed save ambiguity, terminal and recoverable migration, indexed/unindexed and zero-event handoff, exact redirect/snapshot/handoff witnesses, poison barriers on every concrete command/reconciliation/legacy-inspection/full-read/range-read/metadata/redirect/snapshot/reminder turn, repair-save double failures, nonempty-index activation reconciliation, reconciliation-save ambiguity, reminder-stamp ambiguity, deactivation recovery, activity/log fields, redaction, and exact durable end state. Add explicit pre-save intermediate-mutator failures for legacy migration, admission, resume terminal/PublishFailed, drain success, marker, and exhaustion; before-/after-commit stale-handoff saves for event and zero-event shapes including commit-then-cancel; pre-commit drain-retry repair; stale-slot pre-commit replacement cleanup; finalizer publication-owner read failure/repeat recovery; malformed-before-valid same-ID normalization/removal/capacity; conflicting-valid duplicate fail-closed behavior and bounded malformed activation work; concurrent replacement during stale-handoff, snapshot, and redirect inspection; broad-mutator `InvalidOperationException` classification; remediation child/process activity failures and exact operation/type fields; normalized refusal count; and metadata-read cancellation propagation.

**Acceptance Criteria:**
- Given concrete stream and auxiliary values are staged before rehydration, domain-service, or event-persistence failure, when rejection completes, then clear precedes every rejection checkpoint, cleanup, dead-letter outcome, or save and no staged stream value reaches durable state.
- Given any atomic actor batch has staged one mutation, when any later read, set, remove, checkpoint, idempotency, index, counter, or other intermediate operation fails before `SaveStateAsync`, then the partial batch is discarded or the activation is poisoned before any return, retry, replacement staging, or later save; wrapping only the final save is insufficient.
- Given one or more persistence conflicts, when the actor retries or exhausts its budget, then each attempt clears before rehydration/restaging and only winning or pre-existing durable values remain.
- Given clear, dead-letter, checkpoint, cleanup, rejection-save, or finalizer-save failure, when the command terminates, then the earliest causal failure and observed durable consequence remain support-safe and no unproved discard is reported.
- Given any terminal Story 5.1 lane, when state is inspected through a fresh view, then original event/metadata keys are unchanged, only permitted rejection cleanup is observable, and no snapshot, publication index, drain, trigger, idempotency, or pending-count residue leaked.
- Given a cache clear cannot prove discard, when the same activation receives any later state-bearing turn, then it performs no state mutation or save until a clean-cache barrier succeeds, so residual mutations cannot hitchhike onto later work.
- Given a cache clear cannot prove discard, when any later state read or reconciliation runs on that activation, then it crosses the same barrier before consulting actor state and cannot expose a pending failed-attempt value as durable.
- Given the entry barrier clears the cache and a later finalizer or counter recovery step fails, when remediation is reported, then discard remains `true` and the later operation/observation failure is classified separately.
- Given initial Processing admission, stale-Processing takeover, or PublishFailed recovery save fails before commit or commits and then throws, when the command unwinds, then fresh inspection decides whether this command owns a durable pending slot or publication recovery; another command's slot is never decremented and no failed batch remains cached.
- Given stale-Processing takeover reuses a durable excess slot, when replacement admission fails before commit, then the old slot remains owned through unwinding and is finalized or reconciled exactly once; it is never orphaned by assigning the admission-inspection result back over the pre-existing ownership fact.
- Given legacy migration/redirect, stale-checkpoint handoff, or manual snapshot persistence fails, when the bounded result or exception returns, then a non-cancelable discard has completed or the activation is poisoned; an observed post-commit value is classified without being mistaken for cached residue.
- Given a manual snapshot or legacy redirect operation throws, when fresh durable inspection considers success, then inspection runs only after that call attempted a save and compares the exact snapshot value or requested redirect target/digest; sequence-only or source-only matches never become false success.
- Given a multi-operation batch throws `InvalidOperationException`, when failure is classified, then only an exception thrown by the actual `SaveStateAsync` call can enter concurrency-conflict semantics; an earlier state mutator retains its original infrastructure/remediation classification.
- Given stale-handoff save inspection observes a pipeline key, when deciding whether the original batch committed, then only the exact original checkpoint is a pre-commit witness; a concurrent replacement is ambiguous and cannot be classified from key presence alone.
- Given stale-handoff save inspection observes no original pipeline, when deciding whether the batch committed, then it accepts only the complete expected drain record, exact message/correlation owner, and normalized owner count; missing or different witnesses are ambiguous rather than pre-commit or committed.
- Given stale-handoff save commits and then throws caller cancellation, when exact durable inspection completes, then cancellation still propagates and the incoming command performs no subsequent state work; later activation recovery owns any post-commit reminder consequence.
- Given an ambiguous legacy-migration save, when fresh inspection observes the exact migrated record, then both `Terminal` and `Recoverable` dispositions resume their existing duplicate/recovery result instead of rethrowing a save failure that committed.
- Given stale-checkpoint handoff adds a missing publication-recovery owner or removes a zero-event checkpoint, when its batch commits, then `pending_command_count` equals the normalized distinct committed owner count in that same batch; it neither omits the new owner nor decrements an unrelated one.
- Given an indexed or unindexed `EventsStored` checkpoint resumes to terminal or `PublishFailed`, when its batch commits, then `pending_command_count` equals the normalized post-batch publication-owner count; terminal completion never blindly consumes another owner's slot and recovery creation never leaves its new owner uncounted.
- Given persisted publication-index data contains malformed and valid entries sharing a message ID, when it is normalized, capacity-checked, or removed, then the valid owner is preferred, counted once, and removed with all same-ID remnants; malformed non-owners neither consume owner capacity nor strand a pending slot.
- Given persisted publication-index data contains two well-formed entries sharing a message ID but disagreeing on correlation, when normalization or activation recovery reads it, then corruption fails closed without choosing the first entry or pruning the other recovery path.
- Given a persisted publication index contains more malformed entries than one activation's work budget, when activation recovery runs, then state-backed cleanup is bounded, the remainder stays durable for later recovery, and no unbounded idempotency-read/save loop occurs.
- Given a pending-count read fails during admission, when recovery owners may already exist, then the turn performs no counter/pipeline save from a guessed zero value and completes discard/poison handling before propagating or returning the existing bounded infrastructure outcome.
- Given a finalizer save commits and then throws or fails before commit, when remediation inspects durable pending state, then it neither double-decrements nor strands a recoverable pending slot and reports an unambiguous observed consequence.
- Given a finalizer failure is followed by actor deactivation, when the next activation completes recovery, then `pending_command_count` is reconciled to the committed publication index before any actor state API can proceed; activation degradation leaves a mandatory first-turn retry rather than silently losing the obligation.
- Given terminal index repair, finalizer repair, and general reconciliation are simultaneously pending, when one obligation succeeds, then only that obligation is cleared and every independent obligation still blocks state access until its own durable invariant is proved.
- Given a reminder save commits and then throws, when fresh inspection proves drain cleanup or retry/stamp state committed, then cleanup side effects still complete and already-committed state is not redundantly saved again.
- Given caller cancellation reaches conflict clearing or dead-letter publication, when the path terminates, then cancellation propagates after non-cancelable state-safety cleanup and is not reclassified as advisory or remediation failure.
- Given an actor metadata read is canceled, when `GetEventsAsync` unwinds, then the original `OperationCanceledException` propagates and is not wrapped as deserialization failure or mapped to HTTP 500.
- Given remediation or finalization diagnostics are emitted, when focused tests inspect activities and structured logs, then stage, operation, exception type, discard/observation fields, and redaction are exact and no raw secret appears.
- Given `ActorStateRemediationException` escapes a state-bearing turn, when its active process/child activities and structured log are inspected, then they are marked failed and a discard operation's remediation type identifies the clear failure rather than repeating the primary save/mutator type.
- Given publication-index refusal reports backpressure, when malformed retained entries coexist with valid owners, then the result and log expose normalized `OwnerCount`, exactly matching the authority used for capacity and durable pending-count reconciliation.
- Given implementation is complete, when focused tests, the full Server test project, and the Release solution build run, then all pass without warnings or regressions and every changed production await uses `ConfigureAwait(false)`.

## Implementation Notes

- 2026-09-05 -- Hardened conflict retry, infrastructure rejection, conflict exhaustion, and pending-command finalization around explicit clear/restage/save boundaries. Remediation failures now retain support-safe primary/remediation classifications, and throwing dead-letter publication remains advisory.
- Added `ActorStateRemediationException`, a test-local fault-injecting wrapper over `InMemoryStateManager`, 16 focused committed-state cases, and the narrow actor-helper injection seam. A finalizer-save failure intentionally leaves the previously committed pending count at `1`, clears its staged decrement, and logs the observed durable consequence so no later save can hitchhike it.
- Verification: focused infrastructure matrix 16/16 passed; full Server assembly 3,156 passed with 25 pre-existing skips and no failures/errors; Release solution build passed with 0 warnings/errors.
- The project-level `dotnet test --no-build` command discovered zero tests and exited 5 under Microsoft.Testing.Platform. The repository-prescribed direct built-assembly fallback ran the complete Server suite successfully.
- Concurrent commit `68d04065576d31490e14346ffd81fdaf182fe724` landed after the recorded baseline. Its unrelated Story 3.14 changes were preserved; its overwrite of the Story 5.1 sprint transition was reapplied.
- Review loop 1 reverted the Story 5.1 code/tests after finding that a finalizer-operation plus cleanup-clear double failure left cached mutations available to a later actor turn. The passing first-pass behavior remains evidence only; implementation is being re-derived from the strengthened design below.
- 2026-09-05 -- Review loop 1 reimplementation added the activation-local unsafe-cache barrier, bounded pending-finalizer recovery, pre/post-commit durable inspection, caller-cancellation cleanup, structured redacted diagnostics, and the complete conflict/finalizer fault matrix. The focused suite now contains 28 passing cases.
- Independent verification: restore passed; the Release Server test project built with 0 warnings/errors; the focused infrastructure matrix passed 28/28; the full Server assembly passed 3,168 tests with 25 pre-existing skips and no failures/errors; and the Release solution build passed with 0 warnings/errors.
- Concurrent commits `834985a273d396666f2c9cb5b67645b3461de163`, `89564e0c290f4bc32ac7ebdb7d33802ff6d5e9d5`, and `30336c2c` advanced the shared branch during implementation. Their committed spec/status, partial finalizer, Story 3.15, and submodule-pointer changes were preserved. Story 5.1 was re-applied and verified atop the resulting HEAD without staging, committing, or pushing.
- Review loop 2 reverted all six Story 5.1 code/test paths to baseline after finding unprotected pre-Processing saves, pre-return mutators, poisoned reads, pending-slot admission ambiguity, and activation-local-only counter recovery. The externally committed copies remain in Git history; the scoped working-tree rollback preserves that history and every unrelated change.
- 2026-09-05 -- Review loop 2 reimplementation extended the fail-closed boundary to state reads, pre-save staging, redirects, snapshots, reminders, initial admission, PublishFailed and terminal save ambiguity. Durable Processing identity/publication-index/terminal witnesses now distinguish pre- from post-commit outcomes, while activation reconciliation derives the idle pending count from the committed publication index.
- Verification: restore passed; the Release Server test project and Release solution built with 0 warnings/errors; the focused infrastructure matrix passed 48/48; activation recovery passed 39/39; state-machine integration passed 16/16; and the full Server assembly passed 3,191 tests with 25 pre-existing skips and no failures/errors. The production-await audit and `git diff --check` passed, and HEAD remained `c08cb3497768806d80a8e949d320cb28ccc40afc` throughout final verification.
- Independent completion audit found one reminder boundary not covered by the implementation handoff: both drain-exhaustion saves occurred before the reminder's existing catch/discard region. Added non-cancelable discard/poison handling and pre-/post-commit tests for both saves, plus explicit legacy-migration and stale-checkpoint handoff/cleanup ambiguity cases. Final independent verification passed: focused infrastructure matrix 58/58, activation recovery 39/39, state-machine integration 16/16, full Server assembly 3,201 tests with 25 pre-existing skips and no failures/errors, and Release solution build with 0 warnings/errors. The changed-production-await audit and `git diff --check` also passed at HEAD `c08cb3497768806d80a8e949d320cb28ccc40afc`.
- Review loop 3 reverted the Story 5.1 code/tests after finding that independent reconciliation obligations shared one boolean, handoff could create or release the wrong pending-slot ownership, a failed counter read could persist a guessed count, and ambiguous reminder/migration paths still had incomplete durable consequences. The 58-case focused behavior and root-audited drain-exhaustion boundary remain preservation evidence for re-derivation.
- 2026-09-05 -- Review loop 3 reimplementation separated cache-unsafety, finalizer-repair, and index/count-reconciliation obligations; normalized publication ownership; made stale handoff and terminal cleanup persist the exact owner count; inspected ambiguous event-batch and terminal saves for both `InvalidOperationException` and `IOException`; and completed reminder/migration durable consequences. The independent completion audit corrected two remaining seams: terminal pre-commit index-removal failure now preserves its owner and pending slot, and the owner-aware finalizer avoids redundant decrements/saves after terminal cleanup. Verification passed on the exact final source: restore; Release Server test build; focused infrastructure 59/59, state machine 16/16, domain result 31/31, fencing 15/15, event drain 55/55, publication activation 42/42, manual snapshot 3/3, publication index 22/22, publication integration 12/12, aggregate actor 5/5, dead-letter routing 15/15, and tracing 17/17; full Server assembly 3,195 passed with 25 pre-existing skips and no failures/errors; and Release solution build with 0 warnings/errors. The changed-production-await audit and current Story diff check also passed at HEAD `08f90f4bc143b657c712433a179667c88875aecf`; one unrelated fixed-baseline blank-at-EOF warning in the externally committed Story 3.15 artifact was preserved.
- Review loop 4 reverted all 19 Story 5.1 code/test paths exactly to baseline after verifying unguarded intermediate staging in migration, admission, resume, drain-success, and exhaustion batches, plus resume and malformed-index owner-accounting defects. The externally committed copies and unrelated work remain preserved in Git history and the shared worktree.
- 2026-09-05 -- Review loop 4 reimplementation guards each identified multi-operation batch from its first state mutation through save and durable inspection; derives terminal, resumed, and handoff pending counts from normalized publication owners; prefers and removes valid same-ID owners despite malformed remnants; uses exact stale-checkpoint witnesses; preserves proved barrier discard diagnostics; and propagates metadata-read cancellation. All explicitly named loop-4 regressions were added. Verification passed at the initial loop-4 handoff: focused affected actor suites 215/215; focused infrastructure matrix 74/74; full Server assembly 3,217 passed with 25 pre-existing skips and no failures/errors; Release solution build with 0 warnings/errors; restore and `git diff --check` passed. Aspire startup remained degraded by the existing unhealthy Keycloak/security dependency and was stopped cleanly; the deterministic verification gates were unaffected.
- 2026-09-05 -- Independent completion audit moved the resumed `EventsPublished` mutation into the guarded terminal batch and made raw publication-index removal eliminate every same-ID remnant. Mutation-sensitive regressions prove a failed resumed checkpoint cannot hitchhike on a later save and the former single-entry removal fails against a raw duplicate list. The Release Server test project built with 0 warnings/errors; the focused infrastructure suite passed 75/75; the focused publication-index suite passed 25/25; and `git diff --check` passed.
- 2026-09-05 -- Root verification found three legacy dead-letter/tracing fixtures whose substitute state managers could not expose fresh committed state to the new exact save-failure inspector. Those fixtures now use the same transactional fault-injecting manager as the completion-grade matrix while retaining their dead-letter count and correlation assertions. Final exact-source verification passed: restore; Release Server test build with 0 warnings/errors; infrastructure failures 75/75; adjacent actor/index suites 221/221; dead-letter/tracing suites 32/32; full Server assembly 3,218 passed with 25 pre-existing skips and no failures/errors; and Release solution build with 0 warnings/errors. The changed-production-await audit and current diff check passed, and the frozen Story patch remained byte-identical through verification (`19083d9306876d5a06af6d52ddbf59b6bb780740c27dfeef4dcd805fc260d6c8`) at shared-worktree HEAD `a511db66068b7359c775a158d79a7eaaa1441f57` on `docs/story-4-7-review-pass-5`; no staging, commit, push, or unrelated mutation was performed.
- Review loop 5 reverted all 19 Story 5.1 code/test paths exactly to the frozen baseline after finding that stale-slot admission could orphan a durable count, ambiguity inspectors accepted partial same-identity witnesses, broad exception catches changed mutator failures into conflicts, malformed recovery work was not bounded, and committed stale-handoff cancellation was suppressed. The fully green 3,218-test loop-4 behavior remains preservation evidence for the final re-derivation.
- 2026-09-05 -- Review loop 5 reimplementation preserves stale-slot ownership through failed replacement admission; requires exact snapshot, redirect, stale-pipeline, drain, publication-owner, and pending-count witnesses; bounds malformed activation state I/O; fails closed on conflicting persisted owners; maps concurrency only from an attempted save; propagates committed handoff cancellation; and keeps remediation activity/log operation-type fields exact. The independent completion audit added mutation-sensitive stale-slot and same-identity drain-winner regressions and corrected the legacy-redirect discard type. Verification passed on the exact final source: restore; Release Server test build with 0 warnings/errors; infrastructure failures 70/70; domain results 31/31; fencing 17/17; manual snapshots 4/4; activation recovery 43/43; drain recovery 55/55; publication index 25/25; aggregate actor 5/5; backpressure 13/13; dead-letter routing 15/15; publication integration 12/12; state-machine integration 16/16; tracing 17/17; full Server assembly 3,238 total with 25 pre-existing skips and no failures/errors; and Release solution build with 0 warnings/errors. The changed-production-await audit and `git diff --check` passed at shared-worktree HEAD `9eeaebe79c024a2bc6b8cb3e03a1573e4965e57c`; no staging, commit, push, or unrelated mutation was performed.

## Spec Change Log

- 2026-09-05 -- Review found that the generic remediation/finalizer task allowed an unproved double-clear failure to return without preventing a later save. Strengthened Tasks, Acceptance, and Design Notes with a poisoned-activation recovery barrier, commit-then-throw inspection, bounded pending-count reconciliation, explicit cancellation/activity/log contracts, and the missing conflict/finalizer fault matrix. Known-bad state avoided: residual failed-turn mutations hitchhiking on a later save, misleading discard claims, phantom backpressure, and swallowed cancellation. KEEP: clear-before-restage/save ordering; primary failure logged before remediation; support-safe type/stage classification; non-cancellation dead-letter failure remains advisory without custom retry; `InMemoryStateManager` remains the durable oracle; concrete staged-state, concurrent-winner, and one/multiple-conflict tests remain.
- 2026-09-05 -- Review loop 2 found that the strengthened boundary still began too late, guarded writes but not reads, inferred pending ownership from pre-save booleans, and stored finalizer recovery only in activation memory. Expanded the Code Map, Tasks, Acceptance, and Design Notes to cover every state API/save phase, fresh durable outcome inspection, poisoned read barriers, redirect/snapshot/handoff cleanup, and activation-time counter reconciliation against the committed publication index. Known-bad states avoided: decrementing another command's slot after a pre-commit admission failure, later saves committing failed migration/snapshot/drain batches, reads returning pending failed-attempt data, and deactivation making phantom backpressure permanent. KEEP: all prior change-log constraints; clear-before-restage/save; the support-safe exception and redacted logs/activities; caller-cancellation propagation after non-cancelable cleanup; successful conflict winner preservation; pre/post-commit fault injection; the 28-case loop-1 coverage as behavior to retain and expand; and no public-contract, retry-budget, event-identity, or dead-letter-policy changes.
- 2026-09-05 -- Review loop 3 found that the owner-count invariant remained underspecified across terminal finalization, stale handoff, duplicate persisted index entries, and fail-open counter reads, while reminder and migration ambiguity stopped short of their full durable consequence. Expanded Tasks, Acceptance, and Design Notes with independent recovery obligations, normalized distinct-owner accounting, atomic handoff count updates, no guessed counter persistence, exact `Recoverable` migration witnesses, completed drain-success side effects, and the missing concrete poison/activation/save-ambiguity tests. Known-bad states avoided: one successful recovery clearing another obligation, uncounted or wrongly decremented recovery owners, permanent stale `PublishFailed` polling, and redundant post-commit retry saves. KEEP: every constraint and preservation item in the first two change-log entries; the complete fail-closed entry barrier; exact Processing/publication/terminal witnesses; activation rearm then fresh reconciliation; cancellation cleanup; redacted structured diagnostics; root-audited drain-exhaustion cleanup; all 58 focused loop-2 cases as behavior to retain; and no public-contract, retry-budget, event-identity, publication-policy, or dead-letter-policy changes.
- 2026-09-05 -- Review loop 4 found that the implementation guarded several saves but not the full batches that staged state before them, and that resume and malformed-index paths still violated normalized publication-owner accounting. Expanded Tasks, Acceptance, and Design Notes so every batch is guarded from its first state operation through save/inspection; retry persistence must clear partial cleanup first; terminal and `PublishFailed` resume atomically derive the exact owner count; normalization prefers the valid same-ID owner, removes all same-ID remnants, and applies capacity to owners; stale-handoff inspection requires the exact checkpoint; barrier diagnostics retain a proved discard; and metadata-read cancellation propagates. Added explicit intermediate-mutator, stale-handoff ambiguity, retry-repair, finalizer owner-read, malformed-index, concurrent-replacement, and cancellation tests. Known-bad states avoided: partial legacy/admission/resume/drain batches hitchhiking on later saves, retry saves committing terminal cleanup, resume consuming another owner's slot or omitting a new owner, malformed entries blocking/stranding owners, and cancellation surfacing as HTTP 500. KEEP: every prior change-log constraint and positive preservation item; all 59 loop-3 infrastructure cases and the independently green focused/full verification behavior; exact event-batch and terminal pre/post-commit inspection for both concurrency and IO failures; separate recovery obligations; owner-aware finalization; completed reminder consequences; support-safe diagnostics; and no public-contract, retry-budget, event-identity, publication-policy, or dead-letter-policy changes.
- 2026-09-05 -- Review loop 5 found that the final derivation still conflated stale-slot ownership with replacement commit, accepted non-exact snapshot/redirect/handoff witnesses, mapped pre-save mutator `InvalidOperationException` values to conflicts, left malformed-index activation I/O unbounded, exposed raw entry counts, and suppressed caller cancellation after a committed stale handoff. Expanded Tasks, Acceptance, and Design Notes with independent stale-slot ownership, exact complete attempted postconditions, save-phase-only conflict mapping, fail-closed conflicting-valid duplicates, bounded malformed cleanup, normalized refusal counts, escaping-remediation activity/diagnostic consistency, and post-inspection cancellation propagation. Known-bad states avoided: live-activation phantom backpressure, false admin snapshot/redirect success, arming a concurrent replacement, retrying arbitrary mutator defects as conflicts, activation DoS from corrupt indexes, wrong recovery correlation selection, and continued command work after cancellation. KEEP: every prior change-log constraint and positive preservation item; the loop-4 clean-cache and full-batch guards; exact event/terminal and stale-pipeline witnesses; normalized owner counts and same-ID removal; independent recovery obligations; barrier discard truth; metadata cancellation propagation; all 75 infrastructure and 3,218 full-suite passing behaviors; the root-fixed resumed checkpoint boundary and stateful dead-letter fixtures; and no public-contract, retry-budget, event-identity, publication-policy, or dead-letter-policy changes.

## Review Triage Log

- blind-1 -- **medium / patch** -- `ClearCacheAsync(cancellationToken)` in the conflict-retry path catches caller-token cancellation and wraps it as remediation failure; this changes cancellation classification.
- blind-2 -- **medium / patch** -- `IDeadLetterPublisher` explicitly propagates `OperationCanceledException`, but the new caller catch converts every thrown cancellation into advisory failure.
- blind-3 -- **medium / patch** -- the finalizer now catches every `OperationCanceledException` whereas the replaced code propagated it; dependency cancellation would be hidden behind a warning.
- blind-4 -- **medium / bad_spec** -- a successful discard after a finalizer failure does not retry or reconcile the decrement, and the next backpressure read can indefinitely observe the committed count as inflated.
- blind-5 -- **medium / bad_spec** -- `StagedStateDiscarded` is computed before finalization; a later finalizer/discard double failure can leave new cached mutations while the propagated exception still reports discard success for the earlier batch.
- blind-6 -- **false / reject** -- the internal exception intentionally retains support-safe stage/type classification rather than raw exceptions; callers have no typed remediation retry contract, and retaining raw inner exceptions would undermine the redaction boundary.
- blind-7 -- **medium / patch** -- event-persistence remediation exceptions bypass the only catch that records the persistence activity failure, leaving that span without the error status/event even though a structured log is emitted.
- blind-8 -- **medium / bad_spec** -- the fault manager throws only before delegation, so no test models a state save that commits and then reports failure; the durable-consequence requirement needs both pre-commit and ambiguous post-commit faults.
- blind-9 -- **medium / patch** -- conflict-exhaustion clear, cleanup, and rejection-save remediation branches have no injected-failure coverage.
- blind-10 -- **medium / patch** -- finalizer clear/read/write and cleanup double-failure branches are untested; only the final save failure runs.
- blind-11 -- **medium / patch** -- none of the new structured remediation/dead-letter/finalizer diagnostic fields or redaction outcomes is asserted.
- blind-12 -- **high / defer** -- concurrent Story 3.14 work filters both real container-publish tests from CI and provides no automatic heavyweight lane, permitting OCI provenance regressions outside Story 5.1.
- blind-13 -- **low / defer** -- concurrent Story 3.14 ledger text says the malformed-input theory is heavyweight/excluded although the code and CI intentionally keep it in the default gate.
- blind-14 -- **medium / defer** -- the unrelated Windows governance binder scans a fixed 280-character substring rather than the guard body, allowing nearby text to satisfy it.
- blind-15 -- **low / defer** -- the concurrently edited Story 4.7 spec changes its historical `created` date from 2026-08-27 to 2026-09-05 instead of recording a separate replanning timestamp.
- verification-gap-1 -- **high / defer** -- pre-verified: no automatic workflow selects `HeavyweightContainerPublish`; mutating the real multi-RID label hook would evade all in-gate tests.
- verification-gap-2 -- **medium / defer** -- pre-verified: the seven Windows skip outcomes never execute on a Windows runner and are covered only by a weak source-text binder.
- verification-gap-3 -- **medium / patch** -- pre-verified: no test faults conflict-exhaustion clear, cleanup, or save and asserts primary conflict classification plus winner-state durability.
- verification-gap-4 -- **medium / patch** -- pre-verified: remediation tests omit exact operation/type/discard-field assertions, so false safe-discard diagnostics remain possible.
- verification-gap-5 -- **medium / patch** -- pre-verified: finalizer clear, pending-count read, and pending-count write failures have no behavioral coverage.
- verification-gap-6 -- **low / defer** -- the unrelated DW-372 completion entry contradicts the current malformed-input trait and default-gate behavior.
- edge-1 -- **medium / patch** -- caller cancellation during conflict-retry clearing is demonstrably wrapped rather than propagated.
- edge-2 -- **medium / patch** -- finalizer state operations now swallow `OperationCanceledException`, a behavior change from the replaced catch filter.
- edge-3 -- **high / bad_spec** -- if both a finalizer operation and `TryDiscardStagedStateAsync` fail, the method returns and the next actor turn can commit the residual cache; no poison/recovery barrier exists.
- edge-4 -- **medium / defer** -- the unrelated fixed-window Windows source scan can match `Assert.Skip` outside the actual guard body.
- edge-5 -- **false / reject** -- no current test reconfigures the same fault slot, so the stale callback branch is unreachable in the helper's present consumers.
- edge-6 -- **false / reject** -- every current seed call uses a fresh wrapper and the helper promises seeding, not wholesale replacement; no old-key contamination occurs at a cited consumer.
- edge-7 -- **medium / defer** -- concurrent Story 3.14 work replaced the exact 86,401-second upper-bound case with 90,001 seconds, allowing a widened 24-hour authority window to escape boundary detection.
- edge-8 -- **medium / patch** -- the spec's preservation claim is contradicted by conflict-retry cancellation being wrapped as remediation failure.
- edge-9 -- **high / bad_spec** -- the claim that later saves cannot see residual state is false after a finalizer/discard double failure.
- edge-10 -- **medium / bad_spec** -- finalizer failure deliberately leaves a durable pending count of one; repeated failures can create phantom backpressure and the spec did not define a safe reconciliation boundary.
- blind-loop1-1 -- **high / bad_spec** -- verified: `pendingCommandTracked` is set before the initial Processing batch save is known durable, so a pre-commit save failure runs the finalizer against the prior durable count and can decrement another command's pending slot.
- blind-loop1-2 -- **high / bad_spec** -- verified: `drainRecordCreated` is set before the PublishFailed recovery batch save; a pre-commit save failure skips finalization and leaves the entire staged event/idempotency/drain/index batch eligible to hitchhike on a later save.
- blind-loop1-3 -- **high / bad_spec** -- verified: a staged legacy-idempotency migration is saved before the protected command `try/finally`; save failure neither clears nor poisons the cache, so the migration can commit on a later turn.
- blind-loop1-4 -- **high / bad_spec** -- verified: stale-checkpoint handoff cleanup and Processing-checkpoint cleanup occur before the hardened command boundary; their clear/save failures can leave staged handoff or removal state without setting the unsafe-cache barrier.
- blind-loop1-5 -- **high / bad_spec** -- verified: `IdempotencyChecker.SetLegacySourceRedirectAsync` catches its own save failure and returns `Unavailable` while retaining the staged redirect, so the actor cannot poison or clear the cache before a later save.
- blind-loop1-6 -- **high / bad_spec** -- verified: manual snapshot staging/save failures are converted to `InfrastructureFailure` without discard or poisoning, allowing a failed snapshot batch to commit on a later actor turn.
- blind-loop1-7 -- **high / bad_spec** -- verified: reconciliation, legacy inspection, event reads, and metadata reads omit the unsafe-cache barrier while the state manager serves pending values first, so failed-attempt state can escape through read APIs before any mutating turn clears it.
- blind-loop1-8 -- **high / bad_spec** -- verified: pending-finalizer recovery intent exists only in activation fields; deactivation loses it while the durable counter remains inflated, permitting permanent phantom backpressure on the next activation.
- blind-loop1-9 -- **medium / patch** -- verified: the failed discard attempt's exception type is dropped, so double-clear diagnostics identify the first remediation failure but not the later failure that made discard unproved; add a bounded discard-failure classification without retaining raw exceptions.
- blind-loop1-10 -- **medium / defer** -- verified: the concurrent Story 3.14 `v4` handler procedure omits both the trusted-module-name update and replacement of the hard-coded `V3_PACKET_CODEC_SHA256` comparison, so following the documented steps would still fail.
- blind-loop1-11 -- **high / defer** -- carried: same location and claim as `blind-12`/`verification-gap-1`; all automatic workflows still exclude the two real `HeavyweightContainerPublish` tests.
- blind-loop1-12 -- **medium / defer** -- carried: same location and claim as `blind-14`/`verification-gap-2`; the fixed-window source binder still does not execute or structurally prove Windows skip behavior.
- blind-loop1-13 -- **low / defer** -- carried: same location and claim as `blind-13`/`verification-gap-6`; the DW-372 completion text still contradicts the malformed-input test's actual default-gate trait.
- blind-loop1-14 -- **low / defer** -- carried: same location and claim as `blind-15`; the unrelated Story 4.7 spec still rewrites its historical creation date.
- blind-loop1-15 -- **false / reject** -- the `-dirty` suffix is working-tree diff metadata, not a gitlink object proposed for commit; this build neither owns nor mutates the unrelated nested Tenants work and must preserve it.
- blind-loop1-16 -- **medium / defer** -- verified: concurrent commit `89564e0c` moved the `Hexalith.Memories` gitlink inside a review diff whose frozen Story 5.1 boundary excludes `references/`; it is unrelated committed work that this build must preserve rather than rewrite.
- blind-loop1-17 -- **false / reject** -- the build workflow deliberately keeps sprint tracking `in-progress` while the spec is temporarily `in-review`; the sprint entry transitions only at the presentation/completion step, so no lifecycle authority is inconsistent.
- blind-loop1-18 -- **false / reject** -- non-null GitHub `minimized` or `pin` metadata is structurally accepted, but neither changes the authenticated body/identity checks nor violates a cited Story 3.15 requirement; the reviewer did not establish a bad acceptance outcome.
- edge-loop1-1 -- **high / bad_spec** -- verified: reconciliation reads idempotency state without a barrier and can report a poisoned activation's pending failed-attempt value as though it were durable.
- edge-loop1-2 -- **high / bad_spec** -- verified: event and metadata reads lack the barrier and can expose pending residual stream state after an unproved discard.
- edge-loop1-3 -- **false / reject** -- no product path can persist a negative pending count under the configured admission guard, and the pre-existing decrement behavior already left externally corrupted negative state untouched; the proposed overflow requires an undemonstrated corrupted-store premise plus a second failure.
- edge-loop1-4 -- **high / bad_spec** -- verified: the claim that finalizer recovery never strands a slot is false across actor deactivation because its recovery obligation is activation-local only.
- edge-loop1-5 -- **high / defer** -- carried: same location and claim as `verification-gap-1`; no automatic lane positively selects `HeavyweightContainerPublish`.
- edge-loop1-6 -- **medium / defer** -- carried: same location and claim as `blind-14`/`verification-gap-2`; nearby `Assert.Skip` text can satisfy the Windows binder without proving the guard body.
- edge-loop1-7 -- **medium / defer** -- carried: same location and claim as `edge-7`; the exact 86,401-second authority boundary remains untested.
- verification-gap-loop1-1 -- **high / defer** -- carried: same location and claim as `verification-gap-1`; real container-publish provenance behavior has no automatic positive selector.
- verification-gap-loop1-2 -- **medium / defer** -- carried: same location and claim as `verification-gap-2`; Windows skip reporting remains source-text-only.
- verification-gap-loop1-3 -- **medium / defer** -- carried: same location and claim as `edge-7`; the one-second-over-24-hours authority cutoff lacks a regression case.
- verification-gap-loop1-4 -- **medium / patch** -- pre-verified: only `ProcessCommandAsync` exercises a poisoned activation; removing any one of the redirect, manual-snapshot, or reminder barriers would leave all current tests green, so each non-command state turn needs direct poison/block/recovery coverage.
- verification-gap-loop1-5 -- **medium / patch** -- pre-verified: no test faults the pending-finalizer repair save before or after commit and then proves blocked mutation, exact durable count, and next-turn recovery.
- verification-gap-loop1-other-1 -- **high / bad_spec** -- verified: `InMemoryStateManager` returns pending values before committed values, and every stream read entry point lacks a barrier, so the double-discard residual event/metadata values can be returned to callers.
- blind-loop2-1 -- **medium / patch** -- verified: a committed legacy migration of a `Recoverable` record is freshly observed as `RetryableRecoverable`, but the ambiguous-save branch accepts only `ExactTerminalDuplicate` and rethrows despite the successful commit.
- blind-loop2-2 -- **high / bad_spec** -- verified: `ClearPendingFinalizerRecovery` clears the independent reconciliation flag; after publication-index removal fails, terminal save requests reconciliation but the successful finalizer erases it, leaving the durable index and counter inconsistent.
- blind-loop2-3 -- **high / bad_spec** -- verified: stale-checkpoint handoff can atomically add an unindexed recovery owner without adding its pending slot; the incoming command only balances its own slot, leaving the recovered range uncounted until another activation.
- blind-loop2-4 -- **high / bad_spec** -- verified: zero-event stale-checkpoint handoff blindly decrements the counter even though activation derives ownership only from the publication index, so it can release an unrelated recovery owner's slot.
- blind-loop2-5 -- **medium / bad_spec** -- verified: persisted duplicate message IDs survive `UnpublishedPublicationIndex.Normalize`, while reconciliation counts raw entries; one recovery owner can therefore create phantom backpressure.
- blind-loop2-6 -- **high / bad_spec** -- verified: the retained fail-open backpressure read substitutes zero and then saves one; when recovery owners exist, a transient state-read failure overwrites their durable count and loses slots.
- blind-loop2-7 -- **medium / bad_spec** -- verified: a drain-success commit-then-throw is recognized from the missing durable record, but the immediate return permanently skips reminder unregistration and the Completed/Rejected advisory update.
- blind-loop2-8 -- **medium / patch** -- verified: after a reminder retry save commits and throws, the durable retry increment is observed but then redundantly staged and saved again, creating an unnecessary second remediation-failure surface.
- blind-loop2-9 -- **medium / defer** -- verified as pre-existing Story 4.4 behavior: a successful external dead-letter publish followed by a pre-commit marker-save failure can publish the same dead letter on the next reminder; the frozen boundary forbids changing dead-letter retry policy, and the new test only exposed the non-transactional boundary.
- blind-loop2-10 -- **high / defer** -- carried: same location and claim as `verification-gap-1`; no automatic workflow positively selects `HeavyweightContainerPublish`.
- blind-loop2-11 -- **medium / defer** -- carried: same location and claim as `blind-14`/`verification-gap-2`; the fixed-window source binder does not execute or structurally prove Windows skip behavior.
- blind-loop2-12 -- **medium / defer** -- carried: same location and claim as `edge-7`; the first invalid 86,401-second retained-authority boundary remains untested.
- blind-loop2-13 -- **low / defer** -- carried: same location and claim as `blind-13`/`verification-gap-6`; the deferred-work completion text still contradicts the malformed-input test's actual default-gate trait.
- blind-loop2-14 -- **medium / defer** -- carried: same location and claim as `blind-loop1-10`; the documented v4 handler procedure still omits two hard-coded v3 authorities.
- blind-loop2-15 -- **false / reject** -- the referenced gitlink advances are externally committed history after the frozen baseline, not Story 5.1 working-tree mutations, and the Tenants `-dirty` suffix is worktree metadata; repository instructions require preserving rather than removing them.
- edge-loop2-1 -- **high / bad_spec** -- verified: command-time stale-checkpoint handoff runs before the protected command `try/finally`, and `OperationCanceledException` bypasses its catch, so staged handoff state can remain cached for a later save.
- edge-loop2-2 -- **medium / patch** -- verified: if the pending-finalizer repair save fails, the second clear succeeds, and durable inspection then fails, diagnostics return `FailedBatchDiscarded=false` even though discard completed.
- edge-loop2-3 -- **high / defer** -- carried: same location and claim as `verification-gap-1`; both real heavyweight container-publish tests remain absent from automatic execution.
- edge-loop2-4 -- **medium / defer** -- carried: same location and claim as `blind-14`/`verification-gap-2`; nearby text can satisfy the Windows skip binder without proving the guard body.
- edge-loop2-5 -- **medium / defer** -- carried: same location and claim as `edge-7`; 86,401 through 90,000 seconds remain uncovered.
- edge-loop2-6 -- **medium / defer** -- carried: same location and claim as `blind-loop1-10`; following the v4 documentation still leaves hard-coded v3 validation gates.
- verification-gap-loop2-1 -- **high / defer** -- carried: same location and claim as `verification-gap-1`; real multi-RID container publishing has no automatic positive selector.
- verification-gap-loop2-2 -- **medium / defer** -- carried: same location and claim as `verification-gap-2`; Windows skip behavior remains source-text-only.
- verification-gap-loop2-3 -- **medium / defer** -- carried: same location and claim as `edge-7`; the exact first-invalid 86,401-second authority boundary lacks a regression case.
- verification-gap-loop2-4 -- **medium / patch** -- pre-verified: poison tests cover metadata but not `ReconcileFencedCommandAsync`, legacy inspection, `GetEventsAsync`, or `ReadEventsRangeAsync`; removing any one of those four barriers leaves the suite green.
- verification-gap-loop2-5 -- **medium / patch** -- pre-verified: every activation reconciliation assertion uses an empty publication index, so replacing the derived expected count with constant zero would leave all activation tests green.
- verification-gap-loop2-6 -- **medium / patch** -- pre-verified: no test faults the activation pending-count reconciliation save before or after commit and then proves durable observation plus mandatory later-turn recovery.
- verification-gap-loop2-7 -- **medium / patch** -- pre-verified: no test faults the reminder-stamp save before or after commit, so ambiguous command behavior and activation's later `ReminderArmedAt` decision are unproved.
- verification-gap-loop2-other-1 -- **low / defer** -- carried: same location and claim as `blind-13`/`verification-gap-6`; the completion ledger incorrectly says the malformed-input theory is heavyweight and excluded.
- blind-loop3-1 -- **high / bad_spec** -- verified: resumed `PublishFailed` can add a previously missing publication owner without staging the updated owner count, so the activation continues with an undercounted backpressure projection.
- blind-loop3-2 -- **high / bad_spec** -- verified: successful `EventsStored` resume blindly decrements the pending count before index removal; when the checkpoint is unindexed, it consumes an unrelated recovery owner's slot and leaves the counter/index invariant broken.
- blind-loop3-3 -- **high / bad_spec** -- verified: resumed terminal staging and its owner-count read occur outside any discard/poison guard, so a mutator failure can leave pipeline, idempotency, index, or counter state cached for a later save.
- blind-loop3-4 -- **high / bad_spec** -- verified: resumed `PublishFailed` stages checkpoint cleanup, idempotency, drain, and index state before the guarded save; a pre-save failure leaves that partial batch cached and the activation unpoisoned.
- blind-loop3-5 -- **high / bad_spec** -- verified: both drain-exhaustion batches guard only their saves, so a marker or cleanup mutator failure before the save can leave dead-letter or terminal cleanup state cached for a later reminder turn.
- blind-loop3-6 -- **high / bad_spec** -- verified: successful drain cleanup stages removal, index release, terminal idempotency, and count reconciliation before the save guard; a staging failure falls into retry persistence without first clearing and can commit partial terminal cleanup with the retry record.
- blind-loop3-7 -- **high / defer** -- verified as pre-existing Story 4.4 behavior excluded by the frozen publication-recovery boundary: an at-capacity stale handoff can commit an unindexed drain after removing the enumerable checkpoint, leaving a crash window before reminder registration.
- blind-loop3-8 -- **medium / bad_spec** -- verified: the entry barrier reports `FailedBatchDiscarded=false` whenever finalizer or reconciliation recovery fails after its cache clear already completed, contradicting the exact discard diagnostic contract.
- blind-loop3-9 -- **medium / bad_spec** -- verified: stale-handoff inspection treats any durable pipeline value as an exact pre-commit witness instead of matching the original checkpoint, so a concurrent replacement is misclassified rather than reported as ambiguous.
- blind-loop3-10 -- **medium / bad_spec** -- verified in part: malformed retained entries do not inflate `OwnerCount`, but `TryAdd` applies capacity to raw `Entries.Count`, so non-owners can reject a legitimate recovery owner contrary to normalized-owner capacity accounting.
- blind-loop3-11 -- **high / defer** -- carried: same location and claim as `verification-gap-1`/`blind-loop2-10`; no automatic workflow positively selects the real `HeavyweightContainerPublish` tests.
- blind-loop3-12 -- **medium / defer** -- carried: same location and claim as `blind-14`/`verification-gap-loop2-2`; the fixed-window source binder does not structurally prove that each Windows guard skips.
- blind-loop3-13 -- **low / defer** -- carried: same location and claim as `blind-loop2-13`/`verification-gap-loop2-other-1`; DW-372's completion text incorrectly says the deliberately unmarked malformed-input theory is heavyweight and excluded.
- blind-loop3-14 -- **medium / defer** -- carried: same location and claim as `edge-7`/`verification-gap-loop2-3`; the first invalid 86,401-second retained-authority boundary remains untested.
- blind-loop3-15 -- **medium / defer** -- carried: same location and claim as `blind-loop1-10`/`blind-loop2-14`; the documented v4 handler procedure omits replacement of the hard-coded v3 codec-digest authority.
- blind-loop3-16 -- **medium / defer** -- verified as unrelated concurrent Story 3.15 work: `docs/ci.md` says eight subjects/seven re-mints while the superseded-acceptance README says seven/six, leaving conflicting operator arithmetic already tracked in deferred work.
- blind-loop3-17 -- **low / defer** -- carried: same location and claim as `blind-loop1-14`; the unrelated Story 4.7 spec rewrites its historical creation date.
- edge-loop3-1 -- **high / bad_spec** -- verified: legacy `CheckAsync` stages the message-key migration before removing the legacy key; if the second mutator throws, the caller never reaches its save catch and the staged migration can hitchhike onto a later save.
- edge-loop3-2 -- **high / bad_spec** -- verified: initial admission stages the pending count and Processing checkpoint before the save try; a checkpoint mutator failure leaves an abandoned slot or partial pipeline batch cached while the finalizer remains disabled.
- edge-loop3-3 -- **high / bad_spec** -- verified: the cited resume terminal and recovery helpers protect only `SaveStateAsync`, not the preceding state reads and writes, so their partial batches are not discarded or poisoned on pre-save failure.
- edge-loop3-4 -- **high / bad_spec** -- verified: the successful-drain staging sequence precedes its save guard and the outer retry catch performs a new save without a clear, allowing partial terminal cleanup to combine with a retry update.
- edge-loop3-5 -- **high / bad_spec** -- verified: exhaustion marker and cleanup staging both precede their save guards, so a pre-save mutator failure can leave unproved exhaustion state cached.
- edge-loop3-6 -- **high / bad_spec** -- verified: normalization can retain a malformed entry followed by a valid entry with the same message ID; `TryRemove` removes the malformed first match and leaves the real owner and pending slot stranded.
- edge-loop3-7 -- **high / defer** -- carried: same location and claim as `verification-gap-1`/`blind-loop2-10`; every automatic Contracts lane excludes the real heavyweight publish tests.
- edge-loop3-8 -- **medium / defer** -- carried: same location and claim as `blind-14`/`verification-gap-loop2-2`; a nearby skip string can satisfy the fixed-window Windows binder without proving the guard body.
- edge-loop3-9 -- **medium / defer** -- carried: same location and claim as `edge-7`/`verification-gap-loop2-3`; 86,401 seconds, the first invalid retained-authority value, remains unpinned.
- edge-loop3-10 -- **medium / patch** -- verified: `GetEventsAsync` now catches all metadata-read exceptions and wraps `OperationCanceledException` as deserialization failure, causing its admin caller to map cancellation to HTTP 500 instead of propagating it.
- verification-gap-loop3-1 -- **medium / patch** -- pre-verified: stale-handoff save inspection has no before-commit or commit-then-throw test for either event-bearing or zero-event batches, so its durable-witness decision can regress undetected.
- verification-gap-loop3-2 -- **medium / patch** -- pre-verified: drain retry persistence tests cover normal and commit-then-throw saves but not the pre-commit repair branch, so loss of the one-increment restage/save would remain green.
- verification-gap-loop3-3 -- **medium / patch** -- pre-verified: legacy migration tests cover staged state and post-commit ambiguity but not a pre-commit save fault proving the migration is discarded and cannot hitchhike.
- verification-gap-loop3-4 -- **medium / patch** -- pre-verified: finalizer fault rows omit publication-index owner reads, so same-turn repair and mandatory later reconciliation after first/repeated owner-read failures are unproved.
- verification-gap-loop3-5 -- **medium / defer** -- carried: same location and claim as `edge-7`/`verification-gap-loop2-3`; acceptance at 86,400 and rejection at 86,401 seconds are not pinned.
- verification-gap-loop3-6 -- **high / defer** -- carried: same location and claim as `verification-gap-1`/`blind-loop2-10`; actual SDK container publication is absent from every automatic lane.
- verification-gap-loop3-7 -- **medium / defer** -- carried: same location and claim as `blind-14`/`verification-gap-loop2-2`; Windows skip behavior is checked only through source text on Linux.
- verification-gap-loop3-other-1 -- **medium / patch** -- verified: same production defect as `edge-loop3-10`; metadata-read cancellation is wrapped as `EventDeserializationException`, while controller tests only cover a mocked raw cancellation.
- blind-loop4-1 -- **high / defer** -- verified as the pre-existing publication-recovery crash window already represented by `blind-loop3-7`: stale handoff and resumed publication can persist an unindexed drain when the fixed index refuses an already-committed range, leaving reminder registration as the only discovery mechanism. The frozen intent explicitly excludes changing publication-recovery policy.
- blind-loop4-2 -- **medium / bad_spec** -- verified: stale-handoff inspection returns `false` when the original pipeline is absent but the exact drain/index postcondition is also absent, so a concurrent or partial durable state is treated as pre-commit rather than ambiguous.
- blind-loop4-3 -- **medium / bad_spec** -- verified: stale-handoff commit inspection compares only message ID, range, and count; it can accept a different durable drain correlation, command type, rejection flag, retry state, or timestamp and then arm that replacement as if it were the attempted record.
- blind-loop4-4 -- **medium / bad_spec** -- verified: ambiguous legacy-redirect save inspection accepts any source-matching `Redirected` record without comparing its target/digest to the attempted redirect, so a different target can be reported as this call's success.
- blind-loop4-5 -- **medium / bad_spec** -- verified: manual-snapshot failure inspection accepts any durable snapshot with the attempted sequence and runs for every post-metadata exception, not only an ambiguous save; an unrelated same-sequence value can be reported as newly created.
- blind-loop4-6 -- **false / reject** -- the clear at line 2351 discards the original failed finalizer batch, but the later repair stages a new batch; if that repair's own clear fails, `recoveryDiscarded=false` correctly reports that the newest residual mutation remains unproved rather than relabeling it safe.
- blind-loop4-7 -- **medium / bad_spec** -- verified: resumed `PublishFailed` and terminal completion wrap every `InvalidOperationException` from their entire multi-operation batch as a concurrency conflict, so checkpoint, idempotency, index, or counter mutator defects can trigger concurrency retry/rejection semantics.
- blind-loop4-8 -- **medium / bad_spec** -- verified in the legacy-migration, Processing-admission, resume, and terminal paths: remediation exceptions can escape without marking their child/process activities failed. Stale-handoff failures are converted to and marked as identity conflicts, so that cited subcase is not part of the defect.
- blind-loop4-9 -- **medium / bad_spec** -- verified: direct discard-failure branches label the remediation operation as `Discard...Batch` but populate `RemediationExceptionType` from the primary save/mutator exception; the actual clear failure is only in `DiscardExceptionType`, making operation/type diagnostics internally inconsistent.
- blind-loop4-10 -- **medium / bad_spec** -- verified: publication-index refusal reports `Entries.Count` while admission capacity and durable reconciliation use `OwnerCount`, so retained malformed non-owners inflate the user-visible/logged backpressure count.
- blind-loop4-11 -- **high / defer** -- carried: same location and claim as `verification-gap-1`/`blind-loop3-11`; no automatic workflow positively selects the real `HeavyweightContainerPublish` tests.
- blind-loop4-12 -- **medium / defer** -- carried: same location and claim as `blind-14`/`blind-loop3-12`; the fixed-window source binder does not execute or structurally prove the Windows skip branches.
- blind-loop4-13 -- **medium / defer** -- carried: same location and claim as `blind-loop1-10`/`blind-loop3-15`; the documented v4 handler procedure leaves hard-coded v3 module and codec-digest authorities.
- blind-loop4-14 -- **medium / defer** -- carried: same location and claim as `edge-7`/`verification-gap-loop3-5`; the exact 86,400/86,401-second retained-authority boundary remains unpinned.
- blind-loop4-15 -- **low / defer** -- carried: same location and claim as `blind-13`/`blind-loop3-13`; the DW-372 completion record contradicts the malformed-input test's actual default-gate trait.
- blind-loop4-16 -- **medium / defer** -- carried: same location and claim as `blind-loop3-16`; unrelated Story 3.15 records disagree on the subject/re-mint arithmetic.
- blind-loop4-17 -- **low / defer** -- carried: same location and claim as `blind-loop1-14`/`blind-loop3-17`; the unrelated Story 4.7 spec rewrites its historical creation date.
- edge-loop4-1 -- **high / bad_spec** -- verified: when a stale `Processing` checkpoint's proven excess slot is reused and replacement admission fails before commit, inspection resets `pendingCommandTracked` to false; finalization then skips the now-orphaned slot, which can keep the live activation at backpressure indefinitely.
- edge-loop4-2 -- **medium / bad_spec** -- verified: same manual-snapshot exact-witness defect as `blind-loop4-5`; sequence equality alone cannot prove that the attempted snapshot value committed.
- edge-loop4-3 -- **medium / bad_spec** -- verified: same stale-handoff exact-witness defect as `blind-loop4-3`; correlation and the remaining attempted drain fields are not compared before commit is accepted.
- edge-loop4-4 -- **medium / defer** -- carried: same location and claim as `blind-loop2-9`; successful external dead-letter publication followed by a pre-commit marker-save failure can publish the same exhausted range on the next reminder, and the frozen intent excludes changing dead-letter retry policy.
- edge-loop4-5 -- **medium / bad_spec** -- verified: malformed entries do not consume either activation budget, yet every nonblank malformed ID performs idempotency completion state I/O; a large persisted malformed set therefore creates an unbounded activation turn.
- edge-loop4-6 -- **high / bad_spec** -- verified: normalization silently keeps the first of two well-formed same-message entries with different correlations. If only the discarded correlation has the recoverable checkpoint, activation can prune the surviving lookup as stale and strand the committed range.
- edge-loop4-7 -- **medium / defer** -- carried: same location and claim as `blind-loop1-10`/`blind-loop4-13`; the documented v4 handler procedure retains v3-only authorities.
- edge-loop4-8 -- **high / defer** -- carried: same location and claim as `verification-gap-1`/`blind-loop4-11`; actual container publication has no automatic positive selector.
- edge-loop4-9 -- **medium / defer** -- carried: same location and claim as `blind-14`/`blind-loop4-12`; nearby skip text can satisfy the Windows source binder without proving the guard body.
- edge-loop4-10 -- **medium / defer** -- carried: same location and claim as `edge-7`/`blind-loop4-14`; the first invalid 86,401-second authority value remains untested.
- edge-loop4-11 -- **low / patch** -- verified: `DecrementPendingCommandCountAsync` has no remaining caller after owner-count reconciliation replaced blind decrements; direct deletion removes an obsolete corruption-prone helper without adding behavior.
- verification-gap-loop4-1 -- **high / defer** -- carried: same location and claim as `verification-gap-1`/`blind-loop4-11`; the real multi-RID publication tests are excluded and never selected by an automatic lane.
- verification-gap-loop4-2 -- **medium / defer** -- carried: same location and claim as `verification-gap-2`/`blind-loop4-12`; Windows skip behavior is source-text-only on Ubuntu.
- verification-gap-loop4-3 -- **medium / defer** -- carried: same location and claim as `edge-7`/`blind-loop4-14`; the exact first-invalid 86,401-second authority boundary lacks a regression test.
- verification-gap-loop4-other-1 -- **medium / bad_spec** -- verified: same manual-snapshot exact-witness defect as `blind-loop4-5`; a broad catch plus sequence-only observation can turn an unrelated failure/replacement into a successful admin job.
- verification-gap-loop4-other-2 -- **medium / bad_spec** -- verified: a stale-handoff `SaveStateAsync` that commits and then throws caller cancellation is accepted as committed and suppresses the cancellation, bypassing the outer propagation branch and continuing the incoming command.
- blind-loop5-1 -- **medium / bad_spec** -- verified: after an ambiguous legacy-migration save, `InspectAsync` proves only command identity and disposition; the actor neither compares the observed `CommandProcessingResult` with the result staged from the legacy record nor observes the legacy-key removal, so a different same-identity durable result can be returned as this migration's exact witness.
- blind-loop5-2 -- **high / bad_spec** -- verified: `UnpublishedPublicationIndex.TryAdd` replaces an existing same-message owner even when its correlation differs, directly contradicting the fail-closed conflicting-owner rule and allowing the only pointer to the older committed recovery path to be overwritten.
- blind-loop5-3 -- **medium / bad_spec** -- verified: publication-recovery pre-commit inspection compares a present pipeline only through message/causation/command identity; stage, range, result, and the rest of the exact pre-save `PipelineState` are ignored, so a concurrent same-identity checkpoint is misclassified as this batch's pre-commit witness.
- blind-loop5-4 -- **false / reject** -- the bad outcome is disproved by the actor-state transaction boundary and the frozen reminder design: the drain removal, index removal, terminal transition when staged, and pending count share one atomic save, while an absent exact drain plus normalized post-removal ownership proves the idempotent cleanup consequence; the spec explicitly preserves that missing-drain witness.
- blind-loop5-5 -- **false / reject** -- the finalizer clears only its own recovery fields; it does not clear `_pendingCountReconciliationRequired`. A correctly committed concurrent owner-count change changes the observed counter and is rejected unless cardinality remains equal, in which case the counter/index cardinality invariant is still satisfied.
- blind-loop5-6 -- **false / reject** -- exact committed event envelopes, metadata, `EventsStored` pipeline, and publication owner are members of the same atomic actor-state save and therefore prove that batch committed; a staged snapshot commits in that transaction too, while later snapshot absence/replacement is advisory and does not make publishing the exact durable events unsafe.
- blind-loop5-7 -- **medium / defer** -- verified as a pre-existing corrupted-index CPU/allocation risk outside this story's bounded state-I/O repair: blank-message remnants perform no idempotency read/save and therefore do not evade the new probe budget, but the historical activation scan still visits and collects every blank entry.
- blind-loop5-8 -- **high / defer** -- carried: same location and claim as `verification-gap-1`/`blind-loop4-11`; no automatic workflow positively selects the two real `HeavyweightContainerPublish` tests.
- blind-loop5-9 -- **medium / defer** -- carried: same location and claim as `blind-14`/`blind-loop4-12`; the fixed-window source binder does not execute or structurally prove the Windows skip branches.
- blind-loop5-10 -- **medium / defer** -- carried: same location and claim as `edge-7`/`blind-loop4-14`; the exact first-invalid 86,401-second retained-authority boundary remains unpinned.
- blind-loop5-11 -- **medium / defer** -- carried: same location and claim as `blind-loop1-10`/`blind-loop4-13`; the documented v4 handler procedure retains hard-coded v3 authorities.
- blind-loop5-12 -- **low / defer** -- carried: same location and claim as `blind-13`/`blind-loop4-15`; the DW-372 completion record contradicts the malformed-input test's actual default-gate trait.
- blind-loop5-13 -- **medium / defer** -- carried: same location and claim as `blind-loop3-16`/`blind-loop4-16`; unrelated Story 3.15 records still disagree on the subject/re-mint arithmetic.
- blind-loop5-14 -- **low / defer** -- carried: same location and claim as `blind-loop1-14`/`blind-loop4-17`; the unrelated Story 4.7 spec rewrites its historical creation date.
- blind-loop5-15 -- **false / reject** -- carried: same location and claim as `blind-loop2-15`; the gitlink advances are externally committed history after the fixed baseline, not Story 5.1 worktree mutations, and repository instructions require preserving them.
- edge-loop5-1 -- **medium / bad_spec** -- verified: on a resumed recovery save that fails cleanly before commit after refreshing an existing owner, the inspector compares the durable pre-save index with the staged post-save index and therefore reports ambiguity instead of recognizing the exact original pipeline/index/count state.
- edge-loop5-2 -- **medium / bad_spec** -- verified: when several malformed remnants share a message ID, `TryAdd` replaces only the first match and assigns the list through a record `with` initializer that does not re-run constructor normalization, leaving another same-ID malformed remnant beside the valid owner.
- edge-loop5-3 -- **medium / defer** -- carried: same location and claim as `blind-loop2-9`/`edge-loop4-4`; successful external dead-letter publication followed by a pre-commit marker-save failure can republish the range, and the frozen intent excludes changing dead-letter retry policy.
- edge-loop5-4 -- **high / defer** -- carried: same location and claim as `verification-gap-1`/`blind-loop5-8`; actual container publication has no automatic positive selector.
- edge-loop5-5 -- **medium / defer** -- carried: same location and claim as `edge-7`/`blind-loop5-10`; the first invalid 86,401-second authority value remains untested.
- edge-loop5-6 -- **medium / defer** -- carried: same location and claim as `blind-14`/`blind-loop5-9`; nearby text can satisfy the fixed-window Windows binder without proving the guarded branch skips.
- verification-gap-loop5-1 -- **high / defer** -- carried: pre-verified, same location and claim as `verification-gap-1`/`blind-loop5-8`; both real container-publish tests are excluded from every automatic workflow.
- verification-gap-loop5-2 -- **medium / defer** -- carried: pre-verified, same location and claim as `verification-gap-2`/`blind-loop5-9`; Windows skip reporting remains source-text-only on Ubuntu runners.
- verification-gap-loop5-3 -- **medium / patch** -- pre-verified: exact event-batch post-commit fixtures exist, but no negative test replaces or removes an attempted metadata/event witness before fresh inspection, so weakening `HasExactCommittedEventBatchAsync` would remain green.
- verification-gap-loop5-4 -- **medium / patch** -- pre-verified: publication-recovery ambiguity has only exact pre/post endpoint tests; no concurrent partial drain, idempotency, owner, or count witness proves that the committed branch fails closed.
- verification-gap-loop5-5 -- **medium / patch** -- pre-verified: terminal-save ambiguity has no concurrent conflicting idempotency result, publication index, or pending-count witness, so weakening the exact committed predicate would remain green.
- blind-loop6-1 -- **medium / bad_spec** -- carried: same location and claim as `blind-loop5-1`; ambiguous legacy-migration inspection still accepts a same-identity terminal or recoverable record without proving the exact migrated result or legacy-key removal.
- blind-loop6-2 -- **high / bad_spec** -- carried: same location and claim as `blind-loop5-2`; `TryAdd` still replaces a well-formed same-message owner whose correlation differs, discarding the prior recovery pointer instead of failing closed.
- blind-loop6-3 -- **medium / bad_spec** -- carried: same location and claim as `edge-loop5-2`; `TryAdd` still replaces only the first same-message remnant through a `with` assignment that does not normalize the remaining malformed duplicates.
- blind-loop6-4 -- **medium / bad_spec** -- carried: same location and claim as `blind-loop5-3`; publication-recovery pre-commit inspection still compares only command identity rather than the exact original `PipelineState`.
- blind-loop6-5 -- **medium / bad_spec** -- carried: same location and claim as `edge-loop5-1`; a resumed recovery pre-commit failure still compares the durable old publication index with the timestamp-refreshed attempted index and is reported as ambiguous.
- blind-loop6-6 -- **medium / defer** -- carried: same location and claim as `blind-loop5-7`; blank-message malformed entries still bypass the activation budgets and can cause unbounded scan/allocation work, but that pre-existing non-I/O risk is outside Story 5.1's frozen bounded state-I/O intent.
- blind-loop6-7 -- **medium / patch** -- carried: same location and claim as `verification-gap-loop5-3`; event-batch ambiguity still has no partial or replaced metadata/event witness regression.
- blind-loop6-8 -- **medium / patch** -- carried: same location and claim as `verification-gap-loop5-4`; publication-recovery ambiguity still has no partial drain, result, owner, index, or pending-count witness regression.
- blind-loop6-9 -- **medium / patch** -- carried: same location and claim as `verification-gap-loop5-5`; terminal-save ambiguity still has no conflicting idempotency, publication-index, or pending-count witness regression.
- blind-loop6-10 -- **high / defer** -- carried: same location and claim as `verification-gap-1`; every automatic workflow still excludes both real heavyweight container-publication tests.
- blind-loop6-11 -- **medium / defer** -- carried: same location and claim as `verification-gap-2`; Windows skip behavior remains covered only by a fixed-window source-text scan on Ubuntu runners.
- blind-loop6-12 -- **medium / defer** -- carried: same location and claim as `edge-7`; the first invalid 86,401-second retained-authority boundary remains untested.
- blind-loop6-13 -- **medium / defer** -- carried: same location and claim as `blind-loop1-10`; the documented v4 succession procedure still omits the required v4 trust and codec-digest authority changes.
- blind-loop6-14 -- **low / defer** -- carried: same location and claim as `blind-13`; DW-372 still describes the malformed-input test as heavyweight even though it remains in the default gate.
- blind-loop6-15 -- **medium / defer** -- carried: same location and claim as `blind-loop3-16`; the unrelated Story 3.15 records still disagree on retained subject and re-mint counts.
- blind-loop6-16 -- **low / defer** -- carried: same location and claim as `blind-loop1-14`; the unrelated Story 4.7 spec still rewrites its historical creation date.
- verification-gap-loop6-1 -- **high / defer** -- carried: same location and claim as `verification-gap-1`; no automatic workflow positively selects the two real `HeavyweightContainerPublish` tests.
- verification-gap-loop6-2 -- **medium / defer** -- carried: same location and claim as `verification-gap-2`; no Windows lane executes and verifies the seven skip branches.
- verification-gap-loop6-3 -- **medium / patch** -- carried: same location and claim as `verification-gap-loop5-3`; the exact event-batch predicate remains unprotected by a partial-witness negative regression.
- verification-gap-loop6-4 -- **medium / patch** -- carried: same location and claim as `verification-gap-loop5-4`; the publication-recovery committed predicate remains unprotected by partial or conflicting witness regressions.
- verification-gap-loop6-5 -- **medium / patch** -- carried: same location and claim as `verification-gap-loop5-5`; the terminal committed predicate remains unprotected by conflicting publication-state regressions.
- verification-gap-loop6-6 -- **high / bad_spec** -- carried: same location and claim as `blind-loop5-2`; active `TryAdd` still overwrites a well-formed owner with a different correlation instead of preserving the original owner and failing closed.
- verification-gap-loop6-other-1 -- **medium / bad_spec** -- carried: same location and claim as `blind-loop5-1`; legacy migration still accepts a same-identity observed result without the exact migrated-result and legacy-key-removal witnesses.
- verification-gap-loop6-other-2 -- **medium / bad_spec** -- carried: same location and claim as `blind-loop5-3`; publication-recovery pre-commit classification still accepts a same-identity but otherwise different checkpoint.
- edge-loop6-1 -- **medium / defer** -- carried: same location and claim as `blind-loop5-7`; blank malformed message entries remain an unbounded pre-existing activation scan/allocation risk outside the frozen state-I/O budget.
- edge-loop6-2 -- **medium / defer** -- carried: same location and claim as `blind-loop2-9`; successful external dead-letter publication followed by a pre-commit marker-save failure can republish the range, and the frozen intent excludes changing dead-letter retry policy.
- edge-loop6-3 -- **high / defer** -- carried: same location and claim as `verification-gap-1`; real `PublishContainer` regressions remain outside every automatic workflow.
- edge-loop6-4 -- **medium / defer** -- carried: same location and claim as `blind-14`; the fixed-window Windows binder can still be satisfied without proving the guarded branch skips.
- edge-loop6-5 -- **medium / defer** -- carried: same location and claim as `edge-7`; an erroneous validity window between 86,401 and 90,000 seconds would remain green.
- edge-loop6-6 -- **medium / defer** -- carried: same location and claim as `blind-loop1-10`; the v4 handler path retains v3-only trust/codec authority and cannot follow the documented succession procedure safely.
- edge-loop6-7 -- **medium / defer** -- carried: same location and claim as `blind-loop5-7`; the Story 5.1 bounded-activation claim still does not cover arbitrarily many blank-message malformed entries, which perform no state I/O but are all scanned and accumulated.
- edge-loop6-8 -- **medium / bad_spec** -- verified: the legacy-redirect caller has no proof that `SaveStateAsync` was attempted before its catch performs durable-success inspection; a `SetStateAsync` failure followed by a concurrent exact redirect can therefore be returned as this call's success, contrary to the exact save-attempt acceptance rule.
- edge-loop6-9 -- **medium / patch** -- verified in part: `DrainSuccessCleanupMutationFailure_DiscardsPartialCleanupBeforePersistingRetry` covers the pre-save drain-retry lane, but the declared matrix still lacks a zero-event pre/post-save ambiguity test, a finalizer publication-owner-read recovery test, and a direct metadata-read cancellation propagation test.

## Design Notes

Use the deterministic `InMemoryStateManager` pending/committed model as the completion-grade durable-state oracle because the story gate names focused Server tests and a Release build, not a live-sidecar lane. A test-local wrapper should add fault scheduling and operation order without widening the public Testing package. Treat normal infrastructure rejection as advisory status plus durable pipeline cleanup; do not invent a retained `Rejected` pipeline record where checkpoint and removal share one batch.

A save catch is not a batch boundary. State-manager reads and mutators can fail after an earlier mutation has already staged, so every multi-operation atomic batch must enter its discard/poison guard before the first operation that can stage state and remain inside it through the save and durable-outcome inspection. This applies to legacy migration's set/remove pair, initial admission's counter/checkpoint pair, resume terminal and `PublishFailed` batches, drain-success cleanup, retry repair, exhaustion marker, and exhaustion cleanup. A catch that intends to persist a retry must first prove the failed cleanup batch was discarded; otherwise the retry save can commit both batches together.

The pinned Dapr Actors 1.18.5 `Actor`/`ActorHost` surface has no immediate self-deactivation API. Maintain an activation-local unsafe-cache flag after any failed discard. After tenant validation where applicable, every external actor entry point that consults actor state -- reads, reconciliation/inspection, command processing, redirect mutation, snapshot creation, and reminders -- crosses one non-cancelable barrier before its first state access. On barrier failure, fail closed without reading, staging, or saving and keep the flag set. A helper that converts a state failure into a bounded result (notably legacy redirect `Unavailable` and manual snapshot `InfrastructureFailure`) must first complete that same discard/poison protocol; a returned status is never evidence the cache is clean.

No local flag written before `SaveStateAsync` returns proves durable ownership. Protect legacy migration, stale-checkpoint handoff/cleanup, initial Processing admission, and PublishFailed recovery at their actual save sites. After a save exception, clear first and inspect the batch's durable witnesses: exact pipeline identity plus pending count for Processing admission/takeover; pipeline removal and normalized owner count for stale cleanup/handoff; and the committed publication index/drain/idempotency state for PublishFailed recovery. A pre-commit Processing failure owns no slot and must not decrement the prior count; a commit-then-throw owns the observed slot and may finalize it. An exact migrated idempotency witness is authoritative for either Terminal or Recoverable disposition. PublishFailed always retains the slot when the already-committed publication index proves recovery remains outstanding, but its failed recovery batch must still be discarded so staged drain/idempotency changes cannot hitchhike.

For pending-count finalization, establish the committed pre-decrement count after a clean clear. If save fails, clear and inspect durable state: an already-decremented value proves commit-then-throw; the unchanged value permits one clean recovery decrement/save; any other value or failed inspection keeps the activation unsafe and is reported without another save. Exercise both pre-commit and commit-then-throw failure of that repair save. Record the exception type of a failed discard attempt separately from the original remediation operation so `FailedBatchDiscarded=false` is diagnosable without raw messages; once a clear completed, a later inspection failure must not relabel that discard as unproved. Finalizer repair state and general index/counter reconciliation state are separate obligations. Clearing finalizer-local fields never clears the general reconciliation flag; only a fresh proof that the counter equals the normalized committed owner set clears the latter.

Activation memory is not durable recovery. Once publication recovery re-arm/pruning finishes, an idle activation reconciles `pending_command_count` to the freshly re-read committed index's normalized, well-formed, distinct message-owner count; at activation there is no executing command, so those owners are the durable outstanding publications. Normalize duplicate persisted message IDs before capacity and counter calculations so one owner counts once. If activation recovery degrades, retain an activation-local `reconciliation required` flag and make every later entry barrier retry reconciliation before any state API proceeds. Because reconciliation intentionally removes an orphaned Processing slot, a later command that adopts or replaces a committed `Processing` checkpoint must acquire and commit a fresh pending slot instead of assuming the old count survived. A stale committed handoff that adds a missing recovery owner must stage the resulting normalized owner count in the same atomic batch; zero-event cleanup stages the unchanged normalized owner count and never decrements blindly. Inspect after reconciliation-save failure to distinguish commit-then-throw, and never clear the obligation until the committed count equals the committed normalized owner count.

Resume owns the same counter/index invariant as first-pass execution. Terminal resume stages the normalized count returned by exact index removal, even when the recovered checkpoint was never indexed; `PublishFailed` resume stages the count returned after adding or refreshing its recovery owner. Never use a generic decrement for either case. Publication-index normalization groups all same-message entries, prefers one well-formed owner over malformed remnants, applies capacity to `OwnerCount`, and removes the whole same-ID group so malformed historical data cannot block or strand a valid owner. Stale-handoff inspection compares the durable pipeline with the exact original checkpoint; mere key presence is not a pre-commit witness.

The historical backpressure read was fail-open for availability, but admission cannot safely persist `0 + 1` after the read failed because that can overwrite slots owned by committed recovery entries. On a failed pending-count read, do not stage a counter or Processing checkpoint from a guessed value. Complete non-cancelable discard/poison handling, then propagate or use the existing bounded infrastructure result without adding a new public contract. This narrows only the unsafe mutation after a failed read; successful backpressure thresholds and result fields remain unchanged.

Manual snapshot and legacy redirect failures use their existing public result types. Clear cached state before returning a failure; after an ambiguous save, inspect the exact snapshot/redirect key to report or return the observed bounded consequence, and poison the activation when neither discard nor observation succeeds. Stale-handoff failures may keep their existing fail-closed command result only after cached mutations are discarded or the turn is converted to a remediation exception.

Reminder ambiguity follows the same durable-witness rule through the full consequence. If a drain-success batch committed and then threw, the missing committed drain record proves cleanup; continue reminder unregistration and terminal advisory status rather than returning early with a permanent `PublishFailed` view. If the retry record already contains the attempted increment, use it directly for diagnostics/status and do not stage or save it a second time. Reminder-stamp and activation-reconciliation saves require pre-commit and commit-then-throw tests. The non-transactional dead-letter marker behavior and dead-letter retry policy remain unchanged by this story.

Propagate caller/request cancellation from conflict clearing, stale-checkpoint handoff, and dead-letter publication after required non-cancelable cleanup; non-cancellation publication failures remain advisory. Preserve the prior finalizer cancellation contract after best-effort discard. Mark the relevant child and process activities failed whenever remediation escapes, and verify all structured diagnostic fields and redaction.

Propagate state-manager `OperationCanceledException` unchanged from actor read APIs, including the metadata read in `GetEventsAsync`; deserialization wrappers remain limited to actual deserialization failures. At the entry barrier, record whether the initial cache clear completed before attempting finalizer or counter recovery so a later recovery failure does not rewrite a proved discard as false.

Stale `Processing` replacement has two independent facts: whether the old durable count contains an excess executing-command slot, and whether the replacement Processing checkpoint committed. Never overwrite the first fact with the second inspector result. If the old slot was proven and replacement is pre-commit, keep finalization enabled so the orphan is reconciled exactly once; if replacement committed, continue with the same owned slot. Tests must begin with a nonempty owner index plus one excess slot so decrementing an owner's slot and stranding the stale slot are both observable.

Every ambiguity inspector needs both an exact pre-commit witness and an exact attempted postcondition. For event-bearing stale handoff, preserve the attempted `UnpublishedEventsRecord` and compare the complete record plus the exact message/correlation index owner and normalized counter; pipeline absence without those witnesses is corruption/ambiguity, never evidence of pre-commit. For manual snapshot, capture the exact staged `SnapshotRecord` before the save and run durable-success inspection only when that save was actually attempted. For legacy redirect, derive the expected payload-free redirect digest including `TargetAdmissionActorId` before mutation and accept only that exact digest after clear. Concurrent replacements with the same sequence, message ID, range, or source digest but different remaining fields must fail closed.

Normalization may repair one malformed/one valid same-ID pair, but two well-formed same-ID entries that disagree on correlation are not interchangeable. Treat that persisted shape as corruption and preserve it for diagnosis/recovery instead of silently selecting an entry whose correlation can point activation at the wrong checkpoint. Malformed cleanup must also respect an activation I/O budget: charge every idempotency read/save or other state-backed cleanup, stop before the bound, and retain unprocessed index entries. User-visible refusal counts, log counts, capacity, and durable reconciliation all use `OwnerCount`; raw entry count remains diagnostic shape only.

Track whether execution reached the actual `SaveStateAsync` call before mapping an `InvalidOperationException` to a state-store concurrency conflict. Earlier mutator failures are infrastructure/remediation failures even when they share the exception type. At the outer state-bearing boundary, mark the process activity failed for every escaping `ActorStateRemediationException`, and mark the currently active child where one exists. When discard itself fails, diagnostics use the discard exception's type for the `Discard...` remediation operation while retaining the original save/mutator as the primary failure and the same discard type in the dedicated discard field.

Stale-handoff cancellation uses durable inspection only to make the cache and post-commit consequence safe; it never turns cancellation into success. If a save commits and then throws `OperationCanceledException`, preserve the committed handoff for later activation recovery, complete non-cancelable cleanup, propagate the original cancellation, and do not arm a reminder or process the incoming command in that canceled turn.

## Verification

**Commands:**
- `dotnet restore Hexalith.EventStore.slnx -p:UseHexalithProjectReferences=false` -- expected: solution restores with the repository-pinned SDK and package graph.
- `dotnet build tests/Hexalith.EventStore.Server.Tests/Hexalith.EventStore.Server.Tests.csproj --configuration Release -warnaserror -m:1 -p:UseHexalithProjectReferences=false` -- expected: focused test assembly builds without warnings.
- `dotnet tests/Hexalith.EventStore.Server.Tests/bin/Release/net10.0/Hexalith.EventStore.Server.Tests.dll -class Hexalith.EventStore.Server.Tests.Actors.AggregateActorInfrastructureFailureTests` -- expected: every focused failure/state lane passes.
- `dotnet tests/Hexalith.EventStore.Server.Tests/bin/Release/net10.0/Hexalith.EventStore.Server.Tests.dll` -- expected: full Server regression suite passes with only the pre-existing skips.
- `dotnet build Hexalith.EventStore.slnx --no-restore --configuration Release -warnaserror -m:1 -p:UseHexalithProjectReferences=false` -- expected: Release solution builds without warnings.
