---
title: 'Bump .NET SDK, Builds, and effective packages'
type: 'chore'
created: '2026-08-29'
status: 'in-progress'
review_loop_iteration: 2
baseline_commit: '62d28510f3c11904b6b2ce22edc075d55878924b'
tenants_baseline_commit: 'eb965727329c7d7335be4cd341db4e2f9bf57b56'
correction_baseline_commit: 'b34f252c6fbd103acd0e45168ff238a6badd726c'
builds_correction_baseline_commit: '18742168b0bcdc40e5223f7573b1dcca441d781f'
tenants_correction_baseline_commit: '70027ecb39aa0e16d81a8e0f7e275fba1db05147'
context:
  - '{project-root}/_bmad-output/project-context.md'
---

<frozen-after-approval reason="human-owned intent — do not modify unless human renegotiates">

## Intent

**Problem:** EventStore pins SDK `10.0.302`, which cannot resolve when only current SDK `10.0.400` is installed. The parent already points at latest Builds `main`, but its Roslyn 5.9, NBomber 6.6, and xUnit 4 families remain unreconciled with EventStore consumers and live documentation. The root-declared `Hexalith.Tenants` submodule also pins SDK `10.0.302`, preventing the EventStore AppHost's Tenants resources from remaining healthy under the aligned workspace SDK. Under SDK `10.0.400`, both repositories' legacy VSTest workflow paths fail before discovery because their xUnit projects use Microsoft.Testing.Platform.

**Approach:** Pin SDK `10.0.400` in EventStore and the already-initialized root-declared `Hexalith.Tenants` repository, retain the exact latest reachable Builds `main` and its audited catalog, migrate affected EventStore consumers, and synchronize only live snapshots in both owning repositories. Align both repositories and shared Builds workflows on Microsoft.Testing.Platform, add the latest stable centrally governed MTP coverage extension where needed, and preserve Tenants' blocking coverage gate. Re-review/reseal/rebind a successor Story 4.15 OQ8 closure for the required xUnit source change while retaining every prior sealed artifact, and restore the exact Story 1.20 closure comments required by the integrity guard. Prove Release package mode with routine and focused test-runner, generator, load-test, AppHost, Tenants documentation/coverage, and container checks.

## Boundaries & Constraints

**Always:** Resolve Builds `main` immediately before implementation and preserve its full SHA. Keep Builds as sole package authority, `rollForward: latestPatch`, `net10.0`, package mode, coupled adaptations, unrelated work, and historical evidence. Restrict the approved cross-repository change in `Hexalith.Tenants` to its SDK/MTP compatibility, live SDK snapshots, directly coupled tests/workflow/CI blockers, and coverage-provider migration. Preserve its blocking coverage thresholds and all nested submodule worktrees. Govern any required MTP coverage package/version and reusable-workflow behavior in Builds with its deterministic audit and contract tests. Preserve every prior OQ8 evidence byte and identity; use the repository-supported successor/review/reseal model for current-source authority, and restore the removed Story 1.20 comments byte-for-byte from the approved baseline.

**Ask First:** A newer Builds head introduces an uninvestigated package family beyond the human-approved MTP coverage extension; a latest family fails after reasonable consumer migration; preserving coverage requires lowering an existing threshold; or a real container check requires publication/registry credentials rather than a local archive.

**Never:** Add local versions/overrides, downgrade or suppress a catalog family, initialize or update nested submodule worktrees, use recursive/remote updates, change TFMs, delete or mutate prior sealed evidence/history, publish, commit, push, weaken warnings/auditing/coverage, or accept direct-assembly fallback evidence while maintained `dotnet test`/CI commands remain broken.

## I/O & Edge-Case Matrix

| Scenario | Input / State | Expected Output / Behavior | Error Handling |
|----------|--------------|---------------------------|----------------|
| Current upstream | Gitlink, checkout, and remote `main` all equal `18742168...` | Leave gitlink/catalog unchanged; update SDK and consumers | Record the no-op identity proof |
| Upstream advanced | Remote `main` resolves to a newer reachable commit | Advance only the root Builds checkout/gitlink and reassess its catalog delta | Stop on unrelated/local submodule changes |
| Latest family breaks a consumer | Warning-as-error or test failure is caused by Roslyn, NBomber, or xUnit | Apply the supported EventStore API/config migration and rerun the family lane | Ask before shared-catalog rollback or expansion |
| Tenants SDK mismatch | AppHost Tenants resources resolve the submodule's `10.0.302` pin while only SDK `10.0.400` is installed | Pin the already-initialized Tenants repository and its live SDK snapshots to `10.0.400`; rerun its documentation test and live topology | Preserve historical evidence and all other Tenants/submodule content |
| .NET 10 MTP/VSTest incompatibility | `dotnet test` uses the legacy VSTest target for an MTP-enabled xUnit project | Select `Microsoft.Testing.Platform` in each owning `global.json`; use MTP-native filters/TRX/coverage arguments in local and GitHub workflows | Fail before acceptance if any maintained test lane still reaches the VSTest-target error |
| MTP coverage gate | Tenants retains `run-coverage-gate: true` under MTP | Govern the latest stable Microsoft coverage extension in Builds, migrate the Tenants test provider, emit Cobertura at the existing paths, and run unchanged thresholds | Never disable or lower the coverage gate to make the migration pass |
| Frozen OQ8 source drift | Required xUnit 4 migration changes a Story 4.15-bound source file | Create and validate a successor re-review/reseal/rebinding that selects the current source identity while retaining all prior sealed artifacts | Stop rather than editing/deleting the prior seal or weakening integrity assertions |
| Maintained lane exposes baseline blocker | MTP reaches Tenants UI assertions or Linux Dapr placement that VSTest previously masked | Fix the narrow product/test or dynamic placement-port contract and rerun the complete maintained lane | Preserve product behavior and never skip/filter the failure merely to green CI |

</frozen-after-approval>

## Code Map

- `global.json:3-4` -- sole EventStore SDK selector; change `10.0.302` to `10.0.400`, preserving `latestPatch`.
- `global.json`, `.github/workflows/{ci,integration}.yml`, and `scripts/ci-local.sh` -- select Microsoft.Testing.Platform, replace VSTest-only logger/filter/coverage arguments, execute the new load-exit suite in routine Tier 1, and execute the lightweight IntegrationTests serialization assertion in a blocking maintained lane.
- `references/Hexalith.Tenants/global.json`, `docs/quickstart.md`, `_bmad-output/project-context.md`, `_bmad-output/planning-artifacts/architecture.md`, and `tests/Hexalith.Tenants.Server.Tests/Documentation/QuickstartDocumentationTests.cs` -- approved owning-repository SDK alignment and live snapshot/test update; preserve historical implementation, proposal, archive, investigation, and changelog evidence.
- `references/Hexalith.Tenants/.github/workflows/ci.yml`, `tests/Directory.Build.props`, and test-project coverage-provider metadata -- select MTP, replace rather than duplicate the VSTest-only coverage provider, preserve the existing coverage scopes/thresholds, and make the quickstart test compare documentation with parsed `global.json` rather than a duplicated literal.
- `references/Hexalith.Builds` -- root gitlink/check-out authority; currently remote-equal at `18742168b0bcdc40e5223f7573b1dcca441d781f`.
- `references/Hexalith.Builds/Props/Directory.Packages.props:196-201,242,252,317-320` and `Tools/package-version-audit.json` -- latest families and matching 285-row audit; consumer-read-only.
- `references/Hexalith.Builds/Props/Directory.Packages.props`, `Tools/package-version-audit.json`, `.github/workflows/domain-ci.{yml,md}`, and `Tools/test-domain-workflow-test-platforms.ps1` -- add the latest stable `Microsoft.Testing.Extensions.CodeCoverage`, emit MTP Cobertura for coverage-enabled callers, retain threshold validation, and replace the former deliberate-rejection contract with positive coverage contract tests.
- `perf/Hexalith.EventStore.LoadTests/Program.cs:34-36` -- replace obsolete aggregate `NodeStats.AllFailCount` with scenario/step failure statistics.
- `perf/Hexalith.EventStore.LoadTests/LoadTestExitCode.cs`, `tests/Hexalith.EventStore.LoadTests.Tests/`, and `Hexalith.EventStore.slnx` -- isolate and test success, scenario-failure, and step-failure exit classification.
- `tests/Hexalith.EventStore.{Server.LiveSidecar.Tests,IntegrationTests,Server.Tests}/AssemblyInfo.cs` -- replace xUnit 4-obsolete `CollectionBehavior(DisableTestParallelization=true)` with assembly `Parallelization(Mode=ParallelMode.None)` while preserving serialized execution.
- `tests/Hexalith.EventStore.{Server.LiveSidecar.Tests,IntegrationTests,Server.Tests}/AssemblyParallelizationTests.cs` -- reflect each built assembly and prove its xUnit 4 mode is `None`.
- `src/Hexalith.EventStore.RestApi.Generators/` and its test/DomainService consumers -- Roslyn 5.9 compatibility surface; change only compiler-proven incompatibilities.
- `CONTRIBUTING.md`, `docs/getting-started/prerequisites.md`, `docs/guides/{deployment-azure-container-apps,deployment-docker-compose,deployment-kubernetes,troubleshooting}.md`, `docs/brownfield/*.md`, `_bmad-output/project-context.md`, and `_bmad-output/planning-artifacts/architecture.md` -- live SDK/package snapshots only.
- `docs/brownfield/index.md` and `docs/reference/nuget-packages.md` -- keep solution/test-project counts and the effective xUnit assertion package identity/version synchronized with the added test project and Builds catalog.
- `Directory.Build.targets:21-102` and `CorrectiveOciProvenanceReleaseTests` -- SDK-internal container-label workaround requiring real 10.0.400 archive/provenance regression proof; comments are historical observations.
- `tools/validate-oq8-platform-evidence.py`, Story 4.15 OQ8 closure evidence/tests, and `_bmad-output/implementation-artifacts/sprint-status.yaml` -- human-authorized successor review/reseal/rebinding for the xUnit-migrated `Server.LiveSidecar.Tests/AssemblyInfo.cs`, retention of every prior sealed artifact, and byte-exact restoration of the removed Story 1.20 closure comments.
- `tests/Hexalith.EventStore.Server.LiveSidecar.Tests/Fixtures/Oq8PostgresqlFixture.cs` and its focused tests -- resolve the actual Docker-published `dapr_placement` host port on Linux instead of assuming container port `50005` is the host port.
- `references/Hexalith.Tenants/tests/Hexalith.Tenants.UI.Tests/` and directly failing owned UI/localization surfaces -- fix the 12 MTP-exposed structural, safety, localization-double, behavior, and timing regressions without changing coverage thresholds or filtering tests.

## Tasks & Acceptance

**Execution:**

- [ ] `global.json` and Builds gitlink -- pin SDK `10.0.400`; re-resolve latest Builds and change the gitlink only if upstream advanced.
- [x] Package consumers above -- migrate NBomber/xUnit APIs and address only demonstrated Roslyn 5.9 compatibility failures.
- [x] Regression tests above -- prove both load-harness exit outcomes and the three assemblies' effective xUnit 4 serialization metadata.
- [x] MTP and maintained test lanes -- select MTP in root/Tenants, migrate root/custom/local workflows, add LoadTests.Tests to routine Tier 1, and make the IntegrationTests metadata assertion blocking.
- [x] Builds/Tenants coverage compatibility -- centrally govern the latest stable MTP coverage extension, update reusable workflow contracts, replace Tenants' provider, and preserve its existing coverage gate and thresholds.
- [x] Live documentation surfaces -- synchronize current SDK, Roslyn, NBomber, xUnit, solution counts, and assertion-package identity without touching historical/frozen evidence.
- [x] `Hexalith.Tenants` SDK/live surfaces -- pin SDK `10.0.400`, update only approved live SDK/MTP/coverage surfaces and coupled tests, and preserve its historical evidence/nested worktrees.
- [ ] OQ8 successor authority -- preserve prior Story 4.15 evidence, create/review/reseal/rebind the current xUnit-migrated source identity, restore exact Story 1.20 comments, and pass all integrity/closure validators.
- [ ] Maintained-lane blockers -- fix all 12 Tenants UI failures and the Linux dynamic Dapr placement-port contract; rerun full UI and LiveSidecar MTP lanes with TRX/Cobertura.
- [ ] Validation lanes -- run exact Builds governance/workflow contracts, package-mode restore/build, maintained MTP test commands, focused consumers/tests, live AppHost state, Tenants coverage, and SDK container provenance/archive commands below.

**Acceptance Criteria:**

- Given the approved dependency refresh, when identity preflight runs, then EventStore selects SDK `10.0.400`, Builds checkout/gitlink equals the latest reachable remote `main`, and its catalog exactly matches the deterministic audit.
- Given the latest catalog, when Release package-mode consumers build and focused tests run, then Roslyn 5.9 generators compile, NBomber uses supported statistics, xUnit 4 preserves required serialization, and no warnings/errors or stale dependency assets remain.
- Given SDK `10.0.400`, when container regression validation runs, then both local target architectures retain exact provenance labels and no internal-target/type-load regression occurs.
- Given current documentation, when stale-snapshot checks run, then live SDK/package claims match effective pins while historical and checksum-bound evidence remains byte-unchanged.
- Given the human-approved cross-repository SDK alignment, when Tenants documentation verification and the EventStore AppHost run, then Tenants selects SDK `10.0.400`, its live snapshots agree, and the Tenants resources remain runnable/healthy without SDK-resolution errors.
- Given SDK `10.0.400` and MTP-enabled xUnit projects, when maintained local and GitHub-equivalent commands run, then no lane reaches the VSTest-target incompatibility, the load-exit and serialization guards run in routine blocking lanes, and MTP-native TRX artifacts are produced.
- Given Tenants' existing coverage policy, when MTP tests run through the shared Builds workflow contract, then Cobertura evidence is emitted at the validator's existing paths and the unchanged line/branch/isolation thresholds pass.
- Given the human-authorized OQ8 successor, when Contracts/Tier 1 integrity validation runs, then the current xUnit-migrated source identity is selected through a reviewed/resealed successor, all prior sealed bytes remain unchanged, exact Story 1.20 closure comments are restored, and every OQ8/proof-packet test passes.
- Given MTP reaches previously masked maintained tests, when Tenants UI and root LiveSidecar lanes run, then all UI tests pass and the OQ8 production test discovers the Docker-published placement host port on Linux; no test is skipped or filtered to conceal either failure.

## Spec Change Log

- 2026-08-29: Implemented SDK and consumer migrations, synchronized live version snapshots, and completed the focused validation matrix.
- 2026-08-29 review loop 1 (`bad_spec`): verification review found compilation-only coverage for the new NBomber exit branch and xUnit serialization plus non-reproducible placeholder commands. Added durable behavior/metadata tests, exact commands, live AppHost state verification, and final HEAD/diff/submodule binding; this avoids a false green where migrated code compiles but exit/serialization/runtime behavior is unproved. **KEEP:** SDK `10.0.400`, latest Builds no-op/package authority, the working scenario/step and `Parallelization` migrations, scoped live-doc updates, historical-evidence preservation, and the successful package/container validation lanes.
- 2026-08-29 human scope renegotiation: live topology verification found the root-declared `Hexalith.Tenants` repository still pinned to SDK `10.0.302`; the user explicitly approved extending the change to align that repository to SDK `10.0.400`. Added its owning-repository pin, live snapshots, coupled documentation test, baseline binding, and topology health proof while retaining the no-commit/no-push and no-nested-submodule constraints.
- 2026-08-29 review loop 2 (`bad_spec`, human-expanded submodule scope): the supplied GitHub runs `33248201871` and `33248201727` confirmed that SDK `10.0.400` broke maintained EventStore, Contracts, Tenants source-mode, and live-sidecar `dotnet test` lanes before discovery because the spec omitted the MTP runner/workflow migration. Review also found the new load-exit suite and IntegrationTests metadata guard absent from routine blocking lanes, VSTest-only coverage incompatible with Tenants' gate, masked semicolon command failures, non-reproducible Aspire health evidence, a duplicated Tenants documentation literal, and stale live project/package snapshots. Added owning-repository MTP configuration, Builds-governed latest stable coverage support with positive contracts, unchanged Tenants coverage thresholds, routine guard execution, fail-fast exact verification, and durable diff binding. Known-bad state avoided: direct-assembly tests appearing green while both push workflows fail before discovery or coverage is silently weakened. **KEEP:** both SDK `10.0.400` pins, latest Builds authority/no local overrides, working NBomber scenario/step classifier and tests, xUnit `Parallelization(Mode=None)` migrations and reflection guards, synchronized live SDK/package docs, deterministic audit, 0-warning Release package build, dual-architecture provenance proof, 17/17 healthy warm AppHost proof, preserved historical evidence, and all concurrent human-authored commits/history. Because those KEEP changes were captured and pushed concurrently at root `b34f252c` and Tenants `70027ecb`, they are preserved rather than reverse-edited in the worktree; re-derivation applies the corrective delta on top.
- 2026-08-29 human authority resolution during review loop 2: maintained MTP execution proved that the required xUnit migration invalidates Story 4.15's current-source binding and that the external root commit removed exact Story 1.20 integrity comments; it also exposed 12 Tenants UI failures and a Linux placement host-port assumption. The user explicitly authorized re-review/reseal/rebinding frozen OQ8 authority and restoration of the comments, while prior sealed artifacts must remain historical records, and had already authorized fixes in submodules as needed. Added successor evidence selection, exact comment restoration, full Contracts/Tier 1 proof, narrow UI repairs, and dynamic placement-port validation; this avoids either weakening integrity to accept drift or leaving maintained CI red after fixing discovery.

## Verification

**Commands:**

- `set -euo pipefail; dotnet --version; dotnet --info; jq -e '.test.runner == "Microsoft.Testing.Platform"' global.json; (cd references/Hexalith.Tenants && dotnet --version && jq -e '.test.runner == "Microsoft.Testing.Platform"' global.json)` -- expected: both owning repositories resolve SDK `10.0.400` and select MTP.
- `set -euo pipefail; pwsh -NoProfile -File references/Hexalith.Builds/Tools/validate-central-package-versions.ps1 && pwsh -NoProfile -File references/Hexalith.Builds/Tools/test-authoritative-package-catalog.ps1 && pwsh -NoProfile -File references/Hexalith.Builds/Tools/validate-package-version-audit.ps1 && pwsh -NoProfile -File references/Hexalith.Builds/Tools/validate-package-version-exceptions.ps1 -InventoryPath references/Hexalith.Builds/Tools/package-version-exceptions.json -CatalogPath references/Hexalith.Builds/Props/Directory.Packages.props && pwsh -NoProfile -File references/Hexalith.Builds/Tools/validate-dapr-package-versions.ps1 && pwsh -NoProfile -File references/Hexalith.Builds/Tools/validate-consumer-package-authority.ps1 -RepositoryRoot . -CatalogPath references/Hexalith.Builds/Props/Directory.Packages.props && pwsh -NoProfile -File references/Hexalith.Builds/Tools/test-domain-workflow-test-platforms.ps1` -- expected: catalog/audit include latest stable `Microsoft.Testing.Extensions.CodeCoverage` (NuGet source reported `18.10.0` during review) and every governance/positive MTP coverage workflow contract passes.
- `dotnet restore Hexalith.EventStore.slnx -p:Configuration=Release -p:UseHexalithProjectReferences=false && dotnet build Hexalith.EventStore.slnx --no-restore --configuration Release -warnaserror -m:1 -p:UseHexalithProjectReferences=false` -- expected: 0 warnings/errors.
- `set -euo pipefail; dotnet tests/Hexalith.EventStore.LoadTests.Tests/bin/Release/net10.0/Hexalith.EventStore.LoadTests.Tests.dll && dotnet tests/Hexalith.EventStore.RestApi.Generators.Tests/bin/Release/net10.0/Hexalith.EventStore.RestApi.Generators.Tests.dll && dotnet tests/Hexalith.EventStore.DomainService.Tests/bin/Release/net10.0/Hexalith.EventStore.DomainService.Tests.dll && dotnet tests/Hexalith.EventStore.AppHost.Tests/bin/Release/net10.0/Hexalith.EventStore.AppHost.Tests.dll` -- expected: all pass, no skips.
- `set -euo pipefail; dotnet tests/Hexalith.EventStore.IntegrationTests/bin/Release/net10.0/Hexalith.EventStore.IntegrationTests.dll -class Hexalith.EventStore.IntegrationTests.AssemblyParallelizationTests && dotnet tests/Hexalith.EventStore.Server.LiveSidecar.Tests/bin/Release/net10.0/Hexalith.EventStore.Server.LiveSidecar.Tests.dll -class Hexalith.EventStore.Server.LiveSidecar.Tests.AssemblyParallelizationTests && dotnet tests/Hexalith.EventStore.Server.Tests/bin/Release/net10.0/Hexalith.EventStore.Server.Tests.dll -class Hexalith.EventStore.Server.Tests.AssemblyParallelizationTests` -- expected: 1/1 each.
- `set -euo pipefail; dotnet test tests/Hexalith.EventStore.LoadTests.Tests/Hexalith.EventStore.LoadTests.Tests.csproj --no-build --configuration Release --results-directory TestResults/Hexalith.EventStore.LoadTests.Tests --report-xunit-trx --report-xunit-trx-filename Hexalith.EventStore.LoadTests.Tests.trx && ./scripts/ci-local.sh --tier 1 --skip-build` -- expected: the maintained MTP path (including the new suite) passes and emits TRX; the VSTest-target error is absent.
- `set -euo pipefail; python3 tools/validate-oq8-platform-evidence.py; dotnet tests/Hexalith.EventStore.Contracts.Tests/bin/Release/net10.0/Hexalith.EventStore.Contracts.Tests.dll` -- expected: the validator selects the human-authorized successor, all prior sealed evidence remains byte-identical, exact Story 1.20 comment integrity passes, and Contracts completes with every test passing (no 269-case drift cascade or proof-packet comment failure).
- Run the exact MTP-native commands encoded by `.github/workflows/ci.yml` for Contracts and Tenants source-mode plus `.github/workflows/integration.yml` for live-sidecar and the focused IntegrationTests metadata guard -- expected: all execute tests, emit the declared TRX artifacts, and never invoke VSTest-only `--logger`, `--collect`, or `--filter` arguments.
- `dotnet tests/Hexalith.EventStore.Contracts.Tests/bin/Release/net10.0/Hexalith.EventStore.Contracts.Tests.dll -class Hexalith.EventStore.Contracts.Tests.Packaging.CorrectiveOciProvenanceReleaseTests` -- expected: all pass, including real local dual-architecture archive labels.
- `set -euo pipefail; (cd references/Hexalith.Tenants && dotnet restore Hexalith.Tenants.slnx -p:Configuration=Release && dotnet build Hexalith.Tenants.slnx --no-restore --configuration Release -warnaserror -m:1 && dotnet tests/Hexalith.Tenants.Server.Tests/bin/Release/net10.0/Hexalith.Tenants.Server.Tests.dll -class Hexalith.Tenants.Server.Tests.Documentation.QuickstartDocumentationTests)` -- expected: the owning repository resolves SDK `10.0.400`, builds without warnings/errors, and documentation matches parsed `global.json`.
- Run every Tenants project listed in `.github/workflows/ci.yml` through its exact MTP-native command with coverage enabled, Cobertura output beneath `TestResults/<project>/coverage.cobertura.xml`, and TRX output; then run its existing `scripts/validate-coverage.py` command with the unchanged 80% line, 100% required-branch, scope, and isolation arguments -- expected: all tests and the unchanged coverage gate pass.
- Run the complete `Hexalith.Tenants.UI.Tests` MTP command without filters after the targeted fixes -- expected: all `2556` tests (or the newly discovered total) pass, including structural HTML, identifier-safety, localization-double, behavior, and timing coverage; TRX/Cobertura are produced.
- Run `.github/workflows/integration.yml`'s exact LiveSidecar MTP command against initialized Dapr and PostgreSQL -- expected: all `106` tests (or the newly discovered total) pass, including OQ8 production evidence using the dynamically resolved `dapr_placement` host port; TRX/Cobertura are produced.
- `set -euo pipefail; apphost=src/Hexalith.EventStore.AppHost/Hexalith.EventStore.AppHost.csproj; aspire start --apphost "$apphost" --non-interactive --format Json; aspire wait security --status healthy --timeout 180 --apphost "$apphost" --non-interactive; aspire describe --apphost "$apphost" --non-interactive --format Json | tee /tmp/eventstore-aspire-describe.json; jq -e 'length == 17 and all(.[]; (.state | ascii_downcase) == "running" and (.healthStatus | ascii_downcase) == "healthy")' /tmp/eventstore-aspire-describe.json; aspire otel logs --apphost "$apphost" --severity Error --limit 1000 --format Json --non-interactive | tee /tmp/eventstore-aspire-errors.json; jq -e 'length == 0' /tmp/eventstore-aspire-errors.json; aspire stop --apphost "$apphost"` with cleanup trap -- expected: 17/17 Running+Healthy, zero Error telemetry, and no EventStore AppHost remains.
- `set -euo pipefail; bash scripts/check-doc-versions.sh; git diff --check; git -C references/Hexalith.Builds diff --check; git -C references/Hexalith.Tenants diff --check; git status --porcelain=v1 --untracked-files=all --ignore-submodules=none` plus scoped stale-version, solution-count, package-identity, and evidence-path scans -- expected: only intended/categorized concurrent paths, no live stale pins/counts, whitespace errors, unexplained submodule drift, or evidence changes.
- Record final root/Builds/Tenants `git rev-parse HEAD`, `git status`, `git submodule status`, each owning-repository diff hash excluding this self-updating spec, the current spec byte hash, authoritative remote/gitlink identities, the two supplied red GitHub run URLs as red-phase evidence, and exact commands/counts in new Results -- expected: verification durably binds the reviewed state despite concurrent commits without claiming an impossible self-referential full-diff hash.

## Superseded Results (review loop 1; do not use as current acceptance evidence)

- **Identity and dependency authority:** `dotnet --version && dotnet --info` resolved SDK `10.0.400` from the root `global.json`. The approved root baseline remains `62d28510f3c11904b6b2ce22edc075d55878924b`; validation ran at root HEAD `2aa94e805dc8c46d8a50b524f58da2070cd22307`, then the complete validated worktree was concurrently captured by root commit `b34f252c6fbd103acd0e45168ff238a6badd726c` (`fix: update .NET SDK version to 10.0.400 across documentation and project files`). `git ls-tree HEAD references/Hexalith.Builds` plus `git -C references/Hexalith.Builds rev-parse HEAD` and `git -C references/Hexalith.Builds rev-parse refs/remotes/origin/main` all returned `18742168b0bcdc40e5223f7573b1dcca441d781f`; Builds remained clean and its root gitlink/catalog were a no-op.
- **Catalog governance:** the exact six PowerShell commands above passed: central-package validation covered 285 entries; authoritative-catalog tests covered 49 approved identities and 3 shared versions; the deterministic audit covered 285 packages, 140 families, and 1 source; exception validation covered 15 entries; DAPR validation covered 8 unique IDs at `1.18.5`; consumer-authority validation covered 52 projects. Effective catalog evidence was Roslyn `5.9.0`, NBomber `6.6.0`, and xUnit `4.0.0`; no Builds edit or local override was made.
- **Package-mode build and focused tests:** the exact Release restore/build command above completed with 0 warnings and 0 errors. The four executable suites passed with no skips: LoadTests `3/3`, RestApi.Generators `124/124`, DomainService `155/155`, and AppHost `96/96` (378 total). The three assembly metadata commands each passed `1/1`, proving `ParallelMode.None` for IntegrationTests, Server.LiveSidecar.Tests, and Server.Tests. `CorrectiveOciProvenanceReleaseTests` passed `46/46` with no skips, exercising the real local dual-architecture SDK container archive/provenance path under SDK `10.0.400`.
- **Tenants owning-repository lane:** the recorded Tenants baseline remains `eb965727329c7d7335be4cd341db4e2f9bf57b56`. The checkout was already concurrently fast-forwarded to `435771779e32f357ded47988b294dc1da4029a4a` before the SDK edits. It then advanced concurrently to `c085d43291c98a9aab685d00cdf52f68595640f1` (`build(deps): bump .NET SDK version to 10.0.400`), which captured four approved SDK/live-documentation files, and finally to `70027ecb39aa0e16d81a8e0f7e275fba1db05147` (`fix: update .NET SDK version to 10.0.400 in architecture documentation`), which captured the remaining approved architecture snapshot. The Tenants checkout, index, and root gitlink now all equal `70027ecb39aa0e16d81a8e0f7e275fba1db05147`; `git -C references/Hexalith.Tenants status --porcelain=v1 --untracked-files=all --ignore-submodules=none` returned no paths and `git -C references/Hexalith.Tenants diff HEAD | sha256sum` returned the empty-diff hash `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`. The exact Tenants Release build passed with 0 warnings/errors and `QuickstartDocumentationTests` passed `6/6` with no skips. All seven Tenants nested submodules remain uninitialized at their recorded identities; none was initialized or changed. The 25 files still mentioning `10.0.302` are preserved changelog/archive/proposal/investigation/completed-story evidence.
- **Live AppHost proof:** Aspire CLI `13.5.3` started the specified AppHost successfully. The first cold run reached all `17/17` resources `Running` and `Healthy`; one handled startup-time Polly retry logged Error when the Tenants operational-index metadata endpoint returned `500`, followed by repeated `200` responses. After a clean `aspire stop`, the same `aspire start`, resource waits, `aspire describe`, and `aspire otel logs --severity Error --limit 1000` sequence was rerun warm: all `17/17` resources were `Running` and `Healthy`, including `tenants`, `tenants-api`, and their DAPR executables; the Error query returned `[]`. Final `aspire stop` succeeded and `aspire ps --non-interactive --format Json` returned `[]`, leaving no AppHost running.
- **Documentation, evidence, and worktree binding:** `bash scripts/check-doc-versions.sh` passed the 4-row DAPR consistency check; both root and Tenants `git diff --check` passed. A final concurrent-status rerun found only one trailing space after `story_location:` in `sprint-status.yaml`; that space was removed without changing the concurrent value or structure, and the gate then passed again. Scoped live scans returned zero stale `10.0.302`, Roslyn `5.6.0`, NBomber `6.5.0`, or xUnit `3.2.2` claims, while historical/evidence-path dirty scans returned no paths. After concurrent root commit `b34f252c6fbd103acd0e45168ff238a6badd726c` and before this identity-only Results refresh, both `git diff HEAD | sha256sum` and the root diff excluding this spec returned the empty-diff hash `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`; root status contained 0 tracked/submodule paths and 0 untracked paths. `git submodule status` cleanly bound AI.Tools `de38f78e`, Builds `18742168`, Commons `feab4efc`, FrontComposer `85216682`, Memories `e0ecafe6`, PolymorphicSerializations `65fc3361`, and Tenants `70027ecb`; the root Tenants gitlink, index, and checkout all agree.
- **Risks:** the validation agent made no commit or push; Tenants commits `c085d432` and `70027ecb` plus root commit `b34f252c` were concurrent external mutations and are recorded explicitly for provenance. The formerly dirty approved architecture snapshot is now contained by `70027ecb`, Tenants is clean, and the root gitlink is synchronized, so no submodule-drift risk remains. The cold-start retry is a startup-order observation, not an SDK-resolution failure; the required clean warm proof had zero Error telemetry and every resource healthy. A separate FrontComposer AppHost appeared concurrently after the EventStore cleanup; it was preserved as unrelated state, while the prior EventStore-filtered `aspire ps` result was `[]`.

## Superseded Results (review loop 2 corrective delta; blockers authorized for resolution)

- **Implemented MTP ownership and maintained lanes:** both owning `global.json` files select `Microsoft.Testing.Platform`; root reusable/custom/integration/local lanes use MTP-native TRX, coverage, and class/trait arguments; routine Tier 1 includes `Hexalith.EventStore.LoadTests.Tests`; the IntegrationTests serialization guard is blocking; `scripts/ci-local.sh` is executable. Root tests receive the Microsoft coverage extension through `tests/Directory.Build.props`. Tenants passes `test-platform: microsoft-testing-platform`, replaces every owned `coverlet.collector` reference with the Microsoft provider, migrates its IntegrationTests assembly to `Parallelization(Mode=None)`, and derives the quickstart SDK expectation from `global.json`.
- **Implemented shared coverage authority:** Builds centrally pins `Microsoft.Testing.Extensions.CodeCoverage` `18.10.0`; the reusable workflow conditionally emits per-project Cobertura for coverage-gated MTP callers; documentation and positive/negative workflow contracts were updated; the live deterministic audit was regenerated. All Builds validators passed: 286 central versions, 50 authoritative IDs/3 shared versions, 286 audited packages/141 families/1 source, 15 exceptions, 8 DAPR IDs at `1.18.5`, 53 consumer projects, and 23 workflow assertions.
- **Root compilation and focused proof:** Release package-mode restore/build passed with 0 warnings/errors. Direct suites passed LoadTests `3/3`, RestApi.Generators `124/124`, DomainService `155/155`, and AppHost `96/96`; all three root serialization guards passed `1/1`. Maintained MTP proof produced the declared TRX/Cobertura artifacts and passed LoadTests `3/3`, AppHost source-mode filter `10/10`, and the Contracts release-manifest filter `101/101`. `CorrectiveOciProvenanceReleaseTests` passed `46/46`. The first corrective build failure (`SelfRegisteredExtensions.cs` could not compile the CodeCoverage namespace) was fixed by retaining the provider's `compile` asset and the exact build then passed.
- **Tenants coverage proof:** MTP TRX and Cobertura were produced for Contracts `122/122`, Client `50/50`, Testing `181/181`, Sample `39/39`, Server `747/747`, and UI `2544/2556`. The first Server coverage run exposed Microsoft coverage tracker's injected writable static fields; `StatelessHostStateTests` now excludes only the provider namespace and the full `747/747` rerun passed. The unchanged validator passed at `95.59%` line (`1150/1203`) and `100%` isolation/auth branch (`116/116`) against the unchanged `80%`/`100%` thresholds and targets. Quickstart documentation passed `6/6`. UI's 12 failures are existing product/test drift (structural HTML budget, prohibited `ToString` safety checks, stale localizer doubles, and UI behavior assertions), not coverage argument/provider failures.
- **Runtime proof:** the exact local dual-architecture OCI lane passed `46/46`. Aspire cold and warm starts each reached `17/17` resources Running and Healthy, including Tenants; the warm Error telemetry query returned `[]`; final stop succeeded and the immediate `aspire ps` returned `[]`. Aspire `13.5.3` returns `describe` as `{resources:[...]}`, so the spec's top-level-array `jq` expression returned `false`; the schema-correct `.resources` expression enforced the same count/state/health predicate and passed. A later global `aspire ps` showed concurrent Memories and Tenants AppHosts from sibling workspaces; neither is this EventStore AppHost, so both were preserved as unrelated state.
- **Known validation blockers, not accepted:** `./scripts/ci-local.sh --tier 1 --skip-build` runs through MTP after its executable bit was corrected, but the full Contracts project reports `1519/1789` pass and 270 failures: 269 OQ8 cases cascade from `Current bound source identity drift: tests/Hexalith.EventStore.Server.LiveSidecar.Tests/AssemblyInfo.cs`, and `ProofPacketValidatorIntegrityTests.PacketAuthorizingSprintTransformMatchesNestedBlockerComments` reports the correction baseline's absent exact Story 1.20 closure comments. Direct OQ8 validation emits the same source-identity diagnostic. Current AssemblyInfo SHA-256 `36891fd8...6688c` differs from frozen Story 4.15 `20c033d8...2406c`; the validator has no additive supersession lookup, and the repository's previous repair process requires re-review/reseal/rebinding, which this spec forbids. No evidence byte, sprint status, integrity assertion, or validator was changed.
- **Environment/topology blockers:** exact Tenants `Hexalith.Tenants.slnx` restore cannot resolve the intentionally uninitialized nested dependency projects; standalone package mode reaches pre-existing FrontComposer component drift. The available source graph compiles the migrated IntegrationTests assembly with 0 warnings/errors, but its partial-graph MTP run is non-acceptance evidence (`26/149` pass; missing version-`3.99.0` EventStore runtime assemblies). The exact root LiveSidecar MTP lane produced TRX/Cobertura and passed `105/106`; only the OQ8 production case failed because Linux fixture port `50005` refused while the installed placement container publishes host `6050`, a previously documented environmental/source prerequisite outside this corrective delta.
- **Integrity and identity:** `check-doc-versions.sh` and all three `git diff --check` commands passed; owned test configuration contains no `coverlet.collector`; root MTP workflow/local blocks contain no VSTest-only `--logger`, `--collect`, or `--filter` option. Evidence and sprint-status diff counts are both zero. Root/Builds/Tenants HEADs remain `b34f252c6fbd103acd0e45168ff238a6badd726c`, `18742168b0bcdc40e5223f7573b1dcca441d781f`, and `70027ecb39aa0e16d81a8e0f7e275fba1db05147`; both submodule checkouts equal their root gitlinks and local `origin/main`. Diff hashes are root excluding this spec `6ee10dce38244592b72aa84c98a6bea283006b182ee4cdcf91ba5899684c5f84`, Builds `305f5d678e508cfa573f84e0466a7608c33d82eb51fbee00cf86aff8d34a664d`, and Tenants `ac2b013368355d826902c5e04fd6b12e5c589e30c46d75ee5a8487c91f02d791`. No remote fetch, commit, push, nested-submodule initialization, publication, evidence rewrite, or sprint-status edit was performed; therefore only the already-present local `origin/main` identities, not a freshly fetched upstream, are proven. The Builds identity task and validation-lanes task remain open because that limit plus the authority and environment blockers above prevent complete acceptance.
