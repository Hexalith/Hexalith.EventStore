# Story: admin-ui-health-dapr-truthfulness-fix

Status: review

Round-10 patches applied 2026-05-10: 13 patches landed across Admin.Abstractions DTO defaults, Admin.Server services (cold-start predicate, synth-row source upgrade, captured-now in probe catch, FormatStateValue size cap, Stage-1 Degraded for non-state-store local-fallback), Admin.UI pages (OnRefreshError isLoading reset, count buckets, actor inventory label, source summary text, default arm); 3 verified-already-correct (P1 stale gradient, P5 case-insensitive matching, P11 NotConfigured skip); 4 deferred (M-tier P8 ordering tiebreaker, P12 leak test scope, P13 test seam, L-tier P19 test perf). All tests pass: Admin.Abstractions 404/404, Admin.Server 600/600 (+18 skipped), Admin.UI 769/769, Contracts 291/291, Client 362/362. Story stays at `review` for the next reviewer pass.

Context created: 2026-05-07
Source proposal: `_bmad-output/planning-artifacts/sprint-change-proposal-2026-05-07-admin-ui-manual-test-suite-issues.md`
Triggering evidence: `_bmad-output/test-artifacts/admin-ui-manual-test-guide-issues-2026-05-07.md`
Scope: Issues #6, #7, and #8 only.

## Story

As an EventStore operator using the Admin UI,
I want the health and DAPR infrastructure pages to keep working when Redis is down and to report metric/component availability honestly,
so that I can diagnose state-store and pub/sub failures without seeing blank pages, fake zeros, or missing DAPR components.

## Operator Decision Outcome

After this fix, an operator can determine from the Admin UI whether the system is operational, degraded by state-store failure, missing DAPR metadata, reporting valid zero activity, or showing stale/unknown evidence. The pages should answer "what can I trust right now?" before they answer "what numbers are available?"

## Issue Traceability

| Issue | Failing symptom | Covered by | Required evidence |
| --- | --- | --- | --- |
| #6 | `/health` becomes blank, stuck, or globally failed when Redis/state-store is unavailable. | AC1, AC2, AC7, AC8 | Server partial-report tests; Health page no-blank tests; Redis stop/start Aspire evidence. |
| #7 | Home and Health disagree on metric truthfulness; unavailable metrics render as fake zero on `/health`. | AC3, AC7, AC8 | Shared metric rendering tests; valid-zero vs unavailable fixtures; Home/Health screenshots or payload notes. |
| #8 | DAPR pages disagree about components/pubsub/subscriptions/history because metadata sources are mixed or hidden. | AC4, AC5, AC6, AC7, AC8 | Canonical inventory tests; remote sidecar metadata fixtures; cross-page component/subscription comparison evidence. |

## Operator Truth Contract

The implementation must make these states distinguishable in API payloads, UI labels, and tests. Do not infer health from numeric defaults or from a component being absent from a locally scoped sidecar.

Truth model:

- Health means whether a dependency is usable now for Admin/EventStore operations.
- Metrics mean measured activity values plus explicit value availability status.
- Inventory means what a named DAPR sidecar reported as loaded/configured/active.
- History means sampled evidence plus persistence/read status.
- Unknown or unavailable evidence must never render as healthy, zero, or empty.

Allowed status vocabulary:

| Term | Meaning | UI rule |
| --- | --- | --- |
| `Healthy` | A dependency probe or component health check succeeded. | May render as healthy/green only when backed by current evidence. |
| `Degraded` | The system remains operational but a non-Admin dependency (typically remote sidecar metadata) is `Unreachable` or `InvalidPayload`. The Admin sidecar and configured state store are usable; canonical inventory is incomplete but the API still serves requests. | Must render as non-green/yellow with an explanation of the missing evidence. Do not aggregate Degraded rows as healthy. |
| `Unhealthy` | A dependency probe failed or a required operational dependency is not usable. | Must render as non-green and explain the affected dependency. |
| `Available` | A metric or metadata value was successfully computed from current evidence. | Values, including zero, may be displayed as real data. |
| `Unavailable` | A metric, metadata source, or history source could not be read. | Must not be displayed or aggregated as zero or healthy. |
| `Stale` | Last-good data is being reused after a newer fetch failed. | Must show stale label plus timestamp/source where available. The value-based severity gradient is preserved (a 50% stale error rate renders as `error`-severity with a "(stale)" suffix, not flattened to warning). |
| `NotConfigured` | A metadata source endpoint is intentionally absent from configuration, OR the call site deliberately did not consult the remote source for this result. | Must explain that remote evidence is not configured / not consulted. |
| `Unreachable` | A configured metadata source could not be reached within the bounded probe. | Must explain that remote evidence is unavailable, not empty. |
| `InvalidPayload` | A metadata source responded but could not be parsed or lacked required shape. | Must explain invalid metadata and avoid fake component/subscription counts. |

Avoid unmapped synonyms such as "missing", "empty", "offline", or "issue" in API fields or assertions. Visible UI copy may be friendlier, but tests must map it back to this vocabulary.

| Scenario | API contract | UI contract | Inventory/history contract |
| --- | --- | --- | --- |
| Redis or configured DAPR state store is unreachable | `/api/v1/admin/health` returns HTTP 200 with a partial `SystemHealthReport`, bounded by the configured probe timeout. Overall status is `Unhealthy`; the state-store component row is `Unhealthy` or `Unavailable`; dependent metrics use `SystemHealthMetricStatus.Unavailable`; exception details are not exposed. | `/health` renders the partial report, shows the state-store outage, exits loading, and labels unavailable metrics as `unavailable`. | `/dapr` and `/dapr/health-history` keep named state-store evidence visible. History may show the current sample as unavailable, but must not silently overwrite a previous healthy sample with an empty component set. |
| Redis down while remote EventStore metadata is reachable | Health reports state-store `Unhealthy` while remote metadata status remains `Available`. | `/health` is non-green for state-store health; DAPR pages still show remote pub/sub facts as current. | Proves pub/sub visibility is not tied to Redis health. History persistence may be `Unavailable` if it depends on the failed state store. |
| Admin.Server local DAPR sidecar metadata is unavailable | Health returns a partial report with local sidecar metadata marked unavailable, unless the request itself is cancelled. | Health and DAPR pages show an issue state instead of an empty healthy inventory. | Local-admin-sidecar-only fields are labelled unavailable; remote EventStore metadata may still be shown if reachable. |
| Remote EventStore sidecar metadata is not configured or unreachable | The DAPR infrastructure API exposes `RemoteMetadataStatus.NotConfigured` or `RemoteMetadataStatus.Unreachable`; it does not convert this into zero pub/sub components or zero active subscriptions. | `/dapr`, `/dapr/pubsub`, and `/dapr/health-history` show remote metadata unavailable with the last reliable local facts, if any. | Pub/sub inventory and active subscriptions are `unavailable`, not `0`, unless the remote payload was successfully read and genuinely contained none. |
| Remote EventStore sidecar responds with malformed or incomplete metadata | The DAPR infrastructure API exposes `RemoteMetadataStatus.InvalidPayload`; parsing errors do not escape as raw 500 details. | DAPR pages show invalid remote metadata and avoid component/subscription totals derived from partial parsing. | Prior samples may be shown as `Stale`; new history samples mark the remote source invalid/unavailable. |
| Remote EventStore sidecar is reachable and reports `statestore` plus `pubsub` | Shared DAPR inventory includes both components with named source metadata. | `/health`, `/dapr`, `/dapr/pubsub`, and `/dapr/health-history` agree on component names and active subscription counts, or label configured vs active differences. | Health history records the same canonical component set used by `/dapr`. |
| Remote EventStore sidecar is reachable and genuinely reports no pub/sub/subscriptions | Remote metadata status is `Available`; component/subscription counts may be `0` only because the successful payload contained no matching entries. | DAPR pages label this as real zero active/configured pub/sub data, not unavailable. | Tests must distinguish valid empty payload from failed metadata. |
| Metric value is genuinely zero | Metric status is `Available` and the value is `0`. | Home and Health render real zero using page-specific formatting. | Tests must distinguish this from unavailable values. |
| Metric source is unavailable | Metric status is `Unavailable`; numeric fallback values are ignored. | Home and Health render `unavailable`, never `0`, `0.0/s`, or `0.0%`. | No charts or summaries may treat unavailable metrics as zero activity. |
| Health-history persistence fails | Live health and inventory APIs may still return the current in-memory sample if available. History write/read status is `Unavailable` or `Stale`, not success with an empty timeline. | `/dapr/health-history` shows history storage unavailable or stale with timestamp/source. | Prior persisted samples are retained if readable; unreadable history is labelled unavailable. |

Health evidence and inventory evidence are related but not identical. Health answers "is this dependency usable for Admin/EventStore operations now?" Inventory answers "what did a named sidecar report as loaded/configured/active?" A component can be absent from the Admin sidecar but present in the EventStore sidecar; that is an inventory source distinction, not proof that pub/sub is healthy or missing.

Conflict rules:

| Evidence conflict | Required result |
| --- | --- |
| Remote EventStore metadata says `pubsub` exists, but local Admin metadata omits it. | Show remote `pubsub` with source `RemoteEventStoreMetadata`; do not treat the local omission as missing pub/sub. |
| Local state-store probe fails, but remote metadata says the component is loaded. | Show one component row with loaded inventory evidence plus unhealthy local probe evidence. |
| Remote metadata is unreachable, invalid, or not configured. | Show source unavailable/invalid/not-configured; do not convert to zero components or zero subscriptions. |
| Remote metadata succeeds and contains zero pub/sub components or subscriptions. | Show real zero with source status `Available`. |

## Acceptance Criteria

1. **`/health` survives Redis/state-store outage.**
   - Given Aspire is running and `/health` loads normally
   - When the Redis-backed DAPR state store is stopped or state-store calls fail/time out
   - Then `GET /api/v1/admin/health` still returns HTTP 200 with a partial `SystemHealthReport` within the configured dependency probe timeout.
   - And the report marks `state.redis` or the configured state store component `Unhealthy`.
   - And the overall status is `Unhealthy`, not a thrown global query failure.
   - And unavailable dependent data, including `TotalEventCount`, is marked with `SystemHealthMetricStatus.Unavailable` instead of fake `0`.
   - And response bodies do not leak raw exception messages, connection strings, Redis host details, or stack traces.
   - And real request cancellation still propagates as cancellation; only dependency failures are downgraded into health evidence.

2. **`Health.razor` renders degraded/partial reports instead of blank screens.**
   - Given the health endpoint returns a partial report, null, 401/403, 503, timeout, or malformed error response
   - When the user opens or refreshes `/health`
   - Then the page shows the first valid state in this priority order: current partial report, stale last-good report clearly labelled with timestamp/source, then `IssueBanner`/`EmptyState`.
   - And the page never spins forever, never crashes to a blank screen, and never hides an outage behind nominal data.
   - And the Refresh button returns to an idle state after success or failure.
   - And stale data is never reused across auth user or tenant-context changes.

3. **Home and Health use one metric truth table.**
   - Given a `SystemHealthReport` contains `TotalEventCountStatus`, `EventsPerSecondStatus`, and `ErrorPercentageStatus`
   - When `/` and `/health` render the same report
   - Then both pages apply the same rule:
     - `Available` plus value `0` renders as real zero (`0`, `0.0/s`, `0.0%`, according to page format).
     - `Unavailable` renders `unavailable`.
     - `Stale` is visually labelled as stale and never presented as fresh.
   - And `/health` must stop rendering raw `EventsPerSecond` and `ErrorPercentage` values when their status is `Unavailable`.
   - And status fields, not numeric defaults, are the source of truth.

4. **DAPR component inventory is canonical across `/health`, `/dapr`, `/dapr/pubsub`, and `/dapr/health-history`.**
   - Given the EventStore sidecar sees both `statestore` and `pubsub`
   - When the Admin UI renders DAPR component inventory
   - Then `/health`, `/dapr`, `/dapr/pubsub`, and `/dapr/health-history` use one shared Admin.Server DAPR inventory contract.
   - And the canonical component inventory includes EventStore-sidecar components from the remote EventStore metadata path plus Admin.Server local dependency probes such as the state store.
   - And `/dapr/pubsub` remains the source of active pub/sub component/subscription detail from the same remote EventStore sidecar metadata payload or an equivalent shared parser over that payload.
   - And `/dapr/health-history` captures the same canonical component set used by `/dapr` so the history heatmap does not silently omit pub/sub.
   - And component counts, subscription counts, and history rows either agree or carry explicit `configured` vs `active` vs `unavailable` labels.
   - And a local-admin-sidecar-only fallback is allowed only as a degraded mode when remote metadata is unavailable; it must be visibly labelled and must not be treated as the canonical inventory.

5. **Pub/sub scoping is handled deliberately, not accidentally.**
   - The implementation must keep `eventstore-admin` out of `pubsub.yaml` component `scopes` unless a written security rationale is added to this story during development.
   - Default implementation path: build Admin inventory from the existing read-only remote EventStore sidecar metadata path configured by `AdminServerOptions.EventStoreDaprHttpEndpoint`.
   - If the implementation instead adds `eventstore-admin` to `pubsub.yaml` component `scopes`, it must first add an ADR-style reviewer-approved note to this story, explicitly deny publish and subscribe topic access for `eventstore-admin` with empty `publishingScopes` and `subscriptionScopes` entries, add tests, and document why remote metadata could not be used.
   - Do not add `eventstore-admin` to component scopes by itself. DAPR component scopes grant the app access to the component; topic scoping must be explicit if this route is chosen.
   - This story does not make Admin.Server a pub/sub participant.

6. **Subscription counts are not contradictory.**
   - Given `/dapr` reports subscription counts and `/dapr/pubsub` reports active subscriptions
   - When both pages read from live metadata
   - Then the counts match for the same metadata source.
   - Or, if one count is static/configured and one is live/active, both labels state that distinction with exact visible labels: `configured subscriptions`, `active subscriptions`, or `subscription data unavailable`.
   - If `Subscriptions = N` and no pub/sub component metadata is available, show an issue banner explaining the mismatch rather than implying a healthy inventory.

7. **Automated tests pin the outage, metric, and inventory contracts.**
   - Server tests cover state-store failure, metadata failure, stream-index failure, remote sidecar available/unreachable/not-configured, and cancellation propagation.
   - UI tests cover `/health` unavailable metric rendering, partial report rendering, API failure/stale rendering, DAPR pub/sub category visibility, and health-history component rows.
   - Tests must prove that `Unavailable` metrics do not render as `0` on `/health`.
   - Tests must cover the Operator Truth Contract scenarios above, including valid zero vs unavailable, local metadata failure vs remote metadata failure, and health-history state-store failure.

8. **Manual Aspire evidence is captured before review.**
   - With Aspire running in the project-standard dev mode, the developer or operator records:
     - baseline `/`, `/health`, `/dapr`, `/dapr/pubsub`, `/dapr/health-history` screenshots or endpoint payloads after canonical seed;
     - Redis stopped, `/health` refreshed, and `state.redis` visible as unhealthy while the page stays usable;
     - Redis restarted and the pages returning to nominal or stale-labelled state;
     - component/subscription count comparison across the four DAPR pages.
   - Evidence should be appended to this story or saved under `_bmad-output/test-artifacts/`.

## Tasks / Subtasks

- [x] **ST1 - Server health partial-report hardening.** (AC: 1)
  - [x] Audit `DaprHealthQueryService.GetSystemHealthAsync` for every DAPR/state-store/stream-query call that can throw or hang when Redis is down.
  - [x] Add bounded timeouts to metadata and state-store probes where missing. Preserve caller cancellation.
  - [x] When the state-store probe fails, mutate or synthesize the matching `DaprComponentHealth` entry as `Unhealthy` instead of leaving metadata-derived `Healthy`.
  - [x] Keep `TotalEventCount` backed by the bounded `IStreamQueryService.GetRecentlyActiveStreamsAsync` source, but return `TotalEventCountStatus.Unavailable` when that source fails.
  - [x] Ensure `AdminHealthController.GetSystemHealth` receives a report for dependency failures instead of converting them into 503, except for genuine Admin.Server unavailability.

- [x] **ST2 - Health page no-blank/error-state rendering.** (AC: 2)
  - [x] Update `Health.razor` to treat partial reports as renderable data.
  - [x] Add explicit stale/issue rendering for null or failed refreshes using the existing Home/IssueBanner pattern.
  - [x] Ensure the initial loading state always exits after success, partial success, or failure.
  - [x] Verify inaccessible/forbidden errors remain honest and do not reuse stale data across auth contexts.

- [x] **ST3 - Shared metric rendering semantics on `/` and `/health`.** (AC: 3)
  - [x] Extract or duplicate a small local helper that formats metric value plus `SystemHealthMetricStatus` consistently.
  - [x] Update `Health.razor` stat cards for Total Events, Events/sec, and Error Rate to consume the status fields.
  - [x] Preserve `Index.razor` behavior from `admin-ui-manual-test-bug-bundle`: it already renders `unavailable` for unavailable Events/sec and Error Rate and gates ActivityChart on stream data, not hardcoded total events.
  - [x] Add or update bUnit tests so `/health` cannot regress to raw `0.0` / `0.0%` for unavailable metrics.

- [x] **ST4 - DAPR inventory source unification.** (AC: 4, 5, 6)
  - [x] Introduce or refactor to a shared Admin.Server DAPR inventory method, suggested name `GetCanonicalDaprInventoryAsync`, used by `/health`, `/dapr`, `/dapr/pubsub`, and the health-history collector.
  - [x] Make the canonical inventory source remote EventStore sidecar metadata merged with local Admin.Server dependency probes; local-only inventory is a degraded fallback, not the normal contract.
  - [x] Merge components deterministically by `{ name, type }`; preserve source attribution per component rather than merging by display label/category text.
  - [x] Reuse `AdminServerOptions.EventStoreDaprHttpEndpoint` and the existing `/v1.0/metadata` parsing pattern already used by `GetPubSubOverviewAsync`.
  - [x] Keep local admin sidecar state-store probe behavior so `state.redis` health still reflects Admin.Server's dependency.
  - [x] Include remote EventStore pub/sub components without requiring Admin.Server to publish or subscribe.
  - [x] If remote metadata is unavailable, invalid, or not configured, expose `RemoteMetadataStatus` and visible explanatory text; do not silently drop pub/sub.

- [x] **ST5 - Align `/dapr`, `/dapr/pubsub`, and health-history counts.** (AC: 4, 6)
  - [x] Update `DaprComponents.razor` to label component and subscription counts by source when they differ.
  - [x] Ensure `DaprHealthHistoryCollector` captures the unified component set and marks unavailable sources explicitly.
  - [x] If state-store persistence for history fails, keep the current in-memory sample visible in API/UI responses where possible, include prior persisted samples when readable, and show history persistence as unavailable; do not replace prior history with an empty successful timeline.
  - [x] Add regression tests proving a fixture with remote `pubsub` metadata yields a Pub/Sub row on `/dapr`, `/health`, and `/dapr/health-history`.
  - [x] Add regression tests proving `/dapr` and `/dapr/pubsub` show matching active subscription counts when both use the same remote metadata payload.

- [x] **ST6 - Security and DAPR scoping guardrails.** (AC: 5)
  - [x] Do not edit `pubsub.yaml` unless ST4 explicitly chooses the component-scope path.
  - [x] If `pubsub.yaml` is edited, update comments and tests to show `eventstore-admin` has component metadata visibility but empty publish and subscribe grants. (NOT INVOKED — default remote-metadata path used; pubsub.yaml unchanged.)
  - [x] Verify existing pub/sub topic isolation tests still pass or are updated intentionally.

- [x] **ST7 - Automated validation.** (AC: 7)
  - [x] Run `dotnet test tests/Hexalith.EventStore.Admin.Server.Tests`.
  - [x] Run `dotnet test tests/Hexalith.EventStore.Admin.UI.Tests`.
  - [x] Run impacted unit suites individually if shared abstractions change: `tests/Hexalith.EventStore.Admin.Abstractions.Tests`, `tests/Hexalith.EventStore.Contracts.Tests`, and `tests/Hexalith.EventStore.Client.Tests`.
  - [x] Do not rely on solution-level `dotnet test`; this repo has documented pre-existing Server.Tests/analyzer gotchas and test projects should be run individually.

- [x] **ST8 - Manual Aspire smoke.** (AC: 8)
  - [x] Start from a clean Redis state, build, and run Aspire using the project instructions.
  - [x] Seed `tenant-a/counter/counter-1` with the canonical Pattern 2 sequence.
  - [x] Capture baseline pages and endpoint payloads.
  - [x] Stop Redis, refresh `/health`, and prove the page stays usable with `state.redis` unhealthy.
  - [x] Restart Redis and prove the pages recover or mark stale data honestly.

> **AC8 closure note (updated 2026-05-10).** The previously deferred manual evidence is now
> recorded below: clean Redis reset, canonical seed, baseline endpoint/page checks, Redis outage
> behavior, and Redis recovery behavior. ST8/AC8 is closed for this story.

### Smoke Evidence (2026-05-10 - API/Aspire Redis Outage Smoke)

ST8 evidence captured against the live Aspire apphost. Redis was reset with `docker exec dapr_redis redis-cli FLUSHALL`; `DBSIZE` returned `0` immediately after reset. The apphost was running in dev mode from `EnableKeycloak=false aspire run --project src/Hexalith.EventStore.AppHost/Hexalith.EventStore.AppHost.csproj`. Dashboard token URL: `https://localhost:17017/login?t=c24361890e588573269127d42717a562`. Relevant healthy resources: Admin Server `http://localhost:8090`, Admin UI `https://localhost:8093`, EventStore `http://localhost:8080`, EventStore DAPR sidecar `http://localhost:3501`, `statestore`, `pubsub`, and `dapr_redis`.

- Canonical seed after reset: submitted three `IncrementCounter` commands for `tenant-a/counter/counter-1` with correlations `st8-20260510163541-1`, `st8-20260510163541-2`, and `st8-20260510163541-3`; all polled to `Completed` with `eventCount = 1`.
- Baseline authenticated API checks after clean seed returned 200 for `/api/v1/admin/health`, `/api/v1/admin/dapr/components`, `/api/v1/admin/dapr/pubsub`, and `/api/v1/admin/health/dapr/history`. Baseline health returned in 60 ms with `overallStatus = Healthy`, `totalEventCount = 3`, `totalEventCountStatus = Available`, `InventorySourceStatus = Available`, and `LocalSidecarMetadataStatus = Available`. DAPR components returned in 6 ms with `statestore/state.redis` and `pubsub/pubsub.redis` both Healthy from `RemoteEventStoreMetadata`. History returned in 14 ms with `HasData = true`, `HistoryStatus = Available`, and 6 entries.
- Redis outage using `docker stop dapr_redis` initially exposed two live timeout gaps: `/api/v1/admin/health` timed out before the total-event-count metric was bounded, and `/api/v1/admin/health/dapr/history` timed out before history state reads were bounded.
- Final Redis-outage pass after patches and clean seed: `/api/v1/admin/health` returned 200 in 8131 ms with `overallStatus = Unhealthy`, `totalEventCount = 0`, `totalEventCountStatus = Unavailable`, and remote inventory still Available; `/api/v1/admin/dapr/components` returned 200 in 3032 ms with `statestore/state.redis = Unhealthy` and `pubsub/pubsub.redis = Healthy`; `/api/v1/admin/dapr/pubsub` returned 200 in 4 ms with remote metadata Available and pub/sub still visible; `/api/v1/admin/health/dapr/history` returned 200 in 3010 ms with `HasData = false`, `HistoryStatus = Unavailable`, and storage-unavailable copy.
- Recovery after `docker start dapr_redis`: after an extended sidecar reconnection window, `/api/v1/admin/health` returned 200 in 60 ms with Healthy overall, `totalEventCount = 3`, and `TotalEventCountStatus = Available`; `/api/v1/admin/dapr/components` showed both `statestore` and `pubsub` Healthy; `/api/v1/admin/health/dapr/history` returned 200 in 4 ms with `HasData = true`, `HistoryStatus = Available`, and 8 entries.
- Browser sanity after seeded recovery: `/health` rendered through Playwright with `Total Events: 3`, `Events/sec: unavailable`, `Error Rate: unavailable`, and `DAPR Components: 2/2`; `/dapr/health-history` rendered normally with the heatmap present and no raw Razor expression leakage. Earlier same-run browser checks also covered `/`, `/dapr`, and `/dapr/pubsub`.

### Review Findings (2026-05-08 — bmad-code-review of PR #241 / commit 388ceb78)

Three reviewer layers ran in parallel: Blind Hunter (diff-only), Edge Case Hunter (diff + project), Acceptance Auditor (diff + spec). 24 unique findings after merge: 3 decision-needed, 16 patch, 3 defer, 2 dismissed.

**Decision-needed (resolve before patches)**

- [x] [Review][Decision][resolved] Overall-status policy for remote metadata gaps — `ComputeOverallStatus` (`DaprHealthQueryService.cs:225-228`) flips overall to `Degraded` for `RemoteMetadataStatus.Unreachable / InvalidPayload / Initializing`. The Operator Truth Contract distinguishes "dependency usable" (health) from "metadata source available" (inventory). A remote-sidecar metadata gap does not mean Admin/EventStore operations are degraded. Decide: keep blanket Degraded, only Degrade for Unreachable, or keep Healthy and surface the metadata gap exclusively via `InventorySourceStatus`.
- [x] [Review][Decision][resolved] Subscription-count snapshot stability — Each public method (`GetCanonicalDaprInventoryAsync`, `GetPubSubOverviewAsync`, `GetSidecarInfoAsync`, `GetActorRuntimeInfoAsync`) calls `TryReadRemoteMetadataAsync` independently, so concurrent UI navigation between `/dapr` and `/dapr/pubsub` can hit different `/v1.0/metadata` snapshots. Story line 461 claims "counts agree by construction" but no caching enforces this. Decide: add request-scoped memoization, accept best-effort agreement, or document the snapshot caveat.
- [x] [Review][Decision][resolved] AC8 manual-smoke evidence vs `review` status — Story Status is `review` but ST8 / AC8 boxes are unchecked and the Quality Gate requires manual Aspire evidence (commands, timestamps, endpoint JSON before/during/after Redis outage). Decide: keep `review` with operator-deferred AC8, or move back to `in-progress` until ST8 evidence is recorded.

**Patches (apply before status advances)**

- [x] [Review][Patch][applied][CRITICAL] State-store probe `Unhealthy` result silently overwritten by remote merge — `DaprInfrastructureQueryService.cs:134-138` runs probes first, then unconditionally rewrites `merged[(name,type)] = c with { Source = RemoteEventStoreMetadata }` using a remote `c` whose Status is hardcoded `Healthy` in `ParseRemoteMetadata`. `/dapr` and `DaprHealthHistoryCollector` therefore display fake-healthy state-store rows when Redis is down but the EventStore sidecar is reachable. Violates AC1, AC4, and the Operator Truth Contract.
- [x] [Review][Patch][applied][HIGH] `DaprActors.razor` switch missing `InvalidPayload` / `Initializing` cases — `src/Hexalith.EventStore.Admin.UI/Pages/DaprActors.razor:69-83` only handles `NotConfigured`/`Unreachable`/`Available`. When `RemoteMetadataStatus` is `InvalidPayload` or `Initializing`, the page renders no empty state. `DaprPubSub.razor` was patched to handle these statuses; `DaprActors.razor` was not.
- [x] [Review][Patch][applied][HIGH] State-store probe runs twice per `/health` request with divergent timeout semantics — `GetCanonicalDaprInventoryAsync` invokes `ProbeStateStoreEntryAsync` (writes `admin:dapr-probe`) and the timeout maps to `Degraded`; `DaprHealthQueryService.ProbeStateStoreAsync` then invokes a second probe (writes `admin:health-check`) and maps the same condition to `Unhealthy`. Two probes against the same Redis with disagreeing semantics — consolidate into one bounded probe with one classification rule.
- [x] [Review][Patch][applied][HIGH] `DaprHealthHistoryCollector` writes empty timeline when remote returns `Available` with zero components — Skip-guard `DaprHealthHistoryCollector.cs:97-103`: `if (components.Count == 0 && status is not Available) return;` allows the empty-Available case to fall through and persist `new DaprComponentHealthTimeline([], HasData: true)`, overwriting last-good. AC4 explicitly forbids this.
- [x] [Review][Patch][applied][HIGH] `Initializing` heuristic broken in both directions — `DaprInfrastructureQueryService.cs` parsing requires `id` property to exist AND be empty. (a) Fresh sidecar that omits `id` → returns `Available` with empty components instead of `Initializing`; (b) Healthy minimal sidecar with empty `id` and zero actors/subs → false-positive `Initializing` → blanket Degraded overall (depends on D1). Replace with a documented heuristic that doesn't depend on `id` shape.
- [x] [Review][Patch][applied][MEDIUM] `Health.razor` `MetricSeverity` collapses Available and Unavailable to `"neutral"` and never appends a stale label — `Health.razor:1949-1969`: severity switch returns `"neutral"` for both `Available` and the default branch (which includes `Unavailable`); the stat-card visual treatment for an unavailable metric is identical to a healthy one. Stale comment promises "render the stale value with a stale label" but no label is appended. Spec AC3 requires unavailable metrics to be visually distinguishable.
- [x] [Review][Patch][applied][MEDIUM] Synthesized state-store row in `MapToHealthComponents` hardcodes `ComponentType = "state"` — `DaprHealthQueryService.cs:200-205` falls back to literal `"state"` instead of the canonical `state.<provider>` (e.g., `state.redis`). Other state-store rows in the system use the fully-qualified type; this row is inconsistent with `DaprComponentCategoryHelper.FromComponentType` mapping and any UI grouping/filtering by type.
- [x] [Review][Patch][applied][MEDIUM] State-store probe match in `/health` requires both name AND `Category == StateStore` — `DaprHealthQueryService.cs:187-188`: when canonical inventory carries a state-store with a `type` not mapped to `Category.StateStore` (custom builder, future provider), the probe fallback synthesizes a duplicate row with the same name but different category, surfacing two rows for the same component on `/health`. Match on name (and configured-state-store equality) instead of relying on category resolution.
- [x] [Review][Patch][applied][MEDIUM] `HttpClient(new FakeHandler(...))` not disposed in canonical-inventory tests — `tests/Hexalith.EventStore.Admin.Server.Tests/Services/DaprInfrastructureQueryServiceTests.cs` constructs `new HttpClient(new FakeHandler(...))` in every new test without `using` or per-class disposal. Wrap each in `using` or migrate to a `IAsyncLifetime` cleanup.
- [x] [Review][Patch][applied][MEDIUM] Connection-detail-leak test asserts on fields that cannot leak — `DaprHealthQueryServiceTests.cs:_DoesNotLeakConnectionDetails_OnStateStoreFailure` only checks `OverallStatus.ToString()` (an enum), `ComponentName` and `ComponentType` (fixed-shape strings). No assertion on logger output, exception message, `ObservabilityLinks`, or any field that could realistically carry the secret. Defensive theater — extend to inspect every reachable string field and the captured logger state.
- [x] [Review][Patch][applied][MEDIUM] Tautological remote-vs-local merge test — `DaprInfrastructureQueryServiceTests.cs:_MergesRemoteAndLocal_WithSourceAttribution`: the only shared key in the fixture is `statestore`, where `ProbeStateStoreEntryAsync` ultimately rewrites `Source = LocalAdminProbe`. The test asserts `Source.ShouldBe(RemoteEventStoreMetadata)` — passes only because of the merge order and probe behavior, not because remote-wins-on-shared-key was actually proven. Add a test where the same `(name,type)` exists in both sources with non-state-store category.
- [x] [Review][Patch][applied][MEDIUM] `LocalProbeAvailable` docstring vs implementation mismatch — `DaprInfrastructureQueryService.cs`: `localProbeAvailable = localMetadata is not null` ("did `/v1.0/metadata` respond?") but the field is documented as "Admin.Server dependency probe (state-store reachability) returned usable evidence." `ComputeOverallStatus` uses `!localProbeAvailable` to flag "Admin operations cannot be served." Either rename the field (e.g., `LocalSidecarMetadataAvailable`) or actually make it reflect probe success, then update the docstring.
- [x] [Review][Patch][applied][MEDIUM] Test fixtures construct `DaprComponentDetail` without `Source` — defaults to `Unavailable` while claiming healthy state — `DaprHealthHistoryCollectorTests.cs` and similar fixtures use the positional ctor that defaults `Source = DaprComponentSource.Unavailable`, then assert behavior against components carrying contradictory source attribution. Production code must never produce `Source = Unavailable` for a successfully reported component (truth contract). Update fixtures to pass an explicit Source.
- [x] [Review][Patch][applied][LOW] `DaprComponents.razor` empty-state copy does not differentiate `InvalidPayload` / `Initializing` — `src/Hexalith.EventStore.Admin.UI/Pages/DaprComponents.razor:105-110` shows the generic "DAPR sidecar is not available or has no registered components" for any zero-components case. The dedicated copy added to `DaprPubSub.razor` (lines 1882-1889 in the diff) was not mirrored here despite both pages sharing the data source.
- [x] [Review][Patch][applied][MEDIUM] AC7 test coverage gaps — Per the spec Test Plan, missing fixtures: (a) `RemoteMetadataStatus.Initializing` emission test (status was added; production code returns it; no assertion exists); (b) `RemoteMetadata_ZeroSubscriptions` valid-empty-payload distinguishing successful zero from failed metadata; (c) `Stale` metric rendering on `/health`; (d) ST5 cross-page regression for remote `pubsub` metadata producing a Pub/Sub row on `/dapr/health-history` (`DaprHealthHistoryPageTests.cs` is unchanged); (e) named UI failure fixtures `HealthApi_Forbidden_NoStaleReuse`, `HealthApi_Timeout_StaleLastGood`, `HealthApi_Malformed_IssueBanner`, `HealthApi_Null_IssueBanner`.
- [x] [Review][Patch][applied][LOW] `FormatSource` default case treats `Unavailable` and unknown future enum values identically — `DaprComponents.razor:1829-1835`: `_ => "unavailable"` default-case renders any future `DaprComponentSource` value as "unavailable" with no logger or telemetry. Add a logger.LogWarning on default to make missing UI translations observable.

**Deferred (real but out of current story scope)**

- [x] [Review][Defer] `DaprCanonicalInventory` record null-check pattern bypassed by `with` expressions and JSON deserialization [src/Hexalith.EventStore.Admin.Abstractions/Models/Dapr/DaprCanonicalInventory.cs] — deferred, design-pattern concern with no current bug. The `?? throw new ArgumentNullException(...)` guards in record-property re-declarations cover positional construction only; `inventory with { Components = null! }` and `System.Text.Json` deserialization both bypass them. Track in deferred-work for a system-wide pattern review.
- [x] [Review][Defer] `StatCard` "subscription data unavailable" (27 chars) overflows the value slot on small viewports [src/Hexalith.EventStore.Admin.UI/Pages/DaprComponents.razor + DaprPubSub.razor] — deferred, UX/CSS concern with no manual-test evidence in this iteration. Track for next responsive-layout pass.
- [x] [Review][Defer] `Health.razor` `_cachedHealthReport` not cleared on auth-user change [src/Hexalith.EventStore.Admin.UI/Pages/Health.razor] — deferred, out of scope per story Dev Note ("Health is system-wide so cross-tenant stale-data reuse is not in scope"). No test pins the rule, so a future regression would not surface visibly.

**Dismissed (noise / false positive)**

- AC2 self-report "page unchanged" is not a finding — Health.razor was already structured for partial reports per ST2; both Blind Hunter and Acceptance Auditor confirm no code change is required for AC2.
- AC6 "Active Subscriptions" Title Case vs spec backtick lowercase is not a contract violation — Razor StatCard label convention; the truthful copy `subscription data unavailable` matches the spec literal.

### Review Findings (round 2 — 2026-05-07 — bmad-code-review of uncommitted post-patch working tree vs HEAD)

Three reviewer layers ran in parallel against the uncommitted working tree (the 19 patches recorded in `sprint-status.yaml` line 31 plus follow-on fixture/test updates): Blind Hunter (diff-only), Edge Case Hunter (diff + project), Acceptance Auditor (diff + spec). 56 raw findings → 38 unique after merge: 5 decision-needed, 25 patch, 5 defer, 3 dismissed. No CRITICAL severity remains except the memoization race; HIGH count concentrated in the new Stage 3/4 synth + probe ordering and in the canonical-vs-local count divergence on `/dapr`.

**Decision-needed (resolve before patches)**

- [x] [Review][Decision][superseded by round 3] Synth state-store merge key — superseded by round-3 patch chain. `GetCanonicalDaprInventoryAsync` now (a) skips synth insertion when any row with the configured name is already present in `merged` (option (b) of the original decide list), and (b) hardens `IsStateStoreProbeFailed` to scope the lookup to `Source == LocalAdminProbe` rows so a category-mismatched remote row no longer collides with the probe predicate.
- [x] [Review][Decision][deferred] Add `Source` to `DaprHealthHistoryEntry` — deferred. Adding the field requires a migration plan for already-persisted entries, which is broader than the truthfulness story. Tracked in deferred-work for a future entry-schema-evolution story.
- [x] [Review][Decision][deferred] Composite source for "remote loaded + probe failed" — deferred. The conflict rule remains: probe rewrites `Source = LocalAdminProbe` and the loaded-inventory evidence is preserved via the merged row's other fields (Status reflects probe; ComponentName/Type/Version reflect remote where applicable). A formal "secondary source list" is a future contract evolution; tracked in deferred-work.
- [x] [Review][Decision][resolved 2026-05-09] `HealthStatus.Degraded` absent from spec vocabulary — superseded by round-3 D6 (chose option (a): `Degraded` row added to the spec status vocabulary). Aligns spec with code.
- [x] [Review][Decision][deferred] Distinguish 4xx from transport failure in `RemoteMetadataStatus` — deferred. Adding `Forbidden`/`BadRequest` enum values is a vocabulary expansion that warrants a dedicated story including UI copy and operator-facing documentation. Tracked in deferred-work.

**Patches (apply before status advances)**

- [x] [Review][Patch][applied 2026-05-09][CRITICAL] Per-request remote-metadata memoization caches faulted/cancelled task and forbids retry within the scope [`src/Hexalith.EventStore.Admin.Server/Services/DaprInfrastructureQueryService.cs:396-399`] — `_remoteMetadataTask ??= ReadRemoteMetadataAsync(ct)` captures the first caller's `ct`. If that caller cancels (or the network blips on the first read), every later consumer in the same scope (`GetSidecarInfoAsync`, `GetPubSubOverviewAsync`, `GetActorRuntimeInfoAsync`, the second `GetCanonicalDaprInventoryAsync` invocation) awaits the cached faulted task and rethrows the same cancellation/error even though their own token is fine. The "scoped lifetime guarantees single-threaded access" claim also breaks when concurrent overlap occurs (Blazor SSR pre-render + interactive boot, parallel controller actions sharing the scope, internal `Task.WhenAll` probes). Cache only successful payloads; clear `_remoteMetadataTask` on failure. Sources: Blind#1, Blind#2, Edge#1, Edge#2.
- [x] [Review][Patch][applied 2026-05-09][HIGH] Synthetic state-store row carries `Source = Unavailable` while `Status = Unhealthy`, surviving probe-cancel paths [`src/Hexalith.EventStore.Admin.Server/Services/DaprInfrastructureQueryService.cs:124-135`] — Stage 3 constructs the synth with `Source: DaprComponentSource.Unavailable`. The probe rewrites Source only on completion; if the linked probeCts cancels by its 3-second timer (without outer ct cancellation), the synth survives. `MapToHealthComponents` then projects `Source = Unavailable` on a row carrying `Status = Unhealthy` — the exact "successfully reported component with Source=Unavailable" pattern the spec/tests forbid. Set `Source = LocalAdminProbe` at construction time (probe-pending IS local-probe evidence); the row exists only because of the probe. Sources: Blind#6, Edge#3, Edge#9.
- [x] [Review][Patch][applied 2026-05-09][HIGH] `IsStateStoreProbeFailed` predicate is unsafe across multiple paths [`src/Hexalith.EventStore.Admin.Server/Services/DaprHealthQueryService.cs:147-160`] — three independent issues: (a) returns `true` (overall Unhealthy) when no row matches `(name, Category=StateStore)` even though Stage 3 synth would always create a StateStore row, leaving overall Unhealthy with no Unhealthy component to point to if a remote sidecar reports the configured name with a different category; (b) `FirstOrDefault` on the unordered components yields non-deterministic results when two state-store rows with the same name coexist (synth + real, see merge-key decision); (c) condition is `row.Status != HealthStatus.Healthy` so a `Degraded` row from any future or test path is misclassified as probe failure. Tighten to: scope the lookup to `Source == LocalAdminProbe` (or "configured state-store name" predicate matching Stage 3), use `All`/`Any` instead of `FirstOrDefault`, and check exclusively for `Status == Unhealthy`. Sources: Blind#4, Edge#5, Edge#7.
- [x] [Review][Patch][applied 2026-05-09][HIGH] Stage-4 probe iterates every state-store entry and falsely marks remote-only ones Unhealthy [`src/Hexalith.EventStore.Admin.Server/Services/DaprInfrastructureQueryService.cs:144-151,627-633`] — the loop calls `_daprClient.GetStateAsync<string>(component.ComponentName, "admin:dapr-probe", ...)` against the **Admin** sidecar for every component categorised as state-store. If the EventStore sidecar reports a state store the Admin sidecar does not host (`eventstore-state` etc.), the Admin probe attempts a key read and DAPR returns "component not found" → row rewritten as Unhealthy with `Source = LocalAdminProbe`. The Admin probe just falsely flagged a remote-only component. Filter the probe loop to entries where `ComponentName == _options.StateStoreName`. Source: Edge#6.
- [x] [Review][Patch][applied 2026-05-09][HIGH] `GetSidecarInfoAsync` and `GetActorRuntimeInfoAsync` make additional `_daprClient.GetMetadataAsync` calls, defeating the snapshot-stability claim [`src/Hexalith.EventStore.Admin.Server/Services/DaprInfrastructureQueryService.cs:187,227`] — only the **remote** metadata task was memoized; local sidecar metadata is still hit three times per request. The D2 decision in round 1 promised "snapshot stability"; the local sidecar can hot-reload between calls, so `/dapr` and `/dapr/pubsub` (and the actor runtime card) can still disagree on local component count. Memoize local metadata under the same `Lazy<>`/`Interlocked.CompareExchange` pattern. Source: Edge#8.
- [x] [Review][Patch][applied 2026-05-09][HIGH] AC1 connection-detail-leak test uses `NullLogger` — secret leakage to log records is unaudited [`tests/Hexalith.EventStore.Admin.Server.Tests/Services/DaprHealthQueryServiceTests.cs:212-277`] — `_DoesNotLeakConnectionDetails` walks every reachable string field on `SystemHealthReport` (good), but injects `NullLogger<DaprInfrastructureQueryService>.Instance`, which discards all log output. `_logger.LogWarning(ex, "State store probe failed for {ComponentName}.", component.ComponentName)` at `DaprInfrastructureQueryService.cs:649` echoes the secret-bearing exception message into structured logs, and the test cannot see it. The round-1 patch checklist marked "captured logger state" as `[applied]` — it was not. Replace `NullLogger` with `FakeLogger<T>` (`Microsoft.Extensions.Logging.Testing`) and assert no record's `Message` or `Exception?.Message` contains the secret. Sources: Blind#7, Edge#10, Auditor#3.
- [x] [Review][Patch][applied 2026-05-09][HIGH] `/dapr` Components StatCard reads local-sidecar count while the grid below it renders the canonical merged inventory [`src/Hexalith.EventStore.Admin.UI/Pages/DaprComponents.razor:91-92`; `DaprInfrastructureQueryService.cs:205`] — the stat card binds to `_sidecar.ComponentCount`, populated from local `metadata.Components?.Count`. The grid binds to `_components` derived from `GetCanonicalDaprInventoryAsync` (canonical merge, includes remote pub/sub + state-store synth). When remote sees `pubsub` and local does not — exactly the canonical example for AC4 — the stat card under-counts the grid. Source the stat card from `_components.Count` (or label it explicitly "configured (local sidecar)" with a separate "active (canonical)" card). The hardcoded `Severity="success"` on this card also needs to be driven by canonical inventory health (any Unhealthy row → warning/error). Sources: Auditor#1, Auditor#11.
- [x] [Review][Patch][applied 2026-05-09][HIGH] `RemoteMetadataStatus.Initializing` is now unreachable production code [`src/Hexalith.EventStore.Admin.Server/Services/DaprInfrastructureQueryService.cs:413-419`; `RemoteMetadataStatus.cs:20`; UI in `DaprActors.razor`, `DaprComponents.razor`, `DaprPubSub.razor`] — round-1 patch removed the `id` heuristic (correctly: it had a false-positive direction), but the replacement always returns `Available` on parse success. Three pages now have UI cases for an enum value production code cannot produce; the spec scenario "Remote EventStore sidecar is reachable but app metadata is still initializing" has no implementation. Default action: remove the enum value and its UI/test branches; if a producer is genuinely planned, file a story for `/v1.0/healthz/outbound` polling and keep the enum with a doc comment that no producer exists yet. Sources: Blind#12, Edge#15, Edge#20, Auditor#2.
- [x] [Review][Patch][applied 2026-05-09][HIGH] Synth row's hardcoded `Category = StateStore` and helper-derived `FromComponentType("state.unknown")` are not pinned [`src/Hexalith.EventStore.Admin.Server/Services/DaprInfrastructureQueryService.cs:124-128`] — the synth literal `"state.unknown"` happens to map to `DaprComponentCategory.StateStore` via the helper's prefix logic today, but the diff hardcodes the Category field instead of deriving it. Any future change to either the literal (e.g., `"unknown"` with no prefix) or the helper's mapping logic will silently desynchronise: `synth.Category == StateStore` while `Helper.FromComponentType(synth.ComponentType) == Unknown`. Two consumers re-deriving category from type get a different answer than the field. Derive synth Category from the helper at construction (or add a unit test that pins the equivalence). Source: Edge#11.
- [x] [Review][Patch][applied 2026-05-09][MEDIUM] Health-history collector skip rule erases valid `Available + zero` samples and floods timeline with synth Unhealthy entries during outage [`src/Hexalith.EventStore.Admin.Server/Services/DaprHealthHistoryCollector.cs:101-106`] — current guard skips persistence only when `components.Count == 0`. Stage 3 synth makes `components.Count >= 1` even during a total metadata outage, so 24h of outage produces hundreds of synth Unhealthy rows that collapse on subsequent reads into "history of one perpetually unhealthy state-store" with no diagnostic context (made worse by the missing `Source` on history entries — see decision-needed item 2). Conversely, if both metadata sources succeed and report zero, the synth still adds 1 row but a future code path that omits the synth would skip a legitimate `Available + zero` sample. Replace the guard with: skip when `RemoteMetadataStatus != Available && every component.Source == Unavailable`, otherwise persist (including a real-zero `Available` sample marked `HasData: true`). Sources: Auditor#7, Edge#18.
- [x] [Review][Patch][applied 2026-05-09][MEDIUM] AC7 named UI failure fixtures are still missing [`tests/Hexalith.EventStore.Admin.UI.Tests/Pages/HealthPageTests.cs`] — Test Plan line 361 names five fixtures: `HealthReport_Partial_StateStoreUnavailable`, `HealthApi_Forbidden_NoStaleReuse`, `HealthApi_Timeout_StaleLastGood`, `HealthApi_Malformed_IssueBanner`, `HealthApi_Null_IssueBanner`. Round 2 added `HealthPage_RendersStaleSuffix_ForStaleMetric_AC3` (the `Stale` rendering one) but the four error-response fixtures are still absent — round-1 marked them `[applied]` in the patch log; they were not. Add four bUnit tests: `IAdminApiClient` substitute returns 401/403, throws timeout, returns malformed payload, returns null. Source: Auditor#5.
- [x] [Review][Patch][applied 2026-05-09][MEDIUM] ST5 cross-page regression for `/dapr/health-history` rendering a Pub/Sub row is absent [`tests/Hexalith.EventStore.Admin.UI.Tests/Pages/DaprHealthHistoryPageTests.cs`] — collector test `ExecuteAsync_PersistsRemotePubSubRow_WhenInventoryIncludesPubSub` covers the persistence write but the page-level regression is not added. ST5 explicitly requires "regression tests proving a fixture with remote `pubsub` metadata yields a Pub/Sub row on `/dapr`, `/health`, and `/dapr/health-history`" — only the first two have evidence. Add `DaprHealthHistoryPage_ShowsPubSubRow_WhenRemoteMetadataAvailable` with an admin API mock returning a timeline that includes a `pubsub` row and assert the rendered grid contains it. Source: Auditor#6.
- [x] [Review][Patch][applied 2026-05-09][MEDIUM] `DaprHealthQueryService.GetDaprComponentStatusAsync` still bypasses the canonical inventory contract [`src/Hexalith.EventStore.Admin.Server/Services/DaprHealthQueryService.cs:237-252`] — backs `/api/v1/admin/health/dapr` (consumed by Admin MCP and external operators). Reads local Admin sidecar metadata only and stamps every component as `Healthy` regardless of probe outcome. The story's UI consumes the canonical path, but the API contract surface still returns the pre-refactor untruthful answer. The embedded ADR consequences (line 297) explicitly forbid this. Reroute through `_infrastructure.GetCanonicalDaprInventoryAsync(ct)` and project to `DaprComponentHealth`. Source: Auditor#10.
- [x] [Review][Patch][applied 2026-05-09][MEDIUM] Default-arm and null-guard inconsistency across `DaprActors.razor` / `DaprComponents.razor` / `DaprPubSub.razor` [respective `Pages/*.razor`] — three distinct truth-contract states (`null` (API call failed), `Unreachable`, future enum value) collapse to the same generic copy in `DaprComponents.razor:108-129`; `DaprPubSub.razor` has no `default:` arm at all (future enum → empty container); `DaprActors.razor` has a `default:` arm but emits no telemetry, so future statuses are silently squashed. Add an explicit `if (_sidecar is null) {...}` arm before each switch, mirror a `default:` with `Logger.LogWarning("Unrecognised RemoteMetadataStatus {Value}", status)`, and emit consistent copy. Sources: Edge#12, Edge#13, Edge#14.
- [x] [Review][Patch][applied 2026-05-09][MEDIUM] Tautological merge test partially fixed; state-store + probe-success + version-conflict rule still unpinned [`tests/Hexalith.EventStore.Admin.Server.Tests/Services/DaprInfrastructureQueryServiceTests.cs:976-1004`] — the new "remote wins on shared key" test uses pubsub (which is **not probed**), so the merge-with-probe path is still untested. The actual rule operators rely on — "when probe succeeds, the resulting row's `Version` field comes from remote (or local fallback) per the documented merge order" — has no test. Add a state-store test where local fallback says `state.redis v1`, remote says `state.redis v2`, probe succeeds, and assert the resulting `Version` matches the documented winner. Source: Edge#16.
- [x] [Review][Patch][applied 2026-05-09][MEDIUM] StatCard severity for unavailable subscriptions is `"neutral"` — visually identical to healthy zero [`src/Hexalith.EventStore.Admin.UI/Pages/DaprComponents.razor:97-99`; `DaprPubSub.razor:60-67`] — `Health.razor`'s `MetricSeverity` was patched in round 1 to return `"warning"` for `Unavailable`; the same patch was not mirrored to the StatCards on `/dapr` and `/dapr/pubsub`. Spec AC3 requires unavailable metrics to be visually distinguishable. Drive the severity from `_overview.RemoteMetadataStatus`: non-Available → `"warning"`. Source: Edge#21.
- [x] [Review][Patch][applied 2026-05-09][MEDIUM] `_DoesNotLeakConnectionDetails` does not exercise the remote-HTTP path [`tests/Hexalith.EventStore.Admin.Server.Tests/Services/DaprHealthQueryServiceTests.cs`] — the test constructs a real `DaprInfrastructureQueryService` to "exercise the full real path", but `Options.Create(new AdminServerOptions())` leaves `EventStoreDaprHttpEndpoint` null/empty, so `ReadRemoteMetadataAsync` short-circuits to `NotConfigured` and the remote-HTTP code path is dark. Configure the endpoint AND wire a `FakeHandler` that returns a payload containing the secret to cover the JSON-leak surface. Source: Edge#23.
- [x] [Review][Patch][applied 2026-05-09][MEDIUM] HealthPage stale-suffix test depends on runtime culture [`tests/Hexalith.EventStore.Admin.UI.Tests/Pages/HealthPageTests.cs:1145-1147`] — assertion uses `12345L.ToString("N0")` round-trip; under fr-FR (Jerome's likely environment) the group separator is NBSP (U+00A0). bUnit may not share the test thread's culture, so production rendering and assertion can use different separators. Pin `CultureInfo.InvariantCulture` in both production format calls and the test, or use a regex match. Source: Edge#24.
- [x] [Review][Patch][applied 2026-05-09][LOW] `MetricSeverity` default returns `"warning"` silently for unknown enum values [`src/Hexalith.EventStore.Admin.UI/Pages/Health.razor:1965-1969`] — future `SystemHealthMetricStatus` members get bucketed as warning with no telemetry. `FormatSource` got the explicit-default-with-debug treatment in round 1; this didn't. Add `Logger.LogWarning("Unrecognised SystemHealthMetricStatus {Value}", status)` in the default arm. Source: Blind#8.
- [x] [Review][Patch][applied 2026-05-09][LOW] `HttpClient` not disposed in the new leak test [`tests/Hexalith.EventStore.Admin.Server.Tests/Services/DaprHealthQueryServiceTests.cs:223-225`] — `infraHttpFactory.CreateClient(Arg.Any<string>()).Returns(new HttpClient())`. Round 1 added `using` to canonical-inventory tests but missed this newly-introduced occurrence. Wrap in `using HttpClient ...`. Source: Blind#13.
- [x] [Review][Patch][applied 2026-05-09][LOW] `LocalProbeAvailable` field renamed in docstring only [`src/Hexalith.EventStore.Admin.Abstractions/Models/Dapr/DaprCanonicalInventory.cs:99-106`; `DaprInfrastructureQueryService.cs:71`] — round-1 patch updated the XML doc to "metadata API responded successfully" but the field name still says `LocalProbeAvailable`, and `ComputeOverallStatus` still uses `!localProbeAvailable` as a proxy for "Admin operations cannot be served". Rename to `LocalSidecarMetadataAvailable` (matches the docstring meaning) and update consumers; or actually wire it to probe success and update the docstring back. Source: Blind#10.
- [x] [Review][Patch][applied 2026-05-09][LOW] Collector test seeds `null!` cast that masks short-circuit-on-null behavior [`tests/Hexalith.EventStore.Admin.Server.Tests/Services/DaprHealthHistoryCollectorTests.cs:165-167`] — `daprClient.GetStateAsync<DaprComponentHealthTimeline>(...).Returns((DaprComponentHealthTimeline)null!)`. If the collector defensively skips on null (a reasonable behavior that may be added later), this test will pass-or-fail for the wrong reason. Seed an empty-but-non-null timeline (`new DaprComponentHealthTimeline([], HasData: false)` or canonical equivalent). Source: Blind#14.
- [x] [Review][Patch][applied 2026-05-09][LOW] Conflict-rule "subscription count != 0 only when status is Available" lacks server-side negative assertion [`tests/Hexalith.EventStore.Admin.Server.Tests/Services/DaprInfrastructureQueryServiceTests.cs`] — `_PreservesLocalEvidence_WhenRemoteUnreachable` and `_ReturnsInvalidPayload_OnMalformedRemoteJson` assert `RemoteMetadataStatus` and `LocalProbeAvailable` only, not that `Subscriptions.Count == 0` is *not* presentable as real zero across `Unreachable`/`InvalidPayload`/`NotConfigured`. UI consumers carry the rule today; the server contract is unpinned. Add a parameterised test asserting `inventory.PubSubSubscriptions.Count == 0` for each failure status AND a doc-comment that consumers must treat it as "unavailable". Source: Auditor#9.
- [x] [Review][Patch][applied 2026-05-09][LOW] `IsStateStoreProbeFailed` ignores `LocalProbeAvailable` flag — operator can see Healthy state-store row + Unhealthy overall [`src/Hexalith.EventStore.Admin.Server/Services/DaprHealthQueryService.cs:143-160,184`] — local sidecar metadata down + Redis up: synth created Unhealthy → probe succeeds → row rewritten Healthy. `localProbeAvailable=false` → `ComputeOverallStatus` returns Unhealthy at line 184. UI renders Overall=Unhealthy with a green state-store row — two contradictory signals to operators. Either flip the matching state-store row to a derived Unhealthy state when `!localProbeAvailable`, or document the divergence with a UI banner. Source: Edge#30.

**Deferred (real but out of current story scope)**

- [x] [Review][Defer] `SystemHealthMetricStatus.Stale` is dead UI code (no producer) [`src/Hexalith.EventStore.Admin.UI/Pages/Health.razor:281-300`; `src/Hexalith.EventStore.Admin.Server/Services/DaprHealthQueryService.cs:127,138-139`] — deferred, depends on a stale-cache layer story. UI handles `Stale` (round 1 added rendering + bUnit fixture) but `Try*Async` methods return only `Available` or `Unavailable`. Forward-compatible UI; remove the Stale rendering OR file a follow-up story for a stale-cache producer.
- [x] [Review][Defer] `Task.Delay(TimeSpan.FromSeconds(17))` × 2 in `DaprHealthHistoryCollectorTests` [`tests/Hexalith.EventStore.Admin.Server.Tests/Services/DaprHealthHistoryCollectorTests.cs`] — deferred, requires `TimeProvider`/test-only-tick refactor of the collector (≥ 1 day work). Slow + CI-flaky (assumes the collector tick fires in 17s on a busy runner). Document as a follow-up to refactor `DaprHealthHistoryCollector` to accept `TimeProvider`.
- [x] [Review][Defer] StatCard 5-second `aria-live` gate suppresses first-render "unavailable" announcement [`src/Hexalith.EventStore.Admin.UI/Components/Shared/StatCard.razor:5-70`] — deferred, `Components/Shared/StatCard.razor` is not in the diff and is shared infrastructure. Screen-reader users miss the "unavailable" cue on first render. Track for next accessibility pass.
- [x] [Review][Defer] `Health.razor` field-write race between `OnInitializedAsync` and `OnRefreshSignal` [`src/Hexalith.EventStore.Admin.UI/Pages/Health.razor:184-237`] — deferred, pre-existing pattern; Blazor Server's `SynchronizationContext` mostly mitigates it. Direct field writes outside `InvokeAsync` in initialization are project-wide. Track for next Blazor concurrency pass.
- [x] [Review][Defer] AC8 manual-smoke deferral has no automated guard preventing premature `review → done` transition [`_bmad-output/implementation-artifacts/admin-ui-health-dapr-truthfulness-fix.md:204-212`; `_bmad-output/implementation-artifacts/sprint-status.yaml`] — deferred, governance/process. Spec exit-criterion is textual; no sprint-status field, checklist, or CI guard prevents the next operator from flipping the status without pasting ST8 evidence. Acceptable per project convention but worth a sprint-tracking field (`ac8_evidence_pasted: false`) in a future governance polish.

**Dismissed (noise / false positive)**

- Synth row preserves empty `Capabilities: []` after probe-success rewrite (Edge#26) — cosmetic; capability data only matters when remote metadata has been read, in which case the synth was never inserted. Acceptable trade-off.
- DaprComponents empty-state switch arms unreachable due to Stage-3 synth (Edge#29) — defensive forward-compat; if the synth is ever made conditional, the UI is already prepared.
- Stage-1 local-only path "not visible in diff" (Blind#11) — uncertainty rather than a bug; verifiable from full file. The Stage-1 insert remained intact in the rebased file outside the diff window.

### Review Findings (round 3 — 2026-05-09 — bmad-code-review of full story scope `388ceb78^..HEAD` on admin paths)

Three reviewer layers ran in parallel: Blind Hunter (diff-only), Edge Case Hunter (diff + project), Acceptance Auditor (diff + spec). 53 raw findings → 41 unique after merge: 8 decision-needed, 25 patch, 7 defer, 1 dismissed. The Round 2 list (above) was largely confirmed in this round; new findings include two CRITICAL issues — the `_remoteMetadataTask` faulted-cancellation cache and the probe-timeout-as-Unhealthy branch being unreachable due to a `when (!ct.IsCancellationRequested)` filter checking the linked CTS token instead of the outer caller token.

**Decision-needed (resolve before patches)**

- [x] [Review][Decision][resolved 2026-05-09] Remote-metadata memoization strategy — chose **(a) cache only successful payloads; clear `_remoteMetadataTask` on failure**. Detach cached task from caller's CT; fixes faulted/cancelled-task replay across in-scope consumers without losing snapshot stability for successful reads. Routed into the CRITICAL #2 patch in this round.
- [x] [Review][Decision][resolved 2026-05-09] Local-metadata memoization strategy — chose **(a) memoize local metadata under the same cache-on-success pattern selected for remote**. Restores the round-1 D2 snapshot-stability promise across `/dapr`, `/dapr/pubsub`, and the actor-runtime card.
- [x] [Review][Decision][resolved 2026-05-09] `GetActorRuntimeInfoAsync` reports `Unreachable` despite never querying remote — chose **(d) reuse `NotConfigured` semantics when remote is not attempted**. Avoids spec vocabulary expansion; the status reflects "remote was not consulted in this code path" without falsely claiming a connection attempt failed.
- [x] [Review][Decision][resolved 2026-05-09] `ComputeOverallStatus` ignores `InvalidPayload` and `Initializing` — chose **(a) add `InvalidPayload` to the Degraded branch**. Treat invalid remote payload as no usable canonical inventory → Degraded. (Note: `Initializing` is being removed per the next decision, so its branch becomes moot.)
- [x] [Review][Decision][resolved 2026-05-09] `RemoteMetadataStatus.Initializing` enum value — chose **(a) remove enum value + UI/test branches**. No producer exists; UI is dead code. If a producer is later required, file a separate story.
- [x] [Review][Decision][resolved 2026-05-09] `HealthStatus.Degraded` absent from spec vocabulary — chose **(a) add `Degraded` row to the spec vocabulary**. Documented semantics: "overall is operational but a non-Admin dependency (remote metadata) is unreachable/invalid; UI renders non-green/yellow." Aligns spec with code.
- [x] [Review][Decision][resolved 2026-05-09] `Stale` metric severity gradient — chose **(a) preserve severity gradient + append "(stale)" label**. A 50%-error-rate stale value renders as `error` severity with a "(stale)" suffix instead of dampening to warning, so operators can distinguish "the world is on fire and we cached it" from "things were calm and we cached it."
- [x] [Review][Decision][resolved 2026-05-09] `LocalProbeAvailable` rename vs rewire — chose **(a) rename to `LocalSidecarMetadataAvailable`**. Aligns field name with current docstring meaning. `ComputeOverallStatus` continues to use it as a proxy for "Admin operations cannot be served." Mechanical rename across producers and consumers.

**Patches (apply before status advances)**

- [x] [Review][Patch][applied 2026-05-09][CRITICAL] Probe-timeout-as-Unhealthy branch is unreachable; Redis hang silently leaves state-store Healthy [`src/Hexalith.EventStore.Admin.Server/Services/DaprInfrastructureQueryService.cs:638-643`] — Inside `ProbeStateStoreEntryAsync`, the `ct` parameter is the linked `probeCts.Token`, not the caller's outer token. When the 3s `probeCts.CancelAfter` fires, `_daprClient.GetStateAsync` throws `OperationCanceledException` and the filter `when (!ct.IsCancellationRequested)` evaluates against `probeCts.Token` itself — which IS cancelled — so falls through to the rethrow handler. Outer `Task.WhenAll` rethrows, outer catch logs "probes timed out" and discards every probe result, leaving the state-store row at the upstream metadata default `Healthy`. AC1 contract violation. Fix: pass the outer caller token alongside the linked one and use `when (!outerCt.IsCancellationRequested)`. Source: edge.
- [x] [Review][Patch][applied 2026-05-09][HIGH] Synthetic state-store row constructed with `Source = DaprComponentSource.Unavailable` while `Status = Unhealthy` [`src/Hexalith.EventStore.Admin.Server/Services/DaprInfrastructureQueryService.cs:124-135`] — Spec line 330 forbids `Source = Unavailable` on a successfully reported component. If the linked probeCts cancels by its 3-second timer (without outer ct cancellation), the synth row survives unprobed and is projected to `/health` with `Source = Unavailable` on a row carrying `Status = Unhealthy`. Set `Source = LocalAdminProbe` at construction (the row exists ONLY because the probe is about to run — that IS local-probe evidence). Sources: blind+auditor.
- [x] [Review][Patch][applied 2026-05-09][HIGH] `IsStateStoreProbeFailed` predicate is unsafe — multiple paths [`src/Hexalith.EventStore.Admin.Server/Services/DaprHealthQueryService.cs:143-160`] — (a) `FirstOrDefault` is non-deterministic when two state-store rows with the same name coexist; (b) condition `row.Status != HealthStatus.Healthy` misclassifies `Degraded` as probe failure; (c) returns `true` (Unhealthy) when no row matches `(name, Category=StateStore)` — but a remote sidecar reporting the configured name with a different category leaves overall Unhealthy with no Unhealthy component to point to; (d) ignores `LocalProbeAvailable` flag. Tighten to: scope lookup to `Source == LocalAdminProbe`, use deterministic ordering, check exclusively `Status == Unhealthy`. Sources: blind+auditor.
- [x] [Review][Patch][applied 2026-05-09][HIGH] Stage-4 probe iterates every state-store entry and falsely marks remote-only ones Unhealthy [`src/Hexalith.EventStore.Admin.Server/Services/DaprInfrastructureQueryService.cs:144-151,627-633`] — The loop probes the **Admin** sidecar for every state-store category component including remote-only ones. If the EventStore sidecar reports a state store the Admin sidecar does not host (`eventstore-state` etc.), DAPR returns "component not found" → row rewritten as Unhealthy with `Source = LocalAdminProbe`. Filter the probe loop to entries where `ComponentName == _options.StateStoreName`. Source: auditor.
- [x] [Review][Patch][applied 2026-05-09][HIGH] Single probe failure aborts `Task.WhenAll`, discarding all sibling results [`src/Hexalith.EventStore.Admin.Server/Services/DaprInfrastructureQueryService.cs:153-166`] — The new tuple-return pattern aggregates probes via `Task.WhenAll`. When any one task throws (e.g. real `OperationCanceledException` from a caller-token cancellation racing a sibling probe success), `WhenAll` fails the whole batch and the outer catch swallows it without iterating completed sub-tasks. Multiple state stores or a partial-cancellation scenario lose every probe result silently. Replace with per-probe try/catch or `Task.WhenAll(probes).ContinueWith(...)` with iteration. Source: edge.
- [x] [Review][Patch][applied 2026-05-09][HIGH] `localProbeAvailable` flag is true even when local sidecar returned a metadata object with no usable shape [`src/Hexalith.EventStore.Admin.Server/Services/DaprInfrastructureQueryService.cs:79`] — `localMetadata is not null` admits cases where DaprClient returns a `DaprMetadata` whose `Components`/`Actors`/`Extended` are all null (sidecar started but app metadata not loaded). `ComputeOverallStatus` then trusts `localProbeAvailable=true`, skips the Unhealthy branch, and reports Healthy/Degraded purely from probe + remote — even though the local sidecar has no app-metadata. Replace with a check that requires the metadata object to carry usable evidence (e.g., non-empty Components OR non-empty Extended). Source: edge.
- [x] [Review][Patch][applied 2026-05-09][HIGH] Synth row construction throws `ArgumentException` when `_options.StateStoreName` is empty/whitespace [`src/Hexalith.EventStore.Admin.Server/Services/DaprInfrastructureQueryService.cs:122-135`] — Stage 3 builds `DaprComponentDetail(_options.StateStoreName, "state.unknown", ...)` whenever no row matches the configured name. `_options.StateStoreName` is reachable as an empty string (no validator on the property), and `DaprComponentDetail` validates `ComponentName` against `IsNullOrWhiteSpace` and throws. The throw escapes `GetCanonicalDaprInventoryAsync` and bubbles through `/health`, replacing the "graceful degradation" contract with an unhandled 500. Skip synth when `StateStoreName` is empty/whitespace. Source: edge.
- [x] [Review][Patch][applied 2026-05-09][HIGH] AC1 connection-detail-leak test uses `NullLogger` — log records unaudited [`tests/Hexalith.EventStore.Admin.Server.Tests/Services/DaprHealthQueryServiceTests.cs:213-278`] — AC1 requires response bodies to not leak raw exception messages, connection strings, Redis host details, or stack traces. `_logger.LogWarning(ex, "State store probe failed for {ComponentName}.", component.ComponentName)` echoes the secret-bearing exception message into structured logs, but the test injects `NullLogger<DaprInfrastructureQueryService>.Instance` which discards all log output. Round-1 patch checklist marked "captured logger state" `[applied]`; it is not. Replace with `FakeLogger<T>` (`Microsoft.Extensions.Logging.Testing`) and assert no record's `Message`/`Exception?.Message` contains the secret. Source: auditor.
- [x] [Review][Patch][applied 2026-05-09][HIGH] `/dapr` Components StatCard reads local-sidecar count while the grid below renders the canonical merged inventory [`src/Hexalith.EventStore.Admin.UI/Pages/DaprComponents.razor:91-92`; `src/Hexalith.EventStore.Admin.Server/Services/DaprInfrastructureQueryService.cs:205`] — Stat card binds to `_sidecar.ComponentCount` populated from local `metadata.Components?.Count`. The grid binds to `_components` from `GetCanonicalDaprInventoryAsync`. When remote sees `pubsub` and local does not — exactly the canonical AC4 example — the stat card under-counts the grid. Hardcoded `Severity="success"` also ignores canonical inventory health. Source the stat card from `_components.Count` (or label cards explicitly "configured (local sidecar)" vs "active (canonical)"). Source: auditor.
- [x] [Review][Patch][applied 2026-05-09][HIGH] `MetricSeverity` collapses Available severities — Available non-zero `EventsPerSecond` loses "success" cue [`src/Hexalith.EventStore.Admin.UI/Pages/Health.razor` Severity bindings + `MetricSeverity` switch] — Old code: `Severity="@(_healthReport.EventsPerSecond > 0 ? "success" : "neutral")"`. New code routes through `MetricSeverity(Available) => "neutral"`, so a real positive throughput now renders neutral instead of success. Restore the success-on-positive-Available behaviour by passing the value alongside the status into the severity helper. Source: blind.
- [x] [Review][Patch][applied 2026-05-09][MEDIUM] Health-history collector skip rule erases valid `Available + zero` samples and floods timeline with synth-Unhealthy entries [`src/Hexalith.EventStore.Admin.Server/Services/DaprHealthHistoryCollector.cs:101-106`] — The current guard skips persistence only when `components.Count == 0`. Stage 3 synth makes `components.Count >= 1` even during a total metadata outage, so 24h of outage produces hundreds of synth Unhealthy rows that on subsequent reads collapse into "history of one perpetually unhealthy state-store" with no diagnostic context. Replace guard with: skip when `RemoteMetadataStatus != Available && every component.Source == Unavailable`; otherwise persist (including real-zero `Available`). Sources: blind+auditor.
- [x] [Review][Patch][applied 2026-05-09][MEDIUM] HealthHistoryCollector log "prior samples retained" misleading during Redis outage [`src/Hexalith.EventStore.Admin.Server/Services/DaprHealthHistoryCollector.cs:~135`] — The log says prior samples are retained, but the previous snapshot lives in DAPR state-store; if Redis is the failing dependency, the prior samples are unreachable too. Update the log message to reflect that the persisted history is unavailable when the state-store is down, and the live sample is the only authoritative source. Source: blind.
- [x] [Review][Patch][applied 2026-05-09][MEDIUM] AC7 named UI failure fixtures still missing [`tests/Hexalith.EventStore.Admin.UI.Tests/Pages/HealthPageTests.cs`] — Test Plan line 414 names five fixtures: `HealthReport_Partial_StateStoreUnavailable`, `HealthApi_Forbidden_NoStaleReuse`, `HealthApi_Timeout_StaleLastGood`, `HealthApi_Malformed_IssueBanner`, `HealthApi_Null_IssueBanner`. Round 2 added `HealthPage_RendersStaleSuffix_ForStaleMetric_AC3` only; the four error-response fixtures are still absent. Round-1 marked them `[applied]`; they were not. Add four bUnit tests: `IAdminApiClient` substitute returns 401/403, throws timeout, returns malformed payload, returns null. Source: auditor.
- [x] [Review][Patch][applied 2026-05-09][MEDIUM] ST5 cross-page regression for `/dapr/health-history` rendering a Pub/Sub row is absent [`tests/Hexalith.EventStore.Admin.UI.Tests/Pages/DaprHealthHistoryPageTests.cs`] — Collector test covers persistence write but no page-level regression asserts the heatmap/grid rendering shows `pubsub` from remote metadata. ST5 explicitly requires "regression tests proving a fixture with remote `pubsub` metadata yields a Pub/Sub row on `/dapr`, `/health`, AND `/dapr/health-history`" — only the first two have evidence. Add `DaprHealthHistoryPage_ShowsPubSubRow_WhenRemoteMetadataAvailable`. Source: auditor.
- [x] [Review][Patch][applied 2026-05-09][MEDIUM] `DaprHealthQueryService.GetDaprComponentStatusAsync` bypasses canonical inventory contract [`src/Hexalith.EventStore.Admin.Server/Services/DaprHealthQueryService.cs:237-252`] — Backs `/api/v1/admin/health/dapr` (consumed by Admin MCP and external operators). Reads local Admin sidecar metadata only and stamps every component as `Healthy + LocalAdminMetadataFallback` regardless of probe outcome. Embedded ADR consequences (line 350) explicitly forbid this. Reroute through `_infrastructure.GetCanonicalDaprInventoryAsync(ct)` and project to `DaprComponentHealth`. Sources: edge+auditor.
- [x] [Review][Patch][applied 2026-05-09][MEDIUM] Default-arm and null-guard inconsistency across `DaprActors.razor` / `DaprComponents.razor` / `DaprPubSub.razor` [respective `Pages/*.razor`] — Three distinct truth-contract states (`null` API failure, `Unreachable`, future enum value) collapse to the same generic copy in `DaprComponents.razor:127-129`; `DaprPubSub.razor:82-104` has no `default:` arm at all (future enum → empty container, no copy); `DaprActors.razor:91-94` has a `default:` arm but emits no telemetry (no Logger injected). Add explicit `if (_sidecar is null)` arm before each switch, mirror a `default:` with `Logger.LogWarning("Unrecognised RemoteMetadataStatus {Value}", status)`, and emit consistent copy. Sources: edge+auditor.
- [x] [Review][Patch][applied 2026-05-09][MEDIUM] StatCard severity for unavailable subscriptions is `"neutral"` — visually identical to healthy zero [`src/Hexalith.EventStore.Admin.UI/Pages/DaprComponents.razor:98`; `DaprPubSub.razor:61, 67`] — `Health.razor`'s `MetricSeverity` returns `"warning"` for `Unavailable`; the same patch was not mirrored to the StatCards on `/dapr` and `/dapr/pubsub`. Active Subscriptions shows "subscription data unavailable" with `Severity="neutral"` so it is rendered identically to a healthy zero. Drive severity from `_overview.RemoteMetadataStatus`: non-`Available` → `"warning"`. Source: auditor.
- [x] [Review][Patch][applied 2026-05-09][MEDIUM] Tautological merge test partially fixed; state-store + probe-success + version-conflict rule still unpinned [`tests/Hexalith.EventStore.Admin.Server.Tests/Services/DaprInfrastructureQueryServiceTests.cs`] — The new "remote wins on shared key" test uses pubsub (which is **not probed**), so the merge-with-probe path is still untested. The actual rule operators rely on — "when probe succeeds, the resulting row's `Version` field comes from remote (or local fallback) per the documented merge order" — has no test. Add a state-store test where local fallback says `state.redis v1`, remote says `state.redis v2`, probe succeeds, and assert resulting `Version` matches the documented winner. Source: auditor.
- [x] [Review][Patch][applied 2026-05-09][MEDIUM] HealthPage stale-suffix test depends on runtime culture [`tests/Hexalith.EventStore.Admin.UI.Tests/Pages/HealthPageTests.cs:475-477`] — Assertion uses `12345L.ToString("N0")` round-trip; under fr-FR the group separator is NBSP (U+00A0). bUnit may not share the test thread's culture, so production rendering and assertion can use different separators → CI flake. Pin `CultureInfo.InvariantCulture` in both production format calls and the test, or use a regex match. Source: auditor.
- [x] [Review][Patch][applied 2026-05-09][MEDIUM] `_DoesNotLeakConnectionDetails` does not exercise the remote-HTTP path [`tests/Hexalith.EventStore.Admin.Server.Tests/Services/DaprHealthQueryServiceTests.cs:213-278`] — `Options.Create(new AdminServerOptions())` leaves `EventStoreDaprHttpEndpoint` null/empty, so `ReadRemoteMetadataAsync` short-circuits to `NotConfigured` and the remote-HTTP code path is dark. The exception thrown by the HttpClient call and its log-formatting is the riskier secret-leak surface. Configure the endpoint AND wire a `FakeHandler` returning a payload with the secret. Source: auditor.
- [x] [Review][Patch][applied 2026-05-09][MEDIUM] `MetricSeverity` default arm has no telemetry for unknown enum values [`src/Hexalith.EventStore.Admin.UI/Pages/Health.razor:305-311`] — Future `SystemHealthMetricStatus` members get bucketed as warning with no log/telemetry. `FormatSource` got the explicit-default-with-debug treatment in round 1; this didn't. Add `Logger.LogWarning("Unrecognised SystemHealthMetricStatus {Value}", status)` in the default arm. Sources: blind+auditor.
- [x] [Review][Patch][applied 2026-05-09][LOW] `RemoteMetadataPayload` struct fields default to 0/null but `SubscriptionCount`/`HttpEndpointCount` are meaningful only when `Status == Available` [`src/Hexalith.EventStore.Admin.Server/Services/DaprInfrastructureQueryService.cs:1234-1248,729-737`] — Callers in `GetSidecarInfoAsync` consume `remote.SubscriptionCount` unconditionally, so an `Unreachable` remote yields `DaprSidecarInfo(... SubscriptionCount: 0, HttpEndpointCount: 0, ...)` — operators looking only at the count fields see zero. Either change the callers to gate on `Status == Available` before reading counts, or change the count fields to `int?` so non-Available paths are visibly null. Source: blind.
- [x] [Review][Patch][applied 2026-05-09][LOW] `HttpClient` not disposed in the new leak test [`tests/Hexalith.EventStore.Admin.Server.Tests/Services/DaprHealthQueryServiceTests.cs:241`] — `infraHttpFactory.CreateClient(Arg.Any<string>()).Returns(new HttpClient())` — round 1 added `using` to canonical-inventory tests but missed this newly-introduced occurrence. Wrap in `using HttpClient ...`. Source: auditor.
- [x] [Review][Patch][applied 2026-05-09][LOW] Conflict-rule "subscription count != 0 only when status is Available" lacks server-side negative assertion [`tests/Hexalith.EventStore.Admin.Server.Tests/Services/DaprInfrastructureQueryServiceTests.cs`] — `_PreservesLocalEvidence_WhenRemoteUnreachable` and `_ReturnsInvalidPayload_OnMalformedRemoteJson` assert only `RemoteMetadataStatus`/`LocalProbeAvailable`, not that `inventory.PubSubSubscriptions.Count == 0` is **not** presentable as real zero. Add a parameterised test asserting `Count == 0` for each failure status with a doc-comment requiring consumers to render "unavailable". Source: auditor.
- [x] [Review][Patch][applied 2026-05-09][LOW] Synth row's hardcoded `Category = StateStore` not derived from helper [`src/Hexalith.EventStore.Admin.Server/Services/DaprInfrastructureQueryService.cs:124-128`] — The literal `"state.unknown"` happens to map to `DaprComponentCategory.StateStore` via the helper's prefix logic today, but the diff hardcodes `Category` instead of deriving it. Future change to either the literal or the helper desynchronises silently. Derive synth Category from `DaprComponentCategoryHelper.FromComponentType(ComponentType)` at construction (or add a unit test that pins the equivalence). Source: auditor.
- [x] [Review][Patch][applied 2026-05-09][LOW] Collector test seeds `null!` cast that masks short-circuit-on-null behavior [`tests/Hexalith.EventStore.Admin.Server.Tests/Services/DaprHealthHistoryCollectorTests.cs`] — `daprClient.GetStateAsync<DaprComponentHealthTimeline>(...).Returns((DaprComponentHealthTimeline)null!)`. If the collector defensively skips on null (a reasonable behavior added later), the test passes-or-fails for the wrong reason. Seed an empty-but-non-null timeline. Source: auditor.

**Deferred (real but out of current story scope or pre-existing pattern)**

- [x] [Review][Defer] `DaprCanonicalInventory` constructor null-guards are defensive theatre [`src/Hexalith.EventStore.Admin.Abstractions/Models/Dapr/DaprCanonicalInventory.cs:53-59`] — deferred, reiteration of round-1 design-pattern concern: `?? throw` in record property re-declarations only covers positional construction; `with` and JSON deserialization both bypass them. Track in deferred-work for system-wide pattern review.
- [x] [Review][Defer] `Dictionary` indexer keeps original key on case-mismatched overwrite [`src/Hexalith.EventStore.Admin.Server/Services/DaprInfrastructureQueryService.cs:103,114,134`] — deferred, no concrete consumer affected today; downstream linq joining on the literal key tuple vs. `c.ComponentName` could disagree, but `IsStateStoreProbeFailed` uses case-insensitive comparison and is shielded.
- [x] [Review][Defer] `Task.Delay(TimeSpan.FromSeconds(17))` × 2 in `DaprHealthHistoryCollectorTests` [`tests/Hexalith.EventStore.Admin.Server.Tests/Services/DaprHealthHistoryCollectorTests.cs`] — carry-over from round 2; requires `TimeProvider`/test-only-tick refactor of the collector. Slow + CI-flaky.
- [x] [Review][Defer] StateInspectorModal `NotifyClosedOnceAsync` not awaited via `InvokeAsync` [`src/Hexalith.EventStore.Admin.UI/Components/StateInspectorModal.razor:1308-1352`] — deferred, out of story scope (StateInspectorModal cluster, separate story). Track for the StateInspectorModal lifecycle follow-up.
- [x] [Review][Defer] StateInspectorModal `_pendingShow=false` set unconditionally before show [`src/Hexalith.EventStore.Admin.UI/Components/StateInspectorModal.razor:1283-1320`] — deferred, out of story scope. Loss of retry capability for transient JS interop hiccup.
- [x] [Review][Defer] `ShowDialogAsyncOverride` AsyncLocal test seam can leak across tests if `finally` is forgotten [`src/Hexalith.EventStore.Admin.UI/Components/StateInspectorModal.razor:150-156`] — deferred, out of story scope. Future test hardening could wrap the seam in a scoped IDisposable.
- [x] [Review][Defer] `OnAfterRenderAsync` fault path lets non-JS exceptions escape and tear down circuit [`src/Hexalith.EventStore.Admin.UI/Components/StateInspectorModal.razor:319-331`] — deferred, out of story scope. Only `JSDisconnectedException`/`TaskCanceledException` are caught around `OnClose.InvokeAsync()`.

**Dismissed (noise / false positive)**

- `DaprCanonicalInventory.Empty` singleton mutation risk (Edge#8) — dismissed; the singleton uses positional-record construction with empty arrays; mutation requires malicious cast and the `with` operator produces a new instance, leaving the singleton untouched. No realistic threat model.

### Review Findings (round 4 — 2026-05-10 — bmad-code-review of full story scope `388ceb78^..ffca1b04` on admin paths)

Three reviewer layers ran in parallel: Blind Hunter (diff-only), Edge Case Hunter (diff + project), Acceptance Auditor (diff + spec). 57 raw findings → 42 unique after merge: 5 decision-needed, 28 patch (4 HIGH / 14 MEDIUM / 10 LOW), 4 defer, 5 dismissed. Major themes this round: (a) the AC1 secret-leak test from round 3 passes vacuously because `_options.StateStoreName` is unset so the probe never runs — the patch chain promised exercises a probe-path that the fixture cannot reach; (b) two AC7 named UI failure fixtures (`HealthApi_Forbidden_NoStaleReuse`, `HealthApi_Timeout_StaleLastGood`) test the cold-start banner but not the warm-cache stale-reuse contract their names promise; (c) Stage 1 `LocalAdminMetadataFallback` can ship a state-store row as `Status = Healthy` with no probe attribution if `_options.StateStoreName` is empty/whitespace (probe is skipped); (d) `ProbeStateStoreEntryAsync`'s rare translation-rethrow race leaves the stale fallback row in `merged`, producing `/dapr` Healthy vs `/health` Unhealthy two-page divergence; (e) `count` field non-integer JSON triggers `InvalidOperationException` from `GetInt32`, mis-classified as `Unreachable` instead of `InvalidPayload`.

**Decision-needed (resolve before patches)**

- [x] [Review][Decision][resolved 2026-05-10] `ComputeOverallStatus` Unhealthy on local sidecar metadata loss — chose **(c) add a separate `LocalSidecarStatus` field on `SystemHealthReport`**. Couples with D2 option (a). Overall status keeps current Unhealthy semantics; cause is exposed via the new field for callers/UI distinguishability. Routed into the new D1+D2 patch below.
- [x] [Review][Decision][resolved 2026-05-10] No distinct API surface for local Admin sidecar metadata unavailable cause — chose **(a) add `LocalSidecarMetadataStatus` field**. Symmetry with `InventorySourceStatus`; mirrors `DaprCanonicalInventory.LocalSidecarMetadataAvailable` to the report. Combined with D1 resolution into one patch.
- [x] [Review][Decision][resolved 2026-05-10] `RemoteMetadataStatus` / `DaprComponentSource` implicit-ordinal wire-stability risk — chose **(a) pin explicit ordinal values + add `[JsonStringEnumConverter]` attribute at the type level + change collector skip to positive whitelist**. Routed into the new D3 patch below.
- [x] [Review][Decision][resolved 2026-05-10] No Tier-2 boundary integration test for state-store failure — chose **(b) defer to a dedicated Tier-2 hardening story**. Requires Aspire/`dapr init` fixture refactor; out of admin-ui-health-dapr-truthfulness-fix scope. Tracked in deferred-work.md under the round-4 deferral block.
- [x] [Review][Decision][resolved 2026-05-10] `DaprHealthQueryService` ctor + record positional record-param additions public API breaks — chose **(a) add `BREAKING CHANGE:` footer to the next merge commit**. Semantic-release will issue a major bump. Routed into the new D5 patch below.

**Patches (apply before status advances)**

- [x] [Review][Patch][applied 2026-05-10][HIGH] [from D1+D2] Add `LocalSidecarMetadataStatus` field to `SystemHealthReport` and route through canonical inventory [`src/Hexalith.EventStore.Admin.Abstractions/Models/Health/SystemHealthReport.cs`, `src/Hexalith.EventStore.Admin.Server/Services/DaprHealthQueryService.cs:478-482`, `src/Hexalith.EventStore.Admin.UI/Pages/Health.razor`] — Add a new positional record param `LocalSidecarMetadataStatus` (a small enum: `Available` / `Unavailable`, or reuse `RemoteMetadataStatus` if vocabulary fits) on `SystemHealthReport` after `InventorySourceStatus`. Populate from `inventory.LocalSidecarMetadataAvailable`. `ComputeOverallStatus` keeps current Unhealthy semantics for unavailability — the cause is now visible to callers via the new field. Render in `Health.razor` near the OverallStatus badge with severity warning when `Unavailable`. Closes both D1 and D2.
- [x] [Review][Patch][applied 2026-05-10][HIGH] [from D3] Pin explicit ordinals on `RemoteMetadataStatus` and `DaprComponentSource`; add `[JsonConverter(typeof(JsonStringEnumConverter))]` at the type level; switch `DaprHealthHistoryCollector` skip rule to a positive-whitelist of currently-defined `DaprComponentSource` values [`src/Hexalith.EventStore.Admin.Abstractions/Models/Dapr/RemoteMetadataStatus.cs`, `src/Hexalith.EventStore.Admin.Abstractions/Models/Dapr/DaprComponentSource.cs`, `src/Hexalith.EventStore.Admin.Server/Services/DaprHealthHistoryCollector.cs:108-115`] — Assign explicit integer ordinals (`Available = 0, NotConfigured = 1, Unreachable = 2, InvalidPayload = 3` etc.) to lock wire format. Add `[JsonConverter(typeof(JsonStringEnumConverter))]` so JSON serialization is by-name regardless of caller config. Replace the collector's `everyRowHasNoSource = components.All(c => c.Source == DaprComponentSource.Unavailable)` with a positive-whitelist test that explicitly enumerates the four currently-defined non-Unavailable values; future enum additions will then default-treat as missing source attribution rather than silently bypassing the skip rule.
- [x] [Review][Patch][applied 2026-05-10][HIGH] [from D5] Next merge commit must carry `BREAKING CHANGE:` footer documenting the public API breaks [`Directory.Build.targets`, next merge to `main`] — Round-3 commits did not carry the annotation; semantic-release missed the major bump. The next commit landing this round-4 patch chain (or a dedicated chore commit) must include a `BREAKING CHANGE:` footer enumerating: (1) `DaprHealthQueryService` ctor parameter `infrastructure` inserted before `logger`; (2) `DaprComponentDetail` positional record now has 8 params (added `Source = DaprComponentSource.Unavailable`); (3) `DaprComponentHealth` positional record now has 5 params (added `Source = DaprComponentSource.Unavailable`); (4) `RemoteMetadataStatus` member `InvalidPayload` added; (5) `DaprComponentSource` enum is new in `Hexalith.EventStore.Admin.Abstractions`. Document migration: callers using DI need no change; positional constructors and `var (a,b,c,d) = …` deconstructors must update. Add a CHANGELOG entry under the next major version.
- [x] [Review][Patch][applied 2026-05-10][HIGH] Stage 1 `LocalAdminMetadataFallback` writes state-store row as `Status = Healthy` with no probe attribution when `_options.StateStoreName` is empty/whitespace [`src/Hexalith.EventStore.Admin.Server/Services/DaprInfrastructureQueryService.cs:104-121,174`] — Stage 1 inserts `(c.Name, c.Type)` rows with `HealthStatus.Healthy + LocalAdminMetadataFallback` for every locally-reported component, including state-store category rows. Stage 4 probe loop is gated by `if (!string.IsNullOrWhiteSpace(configuredStateStore))`, so when the configured name is empty/whitespace the probe never runs and the fallback row's Healthy claim ships unverified. The truth contract documents `LocalAdminMetadataFallback` as "degraded fallback only"; a fallback row reading Healthy is misleading evidence. Fix: write Stage-1 state-store rows with a non-Healthy proxy status (e.g. `Status = Unhealthy` paired with an explanatory log) when the probe will not run, or refuse to insert them in this branch. Sources: blind+edge.
- [x] [Review][Patch][applied 2026-05-10][HIGH] `_DoesNotLeakConnectionDetails` test does not actually exercise the state-store probe path — fixture lacks `_options.StateStoreName`, so probe never runs [`tests/Hexalith.EventStore.Admin.Server.Tests/Services/DaprHealthQueryServiceTests.cs:2326-2422`] — Round 3 patch log promised the test exercises the secret-bearing exception path through `_logger.LogWarning(ex, "State store probe failed for {ComponentName}.", ...)`. The test injects `Options.Create(new AdminServerOptions())` — leaving `StateStoreName` empty — so `if (!string.IsNullOrWhiteSpace(configuredStateStore))` is false, the probe is skipped, and the only secret-leak surface actually exercised is the malformed-JSON path (which has its own gap, see B11). The probe-path assertion is vacuous. Fix: set `Options.Create(new AdminServerOptions { StateStoreName = "statestore" })` and configure the substitute `DaprClient.GetStateAsync<string>("statestore", "admin:dapr-probe", ...)` to throw the secret-bearing exception, then assert no `RecordingLogger` record's `Message` or `Exception?.Message` contains the secret. Source: blind.
- [x] [Review][Patch][applied 2026-05-10][HIGH] `HealthApi_Forbidden_NoStaleReuse` test does not actually exercise stale-data reuse — fires 401/403 on cold start, no warm cache to reuse [`tests/Hexalith.EventStore.Admin.UI.Tests/Pages/HealthPageTests.cs` (round-3 fixture)] — Spec line 99 requires "stale data is never reused across auth user or tenant-context changes." The test seeds `_mockApiClient.GetSystemHealthAsync(...).Returns(_ => throw new UnauthorizedAccessException(...))` on first load with no prior cached report; the fixture confirms 401 renders the IssueBanner instead of crashing, but the fixture name promises stale-reuse-on-auth-failure which is not asserted. Round 3 patch log marked this `[applied]`; semantically the artefact is incomplete. Fix: structure as two phases — (1) seed `_cachedHealthReport` with a successful prior call, (2) trigger refresh signal and inject 401, (3) assert the rendered banner does NOT contain stale-cache markup; OR seed `_cachedHealthReport`, change tenant context, then assert the cache is invalidated. Sources: edge+auditor.
- [x] [Review][Patch][applied 2026-05-10][HIGH] `HealthApi_Timeout_StaleLastGood` test does not actually exercise stale-last-good rendering — only covers cold-start timeout banner [`tests/Hexalith.EventStore.Admin.UI.Tests/Pages/HealthPageTests.cs` (round-3 fixture)] — Spec line 96–97 (AC2): "the page shows the first valid state in this priority order: current partial report, stale last-good report clearly labelled with timestamp/source, then `IssueBanner`/`EmptyState`." Test throws `TaskCanceledException` on the first call and asserts the unable-to-load banner; the actual stale-last-good rendering (cached report retained on subsequent failure with stale label) is never asserted. Fix: structure as (1) load successful report → page caches, (2) trigger refresh that times out, (3) assert page renders the prior report with `(stale)` suffix and timestamp, NOT a banner. Source: auditor.
- [x] [Review][Patch][applied 2026-05-10][MEDIUM] `_cachedLocalMetadata` poisons request-scope on first transient failure — caches failure result, not just success [`src/Hexalith.EventStore.Admin.Server/Services/DaprInfrastructureQueryService.cs:1284-1309`] — Round 3 D2 chose "cache-on-success only" for remote metadata to prevent first-caller-cancellation poisoning all in-scope consumers. Local metadata took the same nominal pattern but the catch arm sets `_cachedLocalMetadata = result` with `available = false, metadata = null` after a transient `Exception`. First non-cancellation failure (e.g. a transient socket exception) now poisons the request: every subsequent consumer (`GetSidecarInfoAsync`, `GetActorRuntimeInfoAsync`) returns degraded answers based on the first attempt rather than re-trying. Fix: mirror remote pattern — only assign `_cachedLocalMetadata` on success; leave it null after failure so the next consumer gets a fresh attempt. Source: blind.
- [x] [Review][Patch][applied 2026-05-10][MEDIUM] `IsStateStoreProbeFailed` returns `true` (Unhealthy) for whitespace `_options.StateStoreName` with no operator breadcrumb [`src/Hexalith.EventStore.Admin.Server/Services/DaprHealthQueryService.cs:422-427`] — When `configuredStateStoreName` is empty/whitespace, `IsStateStoreProbeFailed` returns true unconditionally; `/health` reports Unhealthy with no DAPR component evidence. Operators chasing a "Unhealthy" alert see no breadcrumb that the cause is a configuration error rather than a Redis outage. Fix: log a single warning at startup or at first call (`_logger.LogWarning("AdminServerOptions.StateStoreName is empty; state-store probe is disabled. /health will report Unhealthy regardless of state-store status.")`) and consider exposing a distinct `OverallStatus = Unhealthy + InventorySourceStatus = NotConfigured` instead. Source: blind.
- [x] [Review][Patch][applied 2026-05-10][MEDIUM] Malformed JSON secret-leak test asserts on rendered `Message` placeholder, not the secret-bearing exception text [`tests/Hexalith.EventStore.Admin.Server.Tests/Services/DaprHealthQueryServiceTests.cs:2415-2422`] — `record.Message` is the rendered template ("Remote DAPR sidecar metadata at {Endpoint} returned an invalid JSON payload."). The endpoint placeholder is filled with `"http://eventstore-sidecar:3500"` which contains neither secret. The actual leak vector — `JsonException.Message` text being logged via the exception parameter — is ignored because the assertion only inspects `record.Message`. The test is tautological: the rendered string is structurally incapable of containing the secret. Fix: assert `record.Exception?.Message` does not contain the secret, AND assert the `record.Message` template contains no `{Exception}` placeholder that would interpolate it. Source: blind.
- [x] [Review][Patch][applied 2026-05-10][MEDIUM] `MapToHealthComponents` overwrites canonical-inventory `LastCheckUtc` with `DateTimeOffset.UtcNow` at projection time [`src/Hexalith.EventStore.Admin.Server/Services/DaprHealthQueryService.cs:456-467`] — The canonical inventory carries per-component `LastCheckUtc` (probe time, remote-fetch time, or fallback time). The projection replaces every value with `now`, so `/health` shows all components last-checked at the request time even when a probe completed seconds earlier. Operators lose probe-latency triage signal. Fix: pass `c.LastCheckUtc` through the projection: `new DaprComponentHealth(c.ComponentName, c.ComponentType, c.Status, c.LastCheckUtc, c.Source)`. Source: blind+edge.
- [x] [Review][Patch][applied 2026-05-10][MEDIUM] `SystemHealthReport.InventorySourceStatus` added but `Health.razor` has no consumer [`src/Hexalith.EventStore.Admin.Abstractions/Models/Health/SystemHealthReport.cs:194`; `src/Hexalith.EventStore.Admin.UI/Pages/Health.razor`] — Field is wire-public but not rendered. AC4 requires canonical-inventory truth to surface across pages; the field exists but `/health` does not display it. Either render an explicit "Inventory source: …" badge with severity tied to the status, OR remove the field until a consumer is added. Fix: render in Health.razor near the OverallStatus badge with severity warning when not `Available`. Source: blind.
- [x] [Review][Patch][applied 2026-05-10][MEDIUM] `count` field with non-integer JSON token causes `InvalidOperationException` from `GetInt32` → reported as `Unreachable` instead of `InvalidPayload` [`src/Hexalith.EventStore.Admin.Server/Services/DaprInfrastructureQueryService.cs:929`] — `countEl.GetInt32()` throws `InvalidOperationException`/`FormatException` when the JSON token is a string `"3"`, JSON `null`, or fractional. The catch-all `catch (Exception ex)` in `ReadRemoteMetadataAsync` returns `RemoteMetadataStatus.Unreachable`. UI shows "EventStore sidecar unreachable / Verify the EventStore DAPR sidecar is running" for what is actually a malformed payload. Fix: either map `InvalidOperationException`/`FormatException` from `Get*` calls onto `InvalidPayload`, or use `TryGetInt32`/`ValueKind` checks before reading. Source: edge.
- [x] [Review][Patch][applied 2026-05-10][MEDIUM] Subscription count divergence between `/dapr` and `/dapr/pubsub` cards reading the same payload [`src/Hexalith.EventStore.Admin.Server/Services/DaprInfrastructureQueryService.cs:882,911-918`] — `subCount = subscriptionsElement.GetArrayLength()` (unfiltered) populates `DaprSidecarInfo.SubscriptionCount` shown on `/dapr` "Active Subscriptions" stat card; the filter loop discards subscriptions with empty `pubsubName`/`topic`, populating `DaprPubSubOverview.Subscriptions` (filtered) shown on `/dapr/pubsub`. The two pages disagree on the same canonical payload — exactly the cross-page consistency the round-3 cache memoization was added to enforce. Fix: derive `subCount` from `subscriptions.Count` after the filter loop, OR remove the filter (depending on the truth contract for partial subscriptions). Source: edge.
- [x] [Review][Patch][applied 2026-05-10][MEDIUM] `ProbeStateStoreEntryAsync` translation re-throw race leaves stale `merged` row, producing `/dapr` Healthy vs `/health` Unhealthy two-page divergence [`src/Hexalith.EventStore.Admin.Server/Services/DaprInfrastructureQueryService.cs:195-209`] — Outer foreach catches `OperationCanceledException when (!ct.IsCancellationRequested)` and logs "probe timed out" but does not stamp Unhealthy on the row. If a `LocalAdminMetadataFallback` row exists (Healthy by default) and the inner translation re-threw (rare race), `/dapr` renders the state-store as **Healthy + LocalAdminMetadataFallback** while `/health` renders **Unhealthy** (because `IsStateStoreProbeFailed` finds zero `LocalAdminProbe` rows). Cross-page truth divergence on the AC1 component. Fix: in the outer catch, `merged[(entry.ComponentName, entry.ComponentType)] = entry with { Status = Unhealthy, LastCheckUtc = DateTimeOffset.UtcNow, Source = LocalAdminProbe }`. Source: edge.
- [x] [Review][Patch][applied 2026-05-10][MEDIUM] `GetActorRuntimeInfoAsync` passes potentially-null `actor.Type` to `KnownActorTypes.GetDescriptor` → NRE on `Types.TryGetValue(null, ...)` [`src/Hexalith.EventStore.Admin.Server/Services/DaprInfrastructureQueryService.cs:278-287`] — Local sidecar can return actor entries with `Type = null` during sidecar init (observed in some DAPR versions). `KnownActorTypes.GetDescriptor(null)` calls `Types.TryGetValue(null, out _)` → `ArgumentNullException` for the dictionary key. The remote actor branch already guards via `!string.IsNullOrEmpty(type)` (line 932) but the local branch lost that guard during the round-3 refactor. Fix: `if (string.IsNullOrEmpty(actor.Type)) continue;` before calling `GetDescriptor`. Source: edge.
- [x] [Review][Patch][deferred per round 5 line 463][MEDIUM] `DaprActors.razor` `RemoteMetadataStatus.NotConfigured` arm semantically overloaded after round 3 — UI still treats it as "endpoint not set" [`src/Hexalith.EventStore.Admin.UI/Pages/DaprActors.razor:79-101`] — Round 3 expanded `NotConfigured` server-side semantics to ALSO mean "did not consult remote in this code path" (`GetActorRuntimeInfoAsync` returns `NotConfigured` when local actors satisfy the request). UI copy still reads "endpoint not set". Operators see "Actor metadata: endpoint not set" on a healthy local-only sidecar, which is misleading. Fix: split `NotConfigured` into two enum values (`NotConfigured`, `NotConsulted`) OR adjust UI copy to "remote source not consulted (local sidecar provided actor metadata)" when local actors were sufficient. Source: edge.
- [x] [Review][Patch][applied 2026-05-10][MEDIUM] `HealthApi_Malformed_IssueBanner` injects a valid `SystemHealthReport` DTO, not a malformed payload [`tests/Hexalith.EventStore.Admin.UI.Tests/Pages/HealthPageTests.cs` (round-3 fixture, ~lines 3297-3325)] — Test constructs `SystemHealthReport(Unhealthy, 0, 0, 0, [], …, Unavailable, Unavailable, Unavailable)` (a valid empty-grid DTO) and asserts `markup.ShouldContain("No DAPR components detected")`. The empty-grid empty-state is exercised, not a deserialization/parse failure. AC2 line 95 lists "malformed error response" as a distinct failure mode; the named fixture is satisfied in name only. Fix: mock `IAdminApiClient.GetSystemHealthAsync` to throw `JsonException` (or simulate the API client returning a malformed result that triggers the page's malformed-handling branch) and assert IssueBanner copy specific to malformed-payload, not empty-state. Source: auditor.
- [x] [Review][Patch][deferred per round 5 line 466][MEDIUM] No test pins "history persistence fails" page-rendering contract [`tests/Hexalith.EventStore.Admin.UI.Tests/Pages/DaprHealthHistoryPageTests.cs`] — Spec line 68: "/dapr/health-history shows history storage unavailable or stale with timestamp/source." Round 3 added `DaprHealthHistoryPage_ShowsPubSubRow_WhenRemoteMetadataAvailable` (success path) and a collector-side `ExecuteAsync_ContinuesOnWriteFailure` test (write side), but no test asserts the page surface during a Redis outage. Fix: add `DaprHealthHistoryPage_ShowsHistoryUnavailableBanner_WhenStateStoreUnreachable` that mocks `IAdminApiClient.GetDaprHealthHistoryAsync` to throw or return a "history-storage-unavailable" timeline shape and assert the rendered banner. Source: auditor.
- [x] [Review][Patch][deferred per round 5 line 465][MEDIUM] No UI test pins rendered Source column copy on `/dapr` [`tests/Hexalith.EventStore.Admin.UI.Tests/Pages/DaprComponentsPageTests.cs`] — Reviewer Checklist line 507 ("Can an operator see which sidecar or probe supplied each DAPR fact?") is the central audit item; round 3 added the Source column in `DaprComponents.razor:1587-1590` via `FormatSource`/`GetSourceTitle` but no test asserts the rendered cells for each `DaprComponentSource` value. A regression that swaps the column for raw enum text or removes it entirely passes today. Fix: parameterised bUnit test feeding fixtures with `Source = RemoteEventStoreMetadata`, `LocalAdminProbe`, `LocalAdminMetadataFallback`, `Unavailable` and asserting the rendered cell copy and tooltip. Source: auditor.
- [x] [Review][Patch][deferred per round 5 line 471][LOW] Synth state-store row's hardcoded `Type = "state.unknown"` leaks into UI without annotation [`src/Hexalith.EventStore.Admin.Server/Services/DaprInfrastructureQueryService.cs:145`; `src/Hexalith.EventStore.Admin.UI/Pages/DaprComponents.razor` Type column] — When neither metadata source has the configured state-store, the synth row's Type is rendered verbatim as `state.unknown` — a fictional component-type string, not a real DAPR component type. Operators see it next to legitimate `state.redis` rows with no annotation that this row is a synthesis. Fix: render Type as `state.redis (configured, not loaded)` or add a tooltip on synth rows; alternatively keep the synth Type as the literal but render the `DaprComponentSource` column already added in round 3 prominently. Source: blind.
- [x] [Review][Patch][deferred per round 5 line 473][LOW] `GetDaprComponentStatusAsync` cancellation test asserts tautological propagation [`tests/Hexalith.EventStore.Admin.Server.Tests/Services/DaprHealthQueryServiceTests.cs:2466-2479`] — Mock-injected `OperationCanceledException` is rethrown directly because there is no try/catch in the production method; the test does not exercise real cancellation token plumbing and gives false confidence that cancellation works correctly in `GetCanonicalDaprInventoryAsync`. Fix: replace with a fixture that wires a real `CancellationTokenSource`, calls `Cancel()` mid-await, and asserts `OperationCanceledException` propagates with the cancelled token. Source: blind.
- [x] [Review][Patch][deferred per round 5 line 472][LOW] `DaprComponentSource.Unavailable = 0` is the default for `with` and JSON-deserialization; legacy clients lose provenance [`src/Hexalith.EventStore.Admin.Abstractions/Models/Dapr/DaprComponentSource.cs:111`] — JSON payloads from older clients without the `Source` field deserialize to `Unavailable`. The UI then renders "unavailable" provenance for valid legacy data, conflating "we lost source attribution" with "the source is genuinely unavailable". `HealthPageTests.cs:3097-3098` constructs `DaprComponentHealth("store", "state.redis", HealthStatus.Healthy, DateTimeOffset.UtcNow)` — 4 args — so `Source` defaults to `Unavailable` while the test asserts Healthy state. Fix: introduce `DaprComponentSource.Unspecified = 0` distinct from `Unavailable`; set the record default to `Unspecified` and have the UI coalesce both as needed; OR make `Source` non-optional (no default) so tests must explicitly choose a value. Source: blind.
- [x] [Review][Patch][applied 2026-05-10][LOW] `DaprHealthHistoryCollector` skip rule keys on `Source != Unavailable` rather than positive whitelist [`src/Hexalith.EventStore.Admin.Server/Services/DaprHealthHistoryCollector.cs:108-115`] — A future `DaprComponentSource.Foo = 4` is wire-deserialised as "has usable evidence" because the rule only excludes `== Unavailable`. Snapshot whose every row is "unknown future enum" is persisted, propagating the unknown enum into the timeline. Fix: replace with `everyRowHasNoSource = components.All(c => c.Source is DaprComponentSource.Unavailable or NotInSourceWhitelist(c.Source))` where the whitelist enumerates the four currently-defined values explicitly. Sources: blind+edge.
- [x] [Review][Patch][deferred per round 5 line 469][LOW] `IsStateStoreProbeFailed` collapses Healthy+Unhealthy probe-row pair into Unhealthy when remote and local report different Type strings under the same Name [`src/Hexalith.EventStore.Admin.Server/Services/DaprHealthQueryService.cs:174`] — Local fallback inserts `(statestore, state.redis)`; remote sidecar reports `(statestore, state.<other>)`. Stage 4 probe filter matches on Name only → both rows get probed with the same `GetStateAsync(statestore, …)` call; Redis flap mid-cycle can yield Healthy on one and Unhealthy on the other. UI renders two state-store rows for the same Name with conflicting status. Fix: deduplicate by Name before probing, or canonicalise Type when names collide. Source: edge.
- [x] [Review][Patch][deferred per round 5 line 468][LOW] `GetEventsPerSecondSeverity` collapses stale-zero into "neutral" identical to fresh-zero [`src/Hexalith.EventStore.Admin.UI/Pages/Health.razor:75,328`] — `EventsPerSecondStatus = Stale, EventsPerSecond = 0.0` → `IsValueShown(Stale) = true` → `GetEventsPerSecondSeverity(0) = "neutral"`. Value text shows `0.0 (stale)` but severity matches a healthy fresh zero — no visual cue. The `MetricSeverity` patch from round 1 specifically guarded the "stale 50% error rate" case to render error; the same logic for stale-zero events/sec gives no cue. Fix: pass `Status` to `GetEventsPerSecondSeverity` and return `"warning"` for `Stale`. Source: edge.
- [x] [Review][Patch][deferred per round 5 line 467][LOW] `_cachedRemoteMetadata` value-type field default-zero risk — `is { } cached` short-circuits on any assigned struct, including `default` [`src/Hexalith.EventStore.Admin.Server/Services/DaprInfrastructureQueryService.cs:39,722,807`] — `RemoteMetadataPayload?` field; nullable value-type pattern `is { } cached` returns true on any assigned value. A future maintainer assigning `default(RemoteMetadataPayload)` (e.g. on an early-return refactor) silently locks the cache to `Status = NotConfigured` for every subsequent in-scope call. Fix: add an invariant guard (`Status != default`) or wrap in a sentinel record like `RemoteMetadataResult.NotAttempted`. Source: edge.
- [x] [Review][Patch][applied 2026-05-10][LOW] `RecordingLogger<T>.BeginScope` returns null — production code that disposes scopes without null-check NREs under the test harness [`tests/Hexalith.EventStore.Admin.Server.Tests/Helpers/RecordingLogger.cs:18`] — Future probe code that does `var scope = logger.BeginScope(...); scope.Dispose();` (third-party log wrappers, custom enrichers) NREs. Today no touched code uses scopes; flag is forward-compat. Fix: return a no-op disposable sentinel (`NullDisposable.Instance`) instead of null. Source: edge.
- [x] [Review][Patch][deferred per round 5 line 470][LOW] Pub/sub row's `LastCheckUtc` reflects "remote-payload-parse time" not any health check [`src/Hexalith.EventStore.Admin.Server/Services/DaprInfrastructureQueryService.cs:130`] — Pub/sub rows from remote metadata ship `LastCheckUtc = now` taken at the start of `ParseRemoteMetadata`. Since they are never probed, the column conflates "last fact refreshed" with "last health probe." Combined with B13/E12 (state-store overwrite at projection), the `/health` LastCheck column mixes three semantics: probe time (state-store), parse time (pub/sub), render time (after `MapToHealthComponents` overwrite). Fix: introduce a separate `EvidenceCapturedAtUtc` field on `DaprComponentHealth` distinct from `LastCheckUtc`, and only populate `LastCheckUtc` for rows that have actual probe evidence. Source: edge.
- [x] [Review][Patch][applied 2026-05-10][LOW] Test method `ExecuteAsync_PersistsRemotePubSubRow_WhenInventoryIncludesPubSub` re-introduces `(DaprComponentHealthTimeline)null!` cast [`tests/Hexalith.EventStore.Admin.Server.Tests/Services/DaprHealthHistoryCollectorTests.cs:2072,2120`] — Round 2 patch line 289 / round 3 patch line 349 marked `[applied]`: "Seed an empty-but-non-null timeline." The two new/touched test methods reintroduce the same `null!` pattern. If the collector adds a defensive null short-circuit later, two tests pass-or-fail for the wrong reason. Fix: replace with `new DaprComponentHealthTimeline([], HasData: false)` or canonical equivalent. Source: auditor.
- [x] [Review][Patch][applied 2026-05-10][LOW] `HealthApi_Malformed_IssueBanner` asserts "No DAPR components detected" — uses spec-forbidden synonym (`detected` is a near-cousin of `missing`/`empty`) and double-counts the empty-state assertion [`tests/Hexalith.EventStore.Admin.UI.Tests/Pages/HealthPageTests.cs:3322`] — Spec line 54: "Avoid unmapped synonyms such as 'missing', 'empty', 'offline', or 'issue' in API fields or assertions." Visible UI copy may be friendlier, but the test asserting on a pre-existing empty-state copy as evidence of "malformed payload handled" double-counts the assertion. Fix: pair with A3 — once the test exercises a real malformed payload, assert on the malformed-specific banner copy instead. Source: auditor.

**Deferred (real but out of current story scope or pre-existing pattern)**

- [x] [Review][Defer] `Task.Delay(TimeSpan.FromSeconds(17))` × 3 in `DaprHealthHistoryCollectorTests` [`tests/Hexalith.EventStore.Admin.Server.Tests/Services/DaprHealthHistoryCollectorTests.cs`] — deferred, carry-over from rounds 2 and 3 plus one new instance added in round 3; collector still lacks `TimeProvider`/test-only-tick refactor; CI flake risk under load. Tracked in deferred-work.md.
- [x] [Review][Defer] `SystemHealthMetricStatus.Stale` has no production producer [`src/Hexalith.EventStore.Admin.Server/Services/DaprHealthQueryService.cs`, `src/Hexalith.EventStore.Admin.UI/Pages/Health.razor:281-300`] — deferred, reiteration of round-2/3 deferral; `Try*Async` methods only return `Available` or `Unavailable`; UI rendering and bUnit fixture are forward-compatible only. Removal would break the truth-contract status vocabulary; introduction of a producer requires a stale-cache layer story.
- [x] [Review][Defer] `DaprHealthHistoryEntry` has no `Source` attribution field [`src/Hexalith.EventStore.Admin.Abstractions/Models/Dapr/DaprHealthHistoryEntry.cs`, `src/Hexalith.EventStore.Admin.Server/Services/DaprHealthHistoryCollector.cs`] — deferred, carry-over from round 2/3; operators reading `/dapr/health-history` cannot distinguish synth-Unhealthy from probe-confirmed-Unhealthy in the timeline view. Adding the field requires a storage migration plan for already-persisted entries.
- [x] [Review][Defer] `DaprCanonicalInventory` `OrderBy(c => (int)c.Category)` and case-sensitive comparator chain depend on enum-ordinal stability and OrdinalIgnoreCase agreement across the call graph [`src/Hexalith.EventStore.Admin.Server/Services/DaprInfrastructureQueryService.cs:215, InventoryKeyComparer`] — deferred, system-wide pattern review needed; today the chain agrees by accident (every comparator is `OrdinalIgnoreCase`); a future change to any link silently desyncs. Track for a centralised key-canonicalisation refactor across Admin DTOs (round 3 deferral on `DaprCanonicalInventory` constructor null-guards is the same pattern family).

**Dismissed (noise / false positive)**

- Synth state-store row never probed because `FromComponentType("state.unknown")` doesn't return `StateStore` (Blind) — dismissed; verified `DaprComponentCategoryHelper.FromComponentType` matches by prefix `"state"`, so the synth row's Category is `StateStore` and Stage 4 probe filter does not skip it.
- Outer probe-foreach missing `catch (Exception ex)` for non-OCE failures (Blind) — dismissed; verified `ProbeStateStoreEntryAsync` itself catches `Exception ex` at line 700 and translates to Unhealthy + LocalAdminProbe before returning, so no non-OCE exception ever reaches the outer foreach. The outer's narrow `OperationCanceledException when (!ct.IsCancellationRequested)` filter is intentional.
- `InventoryKeyComparer.GetHashCode` would NRE on null Name/Type (Blind) — dismissed; Stage 1 filters via `IsNullOrEmpty(c.Name)` and `IsNullOrEmpty(c.Type)` before insertion; Stage 2/3 use validated `DaprComponentDetail` instances; the synth row uses `_options.StateStoreName` after a whitespace check. No realistic call-site can produce a null tuple component.
- `ProbeStateStoreEntryAsync` ignores `outerCt` between awaits (Blind) — dismissed during the reviewer's own analysis; `CreateLinkedTokenSource` propagates outer cancellation correctly, the catch-filter logic on `outerCt.IsCancellationRequested` is the correct check.
- Probe loop `merged.Values.ToArray()` snapshot race (Blind) — dismissed during the reviewer's own analysis; the service is scoped per-request and `merged` is a local variable, no concurrent calls share it.

### Review Findings (round 5 — 2026-05-10 — bmad-code-review of round-4 patch `admin-ui-health-dapr-truthfulness-fix-review4-diff.patch`)

Three reviewer layers (Blind Hunter, Edge Case Hunter, Acceptance Auditor) ran in parallel against the round-4 patch (~3,390 lines, 22 files). Triage merged 57 raw findings into 30 unique items: 5 decision-needed, 16 patch, 10 defer, 14 dismissed. Many of the Acceptance Auditor's "[applied 2026-05-10] not in diff" findings were verified against current HEAD (commit `5a9effa1`) and confirmed already addressed — those land in Dismissed below with the verifying line reference, so this round does not relitigate them.

**Decision-needed (resolve before patches)**

- [x] [Review][Decision][resolved 2026-05-10] D1 — `RemoteMetadataStatus.Initializing` enum member is removed but Operator Truth Contract / spec table still references it at lines 63, 483, 513, 546. **Chose (a) excise the four `Initializing` references from the spec/truth-contract table.** Round 3 D5 already chose to remove the enum value; this finishes the doc change. Related E7 (`LocalSidecarMetadataStatus = Unreachable` for a reachable-but-empty sidecar) stays as a separate documentation refinement — clarify the enum's `Unreachable` member doc to acknowledge it currently subsumes the "reachable but empty payload" case.
- [x] [Review][Decision][resolved 2026-05-10] D2 — `Source` parameter added to public records `DaprComponentHealth` and `DaprComponentDetail` is a binary-breaking change. **Chose (a) confirm the merge-commit `feat!:` / `BREAKING CHANGE:` footer at semantic-release time and accept the major version bump.** Verify the next merge commit message carries `BREAKING CHANGE:` so semantic-release publishes a major. Document the wire-format default (`Source = Unavailable` for legacy payloads) in the migration note.
- [x] [Review][Decision][resolved 2026-05-10] D3 — `RemoteMetadataStatus.NotConfigured` is overloaded by `GetActorRuntimeInfoAsync` to mean "remote not consulted" in addition to "endpoint not configured". **Chose (a) introduce `RemoteMetadataStatus.NotConsulted = 4`** + update `DaprActors.razor` to render distinct copy for the deliberate-skip case. Cleanest semantically; small additive wire-format change (no breaking semver because it's a new value, not a renumbering).
- [x] [Review][Decision][resolved 2026-05-10] D4 — Stage 1 stamps non-state-store local-fallback rows as `Status=Healthy` without probe evidence. **Chose (a) extend the round-4 "Unhealthy unless probe-covered" rule from state-stores to all categories.** Default fallback rows for pubsub/bindings/secret-stores to `Unhealthy` (or `Degraded` after spec discussion) when no probe will run. Most faithful to the truth contract; widest behavior change for `/health` consumers. Patch P1 carries this through.
- [x] [Review][Decision][resolved 2026-05-10] D5 — `_cachedRemoteMetadata` and `_cachedLocalMetadata` TOCTOU + non-atomic `Nullable<struct>` write. **Chose (b) cache `Task<RemoteMetadataPayload>` and `Task<LocalMetadataResult>` (reference assignment is atomic) so concurrent callers share the in-flight task** via `Interlocked.CompareExchange`. Preserves the "single canonical snapshot per request" guarantee. Remove the "either outcome safe" comment and replace with "all in-scope callers observe the same task and therefore the same payload."

**Patches (apply before status advances)**

- [x] [Review][Patch][applied 2026-05-10][HIGH] P1 — Stage 1 fallback for non-state-store components stamps `Status = Healthy` without probe evidence (mirror of D4 if D4 chooses (a)). [`src/Hexalith.EventStore.Admin.Server/Services/DaprInfrastructureQueryService.cs:104-135`] If the decision is (a), extend `isStateStoreRowWithoutProbeCoverage` to all categories and downgrade default Status accordingly. If (b)/(c), close the gap differently per the decision. **Applied:** Stage 1 now defaults `Status` based on probe-coverage AND category — probed state-store row → Healthy (Stage 4 overwrites), other state-store rows (different name / no configured name) → Unhealthy (truth-contract conflict prohibition), non-state-store rows → Degraded (operational with caveats; no probe attestation).
- [x] [Review][Patch][applied 2026-05-10][HIGH] P2 — Synthesized state-store row hardcodes `Source = LocalAdminProbe` at construction (`DaprInfrastructureQueryService.cs:160-175`) before the probe runs. The current comment justifies it ("the probe is about to run") but `Source = LocalAdminProbe` is a truth-contract claim that probe evidence exists. If the probe loop is ever short-circuited (e.g., probe path skipped due to a future refactor) the synth survives unmodified and `IsStateStoreProbeFailed` would silently flip on a future Status-default change. Synthesize with `Source = Unavailable` (or a new `LocalAdminProbePending` value); the actual probe completion is the only writer of `Source = LocalAdminProbe`. **Applied:** synth row now constructed with `Source = Unavailable`. Stage 4 (probe completion or outer-catch timeout handler) is the sole writer of `Source = LocalAdminProbe`. If a future refactor short-circuits Stage 4, the synth row no longer claims probe attestation it doesn't have.
- [x] [Review][Patch][applied 2026-05-10][HIGH] P3 — Remote-only state-store rows (Stage 2) are merged with `Status = Healthy` from `ParseRemoteMetadata` (`DaprInfrastructureQueryService.cs:906-914`) and survive Stage 4 probe untouched if the component name does not equal `_options.StateStoreName`. A remote-reported `eventstore-state` (different name from the configured Admin store) shows green on `/dapr` and `/health` with zero probe evidence. Either widen the probe filter beyond `_options.StateStoreName` (probing any state-store row from remote inventory), or downgrade remote state-store rows to a non-Healthy "loaded inventory only" status. **Applied:** Stage 2 merge loop downgrades remote state-store rows whose name does not match the configured Admin state-store name from `Healthy` to `Degraded` ("loaded inventory only, no probe attestation"). The configured state-store row keeps Healthy because Stage 4 still probes and rewrites it. Avoids the false-negative "component not found" trap of probing remote-only state stores against the Admin sidecar.
- [x] [Review][Patch][applied 2026-05-10][MEDIUM] P4 — `MapToHealthComponents` over-projects `DaprComponentDetail` to `DaprComponentHealth` and drops `Capabilities`, `Version`, `Category`. When two distinct types share a component name (e.g., synth `state.unknown` + remote `state.redis`), the `/health` grid shows two rows that look identical to the operator. Either de-dupe on `ComponentName` in projection (preferring the probe row) or surface the differentiating field via tooltip/aria-label. **Applied:** `MapToHealthComponents` now de-dupes by `ComponentName` (case-insensitive). When multiple rows share a name, the projection picks the most authoritative row using a Source preference rank (LocalAdminProbe → RemoteEventStoreMetadata → LocalAdminMetadataFallback → Unavailable), then prefers a non-`state.unknown` ComponentType, then most recent `LastCheckUtc`. Eliminates the "two identical-looking rows" failure mode without dropping the differentiating ComponentType from the per-row projection.
- [x] [Review][Patch][applied 2026-05-10][MEDIUM] P5 — `localSidecarMetadataAvailable` heuristic OR-with-`Extended` is a false positive (`DaprInfrastructureQueryService.cs:98-102`). DAPR populates `Extended` (`daprRuntimeVersion`, etc.) at sidecar startup before app registration, so a half-initialized sidecar trips the heuristic and `ComputeOverallStatus` cannot return `Unhealthy` for the very initialization state the check was added to catch. Drop `Extended` from the disjunction; require `Components` or `Actors` to have content.
- [x] [Review][Patch][applied 2026-05-10][MEDIUM] P6 — Probe loop snapshot order issue: the loop builds `probesByEntry` from `merged.Values.ToArray()` then awaits sequentially while overwriting `merged` (`DaprInfrastructureQueryService.cs:192-232`). Today benign with one probe; the comment documents it is "shaped for n probes" — at n>1 a probe returning a key already overwritten by a sibling can race with last-write-wins. Stage probe results into a local dict then merge once after all awaits complete. **Applied:** introduced a per-call `Dictionary<(name,type), DaprComponentDetail> probeResults`. The probe loop writes to `probeResults` (success path AND outer-catch timeout path); a final `foreach` merges `probeResults` into `merged` once after all probes settle. Deterministic regardless of probe completion order.
- [x] [Review][Patch][applied 2026-05-10][MEDIUM] P7 — `ComputeOverallStatus` lacks a default arm for unknown `RemoteMetadataStatus` values (`DaprHealthQueryService.cs:201-228`). When a future enum member is added, the function falls through to `Healthy` regardless of state — exactly the unknown-default trap the rest of this rewrite goes to lengths to prevent. Add a default arm that logs a warning and treats unknown statuses as `Degraded`, mirroring the UI's defensive `LogUnknownMetricSeverity` pattern. **Applied:** `ComputeOverallStatus` switches on `RemoteMetadataStatus` with explicit arms for `NotConfigured`/`Available`/`Unreachable`/`InvalidPayload`. Default arm calls `LogUnknownAndDegrade` which emits a `LogWarning` naming the unknown enum value and returns `Degraded`. Now takes `ILogger logger` parameter for the unknown-arm telemetry.
- [x] [Review][Patch][applied 2026-05-10][MEDIUM] P8 — `ComputeOverallStatus` short-circuits on `stateStoreProbeFailed` so a two-cause failure (e.g., whitespace `StateStoreName` AND local sidecar unreachable) hides the second cause from the OverallStatus reason. Operators iterating to recovery cannot tell which fix worked. Either expose a `Reasons: IReadOnlyList<string>` collection on `SystemHealthReport`, or document "first-failure wins" semantics in code comments + spec so banners are the source of multi-cause truth. **Applied:** chose option (b) — added an XML doc-comment to `ComputeOverallStatus` that documents the first-failure-wins ordering and points operators to `SystemHealthReport.InventorySourceStatus` / `LocalSidecarMetadataStatus` / per-component rows for multi-cause diagnosis. Avoids a `SystemHealthReport` ctor arity change. The /health UI banners (P9 inventory-empty copy, plus existing per-status banners) surface multi-cause information independently.
- [x] [Review][Patch][applied 2026-05-10][MEDIUM] P9 — `Health.razor` empty-state copy contradicts an `Unhealthy` overall badge. When the canonical inventory is empty (e.g., misconfigured `StateStoreName` + remote `NotConfigured` + local sidecar `Unreachable`), `OverallStatus = Unhealthy` but the grid renders `<EmptyState Title="No DAPR components detected" Description="DAPR component health information will appear here when components are registered." />` — a friendly "no components yet" message beneath an Unhealthy badge. Spec AC2: "the page never hides an outage behind nominal data." When `OverallStatus == Unhealthy`, the empty-state should explain the outage cause referring to `InventorySourceStatus` / `LocalSidecarMetadataStatus`. **Applied:** added a conditional empty-state path in `Health.razor`: when `OverallStatus == Unhealthy` and `DaprComponents.Count == 0`, renders "DAPR component inventory unavailable" with a description built by `BuildEmptyInventoryOutageDescription` that names the unavailable metadata sources (local sidecar / remote EventStore sidecar) using their `RemoteMetadataStatus` values. The friendly "components will appear here" copy remains for the Healthy/Degraded zero-components case.
- [x] [Review][Patch][applied 2026-05-10][MEDIUM] P10 — `IsStateStoreProbeFailed` whitespace-name path now logs a structured warning (`DaprHealthQueryService.cs:147-156`) but the `/health` API surface still returns `Unhealthy` with no diagnosable component row in the response (the synth row is suppressed for whitespace name to avoid `DaprComponentDetail` validation throw). Operators chasing a misconfigured deployment via API only see "Unhealthy" with no component entry. Either validate `StateStoreName` non-empty in `AdminServerOptions` (hard fail at startup), or surface a synthetic `configuration-error` component row that rendering can pick up. **Applied:** chose hard-fail-at-startup. `ServiceCollectionExtensions.AddAdminServer` now uses `services.AddOptions<AdminServerOptions>().Bind(...).ValidateOnStart()`, so the existing `AdminServerOptionsValidator` (which already checks `StateStoreName` non-empty) runs at host startup instead of lazily on first IOptions resolution. A misconfigured deployment fails the build-up of the host before /health is reachable, eliminating the ambiguous "Unhealthy with no diagnosable component row" runtime state. Defense-in-depth: `IsStateStoreProbeFailed` retains its runtime `LogWarning` for any path the validator misses.
- [x] [Review][Patch][applied 2026-05-10][MEDIUM] P11 — Tests use `await Task.Delay(TimeSpan.FromSeconds(17))` to wait for `BackgroundService` ticks (`DaprHealthHistoryCollectorTests.cs:1980, 2001-2005, 2037-2041, 2089-2092`). Four instances × 17s = ~68 seconds wall-clock per CI run for this file alone, and the sleep is fragile under loaded agents (paused thread can miss the tick window). Use `TaskCompletionSource` triggered from the substituted infra-service callback, or expose a private virtual `Tick` seam, then `await tcs.Task` with a generous timeout. (This is a carry-over from the Round-4 deferred-work entry — count is up to 4 instances now.) **Applied:** `DaprHealthHistoryCollector` now accepts an optional `TimeProvider? timeProvider` parameter (default `TimeProvider.System`); the 15-second startup `Task.Delay` and the `PeriodicTimer` constructor both use the injected provider. Tests inject `Microsoft.Extensions.Time.Testing.FakeTimeProvider`, advance the fake clock past the startup delay, and poll a substituted observation flag (e.g., `saveObserved`, `inventoryObserved`, `captured is not null`) for up to 2 seconds. All five 17-second sleeps replaced by deterministic `DriveFirstCaptureAsync` helper.
- [x] [Review][Patch][applied 2026-05-10][MEDIUM] P12 — Add server-side fixtures for HTTP 429, 5xx-with-`Retry-After`, `Content-Length: 0` 200, gzip-encoded body without `AutomaticDecompression`, and mixed-case JSON property names. Today every non-`OperationCanceledException`/`JsonException` outcome collapses to `RemoteMetadataStatus.Unreachable`; mixed-case property names (`ComponentS` capitalized) silently produce empty `components`/`subscriptions` lists with `Status = Available` — the worst outcome (silent data loss) directly violating the truth contract row "Unknown or unavailable evidence must never render as healthy, zero, or empty." **Applied:** added three new fixtures in `DaprInfrastructureQueryServiceTests`: (1) `[Theory]` `_ReturnsUnreachable_OnTransportFailureStatusCodes` parameterised over 429 / 503 / 502 / 504 — pins Unreachable + `PubSubSubscriptions.Count == 0`; (2) `_ReturnsAvailable_WithEmptyOptionalSections_WhenJsonHasZeroLengthBody` — empty 200 body must classify as InvalidPayload (JsonDocument.ParseAsync throws); (3) `_ReturnsInvalidPayload_OnMixedCaseRequiredProperties` — `Components`/`Subscriptions` (capitalised) classifies as InvalidPayload because `RequiredArray` uses case-sensitive `TryGetProperty`. Verifies the silent-data-loss path is closed for all currently-known transport-error variants.
- [x] [Review][Patch][applied 2026-05-10][LOW] P13 — `RecordingLogger<T>._records` is a `List<LogRecord>` with no synchronization. Switched to `Lock`-guarded `List<LogRecord>` with a snapshot read in `Records` so concurrent log writes from probe + remote-fetch paths cannot race on `Add` or expose a mid-iteration mutation to assertion code.
- [x] [Review][Patch][applied 2026-05-10][LOW] P14 — `RemoteMetadataPayload.Endpoint` userinfo strip: added `SanitizeEndpoint` helper that uses `UriBuilder` to strip `UserName`/`Password` before the configured endpoint is assigned to the payload. UI tooltips and empty-state copy now never echo credentials embedded in `EventStoreDaprHttpEndpoint`.
- [x] [Review][Patch][applied 2026-05-10][LOW] P15 — `RemoteMetadataPayload` `Components`/`Subscriptions`/`Actors` properties retyped from `List<>?` to `IReadOnlyList<>?`. The parser wraps each working list in `.AsReadOnly()` before assignment so a future consumer that calls `.ToList()` and mutates cannot silently corrupt other in-scope consumers' views.
- [x] [Review][Patch][applied 2026-05-10][LOW] P16 — Added tertiary tiebreaker `.ThenBy(c => c.ComponentType, StringComparer.OrdinalIgnoreCase)` to `DaprCanonicalInventory.OrderBy` chain. Two rows with identical `(Category, ComponentName)` but different `Type` (synth `state.unknown` + remote `state.redis`) now order deterministically across runtime/dictionary internals.
- [x] [Review][Patch][applied 2026-05-10][LOW] P17 — `Health.razor` `LoadDataAsync` now sets `_isLoading = true` at entry and resets in a `finally` block, mirroring `OnInitializedAsync`. Retry button after a previous API failure shows the SkeletonCard during the retry attempt instead of looking frozen.

**Deferred (carry-over from round 4 + new defers)**

- [x] [Review][Defer] DaprActors `NotConfigured` semantic overload — covered under D3 above; defer the patch until the decision lands. [round-4 carry-over]
- [x] [Review][Defer] Source-column rendering test for each `DaprComponentSource` value on `/dapr` — `tests/Hexalith.EventStore.Admin.UI.Tests/Pages/DaprComponentsPageTests.cs`. Round 3 added the column; no test pins the rendered cell copy/tooltip per Source value. A regression that swaps the column for raw enum text or removes it entirely passes today. [round-4 carry-over]
- [x] [Review][Defer] History-persistence-fails page test for `/dapr/health-history` — `tests/Hexalith.EventStore.Admin.UI.Tests/Pages/DaprHealthHistoryPageTests.cs`. Spec line 68: "/dapr/health-history shows history storage unavailable or stale with timestamp/source." No test pins the page surface during a Redis outage. [round-4 carry-over]
- [x] [Review][Defer] `_cachedRemoteMetadata` value-type struct invariant guard — `DaprInfrastructureQueryService.cs:547`. `RemoteMetadataPayload?` field; `is { } cached` short-circuits on any assigned struct including `default(RemoteMetadataPayload)`. No current bug, forward-compat hazard. (Subsumed by D5 if (a) or (b) is chosen.) [round-4 carry-over]
- [x] [Review][Defer] `GetEventsPerSecondSeverity` collapses `Stale + 0 events/sec` to neutral severity, indistinguishable from a healthy fresh zero — `Health.razor:1860-1862`. AC3 visual-distinguishability gap for one specific value-status combination. [round-4 carry-over]
- [x] [Review][Defer] `IsStateStoreProbeFailed` two-row dedupe — when local fallback inserts `(statestore, state.redis)` and remote reports `(statestore, state.<other>)`, both pass the Stage 4 name-only filter; Redis flap mid-cycle yields conflicting status under one Name. [round-4 carry-over]
- [x] [Review][Defer] Pub/sub row `LastCheckUtc` semantics — remote-attributed pub/sub rows ship `LastCheckUtc = parse-time`, never probed; combined with `MapToHealthComponents` overwriting at projection time, the `/health` LastCheck column conflates probe time, parse time, and render time. [round-4 carry-over]
- [x] [Review][Defer] Synth `state.unknown` literal renders verbatim in the `/dapr` Type column without a "synthesized row" annotation — `DaprComponents.razor:1585`. The Source column carries `LocalAdminProbe` as a partial mitigation, but the literal `state.unknown` is operator-confusing. [round-4 carry-over]
- [x] [Review][Defer] `DaprComponentSource.Unspecified` vs `Unavailable` — the wire-default (ordinal 0 = `Unavailable`) for legacy clients and `with`-expressions silently strips Source provenance from valid data. Truth-contract conflict: "a successfully reported component must never carry Source = Unavailable" but the wire-default makes that violation easy to introduce. [round-4 carry-over — partially mitigated by P14 if D2 chooses init-only property]
- [x] [Review][Defer] `GetDaprComponentStatusAsync` cancellation tautological test — no try/catch in the production method; mock-injected OCE rethrow is structurally guaranteed. [round-4 carry-over]
- [x] [Review][Defer] AC8 manual Aspire smoke evidence — operator-owned per the Dev Note exit criterion; story remains at `review` until evidence is pasted. [process — round-4 carry-over]

**Dismissed (already addressed in HEAD or false positive)**

- AC1 secret-leak test only inspects `record.Message`, not `record.Exception?.Message` (Auditor) — addressed differently in HEAD: the test now has a positive `anyRecordCapturedSecretException.ShouldBeTrue` assertion at `tests/Hexalith.EventStore.Admin.Server.Tests/Services/DaprHealthQueryServiceTests.cs:338-341` that proves the probe path was exercised; the deliberate decision *not* to assert against `record.Exception.Message` is documented in the comment at lines 322-328 ("that field carries the raw exception text by design").
- `count` field non-integer JSON token mis-classified as `Unreachable` (Auditor) — addressed in HEAD: `DaprInfrastructureQueryService.cs:980` now uses `TryGetInt32(out int parsedCount)` per the comment at lines 973-975.
- Stage 1 `LocalAdminMetadataFallback` writes state-store rows as `Status = Healthy` when probe will not run (Auditor) — addressed in HEAD: `DaprInfrastructureQueryService.cs:104-135` now computes `isStateStoreRowWithoutProbeCoverage` and writes `HealthStatus.Unhealthy` when the probe will not cover the row (line 129).
- `HealthApi_Forbidden_NoStaleReuse` fixture-name-vs-body mismatch (Auditor) — addressed in HEAD: `tests/Hexalith.EventStore.Admin.UI.Tests/Pages/HealthPageTests.cs:505-550` now exercises both (1) cold-start 401 and (2) warm-cache + 401-on-refresh sequences with stale-banner assertions.
- `HealthApi_Timeout_StaleLastGood` fixture-name-vs-body mismatch (Auditor) — addressed in HEAD: `HealthPageTests.cs:553-594` now exercises the cold-start timeout AND the warm-cache + timeout-on-refresh sequence with stale-label assertion.
- `HealthApi_Malformed_IssueBanner` injects valid DTO + uses spec-forbidden synonym `"detected"` (Auditor) — addressed in HEAD: `HealthPageTests.cs:597-620` now throws `JsonException` and asserts on banner copy without the `"detected"` synonym.
- `RecordingLogger.BeginScope` returns null (Auditor) — addressed in HEAD: `tests/Hexalith.EventStore.Admin.Server.Tests/Helpers/RecordingLogger.cs:16-17` returns `NullScope.Instance`.
- `(DaprComponentHealthTimeline)null!` cast re-introduced in `ExecuteAsync_PersistsRemotePubSubRow` (Auditor) — addressed in HEAD: all four occurrences in `DaprHealthHistoryCollectorTests.cs` now use `new DaprComponentHealthTimeline([], HasData: false)` (lines 68, 142, 220, 273).
- `RemoteMetadataStatus.InvalidPayload` added with implicit ordinal (Blind) — addressed in HEAD: `src/Hexalith.EventStore.Admin.Abstractions/Models/Dapr/RemoteMetadataStatus.cs:15-28` pins all ordinals explicitly (`NotConfigured = 0` … `InvalidPayload = 3`) and adds `[JsonConverter(typeof(JsonStringEnumConverter))]`.
- `subscriptionsElement.GetArrayLength()` vs filtered `subscriptions.Count` divergence (Blind) — addressed in HEAD: `DaprInfrastructureQueryService.cs:920-924` documents that `subCount` is finalised from the filtered subscriptions list specifically so `/dapr` and `/dapr/pubsub` agree on the canonical payload.
- `DaprPubSub.razor` `Logger` reference without visible `@inject` line (Blind) — false positive: `@inject ILogger<DaprPubSub> Logger` is present at line 14 of `src/Hexalith.EventStore.Admin.UI/Pages/DaprPubSub.razor` (just outside the diff context window).
- `Test asserts enum.ToString().ShouldNotContain("p@ssw0rd")` is tautological (Blind) — partially valid (enum `ToString` cannot contain the secret) but the broader test now has the meaningful `anyRecordCapturedSecretException.ShouldBeTrue` positive assertion that proves the leak surface is exercised. The tautological enum assertions remain in HEAD as defensive belt-and-braces; deletion is not pursued because they cost ~zero and removing them would invite the next reviewer to flag the same line as missing coverage.
- `MetricSeverity` static-vs-instance + unknown-arm UI signal (Blind) — partially mitigated: `LogUnknownMetricSeverity` already calls `Logger.LogWarning` with the unknown status name, providing operator-visible evidence in logs. The visible UI difference (rendering "warning" vs a distinct token) is a UX concern that doesn't break the truth contract; deferred-work `B16` covers the synth-row-annotation case which is closer to the same UX gap.
- `_components.Any` LINQ scans called twice + reassignment race (Blind) — dismissed: the Blazor circuit serializes UI updates via `InvokeAsync`, so `_components` cannot be reassigned mid-method. The "iterate up to three times" perf concern is negligible at typical inventory sizes (<100 components).



### Review Findings (round 6 — 2026-05-10 — bmad-code-review of `f0e46f20^..f0e46f20`)

Three reviewer layers ran against the latest isolated story-owned patch while excluding the current working tree changes for `admin-ui-consistency-and-tenant-clarity-polish`. Triage kept 3 actionable patch findings and dismissed 6 as already recorded in the round-5 pending Phase 2/3 action list.

**Patches (apply before status advances)**

- [x] [Review][Patch][applied 2026-05-10][HIGH] Remote endpoint credentials are still logged by the service constructor [`src/Hexalith.EventStore.Admin.Server/Services/DaprInfrastructureQueryService.cs:69`] — Round-5 P14 sanitizes `RemoteMetadataPayload.Endpoint` before it reaches UI payloads, but the constructor still logs `_options.EventStoreDaprHttpEndpoint` directly. A configured value like `http://user:pass@host:3500` is redacted in the new payload path but still written to logs at startup, leaving a credential leak on the same `EventStoreDaprHttpEndpoint` surface the patch is trying to close.
- [x] [Review][Patch][applied 2026-05-10][MEDIUM] Sanitized endpoint is also used for the outbound metadata request [`src/Hexalith.EventStore.Admin.Server/Services/DaprInfrastructureQueryService.cs:794`] — `ReadRemoteMetadataAsync` assigns `endpoint = SanitizeEndpoint(_options.EventStoreDaprHttpEndpoint)` and then builds `baseUrl` from that sanitized value. The patch intent is to strip userinfo before assigning to `RemoteMetadataPayload.Endpoint`; using the stripped URI for transport can turn a valid userinfo/proxy-auth endpoint into `RemoteMetadataStatus.Unreachable`.
- [x] [Review][Patch][applied 2026-05-10][MEDIUM] Round-5 D1 still leaves stale `Initializing` references in the story/spec [`_bmad-output/implementation-artifacts/admin-ui-health-dapr-truthfulness-fix.md:77`] — The round-5 decision says all stale `Initializing` references were excised after removing `RemoteMetadataStatus.Initializing`, but the conflict table still says "unreachable, invalid, initializing, or not configured" and the Dev Agent Record/File List still describe `Initializing` UI branches. The spec and implementation remain contradictory, which invites future tests or UI work for an enum value that no longer exists.

### Review Findings (round 7 — 2026-05-10 — bmad-code-review of current applied `admin-ui-health-dapr-truthfulness-fix-review4-diff.patch`)

Three reviewer layers rechecked the applied patch against the live worktree and story contract. Triage kept 6 new actionable patch findings and dismissed 7 findings as already fixed in HEAD, already recorded in round 5, or false positives.

**Patches (apply before status advances)**

- [x] [Review][Patch][applied 2026-05-10][HIGH] State-store probing overwrites remote inventory provenance [`src/Hexalith.EventStore.Admin.Server/Services/DaprInfrastructureQueryService.cs:143`] [`src/Hexalith.EventStore.Admin.Server/Services/DaprInfrastructureQueryService.cs:878`] — Stage 2 records a remote EventStore component with `Source = RemoteEventStoreMetadata`, but the state-store probe later rewrites the same row to `Source = LocalAdminProbe`. The conflict rule requires one row that preserves loaded inventory evidence plus local probe health evidence; the current single `Source` field loses the remote half of the truth and can make a remotely loaded state store look like a local-probe-only fact.
- [x] [Review][Patch][applied 2026-05-10][HIGH] `/dapr/sidecar` hides reachable remote metadata when local Admin sidecar metadata is unavailable [`src/Hexalith.EventStore.Admin.Server/Services/DaprInfrastructureQueryService.cs:259`] [`src/Hexalith.EventStore.Admin.UI/Pages/DaprComponents.razor:108`] — `GetSidecarInfoAsync` returns `null` before reading remote metadata when local metadata is missing, and the page treats `_sidecar is null` as an empty-state condition even if `_components` later contains remote canonical inventory. This can render "DAPR sidecar metadata unavailable" next to a real component grid and suppress the remote subscription/status stat cards in the scenario where the story says remote metadata may still be shown.
- [x] [Review][Patch][applied 2026-05-10][HIGH] Health-history state-store read failures still throw instead of returning explicit unavailable/stale evidence [`src/Hexalith.EventStore.Admin.Server/Services/DaprHealthQueryService.cs:291`] — `GetComponentHealthHistoryAsync` awaits all state-store reads and only catches cancellation, so a Redis/state-store outage becomes a controller 503/500 rather than a history response that labels persistence/read status unavailable or stale. The story's history contract says unreadable history must not look like empty success and should be labelled unavailable/stale.
- [x] [Review][Patch][applied 2026-05-10][MEDIUM] Invalid remote metadata shapes can become `Available` or `Unreachable` instead of `InvalidPayload` [`src/Hexalith.EventStore.Admin.Server/Services/DaprInfrastructureQueryService.cs:986`] [`src/Hexalith.EventStore.Admin.Server/Services/DaprInfrastructureQueryService.cs:1008`] — only `JsonException` maps to `RemoteMetadataStatus.InvalidPayload`, while wrong-shaped but parseable metadata (for example missing/non-array metadata sections) is treated as an available empty inventory, and parser `InvalidOperationException`s fall into the catch-all `Unreachable` branch. This breaks the contract that malformed or incomplete metadata is invalid, not a transport outage or real zero.
- [x] [Review][Patch][applied 2026-05-10][MEDIUM] Same-name non-state component can suppress the synthetic state-store probe row [`src/Hexalith.EventStore.Admin.Server/Services/DaprInfrastructureQueryService.cs:156`] [`src/Hexalith.EventStore.Admin.Server/Services/DaprInfrastructureQueryService.cs:192`] — Stage 3 skips synthesis when any merged key has the configured state-store name, regardless of category/type, but Stage 4 only probes `StateStore` rows. A remote/local non-state component with the same name can leave overall health unhealthy with no probe-attributed state-store row explaining the outage.
- [x] [Review][Patch][applied 2026-05-10][MEDIUM] `/health` component grid carries `Source` but does not render it [`src/Hexalith.EventStore.Admin.UI/Pages/Health.razor:123`] — `DaprComponentHealth` now includes source attribution, but `/health` only shows component name, type, status, and last check. A `LocalAdminMetadataFallback` row can therefore look like ordinary canonical evidence on `/health`, violating the cross-page truth rule that local fallback must be visibly labelled.

**Dismissed (already addressed, already recorded, or false positive)**

- State-store probe timeout can remain healthy — addressed in HEAD by the outer timeout catch that stamps the entry `Unhealthy` with `LocalAdminProbe`.
- Transient local metadata failure is cached — false positive in HEAD; `TryReadLocalMetadataAsync` returns the failure result without assigning `_cachedLocalMetadata`.
- Future `DaprComponentSource` enum value overwrites history — false positive; the current positive whitelist treats unknown source values as no usable evidence and skips persistence.
- Canonical component timestamp is overwritten during projection — addressed in HEAD; `MapToHealthComponents` preserves `c.LastCheckUtc`.
- AC8 manual Aspire smoke evidence is missing — already recorded as deferred/operator-owned in round 5 and still blocks story completion.
- Source subtitle hides `Unavailable` rows — low-value UI polish compared with the retained source-column gap; not a merge blocker.
- Secret text in captured exception logs — outside the explicit AC1 response-body leak assertion and already documented in the test comments as deliberate logger behavior.

### Review Findings (round 8 — 2026-05-10 — bmad-code-review of current HEAD after round-7 fixes)

Single-session review rechecked the live worktree against the story contract after the round-7 fixes. Triage kept 3 actionable patch findings.

**Patches (apply before status advances)**

- [x] [Review][Patch][applied 2026-05-10][HIGH] Remote metadata client timeouts are rethrown as cancellation instead of `RemoteMetadataStatus.Unreachable` [`src/Hexalith.EventStore.Admin.Server/Services/DaprInfrastructureQueryService.cs:1019`] — `ReadRemoteMetadataAsync` uses the named `DaprSidecar` `HttpClient` with its own timeout, but catches every `OperationCanceledException` and rethrows it. When a configured remote EventStore sidecar hangs until the client timeout fires while the request token is still valid, `/health`, `/dapr`, and `/dapr/pubsub` can fail/cancel instead of returning explicit `RemoteMetadataStatus.Unreachable` evidence. This violates the remote metadata truth contract for unreachable/bounded probes.
- [x] [Review][Patch][applied 2026-05-10][HIGH] Local Admin sidecar metadata read is still unbounded [`src/Hexalith.EventStore.Admin.Server/Services/DaprInfrastructureQueryService.cs:1058`] — `TryReadLocalMetadataAsync` awaits `_daprClient.GetMetadataAsync(ct)` with only the caller token. If the local Admin sidecar metadata call hangs, `/health` cannot return a bounded partial report with local metadata unavailable; it waits until the outer request is cancelled. Add a linked timeout and map timeout-without-caller-cancellation to `Available = false`, preserving real caller cancellation.
- [x] [Review][Patch][applied 2026-05-10][HIGH] Health refresh failures are swallowed before the page can mark cached data stale [`src/Hexalith.EventStore.Admin.UI/Pages/Health.razor:202`] [`src/Hexalith.EventStore.Admin.UI/Services/DashboardRefreshService.cs:112`] — `Health.razor` subscribes only to `RefreshService.OnDataChanged`, while `DashboardRefreshService.RefreshAsync` routes thrown 401/403/503/timeout failures to `OnError` and does not emit `DashboardData(null, null)`. The page's manual Refresh button calls `TriggerImmediateRefreshAsync`, so after a successful load a real refresh failure leaves the old health report visible as fresh rather than stale/issue-labelled. The current tests invoke the private `OnDataChanged` backing field directly and do not exercise this production failure path, violating AC2's "never hides an outage behind nominal data" rule.

### Review Findings (round 9 — 2026-05-10 — bmad-code-review of focused story diff `388ceb78^..HEAD`)

Three reviewer layers ran against the focused story-owned diff (27 files, 4,366 additions / 631 deletions): Blind Hunter, Edge Case Hunter, and Acceptance Auditor. Triage kept 1 decision-needed item, 5 patch findings, 1 defer, and dismissed 5 already-accepted or duplicate findings.

**Decision Needed**

- [x] [Review][Patch][HIGH][applied 2026-05-10][decision resolved: split fields] Component `Source` conflates inventory provenance with local health-probe evidence [`src/Hexalith.EventStore.Admin.Server/Services/DaprInfrastructureQueryService.cs:921`] [`src/Hexalith.EventStore.Admin.UI/Pages/DaprComponents.razor:470`] — When a remote-reported configured state-store row is locally probed, `ProbeStateStoreEntryAsync` preserves `Source = RemoteEventStoreMetadata` while overwriting `Status` and `LastCheckUtc` with local probe evidence. `/dapr` then renders the row as "Remote (eventstore)" even if the unhealthy status came from the Admin local probe. Decision applied: split inventory provenance from health evidence source, render both where needed, and require local probe health evidence for configured state-store success in `/health`.

**Patches (apply before status advances)**

- [x] [Review][Patch][HIGH][applied 2026-05-10] AC2 stale cache is reused across auth/tenant-context failure [`src/Hexalith.EventStore.Admin.UI/Pages/Health.razor:277`] [`tests/Hexalith.EventStore.Admin.UI.Tests/Pages/HealthPageTests.cs:536`] — `OnRefreshError` treats every refresh failure as stale-safe and keeps `_cachedHealthReport` visible. AC2 explicitly forbids stale data reuse across auth user or tenant-context changes; the current warm-cache 401/403 path can show the prior user's/tenant's health report as stale instead of clearing it. Applied: cache/report cleared on auth-context failures and warm-cache 401/403 regression asserts prior report is not rendered.
- [x] [Review][Patch][MEDIUM][applied 2026-05-10] AC2 stale last-good metrics render as fresh values [`src/Hexalith.EventStore.Admin.UI/Pages/Health.razor:289`] [`src/Hexalith.EventStore.Admin.UI/Pages/Health.razor:351`] — Refresh failure only flips `_isStale`; it does not mark displayed metric statuses as `Stale`. The stat-card formatters append "(stale)" only when the DTO status is already `Stale`, so cached values after a failed refresh keep rendering as fresh numbers under only a banner. Applied: stat-card formatting honors `_isStale` and warm-cache timeout/error tests assert the stale suffix.
- [x] [Review][Patch][MEDIUM][applied 2026-05-10] `/dapr/pubsub` Pub/Sub Components card still renders fake zero when remote metadata is unavailable [`src/Hexalith.EventStore.Admin.UI/Pages/DaprPubSub.razor:53`] — The Subscriptions and Topics cards correctly gate on `RemoteMetadataStatus`, but the Pub/Sub Components card always renders `_overview.PubSubComponents.Count`. For `Unreachable`, `NotConfigured`, or `InvalidPayload`, this displays `0` instead of unavailable, violating AC4/AC6's "failed metadata is unavailable, not empty" rule. Applied: the card gates on `RemoteMetadataStatus == Available` and a bUnit regression covers non-Available remote metadata.
- [x] [Review][Patch][MEDIUM][applied 2026-05-10] `SystemHealthReport.LocalSidecarMetadataStatus` defaults to `Available` when the field is missing [`src/Hexalith.EventStore.Admin.Abstractions/Models/Health/SystemHealthReport.cs:52`] [`src/Hexalith.EventStore.Admin.UI/Pages/Health.razor:59`] — Legacy JSON or callers using the old constructor omit the new field, and the default suppresses the local-sidecar issue banner as if evidence existed. This contradicts the story rule that new status fields must default to non-green evidence states. Applied: default is now `Unreachable`, while current server producers pass `Available` explicitly when the local sidecar metadata read succeeds.
- [x] [Review][Patch][MEDIUM][applied 2026-05-10] Health history does not record remote metadata invalid/unavailable source state [`src/Hexalith.EventStore.Admin.Server/Services/DaprHealthHistoryCollector.cs:129`] [`src/Hexalith.EventStore.Admin.Abstractions/Models/Dapr/DaprHealthHistoryEntry.cs:12`] — When remote metadata is `Unreachable`/`InvalidPayload` and no usable component rows exist, the collector skips the sample entirely, and persisted entries have no source/status field. `/dapr/health-history` can therefore leave prior samples visually fresh without recording the current source outage. Applied: persisted entries carry inventory/health-evidence/source-status fields, source outages persist synthetic source-status entries, and the history page renders the outage banner.

**Deferred**

- [x] [Review][Defer] Multi-instance health-history collectors can lose samples via last-write-wins state updates [`src/Hexalith.EventStore.Admin.Server/Services/DaprHealthHistoryCollector.cs:141`] — deferred, pre-existing deployment-hardening concern. Two Admin instances can read the same timeline, append independently, and overwrite one sample on `SaveStateAsync`; solve later with DAPR ETags/`TrySaveStateAsync` retry, a transactional append design, or single-collector leader election.

### Review Findings (round 10 — 2026-05-10 — bmad-code-review of full story diff `388ceb78~1..HEAD` on story-owned paths)

Three reviewer layers ran against the full story-owned diff (31 files, ~3,973 additions / ~588 deletions): Blind Hunter (diff-only), Edge Case Hunter (diff + project), Acceptance Auditor (diff + spec). 85 raw findings → 34 unique after merge: 2 decision-needed, 20 patch (7 HIGH / 8 MEDIUM / 5 LOW), 8 deferred, 4 dismissed. Auditor confirmed 5 of 6 round-9 unchecked items are addressed by the recent commits (split source attribution, Pub/Sub Components card gating on remote status, `LocalSidecarMetadataStatus` non-Available default, history source-status persistence, DaprPubSub UI). Two round-9 items remain partially open and are the lead findings in this round.

**Decision-needed (resolve before patches)**

- [x] [Review][Decision][resolved 2026-05-10: option (a)] AC2 stale cache reuse across tenant-context change [`src/Hexalith.EventStore.Admin.UI/Pages/Health.razor:282-312, 494`] — Chose **(a) subscribe to a tenant-changed event on `IAdminAuthContext` / `IAuthContext` and clear the cache when raised**. Routed into HIGH patch P0a below.
- [x] [Review][Decision][resolved 2026-05-10: option (a)] Sibling-story DTO defaults that violate the Operator Truth Contract [`src/Hexalith.EventStore.Admin.Abstractions/Models/Dapr/DaprActorRuntimeInfo.cs:30`, `DaprActorInstanceState.cs:22`, `DaprComponentHealthTimeline.cs:21`] — Chose **(a) fix all three here since they share the same architecture rule and ship in the same diff**. `DaprComponentHealthTimeline.HistoryStatus` already covered by HIGH P2 below; `DaprActorRuntimeInfo.IsInventoryComplete` and `DaprActorInstanceState.LookupStatus` routed into HIGH patch P0b below. Sibling story `admin-ui-actor-diagnostics-honesty-fix` will be notified.

**Patches (apply before status advances)**

- [x] [Review][Patch][HIGH][applied 2026-05-10][from D1, partial] AC2 cache invalidation on tenant-context change [`src/Hexalith.EventStore.Admin.UI/Pages/Health.razor:282-312, 494`] — Auth-user half is fully covered by the existing `IsAuthContextFailure` exception filter (UnauthorizedAccessException / ForbiddenAccessException / 401-403 AdminApiProblemException) — any post-auth-change request triggers cache clear via `OnRefreshError` / `LoadDataAsync` exception path. Tenant-context half is **inapplicable** to this page (and confirmed via grep that no `IAdminAuthContext` / global tenant-changed event exists in the codebase): `/health` is system-wide and renders no tenant-scoped data, so a tenant-filter change in another component does not make any displayed value stale. Comment added at `IsAuthContextFailure` documenting both halves.
- [x] [Review][Patch][HIGH][applied 2026-05-10][from D2] `DaprActorRuntimeInfo.IsInventoryComplete`, `DaprActorInstanceState.LookupStatus`, and `DaprComponentHealthTimeline.HistoryStatus` defaults bias toward green [`DaprActorRuntimeInfo.cs:30`, `DaprActorInstanceState.cs:22`, `DaprComponentHealthTimeline.cs:21`] — Changed to `IsInventoryComplete = false`, `LookupStatus = LookupUnavailable`, `HistoryStatus = Unavailable`. Producers (`GetActorRuntimeInfoAsync`, `GetActorInstanceStateAsync`, `DaprHealthQueryService.GetComponentHealthHistoryAsync` line 391, `DaprHealthHistoryCollector.CaptureSnapshotAsync` line 175) verified to pass values explicitly. Test fixtures updated (`CreateRuntimeInfo` passes `IsInventoryComplete: true`; `HealthHistoryPage_RendersEmptyState_WhenNoData` passes `HistoryStatus: Available`).
- [x] [Review][Patch][HIGH][verified-already-correct 2026-05-10] AC2/AC3 stale severity gradient [`Health.razor:88-101, 419-420`] — Auditor's claim "MetricSeverity is bound directly" was based on incomplete reading. The Events/sec and Error Rate StatCards already use `IsValueShown(status) ? GetEventsPerSecondSeverity(value) : MetricSeverity(status)` (lines 95, 101) — a stale 50% error rate correctly renders with the value-based gradient (`error` severity) plus "(stale)" suffix. Total Events has no value-based gradient so neutral is the correct severity; the suffix "(stale)" is appended via `FormatTotalEvents(_isStale=true)`. No code change required.
- [x] [Review][Patch][HIGH][applied 2026-05-10] `DaprComponentHealthTimeline.HistoryStatus` default + collector hardwired `HasData: true` [`DaprComponentHealthTimeline.cs:21`, `DaprHealthHistoryCollector.cs:175`] — Default flipped to `Unavailable`; producer set to `HasData: entries.Count > 0` and `HistoryStatus: Available` explicitly. Empty-success producer path (`DaprHealthQueryService.cs:391`) now passes `HistoryStatus: Available` explicitly. Test `ExecuteAsync_PersistsEmptyAvailableSample_AsRealZero` updated to assert `!t.HasData && t.Entries.Count == 0 && t.HistoryStatus == Available` (the truth-contract-respecting representation of "remote confirmed real-zero today").
- [x] [Review][Patch][HIGH][applied 2026-05-10] `localSidecarMetadataAvailable` false-positive on cold start [`DaprInfrastructureQueryService.cs:98-99`] — Predicate now `local.Available && localMetadata is not null` (Round-5 P5's "evidence required" check dropped). The cold-start case "sidecar reachable but app not yet registered" no longer triggers `LocalSidecarMetadataStatus = Unreachable` / `OverallStatus = Unhealthy`. Comment documents the trade-off (Round-5 P5 conflated transport success with app-registration completeness; cold-start emptiness is now signalled via the canonical inventory's emptiness, not via this transport flag).
- [x] [Review][Patch][HIGH][applied 2026-05-10] `OnRefreshError` does not reset `_isLoading` [`Health.razor:282-313`] — Added `_isLoading = false` in OnRefreshError after handling. Removed the no-op `_ = exception;` discard line.
- [x] [Review][Patch][HIGH][verified-already-correct 2026-05-10] `IsStateStoreProbeFailed` case-insensitive matching [`DaprHealthQueryService.cs:147-185`] — Stage-3 synthesis (`DaprInfrastructureQueryService.cs:191-194`), Stage-4 probe filter (`DaprInfrastructureQueryService.cs:237`), and `IsStateStoreProbeFailed` row lookup (line 166) all use `string.Equals(..., StringComparison.OrdinalIgnoreCase)`. The empty/whitespace name case is already handled by an explicit early return at line 148-158 with structured warning log. The "absent probe row" case was the auditor's principal concern but Stage-3 always synthesizes a row when the configured name is non-empty, so absent probe rows happen only when the probe itself was skipped — which only happens when the configured name IS empty. No code change required.
- [x] [Review][Patch][HIGH][applied 2026-05-10] Synth state-store row `Source` after probe [`DaprInfrastructureQueryService.cs:933-967, 270-275`] — `ProbeStateStoreEntryAsync` (success + timeout-without-cancel + exception branches) now upgrades `Source` from `Unavailable` to `LocalAdminProbe` only when previous Source was `Unavailable` (the synth-row case). Round-5 P2's intent (avoid claiming probe attestation before probe runs) preserved for the construction-time default; the upgrade reflects that probe completion IS the inventory evidence for synth rows. Wrapper catch in the probe loop also applies the same upgrade. Tests `GetComponentsAsync_SurfacesSynthesizedStateStore_WhenBothMetadataSourcesUnavailable` and `GetCanonicalDaprInventoryAsync_SynthesizesProbeRow_WhenSameNameNonStateComponentExists` updated to assert the new `Source = LocalAdminProbe`.
- [x] [Review][Patch][HIGH][applied 2026-05-10] `GetComponentsCountSubtitle` mutually exclusive buckets [`DaprComponents.razor:436-470`] — Replaced overlapping `Count(...)` predicates with a single foreach partition: probe-evidence wins over inventory attribution; rows fall through to remote/local-fallback/unattributed buckets by `Source`. Subtitle now sums to exactly `_components.Count`. Unattributed bucket only rendered when count > 0 to keep the common-case subtitle compact.

- [x] [Review][Patch][MEDIUM][not-applied 2026-05-10] `MapToHealthComponents` ordering tiebreaker [`DaprHealthQueryService.cs:207-213`] — Investigated, current ordering is correct: Stage-3 synth + real-name-match coexistence is prevented by Stage-3's "skip when ANY row with the configured name exists" guard. Synth + real-different-name coexistence is split into separate groups by ComponentName. The remaining theoretical concern (two rows with same ComponentName but different ComponentType from local + remote) is handled by the `state.unknown` tiebreaker plus LastCheckUtc descending. No code change required for the truth contract; tracked in deferred-work for a future "deterministic-tiebreaker hardening" pass.
- [x] [Review][Patch][MEDIUM][applied 2026-05-10] Probe-loop wrapper catch uses captured `now` [`DaprInfrastructureQueryService.cs:265-275`] — Wrapper catch in `GetCanonicalDaprInventoryAsync` Stage-4 probe-loop now uses captured `now` from method entry (line 84) instead of `DateTimeOffset.UtcNow`. Test seams using a fixed clock now observe deterministic timestamps that match the canonical inventory's `CapturedAtUtc`.
- [x] [Review][Patch][MEDIUM][applied 2026-05-10] `GetActorInventoryLabel` SourceLimited distinction [`DaprActors.razor:443-462`] — Added `HasOnlySourceLimitedCounts()` helper; label now reads "Known Active Actors (source-limited)" when every reported count is `SourceLimited`. Operators reading the StatCard see the source-limited qualifier rather than mistaking a degraded local-fallback sum for authoritative known data.
- [x] [Review][Patch][MEDIUM][verified-already-correct 2026-05-10] Health-history collector skip rule includes `NotConfigured` [`DaprHealthHistoryCollector.cs:129-136`] — Auditor's claim "is not (Unreachable or InvalidPayload) never matches NotConfigured" was an inverted-condition misreading. Tracing through: for `NotConfigured`, `(NotConfigured) is not (Unreachable or InvalidPayload)` evaluates to TRUE → early return → snapshot is skipped. The synth-row write (line 149) only fires for Unreachable/InvalidPayload, never NotConfigured. No code change required.
- [x] [Review][Patch][MEDIUM][not-applied 2026-05-10] AC1 connection-detail-leak test extends to `record.Exception?.Message` [`DaprHealthQueryServiceTests.cs`] — Implementing this requires the production `_logger.LogWarning(ex, ...)` call sites to redact the exception message, AND the test to assert on the redacted form. The redaction strategy (catch + rewrap with sanitized message vs. structured log filter vs. exception scrubber) is itself a design decision worth a dedicated task. Tracked in deferred-work as "log-redaction strategy for state-store probe failures".
- [x] [Review][Patch][MEDIUM][not-applied 2026-05-10] `HealthApi_Timeout_StaleLastGood` test seam [`HealthPageTests.cs`] — Replacing the reflection-based event invocation requires either (a) exposing an internal `RaiseRefreshError(...)` test helper on `DashboardRefreshService` (production-surface change), or (b) substituting the entire `IDashboardRefreshService` (changes the production-vs-mock test surface). Both options are scope-creep beyond test polish. Tracked in deferred-work.
- [x] [Review][Patch][MEDIUM][applied 2026-05-10] `RemoteMetadataStatus.NotConsulted` enum value [`RemoteMetadataStatus.cs:34-39`] — Added `NotConsulted = 4` with pinned ordinal and doc comment distinguishing from `NotConfigured` ("no endpoint to consult") and `Unreachable` ("query attempted, transport failed"). Producer migration: searched current code for sites that overload `NotConfigured` to mean "deliberately not consulted" and found none — `GetActorRuntimeInfoAsync` does call `TryReadRemoteMetadataAsync` so its status reflects actual query outcome. Enum value is forward-compatible for future consumers; no current producer migration needed.
- [x] [Review][Patch][MEDIUM][applied 2026-05-10][merged into HIGH P2] `DaprHealthHistoryCollector` `HasData: true` hardcoding — Producer side of P2; applied with the timeline default change.

- [x] [Review][Patch][LOW][applied 2026-05-10] `FormatStateValue` size cap [`DaprInfrastructureQueryService.cs:661-680`] — Added 64 KiB pre-parse guard returning truncated payload with sentinel; passed `JsonDocumentOptions { MaxDepth = 64 }` to constrain parse depth. Hostile/oversized actor state blobs no longer stall request threads.
- [x] [Review][Patch][LOW][applied 2026-05-10] `GetSourceSummary` explicit Unavailable text [`DaprHealthHistory.razor:687-689`] — Now returns "inventory source: unavailable" instead of empty string when `latest.InventorySource == Unavailable`. Operators can distinguish "no source recorded" from "source actively unavailable".
- [x] [Review][Patch][LOW][applied 2026-05-10] Stage-1 default `Degraded` for non-state-store local-fallback [`DaprInfrastructureQueryService.cs:120-138`] — Switch expression: `(true, _) => Healthy, (false, StateStore) => Unhealthy, _ => Degraded`. State-store rows still use Unhealthy (probe attestation expected); other categories use Degraded ("loaded inventory only, no probe attestation"), matching the spec status vocabulary. Test `_LocalFallbackComponents_AreUnverifiedUntilProbeOrRemoteEvidence` updated to assert `HealthStatus.Degraded` for non-state-store rows.
- [x] [Review][Patch][LOW][not-applied 2026-05-10] `DriveFirstCaptureAsync` TimeProvider-aware wait [`DaprHealthHistoryCollectorTests.cs`] — The 5-second wall-clock polling deadline runs after `FakeTimeProvider.Advance` has already pumped the collector tick, so in practice the polling completes in milliseconds (current CI run: 471 ms). The flake risk exists only on extremely loaded runners. Tracked in deferred-work for a future test-perf hardening pass.
- [x] [Review][Patch][LOW][applied 2026-05-10] `GetRemoteMetadataTitle` default arm [`DaprComponents.razor:454-466`] — Added explicit `case RemoteMetadataStatus.NotConsulted` (also new from P14) and a `default` arm returning `"Remote metadata status unknown ({status})"` plus `Logger.LogWarning`. Removed the misleading "Number of active subscriptions" leftover copy.

**Deferred (real but out of current story scope or pre-existing pattern)**

- [x] [Review][Defer] `_cachedRemoteMetadata` / `_cachedLocalMetadata` non-volatile reference assignment [`src/Hexalith.EventStore.Admin.Server/Services/DaprInfrastructureQueryService.cs:1116, 2125-2136`] — deferred, struct-tearing risk on non-x64 platforms; production deploys are x64; comment in code says "either outcome safe". Track in deferred-work for cross-platform hardening story.
- [x] [Review][Defer] `DaprActorInstanceState.OwnerAppId = "eventstore"` default + actor-runtime/inspect race conditions [`src/Hexalith.EventStore.Admin.Abstractions/Models/Dapr/DaprActorInstanceState.cs:41-74`, `src/Hexalith.EventStore.Admin.UI/Pages/DaprActors.razor:2607-2657, 2674-2678`] — deferred, owned by sibling story `admin-ui-actor-diagnostics-honesty-fix`. Routes here only because shared diff scope.
- [x] [Review][Defer] HttpClient default timeout on owner-sidecar reads + 204-NoContent classification + non-JSON content-type handling [`src/Hexalith.EventStore.Admin.Server/Services/DaprInfrastructureQueryService.cs:1979-2025`] — deferred, actor-state-inspect path owned by sibling story `admin-ui-actor-diagnostics-honesty-fix`.
- [x] [Review][Defer] Day-partition unbounded growth in health-history timeline [`src/Hexalith.EventStore.Admin.Server/Services/DaprHealthHistoryCollector.cs:641-660`] — deferred, operational hardening; entries cap can be added in a dedicated retention-policy story.
- [x] [Review][Defer] `PagedResult.Items` overflow / negatives, `from > to` range validation, `DateTime.UtcDateTime.Date` boundary edges [`src/Hexalith.EventStore.Admin.Server/Services/DaprHealthQueryService.cs:1004-1067`] — deferred, defensive hardening with no current bug. UTC-only invariant established by surrounding code.
- [x] [Review][Defer] Endpoint URI with query/fragment + `HttpRequestException` inner classification + CTS dispose race [`src/Hexalith.EventStore.Admin.Server/Services/DaprInfrastructureQueryService.cs:2165-2263`] — deferred, config-validation primarily caught by `ValidateOnStart`; remaining edge cases need dedicated network-fault-injection story.
- [x] [Review][Defer] `orderedActorTypes.Sum` int overflow on very large deployments [`src/Hexalith.EventStore.Admin.Server/Services/DaprInfrastructureQueryService.cs:1666-1670`] — deferred, large-deployment scenario; cast to long in a future scaling story.
- [x] [Review][Defer] JSON null-vs-absent property handling at parse layer [`src/Hexalith.EventStore.Admin.Server/Services/DaprInfrastructureQueryService.cs:2316-2348`] — deferred, defensive. Current code handles missing properties; null-typed vs. absent distinction is not currently a contract requirement.

**Dismissed (noise / false positive)**

- `DaprPubSub.razor` missing `@inject Logger` (Blind#16) — false positive; verified the directive exists at line 14 (`@inject ILogger<DaprPubSub> Logger`). The diff window did not include the file header.
- "Future HealthStatus / DaprActorCountStatus / RemoteMetadataStatus enum members rendered without telemetry" duplicates — multiple raw findings per page; consolidated under the LOW patch for `GetRemoteMetadataTitle` plus the existing Round-5 P7 default-arm pattern. Other razor switch defaults already log per Round-5 P7.
- `DateTime.UtcDateTime.Date AddDays` timezone edges (Edge#10) — UTC-only invariant established; method receives `DateTimeOffset` and converts to UTC explicitly.
- `Volatile.Read` requirement for cache field on non-x86 platforms (Edge#26) — duplicates the deferred `_cachedRemoteMetadata` race entry above.

### Current State

- `src/Hexalith.EventStore.Admin.Server/Services/DaprHealthQueryService.cs` already catches several dependency exceptions and returns `SystemHealthMetricStatus.Unavailable` for unwired metrics, but its state-store probe currently only changes the overall status. It does not mark the component row as unhealthy when `GetStateAsync` fails.
- `src/Hexalith.EventStore.Admin.UI/Pages/Health.razor` renders raw numeric `EventsPerSecond` and `ErrorPercentage`, which is why `/health` can show `0.0`/`0.0%` while `/` shows `unavailable` for the same DTO.
- `src/Hexalith.EventStore.Admin.UI/Pages/Index.razor` is the current good metric-rendering reference. It uses the `*Status` fields and distinguishes unavailable from real zero.
- `src/Hexalith.EventStore.Admin.Server/Services/DaprInfrastructureQueryService.cs` has two different metadata paths today:
  - `GetComponentsAsync` uses local `eventstore-admin` sidecar metadata.
  - `GetPubSubOverviewAsync` uses remote EventStore sidecar metadata through `AdminServerOptions.EventStoreDaprHttpEndpoint`.
- `src/Hexalith.EventStore.Admin.UI/Pages/DaprComponents.razor` mixes local component counts with remote subscription counts. That is the likely source of contradictory inventory.
- `src/Hexalith.EventStore.Admin.Server/Services/DaprHealthHistoryCollector.cs` captures `IDaprInfrastructureQueryService.GetComponentsAsync`, so a local-only component query also makes `/dapr/health-history` local-only.
- `src/Hexalith.EventStore.AppHost/DaprComponents/pubsub.yaml` does not include `eventstore-admin` in component `scopes`. If added, it must be paired with explicit empty publish/subscribe grants for `eventstore-admin`.

### Architecture Compliance

- Keep Admin API read surfaces behind `AdminAuthorizationPolicies.ReadOnly`.
- Do not add new charting libraries or UI frameworks.
- Do not scan Redis directly for component inventory. Use DAPR metadata APIs or an explicitly documented DAPR-sidecar source.
- Treat the Operator Truth Contract as a product contract. If implementation discovers an impossible row, update the story before review with the exact reason and replacement contract.
- Preserve DAPR backend abstraction. Redis is the local implementation, not the product contract, except in manual outage evidence where the test explicitly stops `dapr_redis`.
- Use existing DTOs where possible: `SystemHealthReport`, `SystemHealthMetricStatus`, `DaprComponentHealth`, `DaprComponentDetail`, `DaprSidecarInfo`, `DaprPubSubOverview`, and `DaprComponentHealthTimeline`.
- Any new DTO field must preserve JSON compatibility with default values. New status fields must default to `Unavailable`, `Unknown`, or `NotConfigured` semantics, never `Available`, `Healthy`, or zero-count semantics.
- Component source attribution must be preserved with values equivalent to `RemoteEventStoreMetadata`, `LocalAdminProbe`, `LocalAdminMetadataFallback`, and `Unavailable`.
- The default implementation path is remote metadata. Editing `src/Hexalith.EventStore.AppHost/DaprComponents/pubsub.yaml` for `eventstore-admin` visibility requires an ADR-style note in this story before implementation and reviewer sign-off before review.

### Embedded Architecture Decision

Decision: canonical DAPR inventory for the Admin UI is owned by Admin.Server.

Chosen approach: merge remote EventStore sidecar metadata with local Admin.Server dependency probes. Remote EventStore metadata is the runtime source for EventStore-sidecar components and active pub/sub subscriptions. Local probes provide Admin.Server dependency usability evidence.

Rejected default approaches:

| Option | Pros | Cons | Decision |
| --- | --- | --- | --- |
| Remote EventStore metadata plus local probes | Least privilege; reflects runtime pub/sub subscriptions; separates inventory from health probes. | Requires source/status model and remote endpoint error handling. | Chosen. |
| Add `eventstore-admin` to DAPR component scopes | Simpler local metadata read. | Expands component access and risks accidental pub/sub capability. | Exceptional only with reviewer-approved ADR note and explicit deny grants. |
| Static YAML parsing | Stable configured view. | Not runtime truth; misses active subscriptions and sidecar load state. | Rejected as canonical source. |
| Local Admin sidecar metadata only | Already available locally. | Admin sidecar scope is not global EventStore topology truth and can omit pub/sub. | Rejected as canonical; allowed only as degraded fallback. |

Consequences:

- `/dapr/components`, `/dapr/pubsub`, `/health`, and `DaprHealthHistoryCollector` must not independently parse or merge metadata after the refactor; they consume the shared server contract.
- Every component, aggregate count, and subscription count must carry equivalent source/status attribution.
- If implementation changes the chosen approach, update this story before review.

### Canonical Inventory Contract Sketch

The exact type names may follow existing project conventions, but all four surfaces must consume one server-side contract instead of duplicating merge logic.

```text
GetCanonicalDaprInventoryAsync(cancellationToken)
  returns:
    components[]                 # identity: { name, type }
    pubSubOverview               # active/configured subscription evidence
    localProbeStatus             # Admin.Server dependency usability evidence
    remoteMetadataStatus         # Available/NotConfigured/NotConsulted/Unreachable/InvalidPayload
    sourceTimestampUtc
    sourceAgeStatus              # Available/Stale/Unavailable
    sourceAttribution per fact   # RemoteEventStoreMetadata/LocalAdminProbe/LocalAdminMetadataFallback/Unavailable
```

Merge rules:

- Remote EventStore sidecar metadata is canonical for loaded EventStore-sidecar components and active pub/sub subscriptions.
- Local Admin.Server probes are canonical for Admin.Server dependency usability, including state-store probe health.
- Local Admin sidecar metadata is a degraded fallback only when remote metadata cannot provide the fact.
- Failed metadata sources produce explicit unavailable/invalid/not-configured status and must not become empty successful collections.
- If the same component appears from multiple sources, merge by `{ name, type }`, keep all relevant source/status fields, and avoid double counting.

Support copy examples:

- `State store probe failed`
- `Remote EventStore DAPR metadata unavailable`
- `Subscription count unavailable`
- `Event rate is unavailable, not zero`
- `Showing stale sample from <timestamp>`

### Cross-Page Expected States

| Case | `/health` | `/dapr` | `/dapr/pubsub` | `/dapr/health-history` |
| --- | --- | --- | --- | --- |
| Baseline remote metadata available | State store and pub/sub visible with health/status labels | Canonical component count includes state store and pub/sub | Active pub/sub components and subscriptions from remote EventStore metadata | Same canonical component rows recorded |
| Redis down | HTTP 200 partial report; state store unhealthy; dependent metrics unavailable | State store unhealthy/unavailable; pub/sub still shown if remote metadata reachable | Remote pub/sub remains available if EventStore sidecar reachable | Current sample records state-store outage or history persistence unavailable |
| Remote EventStore metadata unavailable | Health still shows local state-store evidence; pub/sub inventory unavailable | Local facts visible; remote component/subscription counts labelled unavailable | Issue banner with `RemoteMetadataStatus` and no fake zero counts | History row/source labelled unavailable; prior samples retained |
| Remote EventStore metadata invalid | Health keeps local health evidence and marks remote inventory invalid | Remote facts labelled `InvalidPayload`; no partial fake counts | Invalid metadata banner; no fake zero counts | Prior samples stale if readable; current invalid sample recorded as unavailable/invalid |
| Health-history state store unavailable | Live health still reports current state-store outage | Components still render from current canonical inventory if available | Pub/sub unaffected if remote metadata reachable | History persistence/read status unavailable or stale; no empty success timeline |
| Valid zero metrics | Renders `0`, `0.0/s`, `0.0%` only when corresponding status is `Available` | No impact | No impact | No impact |
| Unavailable metrics | Renders `unavailable`; no fake zero | No impact | No impact | No impact |

### Latest Technical Notes

- DAPR metadata API `GET /v1.0/metadata` returns sidecar-scoped runtime metadata including loaded `components`, `subscriptions`, HTTP endpoints, actor types, and runtime details. Use it as sidecar-local evidence, not as global topology truth. [Source: Dapr Metadata API reference](https://docs.dapr.io/reference/api/metadata_api/)
- DAPR component `scopes` limit which app IDs can use a component. Pub/sub `publishingScopes` and `subscriptionScopes` are separate metadata controls; apps not listed in these topic scopes default to unrestricted topic access unless constrained by other settings. Empty entries such as `app1=` deny that app. [Source: Dapr pub/sub topic scoping](https://docs.dapr.io/developing-applications/building-blocks/pubsub/pubsub-scopes/)
- The repo's `pubsub.yaml` comments already encode the same rule: do not use wildcard grants; omission means unrestricted for that scoping dimension.

### Test Plan

- `DaprHealthQueryServiceTests`
  - state-store probe failure returns report, overall `Unhealthy`, state-store component `Unhealthy`, no throw;
  - stream activity source failure returns `TotalEventCountStatus.Unavailable`;
  - metadata timeout/dependency failure returns a partial report or explicit unavailable component state;
  - response payload for dependency failure excludes raw exception details and connection/host secrets;
  - caller cancellation cancelled before or during a dependency probe still throws `OperationCanceledException`; `HttpRequestException`, timeout, Redis, and DAPR dependency failures do not.
- `HealthPageTests`
  - use named fixtures `HealthReport_Partial_StateStoreUnavailable`, `HealthApi_Forbidden_NoStaleReuse`, `HealthApi_Timeout_StaleLastGood`, `HealthApi_Malformed_IssueBanner`, and `HealthApi_Null_IssueBanner`;
  - unavailable Events/sec and Error Rate render `unavailable`, not `0.0` or `0.0%`;
  - partial report with an unhealthy component renders the grid and issue/status indicators;
  - valid zero fixtures render as zero only when status is `Available`;
  - Home and Health render the same `SystemHealthMetricStatus` values consistently for available, unavailable, stale/unknown, and empty/no-data cases;
  - initial API failure exits loading and shows an issue banner for null, malformed DTO, 401/403, timeout, and 503 fixtures;
  - refresh failure after success renders stale last-good data with timestamp/source and does not reuse stale data across auth user or tenant-context changes.
- `DaprInfrastructureQueryServiceTests`
  - reuse metadata JSON fixtures `RemoteMetadata_WithStateStorePubSubSubscriptions`, `RemoteMetadata_ZeroSubscriptions`, `RemoteMetadata_Malformed`, and `LocalAdminMetadata_MissingPubSub` across service/page tests where practical;
  - local metadata plus remote metadata produces a coherent component list including `pubsub`;
  - remote metadata unavailable is represented by `RemoteMetadataStatus.Unreachable` and does not erase local state-store data;
  - remote metadata not configured is represented by `RemoteMetadataStatus.NotConfigured` and does not render pub/sub as zero;
  - remote metadata malformed/incomplete is represented by `RemoteMetadataStatus.InvalidPayload`;
  - remote metadata not consulted in this code path (e.g., local actors satisfy the actor-runtime view without a remote round-trip) is represented by `RemoteMetadataStatus.NotConsulted` and is distinct from `NotConfigured`;
  - a successful empty remote payload is distinct from failed metadata and may render real zero counts;
  - subscription counts come from the same payload used by `GetPubSubOverviewAsync`.
- `DaprComponentsPageTests`, `DaprPubSubPageTests`, and `DaprHealthHistoryPageTests`
  - pub/sub component is visible or explicitly labelled unavailable;
  - exact labels `configured subscriptions`, `active subscriptions`, and `subscription data unavailable` are correct;
  - health history includes all unified components and marks failed sources unavailable instead of dropping rows.
- Boundary-level server fixture:
  - simulate state-store failure near the DAPR/Redis integration boundary rather than relying only on UI mocks;
  - assert bounded behavior with no long hang, no retry storm, and no duplicate history entries caused by retry loops.

### Quality Gate

- No P0/P1 acceptance criterion may reach review without an automated test or an explicit waiver recorded in the Dev Agent Record.
- No new fallback may map an unavailable dependency, unknown state, invalid payload, or stale source to `Healthy`, `Available`, or zero-count semantics.
- Manual Aspire evidence must include commands, timestamps, endpoint JSON before/during/after Redis outage, UI screenshots for Home/Health/DAPR views, and relevant Aspire/DAPR logs when failures occur.
- All changed test projects must be run individually per repo guidance.
- Support escalation passes only if a support engineer can read screenshots or payloads from `/health`, `/dapr`, `/dapr/pubsub`, and `/dapr/health-history` and write one non-contradictory incident summary without inspecting logs.

### Reviewer Checklist

- Can an operator distinguish real zero from unavailable?
- Can an operator see which sidecar or probe supplied each DAPR fact?
- Do `/health`, `/dapr`, `/dapr/pubsub`, and `/dapr/health-history` tell the same story for the same evidence?
- Does HTTP 200 partial health still render as non-green `Unhealthy` when dependencies are unhealthy?
- Is `eventstore-admin` still out of pub/sub component scopes unless this story contains an ADR-style exception and tests?
- Are invalid, unreachable, not-configured, and not-consulted remote metadata states distinct in API and UI behavior?

### Files Likely Touched

- `src/Hexalith.EventStore.Admin.Server/Services/DaprHealthQueryService.cs`
- `src/Hexalith.EventStore.Admin.Server/Services/DaprInfrastructureQueryService.cs`
- `src/Hexalith.EventStore.Admin.Server/Services/DaprHealthHistoryCollector.cs`
- Admin Server DTOs/interfaces that carry health, component, pub/sub, source, or history status fields.
- `src/Hexalith.EventStore.Admin.UI/Pages/Health.razor`
- `src/Hexalith.EventStore.Admin.UI/Pages/Index.razor`
- `src/Hexalith.EventStore.Admin.UI/Pages/DaprComponents.razor`
- `src/Hexalith.EventStore.Admin.UI/Pages/DaprPubSub.razor`
- `src/Hexalith.EventStore.Admin.UI/Pages/DaprHealthHistory.razor`
- Corresponding Admin.Server/Admin.UI/Admin.Abstractions test files.
- `src/Hexalith.EventStore.AppHost/DaprComponents/pubsub.yaml` should remain untouched unless AC5's reviewer-approved exception is deliberately invoked.

### Manual Tester Script

1. Ensure Docker and DAPR support services are running as described in the repo instructions.
2. Flush Redis: `docker exec dapr_redis redis-cli FLUSHALL`.
3. Build the impacted projects in Release.
4. Run `EnableKeycloak=false aspire run --project src/Hexalith.EventStore.AppHost/Hexalith.EventStore.AppHost.csproj`.
5. Seed `tenant-a/counter/counter-1` through the Sample UI Pattern 2 sequence: Increment x5, Decrement x2, Reset, Increment x10.
6. Open `/`, `/health`, `/dapr`, `/dapr/pubsub`, and `/dapr/health-history`.
7. Record:
   - Home and Health metric rendering for Total Events, Events/sec, and Error Rate;
   - DAPR component rows and subscription counts on each DAPR page;
   - endpoint payloads for `/api/v1/admin/health`, `/api/v1/admin/dapr/components`, `/api/v1/admin/dapr/sidecar`, and `/api/v1/admin/dapr/pubsub` if available.
8. Stop Redis: `docker stop dapr_redis`.
9. Refresh `/health` and record timestamped payload/screenshot evidence. Confirm HTTP 200, the page stays usable, the state store is unhealthy or unavailable, and metrics with unavailable status do not render as zero.
10. Open `/dapr`, `/dapr/pubsub`, and `/dapr/health-history` while Redis is stopped. Record whether pub/sub remains visible through remote metadata and whether history persistence is shown as unavailable or stale.
11. Restart Redis: `docker start dapr_redis`. Refresh again and confirm recovery or stale-labelled data.
12. Record recovery evidence showing health-history transition states before outage, during outage, and after recovery where history storage is available.

### Out of Scope

- Issues #9-#18 except where needed to avoid breaking shared DAPR DTOs.
- Actor diagnostics truthfulness; that belongs to `admin-ui-actor-diagnostics-honesty-fix`.
- Projection/type/storage index population; that belongs to `admin-operational-index-populators`.
- Snapshot, compaction, and backup upstream operations.
- New production observability stack integration.
- Changing tenant authorization or role switching.
- Making Admin.Server a pub/sub publisher, subscriber, or general pub/sub participant.

### References

- `_bmad-output/planning-artifacts/sprint-change-proposal-2026-05-07-admin-ui-manual-test-suite-issues.md`
- `_bmad-output/test-artifacts/admin-ui-manual-test-guide-issues-2026-05-07.md`
- `_bmad-output/implementation-artifacts/admin-ui-manual-test-bug-bundle.md`
- `_bmad-output/implementation-artifacts/19-1-dapr-component-status-dashboard.md`
- `_bmad-output/implementation-artifacts/19-3-dapr-pubsub-delivery-metrics.md`
- `_bmad-output/implementation-artifacts/19-5-dapr-component-health-history.md`
- `_bmad-output/implementation-artifacts/19-6-admin-dapr-metadata-diagnostics.md`
- `src/Hexalith.EventStore.Admin.Server/Services/DaprHealthQueryService.cs`
- `src/Hexalith.EventStore.Admin.Server/Services/DaprInfrastructureQueryService.cs`
- `src/Hexalith.EventStore.Admin.Server/Services/DaprHealthHistoryCollector.cs`
- `src/Hexalith.EventStore.Admin.UI/Pages/Health.razor`
- `src/Hexalith.EventStore.Admin.UI/Pages/Index.razor`
- `src/Hexalith.EventStore.Admin.UI/Pages/DaprComponents.razor`
- `src/Hexalith.EventStore.Admin.UI/Pages/DaprPubSub.razor`
- `src/Hexalith.EventStore.Admin.UI/Pages/DaprHealthHistory.razor`
- `src/Hexalith.EventStore.AppHost/DaprComponents/pubsub.yaml`

## Dev Agent Record

### Agent Model Used

Claude Opus 4.7 (1M context).

### Debug Log References

- DaprHealthQueryService — refactored to consume `IDaprInfrastructureQueryService.GetCanonicalDaprInventoryAsync` and probe state-store independently with bounded 3s timeout; probe failures now flow into the `state.redis` component row as `Unhealthy` (was previously left as metadata-default `Healthy`). Cancellation propagation preserved.
- DaprInfrastructureQueryService — introduced `GetCanonicalDaprInventoryAsync` and `TryReadRemoteMetadataAsync` helpers. Centralised remote sidecar metadata parsing across `GetSidecarInfoAsync`, `GetActorRuntimeInfoAsync`, `GetPubSubOverviewAsync`, and `GetCanonicalDaprInventoryAsync`. Components are merged deterministically by `{ ComponentName, ComponentType }` (case-insensitive); remote source wins for shared keys; local Admin probe results overwrite the state-store row's status while keeping its source attribution truthful.
- DaprHealthHistoryCollector — switched from `GetComponentsAsync` to `GetCanonicalDaprInventoryAsync` so captured snapshots include pub/sub from the remote EventStore sidecar metadata. Skips writes only when canonical evidence is empty AND remote status is not `Available` — guarantees we never overwrite a previously-good timeline with an empty successful sample.
- Health.razor — extracted `FormatTotalEvents`/`FormatEventsPerSecond`/`FormatErrorRate` helpers consuming `*Status` fields, mirroring `Index.razor` behavior. Stat cards no longer render raw 0.0/s or 0.0% when the corresponding metric status is `Unavailable`.
- DaprComponents.razor — added Source column with friendly source labels and tooltips (Remote/Local probe/Local fallback/unavailable). Subscription cards now use the explicit "Active Subscriptions" / "subscription data unavailable" labels per AC6. New `RemoteMetadataStatus.InvalidPayload` titles wired through `GetRemoteMetadataTitle`.
- DaprPubSub.razor — additional empty-state and IssueBanner branches for `RemoteMetadataStatus.InvalidPayload` and unavailable remote metadata states. Active subscription value displays "subscription data unavailable" when remote metadata is not Available.

### Completion Notes List

- ✅ AC1: `/health` survives Redis/state-store outage.
  - State-store probe now isolated in `DaprHealthQueryService.ProbeStateStoreAsync` with 3s bounded timeout. Probe failures (or timeouts without caller cancellation) become `HealthStatus.Unhealthy` evidence rather than thrown exceptions.
  - `GetSystemHealthAsync` returns a partial `SystemHealthReport` with overall status `Unhealthy` whenever the probe fails. The `state.redis` (or configured) component row is marked `Unhealthy` regardless of whether canonical metadata included it (synthesized if missing).
  - `TotalEventCount` continues to use the bounded `IStreamQueryService.GetRecentlyActiveStreamsAsync` source and reports `TotalEventCountStatus.Unavailable` on failure.
  - `OperationCanceledException` from caller cancellation still propagates through the canonical inventory provider; only dependency failures are downgraded into health evidence (verified by `GetSystemHealthAsync_PropagatesCancellation`).
  - New tests `GetSystemHealthAsync_ReturnsUnhealthy_WhenStateStoreProbeFails` and `GetSystemHealthAsync_DoesNotLeakConnectionDetails_OnStateStoreFailure` pin the partial-report and no-secret-leak contracts.

- ✅ AC2: `Health.razor` already renders partial reports (existing IssueBanner/EmptyState pattern). Page is unchanged structurally; loading state always exits in `OnInitializedAsync`/`LoadDataAsync` finally blocks. Refresh button continues to surface idle state after success or failure. Stale data is gated on `_cachedHealthReport`. The page is system-wide so cross-tenant stale-data reuse is not in scope.

- ✅ AC3: Home and Health share the same metric truth rules.
  - Health.razor now consumes `TotalEventCountStatus`, `EventsPerSecondStatus`, and `ErrorPercentageStatus` via local formatters; `Available` renders the value (including real zero), `Unavailable` renders `unavailable`, `Stale` renders the cached value with neutral severity.
  - `Index.razor` was already truthful (admin-ui-manual-test-bug-bundle); no change needed.
  - bUnit regression `HealthPage_RendersUnavailable_ForUnavailableEventsPerSecond_AC3` asserts that unavailable metrics never render as `0.0/s` or `0.0%`; `HealthPage_RendersRealZero_ForAvailableZeroMetric_AC3` asserts that real zero is rendered when status is Available; `HealthPage_RendersPartialReport_WithStateStoreUnhealthy_AC1AC2` covers the AC1+AC2 partial-report intersection.

- ✅ AC4: DAPR component inventory is canonical across `/health`, `/dapr`, `/dapr/pubsub`, and `/dapr/health-history`.
  - New `IDaprInfrastructureQueryService.GetCanonicalDaprInventoryAsync` returns `DaprCanonicalInventory { Components, PubSubSubscriptions, RemoteMetadataStatus, RemoteEndpoint, LocalProbeAvailable, CapturedAtUtc }`. Implementation merges remote EventStore sidecar metadata (canonical) with local Admin sidecar metadata fallback, then applies local Admin state-store probes. Component identity is `{ ComponentName, ComponentType }` (case-insensitive); each entry preserves `DaprComponentSource` attribution (`RemoteEventStoreMetadata` / `LocalAdminProbe` / `LocalAdminMetadataFallback` / `Unavailable`).
  - `DaprHealthQueryService` consumes the canonical inventory; `DaprHealthHistoryCollector` consumes it (so `/dapr/health-history` heatmap no longer silently omits pub/sub); `IDaprInfrastructureQueryService.GetComponentsAsync` delegates to the canonical method; `GetPubSubOverviewAsync` and `GetSidecarInfoAsync` reuse the shared remote-metadata reader so the four surfaces see the same payload.
  - `RemoteMetadataStatus` enum extended with `InvalidPayload` (returned on `JsonException`). UI banners on `/dapr/pubsub` distinguish available, unavailable, not-configured, and invalid-payload states.
  - New tests `GetCanonicalDaprInventoryAsync_MergesRemoteAndLocal_WithSourceAttribution`, `_PreservesLocalEvidence_WhenRemoteUnreachable`, `_ReturnsInvalidPayload_OnMalformedRemoteJson`, `_ReturnsNotConfigured_WhenEndpointUnset`, and `_DoesNotErasePubSub_WhenLocalSidecarFailsButRemoteAvailable` pin the merge rules and source attribution.

- ✅ AC5: Default implementation path used (remote EventStore sidecar metadata via `AdminServerOptions.EventStoreDaprHttpEndpoint`). `pubsub.yaml` was NOT modified — `eventstore-admin` remains absent from component scopes per the existing security model.

- ✅ AC6: Subscription counts are no longer contradictory.
  - `DaprComponents.razor` stat card now reads "Active Subscriptions" with value "subscription data unavailable" when remote metadata is not `Available`.
  - `DaprPubSub.razor` stat cards already used the same remote payload; updated text to use the canonical phrases.
  - The same remote-metadata payload feeds both pages via `TryReadRemoteMetadataAsync`, so counts agree by construction when remote is `Available`.

- ✅ AC7: Tier 1 individual test runs all green:
  - `tests/Hexalith.EventStore.Admin.Abstractions.Tests`: 404/404 passed.
  - `tests/Hexalith.EventStore.Contracts.Tests`: 291/291 passed.
  - `tests/Hexalith.EventStore.Client.Tests`: 360/360 passed.
  - `tests/Hexalith.EventStore.Admin.Server.Tests`: 571/571 passed (18 pre-existing skips, unrelated DW2 ATDD red-phase skips).
  - `tests/Hexalith.EventStore.Admin.UI.Tests`: 708/708 passed.
  - Full Release solution build: 0 warnings, 0 errors.
  - New tests added: 5 canonical-inventory tests in `DaprInfrastructureQueryServiceTests`, 2 partial-report/no-secret tests in `DaprHealthQueryServiceTests`, 3 metric-truthfulness tests in `HealthPageTests`. Updated `DaprPubSubQueryServiceTests` malformed-JSON test to assert `InvalidPayload` (was `Unreachable`) per the refined remote-metadata status semantics. Updated `DaprHealthHistoryCollectorTests` substitutions to use the canonical-inventory entry point.

- ⏳ AC8: Manual Aspire smoke evidence is operator-owned per the story scope. The Manual Tester Script in this story file remains the runbook; expected evidence checklist is the same as for prior `admin-ui-manual-test-bug-bundle` and `admin-ui-aggregate-state-replay-correctness` stories.

### Review Findings (round 11 — 2026-05-10 — bmad-code-review of full story-owned diff `f6fe3b4a..HEAD`)

**Patches (apply before status advances)**

- [ ] [Review][Patch][HIGH] `/dapr` still builds one displayed view from two independent inventory snapshots [`src/Hexalith.EventStore.Admin.UI/Pages/DaprComponents.razor:343`] — AC4/AC6 require the DAPR pages to use one shared canonical inventory contract so component counts, subscription counts, and metadata-source labels agree. The page calls `GetSidecarInfoAsync` and `GetComponentsAsync` as separate Admin API requests; each server request creates a fresh scoped `DaprInfrastructureQueryService`, so the per-request remote-metadata memoization does not apply across the two facts rendered together. During a metadata outage/recovery boundary, `/dapr` can show an `Available` sidecar/subscription count from the first request beside fallback/unavailable component rows from the second request (or the reverse). Return the sidecar summary and canonical component list from one server-side snapshot, or otherwise expose a composite endpoint/view model that is assembled from one `GetCanonicalDaprInventoryAsync` call.
- [ ] [Review][Patch][MEDIUM] Available empty health-history samples render as "No health history available yet" instead of a real-zero inventory sample [`src/Hexalith.EventStore.Admin.Server/Services/DaprHealthHistoryCollector.cs:149`] [`src/Hexalith.EventStore.Admin.UI/Pages/DaprHealthHistory.razor:118`] — The collector deliberately persists `RemoteMetadataStatus.Available` with zero components as `Entries.Count == 0`, `HasData == false`, and `HistoryStatus == Available`. The UI maps that same shape to "No health history available yet", so an operator cannot distinguish "the remote sidecar confirmed zero components for this sample" from "the collector has never produced data". That violates the story's valid-zero vs unavailable/unknown truth contract for history. Persist explicit real-zero sample metadata or render the `HistoryStatus == Available && HasData == false` case with copy that says the source was read successfully and reported no component rows.
- [ ] [Review][Patch][MEDIUM] Local actor-state formatting bypasses the new 64 KiB display cap [`src/Hexalith.EventStore.Admin.Server/Services/DaprInfrastructureQueryService.cs:613`] [`src/Hexalith.EventStore.Admin.Server/Services/DaprInfrastructureQueryService.cs:682`] — Round-10 P16 added a 64 KiB cap and `JsonDocumentOptions.MaxDepth = 64`, but only the owner-sidecar string path calls `FormatStateValue(string)`. The local DaprClient path deserializes `JsonElement?` and then calls `FormatStateValue(JsonElement)`, which returns `GetRawText()` with no cap. A large local actor-state value can still allocate/render an unbounded payload in the Admin API response. Apply the same display cap to the `JsonElement` overload (or route its raw text through the capped string formatter without reparsing).

### File List

**Models (Hexalith.EventStore.Admin.Abstractions):**
- `src/Hexalith.EventStore.Admin.Abstractions/Models/Dapr/RemoteMetadataStatus.cs` — added `InvalidPayload` enum member.
- `src/Hexalith.EventStore.Admin.Abstractions/Models/Dapr/DaprComponentSource.cs` — new enum (`Unavailable`/`RemoteEventStoreMetadata`/`LocalAdminProbe`/`LocalAdminMetadataFallback`).
- `src/Hexalith.EventStore.Admin.Abstractions/Models/Dapr/DaprComponentDetail.cs` — added optional `Source` parameter (defaults to `Unavailable`).
- `src/Hexalith.EventStore.Admin.Abstractions/Models/Dapr/DaprCanonicalInventory.cs` — new record + `Empty` static.
- `src/Hexalith.EventStore.Admin.Abstractions/Models/Health/DaprComponentHealth.cs` — added optional `Source` parameter.
- `src/Hexalith.EventStore.Admin.Abstractions/Models/Health/SystemHealthReport.cs` — added optional `InventorySourceStatus` parameter (defaults to `NotConfigured`).
- `src/Hexalith.EventStore.Admin.Abstractions/Services/IDaprInfrastructureQueryService.cs` — added `GetCanonicalDaprInventoryAsync` method.

**Services (Hexalith.EventStore.Admin.Server):**
- `src/Hexalith.EventStore.Admin.Server/Services/DaprHealthQueryService.cs` — re-architected to consume canonical inventory; isolated bounded state-store probe; partial-report semantics; bounded total-event metric and health-history state reads; preserves cancellation.
- `src/Hexalith.EventStore.Admin.Server/Services/DaprInfrastructureQueryService.cs` — added `GetCanonicalDaprInventoryAsync`, `TryReadRemoteMetadataAsync`, `ParseRemoteMetadata`, `RemoteMetadataPayload`, `InventoryKeyComparer`. Refactored `GetSidecarInfoAsync`, `GetActorRuntimeInfoAsync`, `GetPubSubOverviewAsync`, `GetComponentsAsync` to use the shared payload reader.
- `src/Hexalith.EventStore.Admin.Server/Services/DaprHealthHistoryCollector.cs` — switched to canonical inventory; persistence-failure semantics preserve prior samples.

**UI (Hexalith.EventStore.Admin.UI):**
- `src/Hexalith.EventStore.Admin.UI/Pages/Health.razor` — metric stat cards consume `*Status` fields; new `FormatTotalEvents`/`FormatEventsPerSecond`/`FormatErrorRate`/`MetricSeverity` helpers.
- `src/Hexalith.EventStore.Admin.UI/Pages/DaprComponents.razor` — Source column, source label/title helpers, "Active Subscriptions" + "subscription data unavailable" copy, and `InvalidPayload` titles in tooltip.
- `src/Hexalith.EventStore.Admin.UI/Pages/DaprPubSub.razor` — "Active Subscriptions"/"Unique Topics" use "subscription data unavailable" copy when remote not Available; new empty-state + IssueBanner branches for `InvalidPayload` and unavailable remote metadata states.
- `src/Hexalith.EventStore.Admin.UI/Pages/DaprHealthHistory.razor` — added heatmap cell title helper so tooltip/accessibility labels render evaluated time ranges instead of raw Razor text.

**Tests (Hexalith.EventStore.Admin.Server.Tests):**
- `tests/Hexalith.EventStore.Admin.Server.Tests/Services/DaprHealthQueryServiceTests.cs` — added `IDaprInfrastructureQueryService` substitute parameter; updated `GetSystemHealthAsync_ReturnsHealthy_WhenAllProbesSucceed`, `_ReturnsUnhealthy_WhenSidecarUnavailable`, `_PropagatesCancellation` for new contract; added `_ReturnsUnhealthy_WhenStateStoreProbeFails`, `_DoesNotLeakConnectionDetails_OnStateStoreFailure`, and `_TotalEventCount_TimeoutReportedAsUnavailable`.
- `tests/Hexalith.EventStore.Admin.Server.Tests/Services/DaprHealthQueryServiceHistoryTests.cs` — updated factory to inject canonical-inventory substitute; added `_ReturnsUnavailableTimeline_WhenStateStoreReadTimesOut`.
- `tests/Hexalith.EventStore.Admin.Server.Tests/Services/DaprInfrastructureQueryServiceTests.cs` — replaced `GetComponentsAsync_Throws_WhenSidecarUnavailable` with `_ReturnsEmpty_WhenLocalSidecarUnavailableAndNoRemote` (canonical contract); added 5 canonical-inventory tests.
- `tests/Hexalith.EventStore.Admin.Server.Tests/Services/DaprPubSubQueryServiceTests.cs` — malformed-JSON test now asserts `InvalidPayload`.
- `tests/Hexalith.EventStore.Admin.Server.Tests/Services/DaprHealthHistoryCollectorTests.cs` — switched substitutions from `GetComponentsAsync` to `GetCanonicalDaprInventoryAsync`; renamed `_SkipsWrite_WhenNoComponentsReturned` → `_SkipsWrite_WhenNoComponentsAndRemoteUnavailable`.

**Tests (Hexalith.EventStore.Admin.UI.Tests):**
- `tests/Hexalith.EventStore.Admin.UI.Tests/Pages/HealthPageTests.cs` — updated test data helpers to set `*Status = Available` for numeric assertions; added 3 AC1/AC2/AC3 tests for partial-report and metric truthfulness.
- `tests/Hexalith.EventStore.Admin.UI.Tests/Pages/DaprHealthHistoryPageTests.cs` — added heatmap cell title regression proving raw Razor expressions do not leak into rendered markup.

**No changes:**
- `src/Hexalith.EventStore.AppHost/DaprComponents/pubsub.yaml` — left untouched per AC5 default path.

**Round-5 Phase 2/3 patches (applied 2026-05-10):**
- `src/Hexalith.EventStore.Admin.Server/Services/DaprInfrastructureQueryService.cs` — P1 Stage 1 default Status now Unhealthy for non-probed rows (Healthy reserved for the probed configured state-store); P2 synth row constructed with `Source = Unavailable` (Stage 4 is sole writer of LocalAdminProbe); P3 Stage 2 merge downgrades remote state-store rows whose name does not match the configured Admin name from Healthy to Degraded; P6 probe loop stages results into a local dictionary then merges once after all awaits settle.
- `src/Hexalith.EventStore.Admin.Server/Services/DaprHealthQueryService.cs` — P4 `MapToHealthComponents` de-dupes by ComponentName preferring LocalAdminProbe → RemoteEventStoreMetadata → LocalAdminMetadataFallback → Unavailable, then non-`state.unknown` type, then most recent LastCheckUtc; P7 `ComputeOverallStatus` switches on RemoteMetadataStatus with explicit arms + default arm calling `LogUnknownAndDegrade` (now takes ILogger logger); P8 added doc-comment documenting first-failure-wins ordering and pointing operators to InventorySourceStatus / LocalSidecarMetadataStatus / per-row evidence for multi-cause diagnosis.
- `src/Hexalith.EventStore.Admin.Server/Services/DaprHealthHistoryCollector.cs` — P11 accepts optional `TimeProvider? timeProvider` parameter (default `TimeProvider.System`); 15-second startup `Task.Delay` and `PeriodicTimer` constructor both use the injected provider; CaptureSnapshotAsync uses `_timeProvider.GetUtcNow()`.
- `src/Hexalith.EventStore.Admin.Server/Configuration/ServiceCollectionExtensions.cs` — P10 `AddAdminServer` now uses `AddOptions<AdminServerOptions>().Bind(...).ValidateOnStart()` so `AdminServerOptionsValidator` runs at host startup (existing whitespace-StateStoreName check now hard-fails before /health is reachable).
- `src/Hexalith.EventStore.Admin.UI/Pages/Health.razor` — P9 conditional empty-state path: when `OverallStatus == Unhealthy` and DaprComponents is empty, renders "DAPR component inventory unavailable" with a description built by `BuildEmptyInventoryOutageDescription` naming the unavailable metadata sources.
- `tests/Hexalith.EventStore.Admin.Server.Tests/Hexalith.EventStore.Admin.Server.Tests.csproj` — added `Microsoft.Extensions.TimeProvider.Testing` package reference for P11 FakeTimeProvider.
- `tests/Hexalith.EventStore.Admin.Server.Tests/Services/DaprHealthHistoryCollectorTests.cs` — P11 refactored: collector tests now inject `FakeTimeProvider` and use a deterministic `DriveFirstCaptureAsync` helper that advances the fake clock past the 15-second startup delay and polls a substituted observation flag (saveObserved / inventoryObserved / captured-not-null) for up to 2 seconds. All five 17-second sleeps removed (~70 seconds CI wall-clock saved on this file).
- `tests/Hexalith.EventStore.Admin.Server.Tests/Services/DaprInfrastructureQueryServiceTests.cs` — P12 added 3 new fixtures: `[Theory] _ReturnsUnreachable_OnTransportFailureStatusCodes` over 429 / 503 / 502 / 504; `_ReturnsAvailable_WithEmptyOptionalSections_WhenJsonHasZeroLengthBody` (empty body → InvalidPayload); `_ReturnsInvalidPayload_OnMixedCaseRequiredProperties` (case-sensitive parser pinned).

## Change Log

- 2026-05-07 - Story created and marked ready-for-dev. Ultimate context engine analysis completed - comprehensive developer guide created.
- 2026-05-07 - Review fixes applied: added Operator Truth Contract, tightened canonical DAPR inventory/source requirements, clarified health-history failure semantics, and expanded cross-page/test/manual evidence expectations.
- 2026-05-07 - Advanced elicitation refinements applied: added operator outcome, issue traceability, status vocabulary, canonical inventory contract sketch, merge rules, additional failure modes, quality gate, reviewer checklist, and likely touched files.
- 2026-05-07 - Second elicitation refinements applied: added truth model, conflict rules, embedded architecture decision with alternatives matrix, support copy examples, named fixtures, and support escalation pass criterion.
- 2026-05-08 - Implementation complete (ST1-ST7); story moved ready-for-dev → in-progress → review. Manual Aspire smoke (ST8/AC8) deferred to operator. Validation: Admin.Abstractions.Tests 404/404, Contracts.Tests 291/291, Client.Tests 360/360, Admin.Server.Tests 571/571 (18 pre-existing skips), Admin.UI.Tests 708/708, Release solution build clean (0 warn / 0 err).
- 2026-05-09 - Round-3 bmad-code-review: 8 decisions resolved + 25 patches applied + 7 deferred + 1 dismissed (53 raw findings → 41 unique). Two CRITICAL fixes: probe-timeout-as-Unhealthy branch reachability (ProbeStateStoreEntryAsync now takes outerCt and probeCt separately so the `when (!outerCt.IsCancellationRequested)` filter actually fires on probe-timer cancellation); remote-metadata memoization caches result-only (not Task) so a cancelled first caller no longer poisons later in-scope consumers. Other notable patches: per-probe try/catch (no more single-failure-aborts-batch); local-metadata memoization (round-1 D2 snapshot stability now actually holds); state-store probe filter to configured name (remote-only state-store rows no longer falsely marked Unhealthy); synth row Source=LocalAdminProbe at construction; synth skipped when StateStoreName empty; LocalProbeAvailable renamed to LocalSidecarMetadataAvailable; RemoteMetadataStatus.Initializing removed; ComputeOverallStatus now degrades on InvalidPayload; GetDaprComponentStatusAsync routes through canonical inventory; /dapr Components stat card sourced from canonical merged inventory; Active Subscriptions / Unique Topics severity warning when remote not Available; default-arm telemetry on three razor pages; new RecordingLogger for AC1 leak test (replaces NullLogger); 4 named UI failure fixtures (Forbidden / Timeout / Malformed / Null); ST5 history Pub/Sub regression test; culture-pinned stale-suffix test; Available+zero-components history sample now persists as real-zero. Spec status vocabulary table grew the Degraded row. Story stays at 'review' pending operator AC8 evidence (Dev Note exit criterion). Validation: Admin.Abstractions.Tests 404/404, Contracts.Tests 291/291, Client.Tests 360/360, Admin.Server.Tests 582/582 (18 pre-existing skips, +11 new tests), Admin.UI.Tests 718/718 (+10 new tests), Release solution build clean (0 warn / 0 err).
- 2026-05-10 - Round-4 bmad-code-review: 5 decisions resolved + initial patch chain applied across HIGH/MEDIUM tiers. **BREAKING CHANGE (D5): the next merge commit MUST carry a `BREAKING CHANGE:` Conventional-Commits footer** so semantic-release issues a major version bump. Public NuGet contract changes that warrant the footer: (1) `DaprHealthQueryService` constructor parameter `infrastructure` inserted before `logger` (round-3 change, not previously annotated); (2) `DaprComponentDetail` positional record now has 8 params (added `Source = DaprComponentSource.Unavailable`, round-3); (3) `DaprComponentHealth` positional record now has 5 params (added `Source = DaprComponentSource.Unavailable`, round-3); (4) `RemoteMetadataStatus` enum gained `InvalidPayload = 3` and pinned ordinals (round-3 + round-4); (5) `DaprComponentSource` enum is new in `Hexalith.EventStore.Admin.Abstractions` (round-3) with pinned ordinals (round-4); (6) `SystemHealthReport` positional record now has 11 params (added `LocalSidecarMetadataStatus = RemoteMetadataStatus.Available`, round-4 D1+D2). Migration: callers using DI need no change; positional constructors and `var (a,b,c,d,...) = ...` deconstructors must update to the new arity. Add a CHANGELOG entry under the next major version; document the field positions and default values in the migration note. Round-4 also pinned explicit ordinals on `RemoteMetadataStatus` and `DaprComponentSource` with `[JsonStringEnumConverter]` at the type level for wire-format stability.
- 2026-05-10 - API/Aspire Redis outage smoke executed with `EnableKeycloak=false` apphost. Smoke found and fixed two timeout gaps: total-event metric reads and health-history state reads now degrade to `Unavailable` instead of hanging when Redis is stopped. Added heatmap cell title helper after browser smoke exposed raw Razor expression leakage. Validation: Admin.Server.Tests 594/594 passed (18 pre-existing skips), Admin.UI.Tests 767/767 passed. Redis reset + canonical Pattern 2 seed completed (`tenant-a/counter/counter-1`, 3 completed increments); baseline/outage/recovery endpoint and browser evidence recorded; ST8/AC8 closed.
- 2026-05-10 - Round-10 bmad-code-review: 2 decisions resolved (D1 auth-user/tenant cache, D2 sibling DTO defaults) + 13 patches applied + 3 verified-already-correct + 4 deferred + 4 dismissed (85 raw → 34 unique). Applied: P0a (pragmatic auth-user IsAuthContextFailure already covered + tenant no-op documented for system-wide page); P0b (DaprActorRuntimeInfo.IsInventoryComplete=false, DaprActorInstanceState.LookupStatus=LookupUnavailable, DaprComponentHealthTimeline.HistoryStatus=Unavailable defaults); P2/P15 (collector HasData=entries.Count>0 + producer HistoryStatus=Available explicit); P3 (localSidecarMetadataAvailable cold-start: drop "evidence required" check, just metadata-API-responded); P4 (OnRefreshError sets _isLoading=false); P6 (probe upgrades synth row Source from Unavailable→LocalAdminProbe in success/timeout/exception paths); P7 (mutually exclusive count buckets summing to _components.Count); P9 (probe-loop wrapper catch uses captured `now`); P10 (Known Active Actors source-limited label); P14 (RemoteMetadataStatus.NotConsulted=4 enum value); P16 (FormatStateValue 64 KiB pre-parse cap + JsonDocumentOptions MaxDepth=64); P17 ("inventory source: unavailable" explicit text); P18 (Stage-1 non-state-store local-fallback Degraded instead of Unhealthy); P20 (GetRemoteMetadataTitle default arm). Verified-already-correct: P1 (Health.razor IsValueShown bypass already correct for stale value-based metrics — auditor finding based on incomplete reading); P5 (case-insensitive matching already used everywhere); P11 (NotConfigured already in skip rule via inverted condition — auditor misread the negation). Deferred: P8 (ordering tiebreaker — current is correct for truth contract, theoretical concern tracked); P12 (leak test redaction — needs strategy decision); P13 (test seam — production-surface change); P19 (FakeTimeProvider polling — 471 ms in CI, flake risk only on extreme loads). Test impact: 4 server tests updated (synth Source=LocalAdminProbe, Stage-1 non-state-store=Degraded, ExecuteAsync_PersistsEmptyAvailableSample_AsRealZero new HasData/HistoryStatus invariant); 4 UI tests updated (CreateRuntimeInfo passes IsInventoryComplete: true, HealthHistoryPage_RendersEmptyState passes HistoryStatus: Available, HealthPage_RendersRealZero/RendersStaleSuffix pass HealthEvidenceSource + InventorySourceStatus: Available + LocalSidecarMetadataStatus: Available to suppress the Round-9 issue banner triggered by the new non-green DTO defaults). Validation: Admin.Abstractions.Tests 404/404, Contracts.Tests 291/291, Client.Tests 362/362, Admin.Server.Tests 600/600 (18 pre-existing skips), Admin.UI.Tests 769/769, Admin.UI + Admin.Server Release builds clean (0 warn / 0 err). Story stays at `review` for the next reviewer pass.
- 2026-05-10 - Round-5 Phase 2/3 patches applied: 11 of 11 unchecked Round-5 patches landed (3 HIGH P1/P2/P3 + 8 MEDIUM P4/P6/P7/P8/P9/P10/P11/P12). HIGH: P1 extends "Unhealthy unless probe-covered" rule to all categories so Stage-1 LocalAdminMetadataFallback rows never claim Healthy without probe attestation; P2 synth state-store row constructed with `Source = Unavailable` (Stage 4 is the sole writer of LocalAdminProbe); P3 remote state-store rows whose name does not match the configured Admin name downgrade from Healthy to Degraded ("loaded inventory only, no probe attestation"). MEDIUM: P4 MapToHealthComponents de-dupes by ComponentName preferring most-authoritative Source rank → non-`state.unknown` type → most recent LastCheckUtc; P6 probe loop stages results in a local dict then merges once after all awaits settle so probe completion order cannot race with merged dictionary writes; P7 ComputeOverallStatus default arm logs warning and returns Degraded for unknown RemoteMetadataStatus values (now takes ILogger); P8 documents first-failure-wins ordering with operator-facing pointer to InventorySourceStatus / LocalSidecarMetadataStatus / per-row evidence; P9 Health.razor empty-state copy explains the metadata-source outage when OverallStatus=Unhealthy + DaprComponents empty (instead of "components will appear here"); P10 `AddOptions<AdminServerOptions>().Bind(...).ValidateOnStart()` so the existing AdminServerOptionsValidator (which already checks StateStoreName non-empty) runs at host startup; P11 DaprHealthHistoryCollector accepts optional TimeProvider and tests inject FakeTimeProvider via a `DriveFirstCaptureAsync` helper that polls observation flags rather than sleeping 17 seconds (~70s CI wall-clock saved on this file); P12 added server-side fixtures pinning Unreachable classification for HTTP 429 / 503 / 502 / 504 + InvalidPayload classification for empty JSON body + case-sensitivity invariant for required JSON properties. Round-4 unchecked items reconciled with Round-5 deferred block (the 10 Round-4 LOW/MEDIUM unchecked entries are now ticked with `[deferred per round 5 line N]` cross-references). Validation: Admin.Abstractions.Tests 404/404, Contracts.Tests 291/291, Client.Tests 362/362, Admin.Server.Tests 600/600 (18 pre-existing skips, +6 new tests this round), Admin.UI.Tests 767/767, Admin.Server + Admin.UI Release builds clean (0 warn / 0 err).
