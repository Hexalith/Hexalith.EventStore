# Story 2.12 Dual-Mode Lane Evidence — 2026-07-27 (second session)

- Evidence date: `2026-07-27`
- Lanes exercised: source (Debug, `UseHexalithProjectReferences=true`) and
  package (Release, `UseHexalithProjectReferences=false`)
- Source lane status: `passed`
- Package **compatibility** status: `passed`
- Package **identity** status (AC3): `blocked` — unchanged, see `prerequisites.md`
- Proof working copy: separate clean Tenants clone at
  `/home/administrator/tmp-story-2-12/proof`, not a nested submodule of the EventStore
  umbrella. Only Tenants-root-declared submodules were initialized; no `--recursive`
  and no `--remote` update was used.

This file is intentionally not named for a Tenants SHA. The proof commit below is a local,
unpublished, maintainer-unaccepted commit.

## Authority Revalidation (same shell)

The complete official-main A/B/C verifier was extracted from the packet block
(`1-20-owner-approved-parity-closure-proof-packet.md`, lines 3058-5099) and executed from the
EventStore repository root at `main` `c8c7003052a7f811d3b821f3442379ca5f3a9c65`.

```text
EVIDENCE_COMMIT_A=b695ad3215cd873c41561635e4eb4d7ff29d56a2
POINTER_COMMIT_B=ed48057e9bf9cb5e5e8667fec84f7c70e4534eea
AUTHORIZATION_COMMIT_C=1b219d39cfa8f0349175c356001ba539bfb4aa92
AUTHORIZATION_VERIFICATION_PHASE=official-main
```

- Complete A/B/C verifier: **exit 0**.
- `A_TESTED_RUNTIME_SHA` derived from the verified packet:
  `fa2d1c9910f8976553adb33dcdb1c9ff2ea75594` — not assigned from prose.
- The AWK defect recorded earlier in the story Debug Log did not recur.

## AC2 On Published Tenants `main` — Still Violated, And Still Drifting

The source consumer procedure was run in the same shell against Tenants `main` exactly as
published at `4ca5f86fa777db62efaafb2f305f96a3a548b993`.

| Assertion | Result |
| --- | --- |
| `gitlink == approved` | **FAIL** (`b2d3402552fbadf529c220fcc739da9d06d285fe`) |
| `checkout == approved` | **FAIL** (`b2d3402552fbadf529c220fcc739da9d06d285fe`) |
| consumer worktree clean | PASS |
| EventStore worktree clean | PASS |
| EventStore clean incl. ignored | PASS |

The identity mismatch is again the sole failing assertion.

### The pin is being mechanically overwritten on a recurring cadence

This is no longer a single `/pushall` merge clobber. During this session Tenants `main`
advanced again, unprompted:

| Tenants `main` | `references/Hexalith.EventStore` | Note |
| --- | --- | --- |
| `902065e` / `db09a84` | `fa2d1c9910f8` | approved pin, correctly adopted |
| `a7ca142` | `fa2d1c9910f8` | conditional Gateway change, pin retained |
| `230a533d` | `737b3e5a` | `/pushall` merge discarded the pin |
| `4ca5f86` | `b2d34025` | automated `build(deps)` submodule bump |
| `f1053a31` | `c8c70030` | automated `build(deps)` bump **observed mid-session** |

`git log --format='%h %s' origin/main -- references/Hexalith.EventStore` shows a recurring
`build(deps): bump ... submodules` cadence that advances this gitlink to EventStore `main`
HEAD. The approved SHA is now **46 commits** behind EventStore `main`.

Restoring the pin is therefore provably correct but provably ephemeral: without a guard that
excludes `references/Hexalith.EventStore` from the automated bump, or a CI check that fails
Tenants when the gitlink leaves the approved SHA, AC2 will regress again after any restore.
Recording this as a structural finding, not as a completed task.

## Prior Blockers B1 And B2 Are Resolved

Both blockers recorded in `source-lane-2026-07-27.md` are gone, and neither was worked around.

Cause of the fix: Tenants `main` now pins Builds at
`bb02cdc85735062de3a606f069ec714c9a398032`
(`fix(deps): restore published EventStore package pin`), whose catalog sets
`HexalithEventStoreVersion` back to the published `3.82.0` while **retaining** the central
`Hexalith.EventStore.Gateway` entry added by the approved `8f32f127`.

- **B1 resolved.** `dotnet restore Hexalith.Tenants.slnx` Debug/source at unmodified Tenants
  `main` → **exit 0**, zero errors. The previous `NU1102` for `Hexalith.EventStore.Client`
  via `Hexalith.Memories.Server` no longer occurs.
- **B2 resolved.** `Hexalith.Tenants.IntegrationTests` restores and runs. The
  `TenantsUiCompositionTests` package-mode case that correctly failed closed now passes;
  `UI.Tests` is 1266/1266 (was 1260 passed / 1 failed).

This means the premature package-identity adoption that contradicted the External
Prerequisite Contract has been **rolled back** in Tenants. Package identity is once again the
published `3.82.0`, not the approved proof version.

## Restored Pin And Green Source Guard

The approved pin was restored in the proof clone as local unpublished commit
`b8698e9deb0b2937997d6877b6e145d47977d922`
(`fix(deps): restore the approved EventStore source identity`). Content is otherwise
identical to Tenants `main` at `4ca5f86`.

Complete A/B/C verifier plus source consumer procedure, one shell:

```text
VERIFIER_OK
SOURCE_CONSUMER_OK
EXIT=0
```

Gitlink and checkout both equal `fa2d1c9910f8976553adb33dcdb1c9ff2ea75594`.

**Ordering note for future runs.** The guard's
`git status --porcelain=v1 --ignored=matching --untracked-files=all` assertion on the
EventStore submodule fails after any restore/build, because MSBuild writes ignored `obj/`
artifacts into the submodule. The consumer guard must run on a pristine checkout **before**
the lane's restore, not after. An earlier attempt in this session failed on exactly that
assertion with identity already passing; `git clean -xdff` in both repositories restored the
green result.

## Resolved Dependency Graphs

Both graphs were read from `obj/project.assets.json` after that lane's own
`--force-evaluate` restore. Restore was rerun after every mode change; no prior assets file
was reused.

### Source lane (Debug, `UseHexalithProjectReferences=true`)

| Consuming project | EventStore edges | `type: project` | `type: package` | outside approved checkout |
| --- | --- | --- | --- | --- |
| `src/Hexalith.Tenants` | 7 | **7** | **0** | 0 |
| `src/Hexalith.Tenants.Api` | 3 | 3 | 0 | 0 |
| `src/Hexalith.Tenants.UI` | 2 | 2 | 0 | 0 |
| `src/Hexalith.Tenants.Client` | 2 | 2 | 0 | 0 |
| `src/Hexalith.Tenants.Contracts` | 1 | 1 | 0 | 0 |

The seven `src/Hexalith.Tenants` edges are `Admin.Abstractions`, `Client`, `Contracts`,
`DomainService`, `Gateway`, `Server`, `ServiceDefaults` — every one a project under
`references/Hexalith.EventStore` at the approved checkout. Gateway and DomainService resolve
identically, so **no mixed Gateway-project / DomainService-package graph is reachable**.

### Package lane (Release, `UseHexalithProjectReferences=false`)

| Fact | Value |
| --- | --- |
| EventStore edges in `src/Hexalith.Tenants` | 7 |
| `type: project` | **0** |
| `type: package` | **7** |
| Resolved version | `3.82.0` |
| Approved version `999.1.20-proof.fa2d1c9910f8` present | **No** |

AC4's structural requirement is satisfied in both directions: Release assets contain **zero**
EventStore `ProjectReference`, and Gateway follows the same conditional policy as
DomainService. AC3's exact-identity requirement is **not** satisfied and is not claimed — the
lane resolves the published `3.82.0`, because the approved proof bytes do not exist.

## Compatibility And Regression Matrix — Both Modes

Solution used for restore/build only; tests run by project, never solution-level.

| Lane | Restore | Build `--warnaserror` |
| --- | --- | --- |
| Debug / source | exit 0 | **0 Warning(s), 0 Error(s)** |
| Release / package | exit 0 | **0 Warning(s), 0 Error(s)** |

| Test project | Debug / source | Release / package |
| --- | --- | --- |
| `Hexalith.Tenants.Contracts.Tests` | **115 / 115** | **115 / 115** |
| `Hexalith.Tenants.Server.Tests` | **738 / 738** | **738 / 738** |
| `Hexalith.Tenants.UI.Tests` | **1266 / 1266** | **1266 / 1266** |
| `Hexalith.Tenants.IntegrationTests` | **167 passed, 1 skipped, 0 failed** | **167 passed, 1 skipped, 0 failed** |

Zero failures in either mode. The `Contracts.Tests` pass includes the published
`PackageGovernanceTests` host Gateway/DomainService rule from `a7ca142`. The passing
`IntegrationTests` and `UI.Tests` suites carry the Stories 2.4-2.7 API-host/gateway-boundary
and typed-client-only guards, the Story 2.10 `AddEventStoreDaprServiceInvocation` handler-order
and no-local-handler guards, and the Story 2.11 fail-closed provenance/lifecycle matrices. No
production policy, guard, or test was weakened, and no fixture was adjusted to make a lane pass.

Commands (each lane restored fresh, `-nodeReuse:false -m:1`, unattended, no `--interactive`):

```text
dotnet restore Hexalith.Tenants.slnx --force-evaluate -p:Configuration=Debug \
  -p:UseHexalithProjectReferences=true -p:HexalithMemoriesFromSource=false \
  -p:HexalithCommonsFromSource=false -nodeReuse:false -m:1
dotnet build   Hexalith.Tenants.slnx --configuration Debug --no-restore --warnaserror \
  -p:UseHexalithProjectReferences=true -p:HexalithMemoriesFromSource=false \
  -p:HexalithCommonsFromSource=false -nodeReuse:false -m:1

dotnet restore Hexalith.Tenants.slnx --force-evaluate -p:Configuration=Release \
  -p:UseHexalithProjectReferences=false -nodeReuse:false -m:1
dotnet build   Hexalith.Tenants.slnx --configuration Release --no-restore --warnaserror \
  -p:UseHexalithProjectReferences=false -nodeReuse:false -m:1

dotnet test tests/<project>/<project>.csproj --configuration <mode> \
  --no-build --no-restore <matching -p: properties> -nodeReuse:false -m:1
```

`-m:1` is required: parallel MSBuild instances race on the same EventStore `.deps.json`.

## What This Session Did Not Close

- **AC3 remains blocked and unsatisfiable as literally specified.** The approved 14 `.nupkg`
  files at `999.1.20-proof.fa2d1c9910f8` do not exist in any avenue named by the External
  Prerequisite Contract. The package lane was therefore run as a *compatibility* lane against
  the published `3.82.0` catalog, with no isolated `--packages` directory, no source-mapped
  `nuget.config`, no manifest verification, and no byte comparison — because there are no
  approved bytes to map, verify, or compare. None of those steps is claimed as passed.
- **AC2 is proven achievable but is not adopted on published Tenants `main`.** No gitlink was
  changed in any published repository.
- **AC5's maintainer-accepted final Tenants commit does not exist.** The scope approval in
  Tenants issue #32 stands; the acceptance of a specific final SHA is a separate approval.

## Scope Statement

Nothing was pushed to any remote. No Builds gitlink was changed. No package identity was
adopted. No EventStore submodule content was edited. No `Version`, `VersionOverride`,
fallback property, or local `PackageVersion` entry was added to Tenants. No nested submodule
was initialized and no recursive or remote submodule update was used. The story remains below
`review`.
