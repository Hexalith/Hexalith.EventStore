# Blind Hunter findings (Story 3.13)

- Proof-packet Verification Record still cites **132** focused tests after the sixth hardening pass, while the Spec Change Log’s seventh pass claims **140** and the Dev Agent Record still shows earlier **115** / **999** totals without a refreshed full-suite measurement after later mutations.
- Retained `smoke-results.json` declares overall `"result": "pass"` and per-platform `"outcome": "pass"`, while `runtime-verification.json`, the crosswalk runtime block, and the proof packet all fail-close the same smoke for missing structured `/alive`/platform/timing/exit facts and `Development`≠`Production`.
- Smoke logs and runtime summaries omit HTTP status, redirect count, observed platform, per-platform timestamps, and exit codes, yet still emit `outcome=pass` / `cleanup=pass`, so declared pass strings cannot be independently replayed.
- Child OCI descriptor/body rows are marked `verification: "pass"` while `oci-validation.json` / `registry-readback.json` leave `objects: []` and `response_metadata_result: "missing"`, so reviewers can mistake a partial body check for a complete OCI graph pass.
- Task 8’s checked subtask still requires “all independent checks passing,” and Task 10 is fully checked, even though AC2/AC4 remain open, package bytes / release provenance / Production smoke / acceptances are missing, and the Spec Change Log admits restoring Story 1.20 `environment.txt` and proof-packet bytes.
- Task 1’s checked subtask still says this story must go “no farther than `ready-for-dev`,” but the story/spec/docs status is `in-review` and sprint-status uses `review`.
- Lifecycle vocabulary remains split: story/spec/`docs/ci.md` say `in-review`, `sprint-status.yaml` uses `review`, and older review notes/comments still describe reconciliation to `in-progress`.
- Story File List stays a directory stub and still omits concrete evidence files the verifier depends on for pass lineages (`deployment-authority.json`, `deployment-authority-source.json`, `release-provenance.json`, raw OCI/smoke artifacts), even though that gap was only deferred.
- No `acceptances/{subject_sha256}/` scaffold, receipt template, or worked example exists beside the roster, despite AC4 requiring three content-bound receipts and 0/3 still missing.
- Reviewer roster maps both `eventstore-owner` and `release-owner` to the same `github:jpiquot`, while Task 9 / AC4 require distinct EventStore-owner and Release-owner acceptances.
- The Story 3.13 `sprint-status.yaml` and `docs/ci.md` diffs also advance Epic 4 stories (`4-3`/`4-4`/`4-5`) and insert Story 4.5 LiveSidecar prose, exceeding the stated “ownership paragraph only / no scope leakage” boundary.
- Spec frontmatter still uses `type: 'chore'` for a closure story, and `review_loop_iteration` / Spec Change Log drift was deferred then only partially reconciled across seven hardening narratives.
- `identity-crosswalk.json` `assembled_at` remains `2026-08-04T14:55:55Z` while `review-subject.json` is `2026-08-08T14:54:10Z` after later evidence rebinds, so assembly freshness is not obvious to reviewers.
- Release-authority verification reports `result: "fail"` with method “hash-checked durable predecessor authority record,” without separating a successful hash check from the real scope failure (`deployment_authorized: false` / quarantine-only).
- Review-subject blockers say smoke evidence lacks cleanup facts, but `runtime-verification.json` already records `cleanup_check` and per-platform `cleanup: "pass"`, so the blocker text overstates what is missing.
- Deferred gaps remain open: missing-key removal mutations for `NullReferenceException` catch-filter coverage, and incomplete documentation of how dual Builds pins (`baseline.builds_gitlink_sha` vs tool pin `a5316653...`) should be used by reopen owners.
