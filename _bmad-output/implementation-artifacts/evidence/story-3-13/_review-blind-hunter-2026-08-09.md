# Blind Hunter findings (Story 3.13) — 2026-08-09

- The proof packet’s Artifact Identity Pin still quotes identity-crosswalk SHA-256 `b078cf82…`, while `evidence-sha256.txt` and `review-subject.json` bind `11b17fb0…`.
- The story record frontmatter pins `baseline_commit: 4bcf2484…`, but the spec, crosswalk, and verifier pin `1d6e9321…`.
- Spec `review_loop_iteration` remains `7` after eight Spec Change Log hardening passes.
- Spec frontmatter still uses `type: 'chore'`.
- Tasks 4–7 remain open while Tasks 8 and 10 are fully checked under fail-closed review.
- The story File List is still a directory stub omitting concrete evidence files.
- There is no `acceptances/{subject_sha256}/` scaffold beside the roster.
- `reviewer-roster.json` maps both owner roles to `github:jpiquot`.
- The Story 3.13 change set also advances Epic 4 stories and inserts Story 4.5 LiveSidecar prose in `docs/ci.md`.
- The same `sprint-status.yaml` edit truncates Story 2.12 key to `…package-mode-validatio`.
- The complete Contracts suite was last measured at 1001 on 2026-08-05 and was not re-measured after the eighth hardening pass.
- `identity-crosswalk.json` `assembled_at` remains stale relative to later subject rebinds.
- Crosswalk / runtime / smoke still emit per-platform `outcome: "pass"` while overall runtime verification fails.
- Child OCI descriptor rows are marked `verification: "pass"` while overall OCI/response metadata fail.
- Release-authority verification reports `result: "fail"` under a hash-check method without separating scope failure.
- Missing-key removal mutations for NRE catch-filter coverage remain deferred.
- Dual Builds pins lack reopen-owner guidance in CONTENT.
- Dev Agent Record Completion Notes still narrate lifecycle reconciliation to `in-progress`.
- Frozen Spec I/O matrix omits some documented blocker rows.
- Embedded prior review artifacts can mislead relative to current smoke-results.
- The proof packet does not quote the review-subject SHA-256 or its own packet SHA.
