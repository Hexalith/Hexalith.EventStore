---
title: 'Story 1.20 GitHub approval role authorization'
type: 'feature'
created: '2026-07-17'
status: 'done'
baseline_commit: 'a9718a2106b31ff9c005203e5381c28d06db0e21'
review_loop_iteration: 3
context:
  - '{project-root}/_bmad-output/implementation-artifacts/epic-1-context.md'
---

<frozen-after-approval reason="human-owned intent — do not modify unless human renegotiates">

## Intent

**Problem:** Story 1.20's committed GitHub approval-role allowlist currently has empty arrays for every recognized role. The exact-SHA gate therefore fails closed even when a legitimate repository owner submits a durable approval record.

**Approach:** Add the authenticated repository user's GitHub login, `jpiquot`, once to each of the four existing role arrays. Preserve the allowlist schema, repository identity, role-key set, and all fail-closed validation behavior.

## Boundaries & Constraints

**Always:** Keep the JSON syntactically valid; retain schema `hexalith.eventstore.github-approval-role-allowlist/v1`; retain repository `Hexalith/Hexalith.EventStore`; keep exactly the roles `eventstore_owner`, `release_owner`, `architecture_owner`, and `story_1_16_reviewer`; place `jpiquot` exactly once in every role array; leave existing Story 1.20 packet, story, sprint-status, and submodule changes untouched.

**Ask First:** Any role-key addition or removal; authorization of a different login; changes to proof-packet gate logic; staging, committing, or running the full exact-SHA product gate.

**Never:** Infer additional authorized users; weaken non-empty, uniqueness, login-format, repository, or role-key validation; mark Story 1.20 available or authorize consumer migration; rewrite or revert unrelated working-tree changes.

## I/O & Edge-Case Matrix

| Scenario | Input / State | Expected Output / Behavior | Error Handling |
|----------|--------------|---------------------------|----------------|
| Authorized login | Authenticated GitHub login is `jpiquot`; all four role arrays are empty | Every role array becomes `["jpiquot"]` | Stop if the authenticated login differs |
| Gate schema validation | Updated allowlist is evaluated by the packet's `jq` predicate | Schema, repository, exact key set, non-empty arrays, uniqueness, and login format all pass | Preserve fail-closed behavior on any mismatch |
| Existing worktree changes | Story packet and submodule changes already exist | Only the allowlist receives the authorization edit | Do not stage, revert, or rewrite unrelated changes |

</frozen-after-approval>

## Code Map

- `_bmad-output/implementation-artifacts/1-20-github-approval-role-allowlist.json` -- source of authorized GitHub logins for Story 1.20 approval roles.
- `_bmad-output/implementation-artifacts/1-20-owner-approved-parity-closure-proof-packet.md` -- embeds the fail-closed validator and binds fetched GitHub approval records to the allowlist.

## Tasks & Acceptance

**Execution:**
- [x] `_bmad-output/implementation-artifacts/1-20-github-approval-role-allowlist.json` -- add `jpiquot` once to all four existing arrays so GitHub records authored by this login can satisfy each role check.

**Acceptance Criteria:**
- Given the four recognized approval roles, when the allowlist is read, then each role contains exactly the single login `jpiquot`.
- Given the updated file, when the packet's schema and role predicate runs, then it exits successfully without weakening any check.
- Given the pre-existing dirty working tree, when the change is complete, then no Story 1.20 packet field, sprint status, or submodule state has been altered by this task.

### Review Findings

_Code review 2026-07-17 of committed range `a9718a21..ea6ce49b` (layers: blind-hunter, edge-case-hunter, verification-gap, acceptance-auditor). AC1 (exact membership), AC2 (packet predicate), and the identity check were replayed and pass; the findings below are what remains._

- [x] [Review][Decision] Committed range falsifies the spec's isolation contract — commit `ea6ce49b` bundled the allowlist edit with 7 `references/` submodule bumps and this spec file, and `ba203bde` (Directory.Packages.props) also postdates `baseline_commit` `a9718a21`; Verification commands 4 and 5 therefore fail permanently at HEAD (replayed: 10 changed tracked paths, empty untracked set), command 3 has no exit-code predicate (vacuously passes), commands 4–5 are cwd-dependent, and AC3 ("no submodule state altered") is false in the committed record. The "Ask First: staging, committing" boundary has no recorded approval (consistent with the concurrent auto-commit loop absorbing worktree state; all submodule moves are fast-forward and the commit is owner-authored). **Resolved 2026-07-18: owner ratified the committed state; Verification section restated against the immutable story commit (root-pinned, exit-code-asserted); logged in the Spec Change Log.**
- [x] [Review][Decision] Exact-membership (anti-over-authorization) guard exists only in this spec's manual Verification command 2 — the operative packet validator (`1-20-owner-approved-parity-closure-proof-packet.md` allowlist predicate and per-record `index($login)` membership check) accepts any non-empty, unique, valid-format login arrays, and no CI, test, or script consumes the allowlist; a silently appended valid-format login would be honored by the gate. Hardening the packet is "Ask First" (gate logic), so the owner must choose. **Resolved 2026-07-18: owner approved hardening the packet — exact four-role membership pinned in both operative validators (candidate gate and evidence-commit-A re-check).**
- [x] [Review][Patch] Local `PackageVersion Update` silently pins `Microsoft.Extensions.TimeProvider.Testing` 10.7.0 below the Builds-central 10.8.0 (`references/Hexalith.Builds/Props/Directory.Packages.props:212`), violating the Builds-owns-global-versions invariant with no check observing the divergence [`Directory.Packages.props:31`] **Fixed 2026-07-18: local override removed; central 10.8.0 now resolves (restore-verified).**
- [x] [Review][Defer] Commit `ba203bde` is unbuildable in isolation — `Update` with no central definition until `ea6ce49b` bumps Builds to `cfafcbf1` (NU1010 for the three consuming test projects; bisect/rollback hazard) — deferred, history already on `main`
- [x] [Review][Defer] Login-format regex `^[A-Za-z0-9](?:[A-Za-z0-9-]{0,38})$` admits trailing/consecutive hyphens GitHub forbids [`1-20-owner-approved-parity-closure-proof-packet.md` allowlist predicate] — deferred, pre-existing packet behavior masked today by exact-membership equality
- [x] [Review][Defer] Redundant `PackageVersion Update="Microsoft.Playwright"` duplicates the identical central 1.61.0 and will silently freeze the version on the next central bump [`Directory.Packages.props:32`] — deferred, pre-existing line not introduced by this range

_Code review 2026-07-18, loop 3, of range `a9718a21..working tree` (layers: blind-hunter, edge-case-hunter, verification-gap, acceptance-auditor). All seven restated Verification commands replayed and exit 0 (including the live `gh api` identity check); all three loop-2 resolutions verified genuinely present in the working tree; both packet predicates replayed green with an appended-login probe correctly rejected. 5 findings dismissed as noise. The items below are what remains._

- [x] [Review][Decision] The three loop-2 resolution edits are uncommitted while HEAD `3796e27e` (released 3.70.2) still records the pre-resolution spec (3 unchecked findings, superseded Verification commands) — the packet validator hardening, the TimeProvider override removal, and this spec's restated Verification exist only in the working tree, alongside 6 unrelated changed paths (`epic-3-context.md`, `sprint-status.yaml`, `architecture.md`, `epics.md`, `prd.md`, untracked `sprint-change-proposal-2026-07-18.md`) that the concurrent auto-commit loop may bundle into one commit, repeating the exact isolation breach loop 2 ratified — staging/committing is "Ask First", so the owner must choose how to land them. **Resolved 2026-07-18: owner approved a dedicated scoped commit; the concurrent loop's mixed staged bundle (which had absorbed mid-edit snapshots of these files) was unstaged first, and only the 1.20 resolution set was staged and committed, leaving the loop's planning-doc worktree changes untouched.**
- [x] [Review][Decision] Unratified `references/Hexalith.Memories` forward bump riding the working tree — checkout sits at `272c33bc` (feature commit) while HEAD and `ea6ce49b` pin `ce66d4ca`; no spec, ledger, or ratification narrative mentions it, and it will silently ride the next auto-commit. **Resolved 2026-07-18: owner chose reset; the staged gitlink was unstaged and the submodule checkout restored to pinned `ce66d4ca` (verified).**
- [x] [Review][Decision] Both operative packet allowlist validators accept multi-root JSON — they run plain `jq -e` (no `-s 'length == 1'` slurp guard like spec Verification command 2), so a file whose last document alone is valid exits 0 through the fail-closed gate (probed). Fixing the packet is "Ask First" gate logic; the same decision should settle whether a CI-lane Contracts.Tests allowlist fact is wanted (it would add a fifth copy of the membership literal). **Resolved 2026-07-18: owner approved hardening; both operative validators now slurp with `-s 'length == 1 and (.[0] | …)'` (probed: multi-root rejected, real allowlist passes). The optional CI allowlist fact was declined to avoid a fifth membership-literal copy.**
- [x] [Review][Patch] No automated check that `CLAUDE.md`, `AGENTS.md`, and `.github/copilot-instructions.md` remain identical normalized text — the rewrite introduces the sync contract while removing AGENTS/CLAUDE from the only positive content assertion; add a Contracts.Tests three-way equality fact [`tests/Hexalith.EventStore.Contracts.Tests/Packaging/`] **Fixed 2026-07-18: `SharedInstructionEntryPointTests` asserts three-way normalized equality plus baseline-section presence (Contracts.Tests 711/711 green).**
- [x] [Review][Patch] Copilot baseline-path assertion weakened from resolved-link proof to base-less substring presence, so the historical wrong-base (`.github/references/…`) regression can return; strengthen the negative assertions and correct the misleading "submodule-relative fallback path" message [`tests/Hexalith.EventStore.Contracts.Tests/Packaging/CommitMessagePolicyTests.cs:22`] **Fixed 2026-07-18: negative assertions now ban every `](references/`-anchored relative link form; message corrected to workspace-relative.**
- [x] [Review][Patch] Nothing observes the Builds-owns-versions invariant this range restores — re-adding a masking `PackageVersion Update` passes every test; add a guardrail asserting root `Directory.Packages.props` overrides no Builds-central ID, with an explicit allowlisted exception for the ledgered Playwright line [`Directory.Packages.props:31`] **Fixed 2026-07-18: `Root_package_props_does_not_override_builds_central_versions` added; it immediately caught live divergences `System.CommandLine` 2.0.9 < central 2.0.10 and `ModelContextProtocol` 1.4.0 < central 1.4.1 plus the no-op `NBomber` 6.5.0 pin — all three overrides removed, consumer projects (Admin.Cli, Admin.Mcp, LoadTests) restore/build-verified on the central versions; Playwright remains the sole ledgered exception.**
- [x] [Review][Patch] The deferred-work ledger's recommended replacement login regex `^[A-Za-z0-9](?:-?[A-Za-z0-9]){0,38}$` silently drops the 39-character cap (probed: accepts 77-character logins); correct the recorded recipe, e.g. `^(?=[A-Za-z0-9-]{1,39}$)[A-Za-z0-9](?:-?[A-Za-z0-9])*$` [`_bmad-output/implementation-artifacts/deferred-work.md`] **Fixed 2026-07-18: ledger recipe corrected (jq-probed: keeps the 39-character cap, rejects trailing/consecutive hyphens).**
- [x] [Review][Patch] Restated Verification command 4 passes vacuously (`test "" = ""`) when both command substitutions fail identically, and command 6 pins abbreviated `ba203bde` contradicting the section's immutable-reference preamble; add a non-empty guard and the full 40-hex SHA, logged in the Spec Change Log [`spec-1-20-add-github-approval-login.md` Verification] **Fixed 2026-07-18: command 4 wrapped with a non-empty guard, command 6 pinned to the full 40-hex SHA; all seven commands replayed exit 0.**
- [x] [Review][Defer] Shared instruction-baseline text regressions: the Agent Skills canonical-path (symlink) clause and the standalone-clone submodule-initialization rule were dropped in the rewrite, leaving a fresh standalone clone with no permitted path to the mandatory baseline — deferred, the baseline is shared normalized text owned upstream (Hexalith.AI.Tools); not unilaterally fixable in this repository
- [x] [Review][Defer] `DomainModuleAuthoringGuardrailTests.cs:21,208` cites a CLAUDE.md "Domain-Module Authoring" section that does not exist — deferred, pre-existing (already absent at baseline `a9718a21`); the rule itself is enforced on code and unaffected
- [x] [Review][Defer] Exact-membership literal now lives in four synchronized places with the retained generic predicates reduced to dead code, and the evidence-commit-A validator still lacks the login-format regex — deferred, consolidation note for the next approved gate-logic change

_Code review 2026-07-18, loop 4 (bmad-code-review), of committed range `a9718a21..4fc14c00` — the full spec-baseline range at the owner's choice, which includes the landed loop-3 resolution `f7b2aa1c` and three post-landing commits from other stories (layers: blind-hunter, edge-case-hunter, verification-gap, acceptance-auditor). Confirmed clean: all seven restated Verification commands replayed exit 0 (including the live `gh api` identity check); `f7b2aa1c` is scoped to exactly the seven ratified files with no planning-doc bundling; no commit after `f7b2aa1c` touches any 1.20 gate file (allowlist, packet, spec); both hardened packet validators re-probed green with multi-root and appended-login probes correctly rejected; sprint status still holds 1.20 `in-progress` with no availability or migration authorization. The live findings below concern the post-landing commits inside the range and hardening gaps in the guardrail tests this story added. 8 findings dismissed as noise or already-dispositioned._

- [ ] [Review][Decision] Shared entry-point sync guardrail is red at HEAD — post-landing commit `01830544` added `- Validate the commit message with commitlint.` to `.github/copilot-instructions.md` only, so `SharedEntryPointsCarryIdenticalNormalizedText` (added by this story's `f7b2aa1c`) fails on `main` (independently replayed by three review layers; Contracts.Tests CI lane red). The fix direction is a policy call: (a) remove the bullet from the Copilot entry point (the baseline itself says repository-specific instructions belong in repository documentation, not the universal entry points — restores sync with the upstream-owned text), (b) propagate the bullet to `AGENTS.md` and `CLAUDE.md` (unilateral three-way divergence from the upstream Hexalith.AI.Tools baseline), or (c) route the addition upstream and revert locally meanwhile.
- [ ] [Review][Decision] Unratified `references/Hexalith.Memories` forward bump recurred — `4fc14c00` (a Story 3.5 documentation commit) silently moved the gitlink from the loop-3-ratified pin `ce66d4ca` to `6d7fd8aa` with no ratification narrative in its message, the ledger, or any planning artifact; this repeats the exact pattern loop 3 reset hours earlier, and the same drift is live again right now (`references/Hexalith.FrontComposer` moving in the working tree under the concurrent loop). Options: ratify `6d7fd8aa` retroactively with a recorded narrative, or reset via a dedicated commit re-pinning `ce66d4ca`.
- [ ] [Review][Decision] Catalog-refresh authority (Story 3.11, explicitly including .NET/ASP.NET security patch bands) is transitively chained behind this owner-gated story — 3.11 stays `backlog` until Story 3.5 is `done`, and 3.5's completion gate cannot close AC4 while Story 1.20 has not authorized Story 2.12's exact runtime/package identity. The documented escape valve is "approved change control revises the conflicting boundary"; whether to add a security-patch carve-out now or accept the chain until 1.20 closes is an owner scheduling/policy call.
- [ ] [Review][Decision] The packet's hardened validator predicates have no automated observer — reverting the `-s 'length == 1 …'` slurp guard or the exact-membership object inside the proof packet (or appending a valid-format login to the allowlist) leaves every CI lane green; discovery would wait for the next manual gate replay. The owner declined a CI allowlist-content fact in loop 3 to avoid a fifth membership-literal copy; a predicate-integrity test (asserting the packet's two jq blocks retain the slurp guard and exact-equality *shape*, without restating the membership) is a distinct choice and remains Ask-First gate-logic territory.
- [ ] [Review][Patch] The Builds-central masking guard scans only root-props `PackageVersion` items — a csproj `VersionOverride` (CPM's documented precedence mechanism) re-pins a central package with every test green (demonstrated: `VersionOverride="2.0.9"` on Admin.Cli's `System.CommandLine`); extend the guard to scan `src/`, `tests/`, and `perf/` project files for `VersionOverride` attributes [`tests/Hexalith.EventStore.Contracts.Tests/Packaging/ContractsPackageDependencyTests.cs:60`]
- [ ] [Review][Patch] `allowedOverrides` exempts `Microsoft.Playwright` at any version, not just the ledgered identical-to-central no-op — a future divergent local pin silently freezes the version, the exact masking class this story fixed for TimeProvider; assert allowlisted IDs' local version equals the Builds-central version so the next central bump fails loud [`tests/Hexalith.EventStore.Contracts.Tests/Packaging/ContractsPackageDependencyTests.cs:52`]
- [ ] [Review][Patch] `centralPackageIds` has no non-empty assertion — a benign Builds catalog restructure (sub-imports, xmlns, renamed attributes) would silently disable the masking guard via a vacuous empty-set pass [`tests/Hexalith.EventStore.Contracts.Tests/Packaging/ContractsPackageDependencyTests.cs:54`]
- [ ] [Review][Patch] `LoadSharedPackageVersions` resolves only import fallback 1 (`Hexalith1BuildPackageProps`) — in umbrella layouts where fallback 2/3 is the effective catalog the guard throws file-not-found instead of validating the catalog actually in effect; mirror the four-branch `Exists()` import chain [`tests/Hexalith.EventStore.Contracts.Tests/Packaging/ContractsPackageDependencyTests.cs:151`]
- [ ] [Review][Patch] The anchored-link ban covers only three `](` prefixes — reference-style definitions (`]: references/`) and root-absolute links (`](/references/`) evade it, so the historical wrong-base regression can return through an unbanned markdown form; extend `anchoredLinkForms` [`tests/Hexalith.EventStore.Contracts.Tests/Packaging/CommitMessagePolicyTests.cs:36`]
- [x] [Review][Defer] Commit `01830544` is misdescribed and mixed — its subject claims commitlint policy while 377 of its 378 lines are an unrelated Story 3.5 artifact, the third mixed-bundle recurrence this spec has recorded — deferred, history already on `main`; the operative consequences are the two Decision items above
- [x] [Review][Defer] Story 3.5 dependency-mode truth table has no row for configurations other than Debug/Release (e.g. `Staging`), leaving the reference-graph edge unspecified there [`_bmad-output/implementation-artifacts/3-5-shared-package-catalog-and-source-package-reference-modes.md`] — deferred, Story 3.5 spec content owned by that story's active cycle
- [x] [Review][Defer] Precedence between conflicting explicit `UseNuGetDeps` and `UseHexalithProjectReferences` values is undefined in Story 3.5's contract [`_bmad-output/implementation-artifacts/3-5-shared-package-catalog-and-source-package-reference-modes.md`] — deferred, Story 3.5 spec content owned by that story's active cycle
- [x] [Review][Defer] The seven ratified submodule source bumps are compile-verified only — no CI lane runs unit-test suites in source mode (`UseHexalithProjectReferences=true`); only the filtered `tenants-source-mode` launch-settings job exercises bumped source — deferred, CI-lane design belonging to Story 3.5's dual-mode validation scope

## Spec Change Log

- 2026-07-17, review loop 1: Review found that the documented generic allowlist predicate accepted additional valid-format logins and that the scoped diff command did not verify the isolation acceptance criterion. Verification now requires the exact four-role object with `jpiquot` as the sole member of every array and explicitly checks the packet, story, sprint tracker, and root submodule paths for changes. This avoids silently accepting over-authorization or unrelated mutations. KEEP: preserve the approved `jpiquot` assignment to all four roles, the existing schema/repository identities, and every fail-closed packet predicate.
- 2026-07-17, review loop 2: Review found that the identity command could succeed for a different authenticated login or GitHub host, the JSON command could accept a stream containing more than one root value, and worktree-to-index isolation could miss staged, committed, untracked, or omitted-submodule changes. Verification now pins `github.com`, requires the result to equal `jpiquot`, requires exactly one JSON root, compares every tracked path and submodule against the recorded baseline, and checks the complete untracked-file set. This avoids validating the wrong identity, malformed multi-document input, or hidden unrelated mutations. KEEP: retain loop 1's exact four-role membership assertion and preserve the approved allowlist-only behavior.
- 2026-07-18, post-commit code review (bmad-code-review): The committed range broke the loop-2 isolation contract — commit `ea6ce49b` absorbed seven fast-forward root-submodule bumps and this spec file alongside the allowlist edit (concurrent auto-commit loop), and `ba203bde` (`Directory.Packages.props`) postdated the recorded baseline — so the worktree-scoped commands 3–5 could never pass at HEAD. Owner ratified the committed state and approved restating Verification against the immutable story commit: commands are now root-pinned, exit-code-asserted, prove the roles changed exactly from empty to `["jpiquot"]` with non-role fields unchanged, and pin the exact ratified change-sets of `ea6ce49b` and `ba203bde`. Owner additionally approved hardening both operative proof-packet allowlist validators (candidate gate and evidence-commit-A re-check) to exact four-role membership, closing the loop-1 gap where the exact-membership predicate lived only in this spec; the unrelated local `Microsoft.Extensions.TimeProvider.Testing` 10.7.0 override absorbed into the range was removed in favor of the Builds-central 10.8.0. KEEP: exact single-login membership `["jpiquot"]` in all four roles, every fail-closed packet predicate, and the frozen intent block untouched.
- 2026-07-18, review loop 3 (bmad-code-review): All seven restated Verification commands replayed green and the loop-2 resolutions were verified present, but they were uncommitted while the concurrent loop staged a mixed bundle including mid-edit snapshots of the gate files. Owner approved: landing the resolution set as one dedicated scoped commit; resetting the unratified `references/Hexalith.Memories` checkout to pinned `ce66d4ca`; and hardening both operative packet allowlist validators with a single-document slurp guard (`-s 'length == 1 and (.[0] | …)'`), closing a probed multi-root pass-through. Verification command 4 gained a non-empty guard against a vacuous empty-equals-empty pass and command 6 now uses the full 40-hex SHA. New Contracts.Tests guardrails land in the same commit: three-way entry-point normalized-equality, an anchored-relative-link ban in the Copilot entry point, and a Builds-central version-masking test that immediately caught and removed live `System.CommandLine`/`ModelContextProtocol` divergences and the no-op `NBomber` pin (Playwright remains the sole ledgered exception). KEEP: exact single-login membership `["jpiquot"]` in all four roles, every fail-closed packet predicate, and the frozen intent block untouched.

## Verification

_Restated 2026-07-18 with owner approval after the change was committed as `ea6ce49b8656ef7ee7267ac3cc4949734a31821d` (see Spec Change Log). Commands are root-pinned, exit-code-asserted, and reference immutable commits, so they pass from any working tree that contains the story commit._

**Commands:**
- `test "$(gh api --hostname github.com user --jq .login)" = jpiquot` -- expected: exits zero only for the approved authenticated GitHub login.
- `jq -e -s 'length == 1 and (.[0] | .schema == "hexalith.eventstore.github-approval-role-allowlist/v1" and .repository == "Hexalith/Hexalith.EventStore" and .roles == {"eventstore_owner":["jpiquot"],"release_owner":["jpiquot"],"architecture_owner":["jpiquot"],"story_1_16_reviewer":["jpiquot"]} and all(.roles[]; type == "array" and length > 0 and length == (unique | length) and all(.[]; type == "string" and test("^[A-Za-z0-9](?:[A-Za-z0-9-]{0,38})$"))))' "$(git rev-parse --show-toplevel)/_bmad-output/implementation-artifacts/1-20-github-approval-role-allowlist.json"` -- expected: exits zero (working-tree allowlist holds the exact approved membership).
- `test "$(git -C "$(git rev-parse --show-toplevel)" show ea6ce49b8656ef7ee7267ac3cc4949734a31821d:_bmad-output/implementation-artifacts/1-20-github-approval-role-allowlist.json | jq -cS .roles)" = '{"architecture_owner":["jpiquot"],"eventstore_owner":["jpiquot"],"release_owner":["jpiquot"],"story_1_16_reviewer":["jpiquot"]}'` -- expected: exits zero, proving the story commit records exactly the approved four-role membership.
- `parent_shape="$(git -C "$(git rev-parse --show-toplevel)" show ea6ce49b8656ef7ee7267ac3cc4949734a31821d^:_bmad-output/implementation-artifacts/1-20-github-approval-role-allowlist.json | jq -cS '[del(.roles), all(.roles[]; length == 0)]')" && test -n "$parent_shape" && test "$parent_shape" = "$(git -C "$(git rev-parse --show-toplevel)" show ea6ce49b8656ef7ee7267ac3cc4949734a31821d:_bmad-output/implementation-artifacts/1-20-github-approval-role-allowlist.json | jq -cS '[del(.roles), true]')"` -- expected: exits zero, proving the parent's role arrays were all empty and no non-role field changed; the non-empty guard prevents a vacuous `"" = ""` pass when both extractions fail identically.
- `test "$(git -C "$(git rev-parse --show-toplevel)" -c diff.ignoreSubmodules=none diff-tree --no-commit-id --name-only -r ea6ce49b8656ef7ee7267ac3cc4949734a31821d | sort | paste -sd ' ' -)" = "_bmad-output/implementation-artifacts/1-20-github-approval-role-allowlist.json _bmad-output/implementation-artifacts/spec-1-20-add-github-approval-login.md references/Hexalith.AI.Tools references/Hexalith.Builds references/Hexalith.Commons references/Hexalith.FrontComposer references/Hexalith.Memories references/Hexalith.PolymorphicSerializations references/Hexalith.Tenants"` -- expected: exits zero, pinning the story commit's tracked change-set to exactly the ratified nine paths (allowlist, this spec, and seven fast-forward root-submodule bumps absorbed by the concurrent auto-commit loop).
- `test "$(git -C "$(git rev-parse --show-toplevel)" -c diff.ignoreSubmodules=none diff-tree --no-commit-id --name-only -r ba203bdea0e8e42ff58f24f55ad8d64c3d3b84de | paste -sd ' ' -)" = Directory.Packages.props` -- expected: exits zero, pinning the only other commit inside the recorded baseline range to exactly the out-of-story `Directory.Packages.props` change (ratified as Builds version-alignment work).
- `git -C "$(git rev-parse --show-toplevel)" cat-file -e ea6ce49b8656ef7ee7267ac3cc4949734a31821d:_bmad-output/implementation-artifacts/spec-1-20-add-github-approval-login.md` -- expected: exits zero, confirming this spec is tracked as of the story commit (supersedes the pre-commit untracked-file check).

## Suggested Review Order

**Approved intent**

- Human-owned scope fixes one authenticated login across all four existing roles.
  [`spec-1-20-add-github-approval-login.md:16`](spec-1-20-add-github-approval-login.md#L16)

**Authorization mapping**

- Existing fail-closed roles gain `jpiquot` without changing schema or repository identity.
  [`1-20-github-approval-role-allowlist.json:5`](1-20-github-approval-role-allowlist.json#L5)

**Verification**

- Exact identity, single-document membership, and whole-tree isolation checks prevent drift.
  [`spec-1-20-add-github-approval-login.md:61`](spec-1-20-add-github-approval-login.md#L61)
