---
topic: eventstore Phase 4 implementation readiness recovery
slug: eventstore-phase-4-readiness-recovery
source: _bmad-output/planning-artifacts/implementation-readiness-report-2026-07-05.md
updated: 2026-07-19T15:58
---

- (direction) Use express distillation because the finalized PRD, architecture spine, epics plan, and readiness report are rich structured inputs; no guided elicitation is needed.
- (decision) SPEC slug is eventstore-phase-4-readiness-recovery because the thing being specced is the Phase 4 readiness recovery package, not only the PRD or architecture input shape.
- (decision) Adopt _bmad-output/planning-artifacts/architecture.md as a companion because its ADs and diagrams remain load-bearing downstream and must keep stable AD IDs.
- (decision) Adopt _bmad-output/planning-artifacts/epics.md as a companion because it owns story slicing, sequencing, and acceptance criteria; the SPEC kernel only names capability intent and success.
- (decision) Adopt _bmad-output/project-context.md as a companion because repository build, identity, testing, and workflow rules remain load-bearing for implementation agents.
- (capability) CAP-1 Domain author self-service platform: intent is that domain authors build EventStore-backed domain modules with domain code only while platform libraries supply hosting, query, projection, read-model, cursor, telemetry, health, Aspire, and packaging seams; success is Sample and Tenants adoption proving duplicated platform boilerplate is removed or guarded without changing domain behavior.
- (capability) CAP-2 External integration surfaces: intent is that external API developers get typed generated REST endpoints in dedicated API hosts while interactive UI hosts consume client libraries directly; success is generated controllers delegate to IEventStoreGatewayClient and Sample/Tenants UI hosts contain no generated or hand-written per-message MVC command/query controllers.
- (capability) CAP-3 Release and repository reliability: intent is that maintainers release reproducibly with references-based submodules, deterministic package mode, dedicated live-sidecar coverage, shared security workflows, and manifest-governed package output; success is release validation cannot publish submodule packages and CI separates release-gate tests from live-sidecar tests.
- (capability) CAP-4 Event correctness and recovery: intent is that operators and consumers can trust event identity, idempotency, replay dispatch, append behavior, global position semantics, and crash recovery under duplicates, concurrency, and failures; success is tests prove CloudEvent id stability, duplicate result fidelity, stale pipeline rejection, replay ambiguity handling, stored-but-unpublished recovery, and real DAPR conflict behavior.
- (capability) CAP-5 Security and tenant isolation: intent is that public, internal, domain-service, projection-notification, admin, and generated REST surfaces fail closed and preserve tenant isolation; success is anonymous/cross-tenant admin access fails, production auth rejects insecure modes unless break-glassed, secrets are removed from committed config, and topology tests inspect actual sidecar component paths and ACL posture.
- (capability) CAP-6 Bounded cost and event evolution: intent is that long-lived streams can evolve with bounded snapshot/projection cost, sequence-safe projection updates, event versioning/upcasting, validated identity metadata, and cancellation-aware public seams; success is folded snapshot, projection cost/sequence guard, and event versioning/upcasting specs are approved before implementation and dependent code conforms to them.
- (capability) CAP-7 Operator trust, admin honesty, and future backlog: intent is that operators get explicit delivery semantics, poison/dead-letter handling, attributable admin actions, honest unavailable-operation behavior, hardened deployment posture, meaningful higher-tier evidence, and tracked future capability backlog; success is unavailable admin operations are hidden/disabled or 501, audit records are support-safe, integration tests assert persisted evidence, and backlog artifacts exist for GDPR-1, IAM-1, KIT-1, and generator hardening.
- (capability) CAP-8 Readiness recovery package: intent is that Phase 4 has a coherent planning baseline before full implementation resumes; success is PRD, architecture, UX, story splits, and high-risk NFR traceability are reconciled and implementation readiness is re-run without missing-artifact blockers.
- (constraint) PRD owns FR/NFR truth and readiness traceability; architecture owns component, integration, topology, and decision-record gates; UX owns UI governance and flows; epics.md owns story slicing, sequencing, acceptance criteria, and implementation handoff.
- (constraint) Implementation should not resume as a full Phase 4 package until PRD, architecture artifact, UX artifact, story splits, and high-risk NFR traceability are reconciled and readiness is re-run.
- (constraint) DAPR-backed hexagonal event sourcing is binding: EventStore gateway is the policy edge, DAPR actors own aggregate write serialization, domain services are pure domain adapters, and external adapters call platform seams.
- (constraint) Generated REST controllers live only in dedicated external API hosts and delegate to IEventStoreGatewayClient; interactive UI hosts consume EventStore Client libraries directly and host no generated or hand-written per-message MVC command/query controllers.
- (constraint) AggregateActor owns durable event mutation; domain code returns DomainResult and never writes EventStore state directly.
- (constraint) Read models use IReadModelStore plus ReadModelWritePolicy; cursors use IQueryCursorCodec plus QueryCursorScope and remain opaque, bounded, scoped, and fail safe.
- (constraint) Projection and pub/sub delivery are at-least-once and unordered; notifications are freshness signals, not proof of success; consumers deduplicate by EventStore MessageId.
- (constraint) Runtime topology changes must update AppHost, DAPR component/configuration YAML, app IDs, sidecar options, ACLs, topics, scopes, and topology tests together.
- (constraint) Security fails closed above infrastructure scoping: application-layer credentials and tenant authorization are required before data disclosure; do not trust network location, DAPR ACLs alone, committed secrets, or caller-supplied admin flags.
- (constraint) Release is manifest-governed through tools/release-packages.json; Release/package validation uses package-reference mode by default; submodule packages are not produced by EventStore release jobs.
- (constraint) High-risk verification must assert persisted Redis/state-store/read-model/CloudEvent bodies, topology YAML or sidecar arguments, package outputs, and security denials; API status codes and mock calls are smoke signals only.
- (constraint) Folded snapshots, projection delivery cost, projection sequence guards, event versioning/upcasting, identity metadata validation, cancellation-token public seams, and global-position sharding require approved specs before implementation stories start.
- (constraint) Use Hexalith.EventStore.slnx for restore/build; run unit tests by project; keep package versions centralized; do not recurse submodules; do not modify submodule files without explicit approval.
- (constraint) EventStore envelope identifiers use ULID-safe handling where required; Guid.TryParse is forbidden for messageId, correlationId, aggregateId, and causationId.
- (constraint) UI-facing work must use FrontComposer and Blazor Fluent UI V5, remain support-safe, avoid theme redefinition, and keep detailed UX evidence in ux.md.
- (constraint) AOT/trimming is not a target while reflection conventions remain load-bearing.
- (assumption) No additional audience-facing artifact was requested; SPEC plus companions are the deliverable.
- (note) No product-scope open questions remain in the PRD; remaining gaps are required readiness deliverables and downstream specs, not unanswered product intent.
- (event) Self-validate pass 1 coherence passed: SPEC has eight stable capabilities with intent and success, constraints that bend design, explicit non-goals, concrete success signal, no unresolved open questions, and lean prose without placeholders.
- (event) Self-validate pass 2 preservation passed: PRD product intent, FR1-FR35, NFR1-NFR18, MVP scope/non-goals, readiness gates, architecture ADs, epics ownership, and repository project context landed in SPEC.md or companions; wrapper/process-only content was not carried forward.
- (event) spec finalized
- (direction) Refresh the Phase 4 contract in express mode from the final PRD, the reviewed architecture spine updated 2026-07-15, the 2026-07-15 readiness assessment, and the approved structural-recovery proposal; preserve CAP-1 through CAP-8.
- (decision) Keep architecture.md, epics.md, and project-context.md as adopted companions; the architecture remains separately owned and supplies stable AD-1 through AD-22 without duplication in the kernel.
- (decision) Replace the superseded 2026-07-05 readiness source with the final 2026-07-15 readiness report and absorb the approved 2026-07-15 structural-recovery proposal as the migration authority.
- (capability) CAP-1 Domain author self-service platform now includes route-bound query provenance, deterministic read-model semantics, asynchronous named projections, replay-equivalent rebuilds, and owner-approved exact-artifact parity before consumer infrastructure removal; success requires production-path evidence and matching EventStore source, package, or image identity.
- (capability) CAP-6 Bounded cost and event evolution success now includes the exact normalized ProjectionDispatchResult contract and replay-equivalent paged rebuild baseline before later cost optimizations.
- (capability) CAP-7 Operator trust and admin honesty now includes evolving src/Hexalith.EventStore.Admin.UI in place as the single consolidated Event Store Admin FrontComposer module without creating another UI host.
- (capability) CAP-8 Readiness recovery success now requires the approved replan to remove the Epic 1 forward dependency, replace all eight oversized parents with focused children, migrate active identifiers audibly, and pass a fresh readiness assessment.
- (constraint) AD-19 is binding: preserve the frozen project-v2 wire response and emit one server-owned ProjectionDispatchResult Version 1 with bounded ordinal route entries, stable status codes, and explicit Advanced or NotAdvanced checkpoint state; no equivalent shape is allowed without a new architecture decision.
- (constraint) AD-21 is binding: src/Hexalith.EventStore.Admin.UI remains the only EventStore UI host and eventstore-admin-ui resource, composes matching FrontComposer Shell and Contracts.UI 3.2.2 with Fluent UI V5, owns event-store-admin navigation, and redirects legacy routes to canonical dashboard deep links.
- (constraint) AD-22 is binding: consumer infrastructure removal requires an EventStore-owner-approved parity packet and exact identity evidence for the consumed EventStore source SHA, package versions and hashes, or deployed image digest; a consumer repository SHA is never compared to the EventStore SHA.
- (constraint) The approved story migration preserves the seven-epic order and MVP scope, rehomes platform query provenance into Story 1.2, and makes generated REST and Tenants provenance consumption a later consumer-only slice.
- (constraint) Split children inherit done only through an evidence crosswalk naming implementation, focused tests, review results, and external approval or exact SHA where applicable; missing evidence leaves the child in review rather than done.
- (constraint) Tenants adoption work requires maintainer approval, an approved PR or commit, exact Tenants SHA, accepted repository scope, source and package mode evidence, and an explicit unavailable-approval disposition; EventStore generic platform work may complete independently.
- (decision) No PRD or canonical UX change is required, no implementation rollback is authorized, and no additional EventStore UI host is in scope.
- (event) Self-validate pass 1 coherence passed after refresh: eight stable capabilities retain intent and testable success, constraints encode AD-19, AD-21, AD-22 and migration gates, non-goals remain explicit, and no unsupported product decision was invented.
- (event) Self-validate pass 2 preservation passed after refresh: FR1-FR36, NFR1-NFR18, reviewed architecture AD-1 through AD-22, approved story migration, external authority gates, UI ownership, deterministic acceptance decisions, and readiness outcomes land in SPEC.md or its companions; proposal ceremony and superseded report prose are wrapper-only.
- (event) Post-render validation passed: SPEC contains one each of CAP-1 through CAP-8, all companion paths resolve, no placeholders or superseded FR1-FR35 and 2026-07-05 readiness references remain in rendered files, and the refreshed architecture contract is preserved by adoption plus binding kernel constraints.
- (event) spec refreshed from reviewed architecture and approved structural recovery input
- (event) Correction: rendered CAP-8 uses 'auditably migrated' (superseding the earlier 'audibly' log wording), and the frozen wire path is '/project/v2' (superseding the earlier 'project-v2' log spelling).
- (direction) User directed the canonical Phase 4 spec to adopt architecture AD-24 as a companion invariant for Story 7.6, FR34, NFR4, and NFR17 while preserving all capability identifiers and operational-secret separation.
- (decision) Keep CAP-1 through CAP-8 unchanged and adopt AD-24 through the existing architecture.md companion; the kernel and spec-authored companions bind its consequences while architecture remains the owner of the full invariant.
- (decision) Treat current epics Story 7.6 Secret-Store Configuration as authoritative because the approved July 15 migration reused identifier 7.6 and explicitly superseded the historical query-metadata Story 7.6 artifact.
- (capability) CAP-5 success now requires committed configuration to contain no operational secrets and production evidence to prove fail-closed required-secret handling plus matching default-deny DAPR scopes and OpenBao ACLs.
- (capability) CAP-7 success now requires production operational and application secrets to satisfy AD-24 readiness, runtime-failure, generation-aware rotation, and real-OpenBao evidence gates in addition to existing operator-trust outcomes.
- (constraint) AD-24 binds FR34, NFR4, NFR17, and Story 7.6: production operational and application secrets use DAPR component openbao of type secretstores.hashicorp.vault v1, with logical names and lifecycle fixed by the value-free deploy/dapr/openbao-secret-contract.yaml.
- (constraint) The AD-24 secret contract drives singleton component scopes, per-app DAPR default-deny allowedSecrets, and least-privilege OpenBao ACLs; mismatches fail validation, while OpenBao token, DAPR API token, and TLS trust material remain acyclic out-of-band bootstrap inputs.
- (constraint) AD-24 required secrets gate readiness and runtime lookup failure disables the dependent operation until bounded recovery; rotation uses generation-aware publish-overlap-acknowledge-revoke semantics and must retain old validity or restore a generation when acknowledgement fails.
- (constraint) AD-24 release evidence requires a real OpenBao integration lane, and Azure Container Apps managed DAPR remains non-conforming until a separately approved compatible profile proves OpenBao support and equivalent least-privilege scoping.
- (constraint) AD-24 governs operational and application secret retrieval only; it does not approve, replace, or modify AD-23 or the draft payload-protection Azure Key Vault Premium RSA-HSM KEK proposal because DAPR secret stores are not pdenc-v2 key custody.
- (note) Source reconciliation found compatible FR34, NFR4, and NFR17 requirement text but stale PRD story mappings to 7.3, a historical query-metadata 7.6 artifact still marked ready-for-dev, missing deploy/dapr/openbao-secret-contract.yaml, and deployment guidance that still recommends Kubernetes or Azure Key Vault profiles including managed Azure Container Apps DAPR.
- (note) Wrapper-only content: none; every user-supplied AD-24 binding changes downstream implementation or verification and is preserved in the kernel or spec-authored companions.
- (event) Self-validate pass 1 coherence passed after AD-24 adoption: CAP-1 through CAP-8 remain stable and unique with intent and success, constraints bind design choices, non-goals are explicit, and the success signal preserves Phase 4 and post-MVP gates.
- (event) Self-validate pass 2 preservation passed after AD-24 adoption: provider identity, value-free contract, scopes and ACLs, bootstrap acyclicity, readiness and runtime failure, acknowledged rotation, real-OpenBao evidence, Azure Container Apps non-conformance, and key-custody separation all land in SPEC.md or spec-authored companions.
- (event) Reconciliation verdict: PRD requirement text and the AD-23 draft-backend boundary are compatible; stale PRD story mappings, under-specified current Story 7.6 acceptance, the superseded query-metadata 7.6 status, missing OpenBao contract, and conflicting deployment profiles remain upstream gaps to report.
- (event) Post-render validation passed: exactly eight stable capabilities have intent and success, all companion paths resolve, no placeholders or whitespace errors remain, and AD-24 is traceable to FR34, NFR4, NFR17, and current Story 7.6.
- (event) spec refreshed with adopted AD-24 OpenBao operational-secret invariant
