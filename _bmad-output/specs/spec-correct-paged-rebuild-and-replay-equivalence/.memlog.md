---
topic: Correct paged rebuild and replay equivalence
updated: 2026-09-09T15:53
---

- (direction) Express distillation selected for Story 1.14 from the implementation story, canonical epic criteria, PRD requirements FR7/FR33/FR36 and NFR7/NFR8/NFR16, architecture AD-20 and its governing invariants, and the current brownfield surfaces.
- (capability) CAP-1 — intent: Rebuild participants expose explicit full-replay or incremental semantics; success: no full-replay handler ever receives a lone page as a complete stream, and incremental handlers receive prior staged state plus one contiguous page.
- (capability) CAP-2 — intent: Paged rebuild work remains operation-scoped and non-live until every required projection is durably complete; success: incomplete, failed, or canceled work never replaces the last complete live actor/detail/index state.
- (capability) CAP-3 — intent: Paged and bounded rebuilds are position-equivalent to canonical aggregate replay; success: no event is duplicated, skipped, or reordered and the promoted result equals canonical replay through the same boundary.
- (capability) CAP-4 — intent: Operators can cancel, fail, and resume rebuilds without corrupting live state or overstating progress; success: retry resumes from a durable safe boundary and page-read progress is never reported as projection completion.
- (capability) CAP-5 — intent: Required projections promote as one logical operation while retaining distinguishable per-projection outcomes; success: the operation cannot succeed while any required projection is incomplete and rebuild checkpoints advance only after durable promotion.
- (capability) CAP-6 — intent: Projection lifecycle truthfully exposes rebuild progress; success: authoritative ProjectionBacked evidence is Rebuilding in flight and becomes Current or Stale only after durable promotion, with no terminal stale Rebuilding state.
- (capability) CAP-7 — intent: Replay-equivalence is demonstrated through persisted production-path evidence with bounded temporary full-prefix cost; success: a stream larger than two pages and the mandated edge corpus prove semantic equality of actor/detail/index outputs, freshness versions, and rebuild checkpoints, while limit exhaustion fails safely.
- (decision) Decision D1: the equivalence target is every live projection surface owned by the rebuild operation: projection-actor state, required detail/index read models, persisted freshness projection versions, and rebuild checkpoints; ETags are excluded as version evidence.
- (decision) Decision D2: under the current stateless Domain-keyed handler contract, page reads accumulate the complete required prefix to the bounded target, projection runs against that complete prefix, output remains operation-scoped and non-live, and promotion occurs only after all required outputs are complete.
- (decision) Decision D3: only IProjectionRebuildCheckpointStore advances after proven durable promotion; IProjectionCheckpointTracker delivery checkpoints remain unchanged for Story 1.13.
- (constraint) The existing immediate/poller full-replay path and admin rebuild control flow remain behaviorally compatible; this contract corrects paged rebuild semantics only.
- (constraint) Promotion must reuse the Story 1.10 same-store coordinated batch or marker-gated resumable protocol; no bespoke batch marker, cross-store atomicity claim, or best-effort model-then-checkpoint ordering is permitted.
- (constraint) Story 1.14 does not introduce Story 1.12 named asynchronous dispatch or Story 1.13 MessageId delivery deduplication; released interfaces remain source compatible and new capability is additive.
- (constraint) ProjectionOptions exposes a validated positive rebuild page size defaulting to 256; exact-boundary completion must remain correct for the configured value.
- (constraint) Full-prefix accumulation has a configured safety ceiling and structured fail-closed outcome; reaching the ceiling preserves live state, does not advance completion, and remains optimizable by Stories 6.3/6.4 without weakening equivalence.
- (constraint) Persisted freshness through IReadModelFreshness is the only projection-version authority; lifecycle is never inferred from ETag, HTTP outcome, payload fields, or SignalR.
- (constraint) Aggregate sequence is gapless per aggregate, bounded toPosition is inclusive through its canonical replay boundary, SequenceNumber is never global ordering, and the shared platform JSON options remain authoritative.
- (constraint) Required deterministic evidence runs through the real orchestrator to /project to persistence path; mock counts and isolated AggregateReplayer tests are insufficient, and live DAPR/Redis evidence is recorded when reachable with an exact blocker otherwise.
- (question) Sequencing gate: must Story 1.10 coordinated batching and Story 1.12 named multi-projection dispatch land before 1.14, or is an additive 1.14-local compatibility adapter explicitly authorized to expose the required detail/index projection set without implementing those stories?
- (question) Safety-bound gate: what maximum complete-prefix event count and/or serialized-byte ceiling, default value, configurability scope, and structured failure reason code are approved for the temporary full-sequence strategy?
- (assumption) Internal staging key names and private helper type names are implementation details provided they are operation-scoped, collision-safe, support-safe, and preserve released ABI.
- (note) Wrapper-only content dropped from the contract: story workflow ceremony, agent-record placeholders, narrative history, and command examples that do not alter behavior.
- (event) Self-validation pass 1 coherence: PASS — seven unique stable capabilities each contain intent and testable success; constraints bend implementation; non-goals and concrete success signal are present; assumptions and unresolved choices are explicit; prose is lean.
- (event) Self-validation pass 2 preservation: PASS — all load-bearing Story 1.14, AD-20, epic, PRD, brownfield, edge-corpus, sequencing, compatibility, lifecycle, safety, and persisted-evidence claims are represented in SPEC.md or rebuild-semantics.md; wrapper-only ceremony was intentionally dropped and logged.
- (event) Spec derivation finalized with two open questions that must be resolved before implementation can consume the contract without ambiguity.
- (direction) Administrator approved the temporary full-prefix safety freeze on 2026-07-15: 10,000 events, 64 MiB serialized prefix, both configurable and positive, with reason code rebuild_prefix_safety_limit_exceeded.
- (decision) Decision D4: ProjectionOptions configures both RebuildMaxPrefixEventCount defaulting to 10,000 and RebuildMaxPrefixBytes defaulting to 67,108,864; validation rejects either value when non-positive.
- (decision) Decision D5: exceeding either complete-prefix safety bound fails closed with reason code rebuild_prefix_safety_limit_exceeded, preserves every live surface, advances no completion checkpoint, and clears terminal Rebuilding lifecycle state.
- (decision) Decision D6: the sequencing gate is satisfied because Stories 1.10 and 1.12 are complete; Story 1.14 consumes their coordinated-batch and named-dispatch seams and does not create a local substitute.
- (event) Story 1.14 open questions resolved: the prerequisite sequencing and complete-prefix safety contract are now frozen for downstream implementation.
- (event) Spec update self-validation pass 1 coherence: PASS — seven stable capabilities retain intent and success, exact positive safety defaults and failure behavior are constraints, non-goals and concrete success remain intact, and no open questions remain.
- (event) Spec update self-validation pass 2 preservation: PASS — the approved 10,000-event and 64-MiB bounds, server-wide configurability, reason code, fail-closed effects, and satisfied Story 1.10/1.12 sequencing are present in SPEC.md and rebuild-semantics.md.
- (event) Story 1.14 spec re-derived and finalized with zero assumptions requiring resolution and zero open questions.
- (direction) Re-derive the existing paged-rebuild SPEC against the finalized 2026-09-09 architecture while preserving SPEC identity and CAP-1 through CAP-7.
- (decision) Adopt ../../planning-artifacts/architecture.md as the governing companion; AD-20 remains the replay-equivalence authority and amended/new architecture decisions constrain the production boundary without renumbering or reopening the capability IDs.
- (capability) CAP-7 keeps its ID and now distinguishes replay-correctness evidence from production authority: Redis proves Development/test semantics only, while any production claim requires the complete AD-26 profile and its retained evidence.
- (constraint) AD-19 ratifies ProjectionDispatchResponse v2 and ProjectionDispatchOutcome as the cross-service carrier; rebuild orchestration consumes the server-owned persisted route/checkpoint matrix and does not introduce a separate public ProjectionDispatchResult family.
- (constraint) AD-26 requires any production rebuild proof to run through the content-bound PostgreSQL v1, durable-broker, OpenBao, resiliency, and independent-sidecar profile; existing DAPR/Redis evidence remains valid only for Development/test replay correctness.
- (constraint) AD-27 canonicalizes exactly one explicit tenant before rebuild route resolution, lifecycle fencing, staging, checkpoint, read-model, or state access; missing, conflicting, invalid, inferred, or public system tenant scope fails closed.
- (constraint) AD-28 authenticates non-Development /project and /project/v2 app-channel requests through shared dapr-api-token validation against startup APP_API_TOKEN; caller app ID, mTLS, or ACL success cannot substitute.
- (constraint) AD-29 applies when Admin starts, cancels, resumes, or promotes a rebuild: human and service attribution survives one resumable mutation/audit unit and audit failure cannot silently permit the action.
- (constraint) AD-30 remains distinct from rebuild: Story 1.14/AD-7 projection cleanup and paged replay neither authorize nor prove full erasure, and an Erased outcome may advance only under its separately authorized workflow.
- (constraint) AD-32 requires the first-boundary X-Correlation-ID to propagate unchanged through rebuild orchestration and downstream projection calls; it is never parsed as a GUID or used as rebuild/status identity.
- (constraint) AD-33 resolves every required projection route through one activated catalog generation shared with AD-25; staging and promotion bind the stable route-entry IDs and root/facet digests, and non-Development runtime overrides fail readiness.
- (note) The focused SPEC does not absorb Phase 4 dead-letter implementation ownership; AD-8/AD-31 capture-before-ack remains governing for any subscriber delivery that invokes projection handling and is covered by the Phase 4 readiness SPEC.
- (note) Wrapper-only content dropped: invocation syntax and architecture prose unrelated to paged rebuild behavior; every architecture claim that changes rebuild implementation or evidence is represented in SPEC.md or rebuild-semantics.md.
- (event) Self-validation pass 1 coherence: PASS — seven stable unique capabilities retain intent and testable success; constraints distinguish correctness, production, tenant, channel, attribution, erasure, correlation, and catalog authority; non-goals remain explicit.
- (event) Self-validation pass 2 preservation: PASS — the governing AD-20 claim and every amended/new architecture decision that bends the rebuild path land in SPEC.md or rebuild-semantics.md; unrelated Phase 4 obligations remain discoverable through the adopted architecture companion.
- (event) Paged-rebuild SPEC update finalized with zero new capability IDs and no open questions; production authority remains external to this correctness specification.
- (decision) Adopt the active Story 1.19 reissue as a companion while retaining the historical Story 1.14 source path; Story 1.19 is the current rebuild identity and Story 1.14 now names the distinct AD-7 projection-erasure story.
- (event) Post-render correction: current Story 1.19 identity and historical Story 1.14 evidence are both traceable without renumbering this SPEC or conflating rebuild with AD-7/AD-30 erasure.
- (decision) Current migration identities supersede historical prerequisite labels in the render: Story 1.15 owns coordinated batch writes, Story 1.17 named dispatch, Story 1.18 delivery idempotency/checkpoints, and Story 1.19 rebuild; historical files and evidence remain traceable without changing their bytes.
- (validation) Post-render deterministic validation: PASS — CAP/intention/success cardinality is 7/7/7 with no duplicate capability mentions; all declared paths resolve; no placeholders, trailing whitespace, or malformed final newlines were found.
- (direction) Re-derive the paged-rebuild contract against the current post-condensation architecture corrections while preserving SPEC identity and CAP-1 through CAP-7.
- (decision) Bind the focused derivation to architecture SHA-256 7e3dbc7bd335034bd9b98cadfed8b14650b7d811321b326b8f32e1b280960d51 and architecture memlog SHA-256 7fc7acfbc3f5ae838a9922eab9553f15a95a4f3b788ef044f82c1e09119a98df; the spine governs as draft and AD-26 remains an assumption rather than production authority.
- (capability) CAP-7 keeps its ID and now requires both owner ratification and complete retained evidence for the AD-26 profile before replay-correctness evidence can be labeled production-authorizing; Redis remains Development and test evidence only.
- (constraint) Restored AD-18 requires the sidecar-routed rebuild client to append AddEventStoreDaprServiceInvocation last so the platform handler is innermost and replaces dapr-app-id and dapr-api-token; current fail-open omission blocks non-Development readiness.
- (constraint) AD-26 is a provisional production-profile assumption, not an adopted or delivered capability; Story 1.19 can prove configured-path replay correctness but cannot select the production actor store or confer promotion, traffic, migration, or readiness authority.
- (note) Restored AD-1, AD-11, and AD-25 release, package, and OQ8 evidence clauses do not change paged rebuild behavior; they remain discoverable through the adopted governing architecture companion.
- (event) Self-validation pass 1 coherence: PASS with seven stable capability IDs, explicit draft and assumption posture, an AD-18 client-composition constraint, retained non-goals, and a testable correctness signal.
- (event) Self-validation pass 2 preservation: PASS for every post-condensation architecture correction that changes rebuild implementation or evidence; unrelated restored release and OQ8 clauses remain preserved by the governing companion.
- (event) Post-render deterministic validation: PASS with CAP intent success cardinality 7/7/7, preserved SPEC and CAP identities, resolved companion paths, current governing digests, AD-18 outbound composition, provisional AD-26 production authority, and clean LF, final newline, placeholder, and whitespace checks.
- (event) Paged-rebuild spec update finalized with no new capability IDs or focused open questions; productionelf authority remains outside Story 1.19 until AD-26 is ratified and proven.
- (note) Correction: the immediately preceding finalization event contains the typographical token productionelf; read it as production authority. No contract content changes.
- (event) No-contract-delta revalidation on 2026-09-09: the focused rebuild SPEC already preserves SPEC-correct-paged-rebuild-and-replay-equivalence and CAP-1 through CAP-7 while applying AD-18 through AD-20 and AD-26 through AD-30 plus AD-32 through AD-33 where they bend rebuild behavior or production evidence.
- (event) Self-validation pass 1 coherence: PASS — seven stable capabilities retain intent and success, constraints and non-goals are explicit, and no focused open question is introduced.
- (event) Self-validation pass 2 preservation: PASS — replay equivalence, explicit production gating, tenant canonicalization, DAPR app-channel authentication, Admin attribution, erasure separation, correlation propagation, and unified projection routing remain preserved; AD-31 ownership remains in the Phase 4 readiness SPEC.
