# R4-A5 Tier 3 Pub/Sub Delivery Evidence

Date: 2026-05-01
Story: post-epic-4-r4a5-tier3-pubsub-delivery
HEAD at start: 73d5a1d7b193f2a9c35a76f0c76b9601e73b4b96

## Topology

- AppHost command: `EnableKeycloak=false aspire run --detach --non-interactive --apphost src/Hexalith.EventStore.AppHost/Hexalith.EventStore.AppHost.csproj --format Json`
- Baseline Aspire resources observed healthy before implementation: `eventstore`, `sample`, `eventstore-dapr-cli`, `sample-dapr-cli`, `statestore`, `pubsub`.
- Tier 3 proof fixture: `AspirePubSubProofTestFixture`
- Test subscriber app-id: `eventstore-test-subscriber`
- Proof topic: `tenant-a.counter.events`
- Subscriber authorization: `eventstore-test-subscriber` is present in `pubsub.yaml` component `scopes`, has `subscriptionScopes=tenant-a.counter.events`, and has explicit empty publish grant in `publishingScopes`.
- `sample` remains denied: `sample=` stays present in both publishing and subscription scopes and `sample` is not in component `scopes`.

## Proofs

- `PubSubDelivery_CommandPublished_SubscriberReceivesCloudEvent`
  - Submits `IncrementCounter` for `tenant-a/counter`.
  - Polls command status to `Completed`.
  - Asserts subscriber receipt on `tenant-a.counter.events`.
  - Asserts CloudEvents `type`, `source`, `id`, topic, tenant, domain, aggregate id, correlation id, and sequence number.

- `PubSubDrain_PublishFailsThenRecovers_DrainsPersistedEvents`
  - Uses disabled-by-default file trigger `EventStore:Publisher:TestPublishFaultFilePath`.
  - Fault happens inside `EventPublisher.PublishEventsAsync`, after actor persistence has already completed.
  - Polls command status to `PublishFailed`.
  - Reads exact actor drain key only: `eventstore||AggregateActor||tenant-a:counter:{aggregateId}||drain:{correlationId}`.
  - Asserts `UnpublishedEventsRecord` correlation id, sequence range `1..1`, event count, command type, retry count, and failure reason.
  - Deletes the fault file, waits for drain reminder, then asserts status `Completed`, subscriber receipt for sequence `1`, and drain record removal.

## Validation

- `dotnet build Hexalith.EventStore.slnx --configuration Release --no-restore`: passed, 0 warnings, 0 errors.
- `dotnet test tests\Hexalith.EventStore.IntegrationTests --configuration Release --filter FullyQualifiedName~PubSubDeliveryProofTests --no-restore`: passed, 2/2.
- `dotnet test tests\Hexalith.EventStore.Server.Tests --configuration Release --no-restore`: passed, 1648/1648.
- `dotnet test tests\Hexalith.EventStore.Contracts.Tests --configuration Release --no-restore`: passed, 281/281.
- `dotnet test tests\Hexalith.EventStore.Client.Tests --configuration Release --no-restore`: passed, 334/334.
- `dotnet test tests\Hexalith.EventStore.Sample.Tests --configuration Release --no-restore`: passed, 63/63.
- `dotnet test tests\Hexalith.EventStore.Testing.Tests --configuration Release --no-restore`: passed, 78/78.

## Docker-equipped Re-run (post-hardening)

Date: 2026-05-01T11:18Z
HEAD at re-run: `f1af839381d493854e6eec24bc680046a274973c`
Branch: `codex/checkpoint-and-pubsub-hardening`
Topology: Docker Desktop 29.4.0 + DAPR self-hosted (placement, scheduler, Redis, zipkin all `Up 8 days`).

Closes the verification gap recorded by the code-review session on the same date, where Tier 3 PubSubDeliveryProofTests could not be re-run because Docker was absent on the dev host. The re-run was executed on the Docker-equipped runner against the post-hardening tree (commits c22e2e6 → 778aa88: checkpoint + pubsub hardening, including D1/D3/D4 patches, env-var snapshot/restore, StackExchange.Redis 2.11.0, X-Test-Auth shared secret, bounded subscriber queue, correlation-scoped DELETE).

- `dotnet build Hexalith.EventStore.slnx --configuration Release`: passed, 0 warnings, 0 errors.
- `dotnet test tests\Hexalith.EventStore.IntegrationTests --configuration Release --filter FullyQualifiedName~PubSubDeliveryProofTests --no-build --no-restore`: **passed, 2/2 in 22.5s**.
  - `PubSubDelivery_CommandPublished_SubscriberReceivesCloudEvent`: passed in 4s.
  - `PubSubDrain_PublishFailsThenRecovers_DrainsPersistedEvents`: passed in 1s.

No environment-limited outcome, no product behavior failure, no fixes required. R4-A5 verification gap closed.
