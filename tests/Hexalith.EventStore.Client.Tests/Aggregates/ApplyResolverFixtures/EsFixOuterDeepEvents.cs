// Story 4.3 resolver fixtures — deeper half of the depth pair. See EsFixDeepEvents.cs.
namespace EsFixOuter.EsFixDeep;

internal sealed class Foo {
    public string Name { get; init; } = string.Empty;
}
