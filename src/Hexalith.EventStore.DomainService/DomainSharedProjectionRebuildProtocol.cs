namespace Hexalith.EventStore.DomainService;

/// <summary>Version marker for the additive shared-projection rebuild session contract.</summary>
public static class DomainSharedProjectionRebuildProtocol {
    /// <summary>The current shared rebuild session protocol version.</summary>
    public const int Version = 1;
}
