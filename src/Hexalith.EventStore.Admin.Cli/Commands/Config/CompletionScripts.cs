using System.Text;

namespace Hexalith.EventStore.Admin.Cli.Commands.Config;

/// <summary>
/// Generates shell completion scripts for various shells.
/// </summary>
public static class CompletionScripts
{
    private const string Subcommands = "health stream projection tenant snapshot backup config";
    private const string GlobalOptions = "--url --token --format --output --profile";
    private const string FormatValues = "json csv table";
    private const string ConfigSubcommands = "profile use current completion";
    private const string ProfileSubcommands = "add list show remove";

    /// <summary>
    /// Generates a bash completion script.
    /// </summary>
    public static string GenerateBash()
    {
        StringBuilder sb = new();
        sb.AppendLine($"# Generated by eventstore-admin config completion bash on {DateTime.UtcNow:yyyy-MM-dd}");
        sb.AppendLine("# Add to ~/.bashrc: eval \"$(eventstore-admin config completion bash)\"");
        sb.AppendLine();
        sb.AppendLine("_eventstore_admin_completions() {");
        sb.AppendLine("    local cur prev words cword");
        sb.AppendLine("    _init_completion || return");
        sb.AppendLine();
        sb.AppendLine("    # Dynamic profile names");
        sb.AppendLine("    local profiles=\"\"");
        sb.AppendLine("    local profile_file=\"${HOME}/.eventstore/profiles.json\"");
        sb.AppendLine("    if [ -f \"${profile_file}\" ]; then");
        sb.AppendLine("        profiles=$(grep -o '\"[a-zA-Z0-9_-]*\"\\s*:' \"${profile_file}\" | sed 's/[\":]//g; s/^\\s*//; s/\\s*$//' | grep -v '^version$\\|^activeProfile$\\|^profiles$\\|^url$\\|^token$\\|^format$')");
        sb.AppendLine("    fi");
        sb.AppendLine();
        sb.AppendLine("    case \"${prev}\" in");
        sb.AppendLine("        --format|-f)");
        sb.AppendLine($"            COMPREPLY=( $(compgen -W \"{FormatValues}\" -- \"${{cur}}\") )");
        sb.AppendLine("            return;;");
        sb.AppendLine("        --profile|-p)");
        sb.AppendLine("            COMPREPLY=( $(compgen -W \"${profiles}\" -- \"${cur}\") )");
        sb.AppendLine("            return;;");
        sb.AppendLine("        --output|-o|--url|-u|--token|-t)");
        sb.AppendLine("            return;;");
        sb.AppendLine("    esac");
        sb.AppendLine();
        sb.AppendLine("    local subcmd=\"\"");
        sb.AppendLine("    for ((i=1; i < cword; i++)); do");
        sb.AppendLine("        case \"${words[i]}\" in");
        sb.AppendLine("            -*);;");
        sb.AppendLine("            *) subcmd=\"${subcmd:+${subcmd} }${words[i]}\";;");
        sb.AppendLine("        esac");
        sb.AppendLine("    done");
        sb.AppendLine();
        sb.AppendLine("    case \"${subcmd}\" in");
        sb.AppendLine("        \"\")");
        sb.AppendLine($"            COMPREPLY=( $(compgen -W \"{Subcommands} {GlobalOptions}\" -- \"${{cur}}\") );;");
        sb.AppendLine("        config)");
        sb.AppendLine($"            COMPREPLY=( $(compgen -W \"{ConfigSubcommands}\" -- \"${{cur}}\") );;");
        sb.AppendLine("        \"config profile\")");
        sb.AppendLine($"            COMPREPLY=( $(compgen -W \"{ProfileSubcommands}\" -- \"${{cur}}\") );;");
        sb.AppendLine("        \"config use\")");
        sb.AppendLine("            COMPREPLY=( $(compgen -W \"${profiles} --clear\" -- \"${cur}\") );;");
        sb.AppendLine("        \"config completion\")");
        sb.AppendLine("            COMPREPLY=( $(compgen -W \"bash zsh powershell fish\" -- \"${cur}\") );;");
        sb.AppendLine("        health)");
        sb.AppendLine("            COMPREPLY=( $(compgen -W \"dapr --wait --interval --strict\" -- \"${cur}\") );;");
        sb.AppendLine("        stream)");
        sb.AppendLine("            COMPREPLY=( $(compgen -W \"list events event state diff causation\" -- \"${cur}\") );;");
        sb.AppendLine("        projection)");
        sb.AppendLine("            COMPREPLY=( $(compgen -W \"list status pause resume reset\" -- \"${cur}\") );;");
        sb.AppendLine("        tenant)");
        sb.AppendLine("            COMPREPLY=( $(compgen -W \"list detail quotas users compare verify\" -- \"${cur}\") );;");
        sb.AppendLine("        snapshot)");
        sb.AppendLine("            COMPREPLY=( $(compgen -W \"create policies set-policy delete-policy\" -- \"${cur}\") );;");
        sb.AppendLine("        backup)");
        sb.AppendLine("            COMPREPLY=( $(compgen -W \"list trigger restore validate export-stream import-stream\" -- \"${cur}\") );;");
        sb.AppendLine("    esac");
        sb.AppendLine("}");
        sb.AppendLine();
        sb.AppendLine("complete -F _eventstore_admin_completions eventstore-admin");
        return sb.ToString();
    }

    /// <summary>
    /// Generates a zsh completion script.
    /// </summary>
    public static string GenerateZsh()
    {
        StringBuilder sb = new();
        sb.AppendLine($"# Generated by eventstore-admin config completion zsh on {DateTime.UtcNow:yyyy-MM-dd}");
        sb.AppendLine("# Add to ~/.zshrc: eval \"$(eventstore-admin config completion zsh)\"");
        sb.AppendLine();
        sb.AppendLine("_eventstore_admin() {");
        sb.AppendLine("    local -a subcommands profiles");
        sb.AppendLine();
        sb.AppendLine("    # Dynamic profile names");
        sb.AppendLine("    _eventstore_profiles() {");
        sb.AppendLine("        local profile_file=\"${HOME}/.eventstore/profiles.json\"");
        sb.AppendLine("        if [ -f \"${profile_file}\" ]; then");
        sb.AppendLine("            profiles=($(grep -o '\"[a-zA-Z0-9_-]*\"\\s*:' \"${profile_file}\" | sed 's/[\":]//g; s/^\\s*//; s/\\s*$//' | grep -v '^version$\\|^activeProfile$\\|^profiles$\\|^url$\\|^token$\\|^format$'))");
        sb.AppendLine("        fi");
        sb.AppendLine("    }");
        sb.AppendLine();
        sb.AppendLine("    _arguments -s \\");
        sb.AppendLine("        '--url[Admin API base URL]:url:' \\");
        sb.AppendLine("        '--token[JWT Bearer token or API key]:token:' \\");
        sb.AppendLine("        '--format[Output format]:format:(json csv table)' \\");
        sb.AppendLine("        '--output[Redirect output to file]:file:_files' \\");
        sb.AppendLine("        '--profile[Connection profile name]:profile:{_eventstore_profiles; compadd -a profiles}' \\");
        sb.AppendLine("        '1:command:(health stream projection tenant snapshot backup config)' \\");
        sb.AppendLine("        '*::arg:->args'");
        sb.AppendLine();
        sb.AppendLine("    case $state in");
        sb.AppendLine("        args)");
        sb.AppendLine("            case ${words[1]} in");
        sb.AppendLine("                config) compadd profile use current completion;;");
        sb.AppendLine("                health) compadd dapr;;");
        sb.AppendLine("                stream) compadd list events event state diff causation;;");
        sb.AppendLine("                projection) compadd list status pause resume reset;;");
        sb.AppendLine("                tenant) compadd list detail quotas users compare verify;;");
        sb.AppendLine("                snapshot) compadd create policies set-policy delete-policy;;");
        sb.AppendLine("                backup) compadd list trigger restore validate export-stream import-stream;;");
        sb.AppendLine("            esac");
        sb.AppendLine("            ;;");
        sb.AppendLine("    esac");
        sb.AppendLine("}");
        sb.AppendLine();
        sb.AppendLine("compdef _eventstore_admin eventstore-admin");
        return sb.ToString();
    }

    /// <summary>
    /// Generates a PowerShell completion script.
    /// </summary>
    public static string GeneratePowerShell()
    {
        StringBuilder sb = new();
        sb.AppendLine($"# Generated by eventstore-admin config completion powershell on {DateTime.UtcNow:yyyy-MM-dd}");
        sb.AppendLine("# Add to $PROFILE: Invoke-Expression (eventstore-admin config completion powershell | Out-String)");
        sb.AppendLine();
        sb.AppendLine("Register-ArgumentCompleter -CommandName eventstore-admin -ScriptBlock {");
        sb.AppendLine("    param($wordToComplete, $commandAst, $cursorPosition)");
        sb.AppendLine();
        sb.AppendLine("    $commands = $commandAst.CommandElements | ForEach-Object { $_.ToString() }");
        sb.AppendLine("    $subcommands = $commands | Select-Object -Skip 1 | Where-Object { $_ -notmatch '^-' }");
        sb.AppendLine();
        sb.AppendLine("    # Dynamic profile names");
        sb.AppendLine("    function Get-Profiles {");
        sb.AppendLine("        $profileFile = Join-Path $env:USERPROFILE '.eventstore' 'profiles.json'");
        sb.AppendLine("        if (Test-Path $profileFile) {");
        sb.AppendLine("            $json = Get-Content $profileFile | ConvertFrom-Json");
        sb.AppendLine("            if ($json.profiles) {");
        sb.AppendLine("                $json.profiles | Get-Member -MemberType NoteProperty | Select-Object -ExpandProperty Name");
        sb.AppendLine("            }");
        sb.AppendLine("        }");
        sb.AppendLine("    }");
        sb.AppendLine();
        sb.AppendLine("    # Check if previous word is an option that expects a value");
        sb.AppendLine("    $prevWord = if ($commands.Count -gt 1) { $commands[-2] } else { '' }");
        sb.AppendLine("    switch ($prevWord) {");
        sb.AppendLine("        { $_ -in '--format', '-f' } {");
        sb.AppendLine("            'json', 'csv', 'table' | Where-Object { $_ -like \"$wordToComplete*\" } |");
        sb.AppendLine("                ForEach-Object { [System.Management.Automation.CompletionResult]::new($_, $_, 'ParameterValue', $_) }");
        sb.AppendLine("            return");
        sb.AppendLine("        }");
        sb.AppendLine("        { $_ -in '--profile', '-p' } {");
        sb.AppendLine("            Get-Profiles | Where-Object { $_ -like \"$wordToComplete*\" } |");
        sb.AppendLine("                ForEach-Object { [System.Management.Automation.CompletionResult]::new($_, $_, 'ParameterValue', $_) }");
        sb.AppendLine("            return");
        sb.AppendLine("        }");
        sb.AppendLine("    }");
        sb.AppendLine();
        sb.AppendLine("    $completions = switch ($subcommands -join ' ') {");
        sb.AppendLine($"        '' {{ '{Subcommands}'.Split() }}");
        sb.AppendLine($"        'config' {{ '{ConfigSubcommands}'.Split() }}");
        sb.AppendLine($"        'config profile' {{ '{ProfileSubcommands}'.Split() }}");
        sb.AppendLine("        'config use' { @((Get-Profiles) + '--clear') }");
        sb.AppendLine("        'config completion' { 'bash', 'zsh', 'powershell', 'fish' }");
        sb.AppendLine("        'health' { 'dapr' }");
        sb.AppendLine("        'stream' { 'list', 'events', 'event', 'state', 'diff', 'causation' }");
        sb.AppendLine("        'projection' { 'list', 'status', 'pause', 'resume', 'reset' }");
        sb.AppendLine("        'tenant' { 'list', 'detail', 'quotas', 'users', 'compare', 'verify' }");
        sb.AppendLine("        'snapshot' { 'create', 'policies', 'set-policy', 'delete-policy' }");
        sb.AppendLine("        'backup' { 'list', 'trigger', 'restore', 'validate', 'export-stream', 'import-stream' }");
        sb.AppendLine("    }");
        sb.AppendLine();
        sb.AppendLine("    $completions | Where-Object { $_ -like \"$wordToComplete*\" } |");
        sb.AppendLine("        ForEach-Object { [System.Management.Automation.CompletionResult]::new($_, $_, 'ParameterValue', $_) }");
        sb.AppendLine("}");
        return sb.ToString();
    }

    /// <summary>
    /// Generates a fish completion script.
    /// </summary>
    public static string GenerateFish()
    {
        StringBuilder sb = new();
        sb.AppendLine($"# Generated by eventstore-admin config completion fish on {DateTime.UtcNow:yyyy-MM-dd}");
        sb.AppendLine("# Add to ~/.config/fish/completions/eventstore-admin.fish or eval:");
        sb.AppendLine("# eventstore-admin config completion fish | source");
        sb.AppendLine();
        sb.AppendLine("# Dynamic profile names");
        sb.AppendLine("function __eventstore_profiles");
        sb.AppendLine("    set -l profile_file \"$HOME/.eventstore/profiles.json\"");
        sb.AppendLine("    if test -f \"$profile_file\"");
        sb.AppendLine("        grep -o '\"[a-zA-Z0-9_-]*\"\\s*:' \"$profile_file\" | sed 's/[\":]//g; s/^\\s*//; s/\\s*//' | grep -v '^version$\\|^activeProfile$\\|^profiles$\\|^url$\\|^token$\\|^format$'");
        sb.AppendLine("    end");
        sb.AppendLine("end");
        sb.AppendLine();
        sb.AppendLine("# Global options");
        sb.AppendLine("complete -c eventstore-admin -l url -s u -d 'Admin API base URL'");
        sb.AppendLine("complete -c eventstore-admin -l token -s t -d 'JWT Bearer token or API key'");
        sb.AppendLine("complete -c eventstore-admin -l format -s f -d 'Output format' -xa 'json csv table'");
        sb.AppendLine("complete -c eventstore-admin -l output -s o -d 'Redirect output to file' -rF");
        sb.AppendLine("complete -c eventstore-admin -l profile -s p -d 'Connection profile name' -xa '(__eventstore_profiles)'");
        sb.AppendLine();
        sb.AppendLine("# Top-level subcommands");
        sb.AppendLine("complete -c eventstore-admin -n '__fish_use_subcommand' -a health -d 'Check server health'");
        sb.AppendLine("complete -c eventstore-admin -n '__fish_use_subcommand' -a stream -d 'Manage event streams'");
        sb.AppendLine("complete -c eventstore-admin -n '__fish_use_subcommand' -a projection -d 'Manage projections'");
        sb.AppendLine("complete -c eventstore-admin -n '__fish_use_subcommand' -a tenant -d 'Manage tenants'");
        sb.AppendLine("complete -c eventstore-admin -n '__fish_use_subcommand' -a snapshot -d 'Manage snapshots'");
        sb.AppendLine("complete -c eventstore-admin -n '__fish_use_subcommand' -a backup -d 'Manage backups'");
        sb.AppendLine("complete -c eventstore-admin -n '__fish_use_subcommand' -a config -d 'Manage connection profiles and CLI configuration'");
        sb.AppendLine();
        sb.AppendLine("# Config subcommands");
        sb.AppendLine("complete -c eventstore-admin -n '__fish_seen_subcommand_from config; and not __fish_seen_subcommand_from profile use current completion' -a profile -d 'Manage profiles'");
        sb.AppendLine("complete -c eventstore-admin -n '__fish_seen_subcommand_from config; and not __fish_seen_subcommand_from profile use current completion' -a use -d 'Set active profile'");
        sb.AppendLine("complete -c eventstore-admin -n '__fish_seen_subcommand_from config; and not __fish_seen_subcommand_from profile use current completion' -a current -d 'Show resolved settings'");
        sb.AppendLine("complete -c eventstore-admin -n '__fish_seen_subcommand_from config; and not __fish_seen_subcommand_from profile use current completion' -a completion -d 'Generate completion scripts'");
        sb.AppendLine();
        sb.AppendLine("# Profile subcommands");
        sb.AppendLine("complete -c eventstore-admin -n '__fish_seen_subcommand_from profile; and not __fish_seen_subcommand_from add list show remove' -a add -d 'Create or update a profile'");
        sb.AppendLine("complete -c eventstore-admin -n '__fish_seen_subcommand_from profile; and not __fish_seen_subcommand_from add list show remove' -a list -d 'List all profiles'");
        sb.AppendLine("complete -c eventstore-admin -n '__fish_seen_subcommand_from profile; and not __fish_seen_subcommand_from add list show remove' -a show -d 'Show profile details'");
        sb.AppendLine("complete -c eventstore-admin -n '__fish_seen_subcommand_from profile; and not __fish_seen_subcommand_from add list show remove' -a remove -d 'Remove a profile'");
        sb.AppendLine();
        sb.AppendLine("# Config use dynamic profile completions");
        sb.AppendLine("complete -c eventstore-admin -n '__fish_seen_subcommand_from config; and __fish_seen_subcommand_from use' -xa '(__eventstore_profiles)' -d 'Profile name'");
        sb.AppendLine("complete -c eventstore-admin -n '__fish_seen_subcommand_from config; and __fish_seen_subcommand_from use' -l clear -d 'Clear active profile'");
        sb.AppendLine();
        sb.AppendLine("# Config completion shell types");
        sb.AppendLine("complete -c eventstore-admin -n '__fish_seen_subcommand_from completion' -xa 'bash zsh powershell fish' -d 'Shell type'");
        sb.AppendLine();
        sb.AppendLine("# Stream subcommands");
        sb.AppendLine("complete -c eventstore-admin -n '__fish_seen_subcommand_from stream; and not __fish_seen_subcommand_from list events event state diff causation' -a 'list events event state diff causation'");
        sb.AppendLine();
        sb.AppendLine("# Projection subcommands");
        sb.AppendLine("complete -c eventstore-admin -n '__fish_seen_subcommand_from projection; and not __fish_seen_subcommand_from list status pause resume reset' -a 'list status pause resume reset'");
        sb.AppendLine();
        sb.AppendLine("# Tenant subcommands");
        sb.AppendLine("complete -c eventstore-admin -n '__fish_seen_subcommand_from tenant; and not __fish_seen_subcommand_from list detail quotas users compare verify' -a 'list detail quotas users compare verify'");
        sb.AppendLine();
        sb.AppendLine("# Snapshot subcommands");
        sb.AppendLine("complete -c eventstore-admin -n '__fish_seen_subcommand_from snapshot; and not __fish_seen_subcommand_from create policies set-policy delete-policy' -a 'create policies set-policy delete-policy'");
        sb.AppendLine();
        sb.AppendLine("# Backup subcommands");
        sb.AppendLine("complete -c eventstore-admin -n '__fish_seen_subcommand_from backup; and not __fish_seen_subcommand_from list trigger restore validate export-stream import-stream' -a 'list trigger restore validate export-stream import-stream'");
        return sb.ToString();
    }
}
